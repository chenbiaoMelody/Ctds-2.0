# AGENTS.md — C-TDS 项目 AI 智能体执行规则（V2.3）

> 本文件是所有 AI 编码智能体在仓库中的最高行为准则（对智能体而言）。完整制度见
> `docs/03-C-TDS项目研发章程与开发规范.md`（V2.3），与本文件冲突时以章程为准。

## 0. 关键背景（决定你的工作方式）

**本项目全体人类成员均无编程经验。**没有任何人会阅读、评审或修改你的代码——技术质量的全部责任由你（以及 AI 评审链与自动门禁）承担，人类只做业务功能验收。因此：

- 你的产出必须让"不写代码的人"能验证：每次交付附**业务可读交付说明**（做了什么功能、如何演示、剧本是否需更新）。
- 你提交前无人兜底：必须先完成 AI 自检单（章程附录 B1），再由 4 个独立评审会审，之后才能请人验收。
- 你必须写"普通"代码：选最主流、最常见的实现（章程 4.1 技术栈平庸化），禁止炫技。
- **会话是易失的**：你的记忆随会话关闭而消失，唯一合法的状态载体是开发日志与仓库文件。

## 1. 会话生命周期（强制，无例外）

### 1.1 冷启动（每个新会话的第一步）

1. 依次读取：`AGENTS.md` → `docs/logs/` 中**最新开发日志**（续接任务按其"文件指针"回读此前日志）→ 当前任务卡与规格；
2. 复述"当前任务 + 日志续点四要素（已完成/下一步/关键决策/文件指针）"，停下等人确认；
3. **禁止凭旧会话记忆继续任务**：不接受粘贴旧会话输出、聊天截图或口头转述作为状态来源；未写入日志与仓库文件的信息一律视为不存在。

### 1.2 运行纪律

1. **单会话单任务**：一个会话只做一个任务卡；禁止混做其他任务或无任务卡的自由探索。
2. **两错即清**：同一问题纠正两次仍未解决 → 停止纠缠，落盘日志（含教训）后建议人工重开会话。
3. **上下文用量报告**：每完成一个阶段性步骤，主动估算并向人报告当前上下文用量。
4. **探索隔离**：大范围调研交给子智能体/独立会话，只带回业务可读结论并写入日志或任务笔记。

### 1.3 上下文 50% 停机规则（强制）

1. 上下文用量达到 **50%**：完成当前**最小步骤**（一个原子动作：一个测试的修复、一次提交、一段文档写入）后**立即停止**，不开启新工作项；
2. 宁可提前停机（如 45% 时已无实质进展），不得越过 50%；
3. 停止前必须先完成开发日志落盘（1.4），然后提示人关闭会话、开新窗口续接。

### 1.4 开发日志（每次会话结束前必做）

1. **每次会话结束前**（无论原因：任务完成 / 50% 停机 / 两错即清 / 等待人工确认）必须按附录 G 格式**新建一份**开发日志；**禁止在既有日志上增量追加**——历史日志只读归档，错漏以文中"补正说明"标注；没有日志的会话结束 = 流程违规；
2. 文件：`docs/logs/Ctds-项目开发日志-yy-mm-dd-hhss.md`（`hhss` 为 4 位时分，如 20:35 = `2035`；**时间戳 = 本日志落盘时点**；内容修正后按当时时间重命名同步（`git mv`），提交与重命名动作本身不再触发重命名）；
3. **续点四要素缺一不可**：已完成 / 下一步（写到"可直接开工"粒度，含文件路径与第一个动作）/ 关键决策（决策+理由+关联文档位置）/ 文件指针（相关文件路径清单，含此前日志）；
4. **续接任务**：新会话新建自己的日志，续点承接上一份日志的"当前续点"，并在文件指针中引用该日志；
5. 起草后展示全文供编排师核对，再提交入库。

### 1.5 禁止压缩续命（强制）

禁止以自动压缩（auto-compact）或手动压缩（/compact 等）延续任务——压缩对关键信息的丢失不可控。上下文达 50% 一律停机落盘重启；需要长期保留的结论写入日志、任务笔记或 ADR，不留在会话里。

## 2. 八条原则（违反即打回）

1. **规格先行**：只实现 `docs/specs/C-x.y.md`（已评审）中明确列出的行为。规格不存在 → 停下要求提供，禁止自行猜测需求。
2. **人管业务，你管代码，机器把关**：人不做代码判断；你不要向人请求技术裁决，技术方案分歧在你的链路内解决（或升级流程），人只做业务影响选择。
3. **验证闭环**：交付前必须本地运行并通过：编译、单测、lint、扫描；声称完成 = 附验证命令与结果。人只认绿灯信号，你不得让绿灯失真。
4. **可演示**：你完成的定义 = 验收剧本可执行通过；每个交付必须回答"剧本是否需要更新"。
5. **最小实现**：禁止实现规格外功能、禁止"顺手优化"、禁止引入未批准依赖；改进建议写进交付说明。
6. **门禁无特批**：门禁红灯必须修复后重交；不存在任何例外通道，你也不得请求例外。
7. **安全默认**：加解密/密钥一律调用 `common` 国密组件（SM2/SM4/KMS），**禁止自行实现密码学**；禁止在代码、日志、提示词中出现真实密钥、生产数据、个人信息。
8. **会话纪律与日志强制**：单会话单任务；上下文达 50% 完成当前最小步骤即停；停止前续点四要素落盘开发日志；新会话一律冷启动，禁止凭旧会话记忆续任务；禁止以压缩续命复杂任务。

## 3. 技术约定（AI 无法从代码推断的部分）

- 微服务架构；分层：`接口层 → 应用服务 → 领域层 → 基础设施`；Controller 不得直连数据层。
- 信通院测评/互联互通相关实现一律收敛到 `std-adapter` 模块，不得散落业务代码。
- 横切能力（分页、鉴权、错误码、日志、幂等、分布式锁）先查 `common` 公共组件，找到即复用；找不到 → 在交付说明中提出沉淀建议，而不是各自实现。
- 错误码统一体系；禁止吞异常；对外错误信息不暴露内部实现。
- 技术栈以 `docs/adr/ADR-001` 为准且冻结：新框架/中间件/依赖走变更流程，你不得擅自引入；同一功能多种实现方式时，选最主流的写法并说明理由。
- 新依赖引入前必须：核对官方注册表真实存在 → 报告流程审批 → 更新锁定文件。**禁止直接安装任何未经核验的包名（包括你自己生成建议的包名）。**

## 4. 编码与测试

- 测试先行：先写失败测试（映射规格中每条 Given/When/Then），经确认后再实现。
- 核心模块（policy-engine、metering-billing、crypto、auth、did）：行覆盖 ≥80%；整体 ≥70%；变异杀除率 ≥60%；禁止空洞断言与仅 happy-path。
- 使用控制策略（C-4.3）每个策略必须有"策略生效"+"绕过被拒"双向用例；计费必须有对账断言。
- 禁止提交：TODO（除非已在工单登记编号）、注释掉的死代码、调试输出、生成后废弃的文件。
- 性能敏感路径（网关、目录检索、计量流水）完成后运行基准脚本并在交付说明附结果（业务可读）。

## 5. 提交前自检（无人兜底，此单即你的质量签名）

提交评审前必须完成并随 PR 附上（对应章程附录 B1）：

1. 规格验收标准 → 测试用例 → 剧本步骤 三层映射表；
2. 复用声明（含检索过程）与规格外实现声明（应为空）；
3. 本地全部检查的命令与结果摘要；
4. 高风险点自查：并发、事务、加解密、性能；
5. 验收剧本更新建议（业务语言文本）；
6. 业务可读交付说明：完成了什么功能 | 如何演示 | 有无注意事项。

提交后接受 4 视角评审（规格符合性/安全供应链/一致性重复/测试质量），循环 ≤3 轮，超限按升级流程处理，不得反复重交相同内容。

## 6. 版本与提交

- 分支：`feat/C-x.y-短描述` / `fix/C-x.y-短描述`；禁止推送/合并 `main` 与 `release/*`。
- Commit：`<type>(<scope>): <C-x.y> <摘要>`；一个 PR 一个目标，≤400 行有效变更（超出自动拆分任务）。
- PR 描述必含：需求编号与规格链接 + 自检单 + 四项声明 + 业务可读交付说明。
- 并行任务使用独立分支/worktree，不碰他人模块；批量任务先处理 2–3 个对象等人确认效果后再全量。
- **日志与代码同库提交**：开发日志的变更随任务分支一起提交，不得只留在本地。

## 7. 需求与变更

- 需求变化（含口头、聊天中传达的）一律回复："请先更新规格文档并走变更流程"，然后等待新任务卡。
- 剧本同步：规格变更后，对应验收剧本必须同步更新并提示 PO 审批，才算完成。
- 冻结期（里程碑前 2 周及 `release/*` 分支）只接受缺陷修复。

## 8. 红线（存疑即停，先问再做）

1. 白名单外工具不接触项目资产；不做无权限范围的无人值守操作。
2. 不自行解释模糊需求；架构决策提"业务影响对比表"给 PM/PO 选择，不代选。
3. 不绕过、关闭或修改 CI 门禁配置（配置变更只能经架构变更流程留痕执行）。
4. 发现规格与代码/ADR 冲突、或疑似 S0 安全问题 → 立即停止并上报，不擅自修复绕过。
5. 人类成员不编辑代码：若发现文件被人手改，立即报告编排师，不要在其基础上继续开发。
6. 不凭记忆续任务、不用压缩续命、不落盘不收工——这三条是会话纪律的底线。
