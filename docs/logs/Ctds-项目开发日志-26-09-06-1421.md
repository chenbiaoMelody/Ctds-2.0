# Ctds-项目开发日志-26-09-06-1421

- 任务卡 / 需求编号：【决策落档（ADR-001~005 批准 + 白名单签发，用户已写入文档）+ 上下文用量校准（用户指示）】
- 关联规格 / 剧本：【制度依据 = 章程 V2.3（4.4 ADR 治理、3.3.2 50% 停机、1.2.3 上下文报告）；WBS 2.1.x 里程碑条件】
- 会话目标：【① 从文档读取用户决策意见并完成落档（状态同步/编号修正/签发生效）；② 校准上下文用量报告口径（AI 自估 85% vs 任务窗口 13.4%）】
- 状态：【已完成】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. **决策读取与核实**：用户在 6 份文档中写入批准意见（`git diff --stat` 核实改动面 = 6 文件 +32/-1，全部为签发表，无其他改动）——ADR-001~005 各追加"批准签发"表（**批准方案 A**，签名 melody-C，2026-09-06 14:08~14:10）；白名单 §7 签发"**通过**"（14:06）；
  2. **落档同步**：ADR-001~005 状态字段"提案"→"**已批准**"（ADR-001 注明技术栈自此冻结；ADR-004 注明顾问复核义务保留）；ADR-002~005 批准节编号 §8→§6（内容未动）；白名单版本行转"已签发生效"、ZCode/GLM"待核实项"转"已核实签发"、§2.3 待准入条件更新（ADR-001 已批准）、§4 评估记录同步；
  3. **上下文校准（用户指示）**：AGENTS.md 1.2.3 写入口径——**以任务窗口显示的实际用量为权威值，AI 自估仅作趋势参考；当前模型 GLM-5.3-Flash 窗口 1M tokens，50% 停机线 ≈ 50 万 tokens；换模型须同步更新**；持久记忆已同步记录；
  4. 门禁复跑 **GREEN**（PASS=4/FAIL=0/PENDING=9，gate-report-20260906-142115.md）；提交 8a43820。
- **下一步**：ADR-001 已批准 → 依赖链解锁。**下一任务卡 = WBS 2.4.1 monorepo 工程骨架**（Maven 多模块分层骨架 + ArchUnit 依赖规则 + 示例模块），第一个动作 = 在仓库根创建 `pom.xml`（父 POM）与 `services/`、`common/`、`std-adapter/` 模块结构（按 ADR-002 目录规划）。开工前先把 gates-config.json 中 compile/lint/unitTest 等 PENDING 阶段随工具链接入逐个启用。
- **关键决策**：
  1. **ADR-001~005 全部批准（用户，方案 A）**：技术栈冻结为 Java17+SpringBoot3+Maven monorepo+MySQL+Redis+Nacos+MyBatis-Plus / Vue3+ElementPlus；monorepo；模型网关抽象+pgvector；FISCO BCOS（顾问复核义务保留）；Spring Cloud Gateway+REST 规范——**这是全项目最关键的基线决策**；
  2. **白名单签发（用户，通过）**：ZCode CLI + GLM 正式启用，训练条款核实责任由安全专员（用户）履行完毕；
  3. **上下文用量口径校准（用户指示）**：50% 停机规则按真实窗口计算——1M 窗口 = 50 万 tokens；纠正本会话早前"85%"的严重高估（实际 13.4%），并已在 AGENTS.md 固化防止复发。
- **文件指针**：
  - 本批修改：`docs/adr/ADR-001~005`（状态+批准签发）、`docs/04-AI工具白名单-评估与发布.md`（签发生效）、`AGENTS.md`（1.2.3 校准口径）
  - 门禁报告：`scripts/gates/reports/gate-report-20260906-142115.md`
  - 此前日志：`docs/logs/Ctds-项目开发日志-26-09-06-0916.md`（基建首批，承接其续点）
- **补正说明**：无（本文件一次成文）。

## 任务记录（一次成文，落盘核对后即为定稿）

### 任务记录 ｜ 26-09-06 14:00–14:21 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM）
- 做了什么：从 6 份文档读取用户批准意见并核实改动面（git diff --stat，确认仅签发表变更）；状态字段同步为"已批准/已签发"；批准节编号修正；AGENTS.md 写入上下文校准口径；门禁复跑；提交入库；持久记忆更新（项目状态 + 校准规则）。
- 验证结果：门禁 GREEN（gate-report-20260906-142115.md，29 文件扫描 0 密钥命中，5 份 ADR 字段齐备）；提交 8a43820 入库，工作区干净。
- 问题与教训：① **上下文自估 85% vs 实际 13.4%——根源是 AI 按"常见 200k 窗口"经验值折算，未锚定真实窗口**；已通过 AGENTS.md 1.2.3 固化"任务窗口数字为权威 + 窗口大小落档"双保险；该误估导致上一会话过早建议停机重开（实际余量充足），属于流程成本浪费；② 用户手改文件后 Edit 工具要求重读——流程上"先 git diff 核实改动面再在其上工作"的顺序正确，避免了在未知改动上叠加编辑。
- 停机原因：【任务完成】（按校准后口径，本会话实际用量约 15%，余量充足）
- 交接提醒：① 技术栈已冻结——任何新依赖走章程第 7 章变更流程，禁止直接安装；② 下一任务卡 WBS 2.4.1 工程骨架可直接开工（ADR 依赖已满足）；③ gates-config.json 的 9 个 PENDING 阶段在骨架建立后逐个启用；④ 用户签发时间戳格式为"2026:09:06:14:08"（冒号分隔），后续文档沿用其原样，不再改写。
