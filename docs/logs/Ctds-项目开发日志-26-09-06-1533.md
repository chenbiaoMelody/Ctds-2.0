# Ctds-项目开发日志-26-09-06-1533

- 任务卡 / 需求编号：【WBS 2.4.1 monorepo 工程骨架】
- 关联规格 / 剧本：【无业务规格；制度依据 = ADR-001（已批准，冻结栈）/ ADR-002（已批准，目录与分支）/ 章程 4.3 分层 / 上会话日志续点（含门禁三阶段接入要求）】
- 会话目标：【建成 Maven 多模块 monorepo 分层骨架（接口/应用/领域/基础设施）+ ArchUnit 分层依赖规则 + 示例模块验证；同步把门禁 compile/lint/unitTest 接入工具链】
- 状态：【已完成】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. **环境探测**：本机已有 JDK 17.0.12（Oracle）与 Maven 3.9.16（`%USERPROFILE%\tools`），零下载成本；新建用户级 `%USERPROFILE%\.m2\settings.xml`（阿里云镜像 mirrorOf=central，不入库）；
  2. **依赖核验**（章程：新依赖须核对官方注册表）：spring-boot-starter-parent **3.5.16**（3.x 最新稳定补丁）、archunit-junit5 **1.5.0**、maven-checkstyle-plugin 3.6.0 均经阿里云仓库 metadata 确认真实存在，评审②又经 repo1.maven.org 独立复核；
  3. **工程骨架**（commit 6f29440）：根 POM（`com.ctds:ctds-platform:2.0.0-SNAPSHOT`，packaging=pom，Boot 3.5.16 父链）+ `services/example-service` 四层示例（interfaces/application/domain/infrastructure，Greeting 全链路）+ `architecture/LayerRulesTest` 三条 ArchUnit 规则（接口层↛基础设施、领域层仅依赖 JDK、应用层↛接口层）+ 8 个测试用例 + `config/checkstyle/checkstyle.xml` + ADR-002 目录占位（common/std-adapter/frontend/connector-sdk/deploy）；
  4. **门禁工具链接入**（commit b93aef40→b93aefb）：run-gates.ps1 新增 Maven 工具链阶段（ProcessStartInfo + JAVA_HOME 配置/env 双源 + 600s 超时 + 字符白名单防注入），gates-config.json 启用 compile/lint/unitTest；新增 coverage PENDING 阶段（PENDING-JACOCO）；**门禁终态 GREEN（PASS=7/FAIL=0/PENDING=7，mvn test 8/8）**；
  5. **白名单增补**：JDK 17 / Maven 3.9.16 评估记录与 2.1 清单行（随本任务卡启用，待安全专员追认）；AGENTS.md 增构建命令条；章程 3.2/ADR-002 补 `config/` 目录登记；
  6. **4 视角评审试运行 2**：**4/4 PASS**，7 项修复/声明——详见 `docs/manuals/ai-review/trial-2026-09-06.md` 试运行 2（含跟踪项 T6）。
- **下一步**：WBS 2.4.2「common-错误码与异常」可直接开工——第一个动作 = 在 `common/` 下建 `common-errorcode` Maven 模块（继承 2.0.0-SNAPSHOT 父 POM、`<relativePath>../../pom.xml</relativePath>`），实现错误码体系（ADR-005 §3.3：平台域+模块+类型+4位序号）与统一异常处理组件 + 单测，随后根 POM `<modules>` 增加 common 模块并接入门禁。**开工前必须先产出对应规格卡或确认"2.4.2 组件级任务可用 ADR-005 作为规格依据"**（规格先行原则，向编排师确认）。
- **关键决策**：
  1. **构件版本 2.0.0-SNAPSHOT**：本地仓库残留旧项目（疑似 Ctds 1.0）`com.ctds:*:0.1.0-SNAPSHOT` 缓存 11 项，曾导致子模块继承到旧父 POM（Boot 3.3.3 依赖栈、无 archunit 管理）——以版本号隔离，禁止回用旧坐标（AGENTS.md 已固化）；**建议用户归档/清理 `%USERPROFILE%\.m2\repository\com\ctds` 旧缓存（跟踪项 T6，AI 不动用户本机文件）**；
  2. **嵌套模块 relativePath 显式化**：`services/<服务名>` 两级目录下默认 `../pom.xml` 指不上根 POM，必须写 `<relativePath>../../pom.xml</relativePath>`（AGENTS.md 已固化）；
  3. **分层规则集 v1**：接口层↛基础设施、领域层纯净、应用层↛接口层三条规则固化；"接口层是否允许依赖领域层"留设计口径（现允许，Controller 做 DTO 映射）——后续如需收紧属架构变更走 ADR（跟踪 T7）；
  4. **coverage 独立 PENDING 阶段**：JaCoCo 接入属工具链变更（评审④判定 includeBranchCoverage 无效键有"绿灯失真"风险），先行移除并以 PENDING-JACOCO 显式登记；
  5. Checkstyle 采用精简主流规则集（checkstyle 10.x 语法，LineLength 挂 Checker 层）而非全量 google_checks——降低 AI 生成代码的风格摩擦，阈值仍零违规。
- **文件指针**：
  - 代码：`pom.xml`、`services/example-service/**`（16 文件）、`config/checkstyle/checkstyle.xml`、`common|std-adapter|frontend|connector-sdk|deploy/.gitkeep`
  - 门禁：`scripts/gates/{run-gates.ps1, gates-config.json, README.md}`、报告 `scripts/gates/reports/gate-report-20260906-153217.md`
  - 文档：`AGENTS.md`（构建命令）、`docs/04-AI工具白名单`（JDK/Maven）、`docs/adr/ADR-002`（config/ 登记）、`docs/03-章程`（3.2 表）、`docs/manuals/ai-review/trial-2026-09-06.md`（试运行 2）
  - 本机配置（不入库）：`%USERPROFILE%\.m2\settings.xml`
  - 此前日志：`docs/logs/Ctds-项目开发日志-26-09-06-1421.md`
- **补正说明**：无。

## 任务记录（一次成文，落盘核对后即为定稿）

### 任务记录 ｜ 26-09-06 14:35–15:33 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM）
- 做了什么：环境探测（JDK/Maven 已装，仅补镜像配置）→ 依赖核验 → 骨架编码（父 POM/示例服务四层/ArchUnit/测试/checkstyle）→ 首次构建遇双重故障并修复（旧构件缓存继承 + 嵌套 relativePath）→ 构建成功（6 测试）→ checkstyle 配置层修正（LineLength 移 Checker 层）→ 门禁接入三阶段并修复 PS 5.1 ExitCode 陷阱 → 门禁 GREEN → 白名单/AGENTS/章程登记 → 4 视角评审（4/4 PASS）→ 7 项修复（含补 2 用例、注入面校验、无效键移除、目录登记）→ 终态门禁 GREEN → 2 笔提交（6f29440、b93aefb）→ 本日志。
- 验证结果：`mvn -B -ntp test` 8/8 通过；`mvn -B -ntp checkstyle:check` 0 violations；门禁全绿 PASS=7/FAIL=0/PENDING=7（gate-report-20260906-153217.md）；依赖坐标经阿里云 metadata + 评审② repo1 双重核验；提交入库 2 笔，工作区干净。
- 问题与教训：① **本地仓库同 GAV 旧缓存会让 Maven 静默继承旧父 POM**——新项目起版本号必须避开历史缓存（教训已固化 AGENTS.md）；② Maven 嵌套模块 relativePath 默认只上一级，两级目录必须显式 `../../pom.xml`；③ Checkstyle 10.x 中 LineLength 属 Checker 层（挂 TreeWalker 直接报错）；④ PowerShell 5.1 `Start-Process -PassThru` 的 ExitCode 在该用法下为空——用 ProcessStartInfo（UseShellExecute=false）才可靠；⑤ cmd 下 `tail` 不存在，统一用 PowerShell `Get-Content -Tail`；⑥ 评审④实测重跑构建验证可复现性、评审②独立核验依赖坐标——评审智能体做实证而非只读文档，价值显著更高。
- 停机原因：【任务完成】（按校准口径本会话实际用量约 30%，余量充足，可继续承接下一任务卡）
- 交接提醒：① 下一任务 WBS 2.4.2 需先确认规格依据（规格先行）；② 旧缓存清理（T6）待用户动作；③ JaCoCo 接入（coverage 阶段）属工具链变更，须先走 ADR/变更流程再启用；④ 接口层依赖领域层的设计口径（T7）在首个真实业务模块开发前定稿。
