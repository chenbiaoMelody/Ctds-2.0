# Ctds-项目开发日志-26-09-06-1648

- 任务卡 / 需求编号：【WBS 2.4.2（common-错误码与异常）】
- 关联规格 / 剧本：【规格依据 = 已批准的 ADR-005 §3.3（错误码体系）+ §3.2（响应封套）+ WBS 2.4.2 产出定义（用户指令"继续下一任务"视为确认按 ADR 为规格依据，未另出规格卡）】
- 会话目标：【建成统一错误码体系、业务异常与全局异常处理公共组件（common-errorcode），并完成 example-service 接入验证】
- 状态：【已完成】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. **用户指令落档**：① 旧 Maven 缓存清理经实测确认（`%USERPROFILE%\.m2\repository\com\ctds` 文件数=0，T6 闭环，仅剩空目录）；② 白名单 JDK/Maven 增补由用户在 §7 签发表确认追认（签名 melody-C，15:52）；
  2. **common-errorcode 组件**（commit f98abc2）：`ErrorCode`（9 位码值对象，正则强校验 `^\d{4}[CBS]\d{4}$`）+ `ErrorType`（C/B/S→HTTP 默认映射）+ `ErrorCodes` 公共域码表（5 条：1000C0001~0004、1000S9999）+ `BizException`（携带错误码；message 禁拼用户输入已入 javadoc）+ `GlobalExceptionHandler`（C/B→400 透业务文案、S→500 通用文案脱敏、未知异常→500 兜底、404/405/坏 JSON 框架语义还原）+ `TraceIdFilter`（X-Trace-Id 头→MDC→响应头，成功/异常封套同源）+ `ApiResult`（code 统一字符串，"0"=成功）+ Boot 3 自动装配（AutoConfiguration.imports）；
  3. **example-service 接入**：域模型 Greeting 改抛 BizException（领域规则同步收紧为仅放行 `..common.errorcode..`）、控制器改用公共 ApiResult（本地实现删除）、错误路径端到端测试（空白消息→400+"1000C0001"）；
  4. **验证**：`mvn test` **23/23 通过**（common 14 + example 9）；checkstyle 0 违规；**门禁 GREEN（PASS=7/FAIL=0/PENDING=7）**，报告 gate-report-20260906-164450.md；
  5. **4 视角评审试运行 3**：**4/4 PASS**，10 项修复/跟踪——含 3 项实质修复（框架语义异常还原、traceId 贯通、S 型脱敏断言补强）与 3 项转跟踪（T8 码表唯一性校验、T10 模块拆分评估），详见 `docs/manuals/ai-review/trial-2026-09-06.md` 试运行 3；
  6. **ADR-005 口径澄清**：§3.2 补充说明"code 为字符串（"0"=成功）"（接口契约澄清，已入库）。
- **下一步**：WBS 2.4.3「common-分页与查询」可直接开工——第一个动作 = 开工前先做 T10 裁决（分页组件放 common-errorcode 内 or 拆新模块 `common/pagination`，建议拆分：`common/pagination` 依赖 common-errorcode），然后建模块写 `PageResult<T>`/`PageQuery`（pageNum/pageSize/orderBy，ADR-005 §3.2 分页参数）+ 用例 + 挂根 POM。
- **关键决策**：
  1. **成功码定字符串 "0"**：ADR-005 §3.3"0/0000…"授权 + 全字段类型一致；§3.2 已补澄清，消除评审发现的 ADR 内部歧义；
  2. **S 型异常双保险脱敏**：处理器强制替换通用文案（不信 message）+ log.error 服务端留档——对"对外不暴露内部实现"（章程 4.3）的机器化落实；
  3. **traceId 单源化**：Filter→MDC→封套，拒绝在控制器各自读头；净化规则（`[A-Za-z0-9._-]{1,64}`，非法置 "-"）防响应回显；
  4. **框架语义异常还原**：404/405/坏 JSON 不再被兜底 500（评审②发现的语义吞噬缺陷），同时客户端错误降为 log.warn 防日志刷量；
  5. **领域层 common 依赖白名单制**：ArchUnit 规则从 `..common..` 收紧到 `..common.errorcode..`，后续每纳入一个公共组件按需放行（防"公共包=任意依赖"大口子）。
- **文件指针**：
  - 组件：`common/errorcode/**`（pom + main 9 文件 + test 4 类）
  - 接入：`pom.xml`、`services/example-service/**`（pom、Greeting、GreetingController、3 测试类；本地 ApiResult 已删除）
  - 文档：`docs/adr/ADR-005`（§3.2 澄清）、`docs/04-白名单`（追认）、`docs/manuals/ai-review/{README, trial-2026-09-06}`（试运行 3 + T8/T10）
  - 门禁报告：`scripts/gates/reports/gate-report-20260906-164450.md`
  - 此前日志：`docs/logs/Ctds-项目开发日志-26-09-06-1533.md`
- **补正说明**：无。

## 任务记录（一次成文，落盘核对后即为定稿）

### 任务记录 ｜ 26-09-06 15:55–16:48 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM）
- 做了什么：核实用户清理缓存（0 文件）并落档追认 → 组件编码（错误码/异常/处理器/过滤器/自动装配）→ example-service 接入（封套统一、域模型改造、ArchUnit 收紧）→ 构建（1 次编译错：漏 Pattern 导入，即改）→ 门禁 GREEN → 4 视角评审试运行 3（4/4 PASS）→ 按评审修复 3 项实质问题（框架异常还原、traceId 贯通、脱敏断言），期间 3 次测试失败均为测试自身笔误（端点打错/缺导入/期望值笔误）即改即验 → 终态门禁 GREEN → 2 笔提交（f98abc2、788c60e）→ 本日志。
- 验证结果：mvn test 23/23；checkstyle 0 违规；门禁 GREEN（gate-report-20260906-164450.md）；评审 4/4 PASS 且 ④ 实测复跑构建确认。
- 问题与教训：① **评审④实测复跑纠正了我交付说明里的数字（20→23）**——量化数字必须取自最新构建输出，不得手估（教训已写入试运行记录）；② 异常处理器兜底 Exception 会吞掉 404/405/400 语义——全局兜底必须先还原框架语义再兜底，这是评审②最有价值的发现；③ traceId 成功/异常两套来源的隐患在组件阶段就要堵住（Filter 单源），拖到网关阶段再改成本翻倍；④ 子智能体并发受"model concurrency limit"限制——评审分批派发（2+2 或逐个）更稳。
- 停机原因：【任务完成】（本会话实际用量约 45%，仍有余量）
- 交接提醒：① T8（码表唯一性机器校验）、T10（common 模块拆分评估）登记于试运行 3——T10 建议在 2.4.3 开工前先裁决；② 错误码登记规则：各模块建 `<模块>ErrorCodes` 常量类（参照 ErrorCodes javadoc），码值待跨模块门禁上线前靠人工登记唯一；③ 验收剧本：本组件无独立剧本（基建组件），example-service 的 /api/v1/greetings 可作冒烟演示。
