# Ctds-项目开发日志-26-09-06-1856

- 任务卡 / 需求编号：【WBS 2.4.4（common-日志与审计埋点）——续接 26-09-06-1841】
- 关联规格 / 剧本：【制度依据 = WBS 2.4.4 产出定义 + 章程 4.3 + ADR-005；设计文件 = docs/designs/WBS-2.4.4-{lofi,hifi}.md；无验收剧本（基建组件）】
- 会话目标：【低保真获 PO 确认后，产出高保真设计并停机等定稿确认】
- 状态：【进行中（已停机待续：等待 PO 确认高保真）】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. **低保真获 PO 确认**（会话回复"确认，1 同意引入、2 同意"）：签署已写入 `docs/designs/WBS-2.4.4-lofi.md` 确认记录块——① 批准依赖 logstash-logback-encoder（8.x 系列）；② 审计事件 V1.0 先落按天滚动 JSONL 文件；
  2. **编码契约打底**（只读代码，零代码变更）：errorcode 模块（TraceIdFilter MDC 键 = `ApiResult.TRACE_MDC_KEY`、BizException/ErrorCode 9 位码、GlobalExceptionHandler 现状：仅 S 型记 error 日志）、example-service 分层与 GreetingController（POST create / GET list 分页）、AutoConfiguration 注册方式（`META-INF/spring/...imports`）、根 pom（Boot 3.5.16/Java 17）、ADR-005 结构（§3 决策 1–5 项，日志契约将增补第 6 项）；deploy/ 目前为空（DDL 占位将落 `deploy/sql/ai-call-log.sql`）；
  3. **依赖精确版本锁定**：repo1.maven.org 官方 metadata 核验——8.x 系列最新 = **8.1**；9.0（2025-10-26 发布）需 Jackson 3，与 Boot 3.5.16 管理的 Jackson 2 不兼容，故不采用；
  4. **高保真设计落盘**：`docs/designs/WBS-2.4.4-hifi.md`——行为清单 B1–B9（含计划测试映射）、LogContext/AuditRecorder/AuditEvent 接口契约、JSONL 与 JSON 日志字段表、边界值（detail 20 键/512 字符截断、队列 10000 丢事件计数、IO 故障限频 ERROR）、编码会话实施清单 6 步、变更影响声明；
  5. **与低保真的差异声明**：结构化日志由"组件自动装配"细化为 **Spring Boot 3.5 内置 logstash 格式 + 每服务 3 行 yml**（框架内置最主流、零自研维护）——hi-fi 已显式声明，属定稿内容，待 PO 确认。
- **下一步**：等 PO 在 `docs/designs/WBS-2.4.4-hifi.md` 确认记录块签署定稿确认。确认后第一个动作 = 编码会话冷启动（读本日志 + 已确认 lofi/hifi），按 hifi"编码会话实施清单"执行：① ADR-005 §3 增补日志契约 → ② 建 `docs/dependencies.md` 登记 8.1 → ③ 按 B1–B7 测试先行（含 B5 溢出/B6 IO 故障异常路径）→ ④ 实现 `common/logging` → ⑤ example-service 接入 → ⑥ 门禁全绿 + 4 视角评审 + 冒烟。PO 可在定稿确认时一并选择：编码在本会话继续（用量充足时）或新开会话冷启动。
- **关键决策**：
  1. **结构化日志改用 Spring Boot 3.5 内置结构化日志**（`logging.structured.format: logstash`）而非组件自研 JsonLogSetup——理由：内置写法即主流实现、删减组件代码面、MDC 字段自动进 JSON；属对低保真"自动装配"的细化，已声明待确认；
  2. **依赖精确版本 8.1**（8.x 最新；9.0 需 Jackson 3 冲突）——登记簿随编码建立；
  3. **LogContext 不重复造 traceId 键**，直接引用 `ApiResult.TRACE_MDC_KEY`——理由：单一事实源，logging 依赖 errorcode 方向正确；
  4. **审计 DENIED 事件接入点** = GreetingService 排序字段白名单拒绝分支（对齐章程"策略生效+绕过被拒"精神，作为演示接入点之一）；
  5. **AuditRecorder "绝不抛错、绝不阻塞"**为接口级铁律（含 null 事件忽略、IO 故障吞并限频、队列满丢弃计数）——理由：审计组件故障不得连坐业务。
- **文件指针**：
  - 设计：`docs/designs/WBS-2.4.4-lofi.md`（已确认）、`docs/designs/WBS-2.4.4-hifi.md`（待确认）
  - 代码参照（只读）：`common/errorcode/**`（TraceIdFilter/GlobalExceptionHandler/BizException/ApiResult）、`services/example-service/**`（GreetingController/GreetingService/application.yml）、`pom.xml`（根）
  - 制度：`docs/adr/ADR-005`、章程 V2.4 §2.6、AGENTS.md §4
  - 此前日志：`docs/logs/Ctds-项目开发日志-26-09-06-1841.md`
- **补正说明**：无。

## 任务记录（一次成文，落盘核对后即为定稿）

### 任务记录 ｜ 26-09-06 18:42–18:56 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM）
- 做了什么：低保真签署回写 → 7 个源文件/配置只读打底 → 依赖版本经 repo1 官方 metadata 锁定 8.1 → 高保真设计落盘（B1–B9 + 契约 + 边界值 + 实施清单）→ 本日志。
- 验证结果：hi-fi 行为清单逐条可测（B1–B7 自动化、B8/B9 人工核对）；依赖版本引自 repo1.maven.org 官方 maven-metadata（非第三方转载）；接口契约与既有 errorcode 契约无方向性冲突（logging→errorcode 单向依赖）。
- 问题与教训：① mvnrepository 反爬 403、search.maven.org 超时——Maven 依赖核验优先用 repo1.maven.org 的 maven-metadata.xml（稳定直出全版本）；② Spring Boot 3.4+ 内置结构化日志是"加依赖即得"的能力，自研编码器属重复造轮——高保真阶段发现后按铁律以"差异声明"显式提交裁决，未静默改低保真结论。
- 停机原因：【等待人工确认】（高保真设计待 PO 定稿签署；上下文用量：任务窗口 9.8%（用户上一口径），自估本会话累计约 15–20%，趋势参考）
- 交接提醒：① 定稿确认后编码会话必读本日志 + lofi/hifi 两设计文件；② 编码实施清单第一步是 ADR-005 增补（契约先于代码入库）；③ B5/B6 两个异常路径测试禁止省略（评审④关注点）；④ 提交通道 = WSL git（Windows PATH 无 git），中文提交信息用 `commit -F` UTF-8 文件。
