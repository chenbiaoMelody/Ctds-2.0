# Ctds-项目开发日志-26-09-06-2332

- 任务卡 / 需求编号：【WBS 2.4.4（common-日志与审计埋点）——续接 26-09-06-2015，实现与评审收尾】
- 关联规格 / 剧本：【设计 = docs/designs/WBS-2.4.4-{lofi,hifi}.md（均确认，hifi 签署已回写）；契约 = ADR-005 §3 第 6 项；依赖 = docs/dependencies.md（8.1 已登记）；无验收剧本（基建组件，冒烟演示见交付说明）】
- 会话目标：【实现 common/logging 使测试转绿 → 门禁 → 4 视角评审 → 修复循环 → 收尾】
- 状态：【已完成】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. **实现（TDD GREEN）**：LogContext（MDC 错误码/模块，空值清除，clear 不动 traceId）、AuditEvent（必填校验复用 PARAM_INVALID、actor 缺省 anonymous、detail 保序不可变拷贝）、AsyncFileAuditRecorder（单守护线程+有界队列+Jackson JSONL 按天滚动+调用线程 traceId 快照+队列满丢弃计数+写失败 60 秒限频 ERROR+shutdown 排空 5 秒+detail 双截断）、LoggingAutoConfiguration（ctds.audit.{enabled,file-dir,queue-capacity}，开关 bean 级隔离）+ LogContextCleanupFilter（评审②P1 修复产物）；
  2. **example-service 接入**：application.yml 3 行启用 Boot 内置 logstash 结构化格式；GreetingService 注入 AuditRecorder——create 记 SUCCESS（targetId=greeting id）、排序白名单拒绝记 DENIED（detail.orderBy）+ LogContext.setErrorCode + 服务端常量 warn；
  3. **AI 调用日志占位**：deploy/sql/ai-call-log.sql（V1.0 预留声明，V1.5 由 3.10.3 填充）；
  4. **4 视角评审全 PASS**（各经 1 轮修复，trial 试运行 5 全记录）：②P1=MDC 泄漏→清理过滤器+开关解耦；①P1=hifi 签署未回写→补签；③P1=.gitignore 误伤 docs/logs→根锚定+例外（评审方独立 check-ignore 四场景复验）；④2P1+4P2=detail 边界/B5 WARN 零覆盖等→补测试+限频改注入 Clock+B7 竞态修复；附加修复=Map.copyOf 不保序→AuditEvent 改 unmodifiableMap(LinkedHashMap)（契约加强，兼消除 detail 含 null 经 record() 抛 NPE 的违约路径）；
  5. **门禁终态 GREEN**：gate-report-20260906-232825.md（PASS=7/FAIL=0/PENDING=7）；**mvn test 72/72**（errorcode 14 + pagination 11 + logging 30 + example 17）+ checkstyle 0 违规；
  6. **工程卫生**：误提交的运行期日志 app.log 已从提交剔除并补 .gitignore（logs/**.log）。
- **下一步**：下一个公共组件任务卡（按 WBS 文档顺序应为 WBS 2.4.5 common-鉴权，以 WBS 文档为准）——第一个动作 = 开设计会话按附录 A8 产出 docs/designs/WBS-2.4.5-lofi.md（非界面类一页方案要点：鉴权基线按 ADR-005 §3 第 4 项 JWT+RBAC、网关注入上下文头的组件形态），停机等 PO 确认方向。另：T10/T13 拆分评估、T12 敏感键黑名单变更流程，均在后续任务卡处置，不阻塞下一组件。
- **关键决策**：
  1. **结构化日志不自研编码器**：Boot 3.5 内置 logstash 格式 + 3 行 yml（已确认差异声明）——组件只管上下文与审计，编码器交给框架；
  2. **MDC 按请求清理由独立过滤器承担**且不受审计开关约束（评审②P1/P2-1）——业务写 MDC 不受开关控制，清理必须无条件；
  3. **AuditEvent detail 保序拷贝**（评审④触发）：Map.copyOf 不保序使"保留前 20 键"无确定语义，改 unmodifiableMap(LinkedHashMap)；
  4. **限频/丢弃告警计时全部走注入 Clock**（评审④）：异常路径可确定性测试，杜绝"永久静默"缺陷穿闸；
  5. **.gitignore 的 logs 规则必须例外 docs/logs/**（评审③ P1）：新增忽略规则一律先 check-ignore 验证强制入库目录。
- **文件指针**：
  - 组件：`common/logging/**`（pom + main 8 类 + resources 装配注册 + test 5 类 30 用例）
  - 接入：`services/example-service/**`（pom、application.yml、GreetingService、AuditTrailIntegrationTest、既有测试构造更新）、`pom.xml`（根：模块注册+依赖锁版）、`deploy/sql/ai-call-log.sql`、`.gitignore`
  - 设计与契约：`docs/designs/WBS-2.4.4-{lofi,hifi}.md`、`docs/adr/ADR-005`（§3 第 6 项）、`docs/dependencies.md`
  - 评审：`docs/manuals/ai-review/trial-2026-09-06.md`（试运行 5 + T12/T13）、门禁报告 `scripts/gates/reports/gate-report-20260906-232825.md`
  - 此前日志：`docs/logs/Ctds-项目开发日志-26-09-06-2015.md`（同日链：1746→1814→1841→1856→2015→本篇）
- **补正说明**：无。

## 任务记录（一次成文，落盘核对后即为定稿）

### 任务记录 ｜ 26-09-06 20:20–23:32 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM）
- 做了什么：测试确认 → 实现三件套与接入 → 误提 app.log 剔除+gitignore 补齐 → RED→GREEN（64/64）→ 4 视角评审（②①③④各 FAIL 一轮）→ 五个修复提交（b3e9424/6085a00/15091e6/74f3194/7169964）→ 三评审独立复核 PASS → 门禁 GREEN（72/72）→ trial 试运行 5 落盘 → 本日志。
- 验证结果：门禁 GREEN（PASS=7/FAIL=0/PENDING=7，gate-report-20260906-232825.md）；mvn test 72/72（数字取自构建输出）；checkstyle 0 违规；4 视角终审 PASS（②复审确认 P1 修复有效；③独立 check-ignore 四场景实测；④确认六项闭合且附加修复为契约加强）。
- 问题与教训：① **评审子智能体派发 5 次因"模型并发限制/请求失败"中断**——并行 4 派发不可靠，改串行重试后全部完成；② **"确认"的意思表示必须当轮回写全部对应文件**：PO 说"定稿确认"我只签了 lofi 链路惯性漏了 hifi，被评审①抓为 P1（流程门禁自身也要有测试意识）；③ **新增 .gitignore 规则必须 check-ignore 验证强制入库目录**（docs/logs 险被静默忽略）；④ **Map.copyOf 不保序且拒 null 值**——保序不可变用 Collections.unmodifiableMap(new LinkedHashMap<>())；⑤ cmd 陷阱延续：`;` 非分隔符、`2>/dev/null` 报错、`%%` 不转义、findstr 中文失灵——一律 PowerShell Select-String + `&&`。
- 停机原因：【任务完成】（上下文用量以任务窗口显示为准，本会话为长会话，下一任务卡建议新会话冷启动续接）
- 交接提醒：① 冒烟演示命令见交付说明（POST create→SUCCESS 事件、GET 非法 orderBy→DENIED+errorCode）；② T12/T13 已登记 trial 试运行 5，勿再口头跟踪；③ 评审子智能体派发失败时串行重试（勿并行 4 发）；④ 运行期 logs/ 与 *.log 已全局忽略，勿再提交；⑤ pagination 用例数 12→11 系既往会话口径差异（评审④已核实该模块本任务零改动），非本任务削弱。
