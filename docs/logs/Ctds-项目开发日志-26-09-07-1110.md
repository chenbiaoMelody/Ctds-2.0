# Ctds-项目开发日志-26-09-07-1110

- 任务卡 / 需求编号：【WBS 2.4.5（common-鉴权框架）——设计会话续：高保真产出与依赖登记】
- 关联规格 / 剧本：【制度依据 = WBS 2.4.5 产出定义 + ADR-005 §3 第 4 项；低保真 = docs/designs/WBS-2.4.5-lofi.md（PO 已确认 2026-09-07）；高保真 = docs/designs/WBS-2.4.5-hifi.md（本次落盘，待定稿确认）；无验收剧本（基建组件）】
- 会话目标：【冷启动 → 承接低保真确认（含五问回答）→ 产出高保真 + Maven Central 核验与依赖登记 → 停机等 PO 定稿确认】
- 状态：【进行中——停机等待 PO 高保真定稿确认】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. 冷启动：AGENTS.md + README + 最新日志 26-09-07-0012 复述续点；PO 声明已签署低保真并给出五问回答（1 认可 Resource Server 判定、2 选模式 A、3 同意 401/403 口径、4 同意审计默认开启、5 演示规则可以），已按声明把低保真确认记录表轮次 1 代录留痕；
  2. **契约依据全部回读核对**：ADR-005 §3（错误码映射"特定语义码可在处理器映射更精确状态"的既有口径）、WBS 2.4.5/3.9.2/2.4.12 行、common-errorcode（码表仅 UNAUTHORIZED，无 403 码；GlobalExceptionHandler C/B 默认 400）、common-logging（AuditEvent.of/清理过滤器/自动装配惯例）、example-service（现无 DELETE 端点）、2.4.4-hifi（格式基准）；
  3. **Maven Central 核验**：`spring-boot-starter-oauth2-resource-server` 官方 maven-metadata.xml 实测存在，3.5.x 最新 3.5.16（与项目 Boot 父版本一致，版本由 BOM 管理），已登记 `docs/dependencies.md`（审批栏注明"PO 预授权：按判定原则自主选定，2026-09-07"）；
  4. **高保真落盘** `docs/designs/WBS-2.4.5-hifi.md`：行为清单 B1–B11、接口契约（AuthContext / @RequirePermission+拦截器 / AccessControl / RolePermissionMapper / AuthAdvice / 网关侧三过滤器+JwtDecoder）、配置项全表、数据结构与边界值、验收演示 4 用例、编码实施清单 6 步；确认记录表待 PO 签署。
- **下一步**：PO 签署高保真（`docs/designs/WBS-2.4.5-hifi.md` 确认记录轮次 1；重点过目"与低保真的差异声明"三条：网关交付形态=自动配置组件、resource-server 以 optional 引入、FORBIDDEN=1000C0005 增量登记）。若确认：新会话冷启动进入编码，第一个动作 = 读本日志 + hifi 全文，按 hifi"编码会话实施清单"第 1 步开工（ErrorCodes 增 FORBIDDEN + ADR-005 §3 第 7 项"鉴权契约"增补）；关键文件 `common/errorcode/src/main/java/com/ctds/common/errorcode/ErrorCodes.java`、根 `pom.xml`、新模块 `common/auth/`。
- **关键决策**：
  1. **网关侧校验交付形态 = 自动配置组件 + 组件级集成测试**（非可运行网关服务）：依据 = WBS 实测确认 V1.0 无网关服务包、真实流量入口在 3.5.2；3.5.2 届时引入组件 2 行配置启用；全文见 hifi"差异声明-1"；
  2. **resource-server 依赖声明为 optional**：Spring Security 上 classpath 会触发 Boot 默认安全策略接管全部端点，会破坏 example-service 等未开启服务；optional + 网关服务显式再加依赖才生效，普通服务零排除配置；全文见 hifi"差异声明-2"；
  3. **403 错误码 = ErrorCodes 增量登记 FORBIDDEN(1000C0005)，401 复用 UNAUTHORIZED(1000C0002)**；HTTP 精确状态映射由鉴权组件自带 AuthAdvice（@Order 前置）完成，GlobalExceptionHandler 与 errorcode 既有行为零改动（沿 ErrorType 注释预留口径）；全文见 hifi"差异声明-3"与"AuthAdvice"节；
  4. **上下文头命名定稿**：默认 `X-Ctds-Subject` / `X-Ctds-Roles`（角色逗号分隔），可配置；信任边界由网关侧"先剥离、认证后注入"两过滤器落地；全文见 hifi 接口契约与配置表；
  5. RBAC 强制点 = HandlerInterceptor（预检主流写法，不引入 AOP）；注解单权限、命名沿审计 action"对象.动作"规约；映射来源接口化（模式 A 配置实现，3.9.2 换库表仅覆盖 Bean）。
- **文件指针**：
  - 本次产出：`docs/designs/WBS-2.4.5-hifi.md`（待签署）、`docs/dependencies.md`（新增 2.4.5 登记行）、`docs/designs/WBS-2.4.5-lofi.md`（确认记录代录）
  - 契约依据：`docs/adr/ADR-005-API规范与网关.md`（§3 第 3/4 项，编码会话增补第 7 项）、`docs/C-TDS项目总体实施计划与WBS.md`（200/337 行）、`docs/designs/WBS-2.4.4-hifi.md`（格式基准）
  - 实现参照：`common/errorcode/**`（码表/异常/自动装配）、`common/logging/**`（AuditEvent.of、清理过滤器、imports 注册）、`services/example-service/**`（controller/service/yml/两处待补头测试）
  - 此前日志：`docs/logs/Ctds-项目开发日志-26-09-07-0012.md`（同日链：…→2332→0012→本篇）
- **补正说明**：无。

## 任务记录（一次成文，落盘核对后即为定稿）

### 任务记录 ｜ 26-09-07 10:50–11:10 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（Qwen）
- 做了什么：冷启动复述 → 契约依据与组件惯例逐项实测回读 → Maven Central 核验并登记依赖 → 高保真设计落盘 → 低保真确认记录代录 → 本日志。
- 验证结果：文档任务无测试门禁；hifi 已对照 2.4.4-hifi 结构自查（确认记录块/差异声明/行为清单含计划测试/契约表/边界值/实施清单/影响声明齐备）；关键事实经代码实测核对——ErrorCodes 无 403 码（读取原文）、GlobalExceptionHandler C/B→400（读取原文）、example-service 无 DELETE（读取原文）、WBS 网关归属 3.5.2（Select-String 行号 288）、依赖 3.5.16 存在（repo1.maven.org 实测）。
- 问题与教训：① Spring Security 上 classpath 即触发 Boot 默认接管全部端点——公共组件引用安全依赖必须评估"未开启服务"的连带影响（本轮以 optional 化解，写入差异声明供评审复核）；② 演示规则要兼容既有已通过测试：GET 列表加权限标注会波及 2.4.4 两处用例，影响已在"变更影响声明"显式登记而不是编码时偷偷改测试。
- 停机原因：【等待人工确认】（PO 签署 hifi 定稿；上下文用量以任务窗口显示为准，本次自估约 4 万 tokens，远低于 50% 停机线）。
- 交接提醒：① 依赖核验与登记已闭环，实现会话只需复核不必重查；② hifi"编码会话实施清单"六步即实现会话的工作分解，勿重排；③ 评审子智能体串行派发（沿 2.4.4/2.4.5 两连教训）；④ example-service 两处既有测试需补上下文头（GreetingControllerTest、AuditTrailIntegrationTest），改的是用例准备而非行为契约。
