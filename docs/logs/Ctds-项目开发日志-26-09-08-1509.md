# Ctds-项目开发日志-26-09-08-1509

- 任务卡 / 需求编号：【WBS 2.4.5（common-鉴权框架）——编码会话：实现 + 4 视角评审 + 附带 2.4.4 审计滚动 S2 缺陷修复】
- 关联规格 / 剧本：【高保真契约 = docs/designs/WBS-2.4.5-hifi.md（PO 已签署 2026-09-07 11:17；本会话新增"评审修复记录"增补节）；契约固化 = ADR-005 §3 第 7 项（本会话写入）；依赖登记 = docs/dependencies.md（resource-server 3.5.16 optional / reactor-core / spring-webflux test，均 PO 预授权口径）；无验收剧本（基建组件，演示=交付说明表 4 用例，已实测通过）】
- 会话目标：【承接 26-09-07-1110：PO 签署高保真 → 按 hifi 实施清单 6 步编码 → 门禁 → 4 视角评审（串行）→ 修复 → 演示实测 → 本日志，停机待业务验收】
- 状态：【已完成待验收——等 PO 按演示剧本验收；验收通过后随任务分支提交入库】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. 实现 common-auth 全量：服务侧（AuthContext/AuthContextFilter/RequirePermission/PermissionInterceptor/AccessControl/RolePermissionMapper 可插拔/AuthAdvice 二值映射）+ 网关侧（GatewayAuthAutoConfiguration fail-fast 安全链、JwtDecoder 二选一密钥源+≥32 字节强度闸、原因归类 MappingJwtDecoder+ClaimValidator 60s 偏移、信任边界剥剥离/注入两过滤器）+ example-service 三结果演示接入（DELETE 端点+两注解+yml 映射 user/admin）；
  2. ErrorCodes 增量登记 FORBIDDEN(1000C0005)、`ApiResult.currentTraceId()` 公共方法沉淀（评审③）、ADR-005 §3 第 7 项鉴权契约固化；
  3. 4 视角评审串行完成并全部处置：①规格符合性（1P1+2P2+4P3）、②安全供应链（1P2+5P3，含 optional 生效的 dependency:tree 机器证明）、③一致性重复（1P2+6P3）、④测试质量（1P1+2P2+9P3）——P0/P1 全修，P2 全修或登记，P3 择要修复、其余登记为后续改进（全部留痕见 hifi"评审修复记录"节）；
  4. 附带缺陷修复（非 2.4.5 范围，由其测试暴露）：**2.4.4 审计按天滚动原以写线程消费时刻定日**，队列积压/跨天竞态会致事件落错日文件且测试偶发红（本会话两次复现后定位）→ 改为**入队时刻按事件 eventTime 定稿日文件名**（AsyncFileAuditRecorder.Line 携带 day），跨天用例转为确定性；
  5. 门禁 run-gates.ps1 最终 **GREEN（PASS=7 FAIL=0，unitTest 128/128）**，密钥扫描 0 命中；
  6. 验收演示 4 用例实测通过（2026-09-08 15:07）：user 查 200 / user 删 403(1000C0005) / 无头查 401(1000C0002) / admin 删 200；当日审计文件（logs/audit-2026-09-08.jsonl）三条事件齐全（create SUCCESS / rbac.check DENIED actor=u-1 / greeting.delete SUCCESS actor=a-1 真实身份）。
- **下一步**：PO 业务验收（照交付说明演示表操作，或复跑同等命令）。验收通过后：第一个动作 = 用 WSL git 将本任务全部变更按提交规范 commit（`feat(C-2.4.5): <摘要>`，中文信息用 `commit -F UTF-8 文件`）；随后关闭会话。若 PO 提出打回：按打回意见在 hifi 确认记录登记轮次 2 并修复。下一任务卡 = WBS 2.4.6 common-国密封装（另起新会话冷启动）。
- **关键决策**：
  1. **附带 2.4.4 缺陷修复入本会话（登记留痕，不扩范围）**：门禁红灯必须修复后重交、不存在特批通道（章程 2.6）；缺陷定性=数据一致性（事件落错日文件）S2，修复口径=入队定日，hifi"评审修复记录"末条与交付说明均已声明，PO 若认为应拆分独立任务卡可打回本条；
  2. **链级 E2E 测试不经 ReactiveWebApplicationContextRunner**（评审④P1-1）：该 runner 下 `@ConditionalOnWebApplication(REACTIVE)` 不成立（实测诊断），改为直调自动配置 Bean 方法构造真实安全链 + WebFilterChainProxy 依 Ordered 串 strip/链/inject 的真实管线跑两条端到端（合法放行注入 / 无令牌 401 封套）+ 链过滤器构成断言钉住粘合装配；
  3. **fail-fast 取代条件装配**（评审①P1-1）：网关开关开而 ServerHttpSecurity 缺失=配置错误，启动失败并给出明确原因，杜绝"静默跳过校验链"这一鉴权红线旁路面；
  4. **AuthException 子类化 + 构造限两码**（评审①P3-1/P3-2）：BizException 语义不变、鉴权错误映射由组件自带 advice 完成，errorcode 既有 400/404/405 行为零改动；
  5. **观察项登记（不在本任务处理）**：HS256 弱密钥熵检测归 KMS 下发策略（3.9.1 前提）、链上阻塞解码改 boundedElastic 归 3.5.2、网关生产日志级别 ≥INFO 写入部署基线、roles-claim 配置化与时间边界测试用例补充——全文见 hifi"评审修复记录"；
  6. 上下文用量自估约 15 万 tokens（模型窗口口径以任务窗口显示为准），远低于 50% 停机线；本会话含 4 轮评审+修复属正常 1 天任务卡工作量，未越单任务纪律（全程 WBS 2.4.5 一张卡）。
- **文件指针**：
  - 本任务代码：`common/auth/**`（主 19 类+imports+48 用例）、`services/example-service/**`（controller/service/repository/yml/pom/测试）、根 `pom.xml`、`common/errorcode/**`（ErrorCodes+ApiResult+GlobalExceptionHandler）、`common/logging/**`（AsyncFileAuditRecorder 缺陷修复+测试）
  - 本任务文档：`docs/designs/WBS-2.4.5-hifi.md`（评审修复记录节）、`docs/adr/ADR-005-API规范与网关.md`（§3 第 7 项）、`docs/dependencies.md`（2.4.5 三行）
  - 此前日志：`docs/logs/Ctds-项目开发日志-26-09-07-1110.md`（高保真落盘会话）、同日链 …→2332→0012→1110→本篇
- **补正说明**：无。

## 任务记录（一次成文，落盘核对后即为定稿）

### 任务记录 ｜ 26-09-08 11:20–15:09 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（Qwen3.8-Flash-Next）
- 做了什么：PO 签署高保真后同会话续接编码（章程 2.6.3 口径）→ 契约先行（错误码+ADR 第 7 项）→ 组件实现 → 测试 48 用例 → example 接入 → 门禁两轮修复 → 4 视角串行评审（共 4P1+5P2+24P3 全部处置）→ 附带 2.4.4 审计定日缺陷修复 → 门禁 GREEN + 演示实测。
- 验证结果：`mvn -B -ntp test` 全仓 128/128 绿（errorcode14/pagination11/logging30/auth48/example25）；`checkstyle:check` 零违规；run-gates.ps1 最终 GREEN（PASS=7 FAIL=0 PENDING=7 为未接入工具链，coverage 达标声明待 JaCoCo，见 hifi 记录④P2-2）；`dependency:tree` 证明 example-service 运行时 classpath 无 spring-security（optional 生效）；演示 4 用例+审计三事件实测在案（2026-09-08 15:07）。
- 问题与教训：① **PowerShell 文本替换不带 -Encoding 会把 UTF-8 中文文件读写损坏**（Set-Content 按 ANSI 读），本会话一次事故致测试源混入 GBK 字节、javac 静默替换 U+FFFD、断言假性失败——修复文件删除重建，教训：中文文件一律走编辑器工具或显式 -Encoding UTF8；② 单模块 `-o -pl` 测试会用本地仓库旧 jar，上游增量改动后必须重 install 或 `-am`（NoSuchMethodError 教训）；③ 评审②揭示"条件装配静默失效"与"安全链 fail-fast"是鉴权组件生死线，此类 fail-closed 原则应进后续组件设计检查表；④ 时间竞态缺陷（写时刻定日）潜伏 2.4.4 多时，根因是"异步组件的语义归属时刻（事件时间）vs 处理时刻"未在设计期显式区分——高保真边界值表今后应写明。
- 停机原因：【等待人工验收】（上下文自估约 15 万 tokens，窗口权威值更低处为准；本会话任务卡工作项已全部完成）。
- 交接提醒：① 验收通过后**先 WSL git 提交入库再关会话**（日志+代码同库，AGENTS 6）；② 评审遗留改进清单（roles-claim 用例、nbf 边界、normalize 合并、ADR 记法统一、KMS 弱密钥策略、3.5.2 反应式调度与日志级别基线）已全量登记 hifi"评审修复记录"，勿在 2.4.6 会话顺手处理（单任务纪律）；③ 3.9.1 规格任务卡需澄清"令牌签发归属"（lofi 观察项 2 承接不变）。
