# Ctds-项目开发日志-26-09-08-2330

- 任务卡 / 需求编号：【WBS 2.4.6（common-国密封装）——编码会话：实现 + 4 视角评审 + 修复 + 演示实测】（续接日志 26-09-08-1548，PO 已手签高保真）
- 关联规格 / 剧本：【高保真契约 = docs/designs/WBS-2.4.6-hifi.md（PO 手签 2026-09-08 15:53，含本会话新增"评审修复记录"节）；契约固化 = docs/adr/ADR-006-国密加密封装契约.md（本会话新建）；依赖登记 = docs/dependencies.md（bcprov-jdk18on 1.85.2）；无验收剧本（基建组件，演示=交付说明 5 项实测通过）】
- 会话目标：【承接 1548：PO 签署 hifi → 三重独立验证合规值 → 组件实现 → 测试 → example 演示接入 → 门禁 → 4 视角评审（串行，含两次限流重试）→ 修复 → 演示实测 → 本日志，停机待 PO 业务验收】
- 状态：【已完成待验收——等 PO 按交付说明验收；验收通过后随任务分支提交入库（合并策略见交接提醒⑤）】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. **合规测试值三重独立验证（B8 引文核对义务的执行留痕，评审②P2-2 要求的溯源材料）**——因本机网络不可达标准原文站点，采用三独立实现交叉计算并全部一致后锁定（防凭记忆臆造密码学数值）：
     - ① **OpenSSL 3.5.5**（WSL）：`printf 'abc' | openssl dgst -sm3` → `66c7f0f462eeedd9d1f2d46bdc10e4e24167c4875cf2f7a2297da02b8f4ba8e0`；`(printf 0123…3210; head -c 15999984 /dev/zero) | openssl enc -sm4-cbc -K 0123…3210 -iv 0 -nopad | tail -c16` → SM4 百万次迭代链值 `595298c7c6fd271f0402f804c33d3f66`；SM4 单块 `681edf34d206965e86b3e94f536e4246`；
     - ② **Python gmssl**（WSL venv 独立安装）：SM3("abc")/SM3(abcd×16)/SM4 单块 与 ① 一致；纯迭代 1e6 次链值 `595298c7c6fd271f0402f804c33d3f66` 与 ① 一致；
     - ③ **BouncyCastle 1.85.2**（SmCheck 临时 Java 程序，依赖已从官方仓库解析）：三值全部与 ①② 一致；
     - **关键发现**：AI 记忆中的"百万次迭代值尾段 040ee81d666e97e6"与三重计算值不符——按 B8 纪律"信计算不信记忆"，以三重一致计算值 `0402f804c33d3f66` 为测试断言，教训登记（见问题与教训①）；SM2 附录固定 k 向量同因原文不可达**不凭记忆写入**，登记 4.3.3 前置补齐义务（ADR-006 §7）；
  2. 组件实现 `common/crypto`（14 个主类文件）：SM4/GCM 密文信封（CTSE 魔数+版本位+IV12+GCM16）、SM2（C1C3C2、签名 DER、密钥参数传入）、SM3（空输入语义统一）、KeyProvider+LocalFileKeyProvider（只读不回写、fail-fast）、CryptoErrorCodes（1001 段四码）、两处 fail-fast（enabled=false 全不注册 / key-file 配置缺失启动失败）、keyRef 字符集限制、字符串入口先校验后分配；
  3. example-service 保密备注演示（B11）：SecretNote 域/仓储/内存实现/应用服务/Controller 四端点（POST 存 / GET 读 / GET envelope / POST tamper）+ DemoKeyProviderConfig（模式 A 的 Bean 覆盖实例，演示密钥运行期随机、零入库）+ 集成测试 6 用例；
  4. 4 视角评审**串行**全部完成并处置（沿 2.4.4 教训：并行 2 发遭 Provider 限流失败后改全串行重试）：①规格符合性（P0=0 P1=0 P2=3 P3=5，有条件通过）、②安全供应链（P0=0 P1=0 P2=4 P3=7，需修复后发布）、③一致性重复（P0=0 P1=1 P2=2 P3=3，有条件通过）、④测试质量（P0=0 P1=1 P2=3 P3=6，有条件通过）——全部发现项的处置留痕见 hifi"评审修复记录"节（P0/P1 全修，P2 全修或登记，P3 择要修复其余登记）；
  5. 修复要点（评审驱动）：①P2-1 64MiB 边界往返不对称（解密上界改信封口径，补恰为上限往返用例）；②P2-1 字符串入口 64MiB 旁路（先长度校验后分配，含多字节文本）；②P3-1 keyRef 字符集 `[A-Za-z0-9._-]`；④P1-1 拒绝用例统一断言"文案=服务端常量"（不含敏感内容）；④P3-2/3/5 SM2 130 字符非 hex 路径、信封长度硬编码 33 钉桩、垃圾签名返回 false；③P1 SM3 空输入语义统一放行并留痕；
  6. 门禁最终 **GREEN（PASS=7 FAIL=0 PENDING=7 为未接入工具链；密钥扫描 0 命中）**；全仓测试 171/171 绿（errorcode14/pagination11/logging30/auth48/crypto36/example32）；
  7. 演示实测 5 项全部通过（2026-09-08 真实启动服务，HTTP 实测）：①POST 写入返回 id ②GET envelope 为 `CTSE` 前缀密文（非明文）③GET 读回原文一致 ④同文两次入库密文互异（随机 IV）⑤tamper 后 GET → 400 `1001C0002`"数据校验未通过，已拒绝"；另实测 SM4 吞吐：1 MiB 加密 ~18.7ms（~53 MiB/s）、解密 ~21.4ms（~47 MiB/s）（开发机单线程，hifi 性能声明兑现）。
- **下一步**：PO 业务验收（照交付说明 5 用例操作，或复跑同等命令）。验收通过后：第一个动作 = 用 WSL git 按提交规范 commit（中文信息用 `commit -F UTF-8 文件`）；随后关闭会话。若 PO 提出打回：按打回意见在 hifi 确认记录登记轮次 2 并修复。下一任务卡 = WBS 2.4.7 common-幂等与分布式锁（另起新会话冷启动）。
- **关键决策**：
  1. **合规值信计算不信记忆**（B8 核心执行）：三独立实现（OpenSSL/gmssl-py/BC）交叉一致的值才可作测试断言；AI 记忆与计算冲突时以计算为准并留痕；标准原文不可达 → SM2 附录向量登记 4.3.3 前置补齐（ADR-006 §7），交付说明不得宣称 B8 全绿；
  2. **bcprov 以 compile 传递依赖为有意决策**（评审②P2-3）：common-crypto 即"全平台唯一加解密入口"，引入即使用、凡引必带算法库，与 2.4.5 resource-server 用 optional（因 auth 有"只服务侧"消费面）场景不同；出现"引 crypto 不用国密"消费方再改 optional；
  3. **enabled=false 全禁用 fail-fast 为有意决策**（评审③P2-2）：加密组件"静默跳过=以为加密其实没加密"是红线旁路面，沿 2.4.5 教训，与 auth/audit 型"部分禁用"语义并存，登记两种语义待公共文档收敛；
  4. **S 码对外文案口径待 PO 裁决**（评审①P2-3）：契约错误码表所载 S 码文案仅存日志，出站经 GlobalExceptionHandler 统一替换为"系统繁忙，请稍后重试"（B9 要求 errorcode 零改动，实现忠实执行后者）——请 PO 在验收时确认该口径；
  5. **门禁配置零改动**（评审②P2-4）：secretsScan 无引号密钥行正则属门禁配置（红线 3：变更须走架构变更流程），以观察项登记待流程放行；已用 `.gitignore` 增 `*.keys/*keys*.properties/*keyfile*` 从仓库侧兜底；
  6. 上下文用量自估约 25 万 tokens（模型窗口权威值以任务窗口显示为准），低于 50% 停机线；本会话含 4 轮评审+修复属 2 人天任务卡正常工作量，全程 WBS 2.4.6 一张卡。
- **文件指针**：
  - 本任务代码：`common/crypto/**`（主 14 类 + 测试 7 类 36 用例）、`services/example-service/**`（SecretNote 系列 + DemoKeyProviderConfig + 集成测试）、根 `pom.xml`（模块+bcprov 锁版）、`.gitignore`（密钥文件拦截）
  - 本任务文档：`docs/designs/WBS-2.4.6-lofi.md`（已确认）、`docs/designs/WBS-2.4.6-hifi.md`（PO 手签 + 评审修复记录节）、`docs/adr/ADR-006-国密加密封装契约.md`（新建，含备选/可替换性节）、`docs/dependencies.md`（bcprov 行）
  - 此前日志：`docs/logs/Ctds-项目开发日志-26-09-08-1548.md`（设计会话）、同日链 …→1110→1509→1537→1548→本篇
- **补正说明**：无。

## 任务记录（一次成文，落盘核对后即为定稿）

### 任务记录 ｜ 26-09-08 15:53–23:30 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（Qwen3.8-Flash-Next）
- 做了什么：PO 手签 hifi 后同会话续接编码（章程 2.6.3 口径）→ 三重独立实现交叉验证合规值 → 组件实现 + 36 组件用例 → example 保密备注演示 + 6 集成用例 → ADR-006 新建 → 门禁 GREEN → 4 视角评审串行（2 发遭限流后全串行，共 4P1+12P2+21P3 全部处置）→ 修复 9 处 → 门禁复绿 → 演示 5 项实测 + 吞吐基准。
- 验证结果：`mvn -B -ntp test` 全仓 171/171 绿；`checkstyle:check` 零违规；run-gates.ps1 最终 GREEN（PASS=7 FAIL=0 PENDING=7 为未接入工具链，coverage 达标声明待 JaCoCo 沿 2.4.5 口径）；密钥扫描 0 命中；演示 5 用例 + 吞吐基准实测在案（2026-09-08）。
- 问题与教训：① **AI 记忆中的密码学标准数值不可信，且与三重独立计算值实际冲突过（SM4 百万次迭代尾段）**——"信计算不信记忆"应作为密码学场景的通用纪律（本次以 B8 引文核对义务兜住，教训已登记 ADR-006 §7 与测试注释）；② **评审子智能体并行 2 发即触发 Provider 限流，全串行后稳定**——今后评审一律单发串行，勿并行（2.4.4 教训的再次验证）；③ **字符串入口的"先校验后分配"要按字符数预算而非字节数**（UTF-8 多字节字符可绕过字节级预检），评审④P2-2 抓到后改为 MAX/3 字符预算 + 独立 base64 上界；④ 门禁 adrFieldsCheck 按"含词匹配"校验字段（背景/决策/理由/备选/业务影响说明/影响范围/可替换性），ADR-006 首版缺"备选/可替换性"两节被红灯打回——新 ADR 模板应含这两节。
- 停机原因：【等待人工验收】（上下文自估约 25 万 tokens，窗口权威值更低处为准；本会话任务卡工作项已全部完成）。
- 交接提醒：① 验收通过后**先 WSL git 提交入库再关会话**（日志+代码同库，AGENTS 6）——本会话变更量大（约 40+ 文件），建议按 4 笔提交：feat(common-crypto) / feat(example-service) / docs(adr)+docs(design)+dependencies / docs(logs)，均以 `commit -F UTF-8 文件` 提交中文信息；② 分支 `feat/C-2.4.6-common-crypto` 已含设计会话提交，验收后按 AGENTS 6 提交规范收尾（合并回 main 的策略属编排师裁决，勿自行合并）；③ 遗留登记：SM2 附录向量 4.3.3 补齐、S 码文案口径待 PO 裁决、secretsScan 无引号正则待变更流程、enabled 两种开关语义待公共文档收敛、DemoKeyProviderConfig 的 KEY_REF 待 2.6.3 下沉 domain、common-errorcode 传递 web 栈待组件卫生库——均不得在 2.4.7 会话顺手处理（单任务纪律）；④ 演示密钥运行期随机、重启即失效，PO 复测需在同一运行窗口内完成存取往返；⑤ 交付说明（业务可读）已随本日志附录，验收时按 5 用例操作即可。
