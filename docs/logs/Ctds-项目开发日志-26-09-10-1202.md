# Ctds-项目开发日志-26-09-10-1202

- 任务卡 / 需求编号：【Git 环境治理——编排师两项指示执行会话】（承接日志 26-09-10-1117：推送成功收尾；编排师后续指示：①永久移除全局 gitclone 配置 ②WSL git 日常访问 GitHub 改走 Windows git）
- 关联规格 / 剧本：【编排师会话指示两项；AGENTS 红线（不擅改全局配置——本次为明确指示 + 备份留痕）】
- 会话目标：【执行两项指示；验证生效；落盘收尾】
- 状态：【两项均完成并验证通过——①全局 gitclone 改写与凭据段已永久移除（备份 .gitconfig.bak-gitclone-removal）；②WSL 交互 shell 的 git 已包装为经 PowerShell 调用 Windows git（bashrc 函数 + C:\Users\Melody\AppData\Local\gitwrap.ps1），github 访问实测通（ls-remote 显示 main=da97bc9，与本地一致）】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. **gitclone 永久移除**：`%USERPROFILE%\.gitconfig` 删除 `[url "https://gitclone.com/github.com/"] insteadOf = https://github.com/` 与 `[credential "https://gitclone.com"] provider = generic` 两段；其余配置（windows/safe/http/sslverify=false/postBuffer/lfs）原样保留；验证 CLEAN；备份 = `.gitconfig.bak-gitclone-removal`；
  2. **WSL git → Windows git 包装**：hermes 用户 `~/.bashrc` 追加 `git()` 函数——`$PWD` 在 `/mnt/*` 下时经 `powershell.exe -File C:\Users\Melody\AppData\Local\gitwrap.ps1 -Repo <win路径> "$@"` 调用 Windows git（gitwrap.ps1 强制 `$env:HOME=$env:USERPROFILE` 防 WSL HOME 导致 git 读 `\\wsl.localhost` UNC 挂起）；`/mnt` 之外路径 fallback WSL 原生 git；
  3. 验证（WSL 交互 shell）：`git --version` = 2.55.0.windows.5（Windows git）；`git ls-remote origin` 成功（main=da97bc9）；`git status -sb` = `## main...origin/main`。
- **下一步**：无（本任务收尾）。WSL 终端日常 `git push/pull/fetch` 即走 Windows git + Watt Toolkit 通道。后续任务 = WBS 2.4.8（另起新会话冷启动）。
- **关键决策**：
  1. 全局配置改动 = 备份-修改-验证（不留无备份的修改）；gitclone 移除基于编排师明确指示（镜像已 502 失效）；
  2. WSL 包装层选 powershell.exe（与已验证成功的 PowerShell 直调 git 路径等价）而非 WSL 直接 exec git.exe（实测网络挂起，根因未完全定位但被包装层规避）；
  3. 调试教训：`timeout 20 git ...` 会绕过 bash 函数直接 exec PATH 中的 WSL git——此前多次"挂起"实为 WSL 原生 git 连本机 127.0.0.1:443 黑洞（WSL DNS 转发应用 Windows hosts）所致，函数本身一直正常。
- **文件指针**：`C:\Users\Melody\.gitconfig`（gitclone 已移除）、`.gitconfig.bak-gitclone-removal`（备份）、`C:\Users\Melody\AppData\Local\gitwrap.ps1`（稳定位置，勿删）、`/home/hermes/.bashrc`（git 函数）；此前日志 26-09-10-1117（推送成功）、26-09-10-1050（网络排查）。
- **补正说明**：无。

## 任务记录（一次成文，落盘核对后即为定稿）

### 任务记录 ｜ 26-09-10 12:0x ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode
- 做了什么：永久移除全局 gitclone 配置（备份留痕+验证）；实现并验证 WSL git 经 PowerShell 包装走 Windows git（bashrc 函数 + gitwrap.ps1，HOME 强制防 UNC 挂起）；排障锁定"timeout 绕过函数"假象。
- 验证结果：`git config --global --get-regexp url` 无 gitclone（CLEAN）；WSL 交互 shell `git ls-remote origin` 成功（main=da97bc9）、`git --version`=2.55.0.windows.5。
- 问题与教训：① `timeout` 等外部命令会绕过 bash 函数（exec PATH 可执行文件）——验证 shell 函数必须直接调用或 source，否则测的是 PATH 里的程序（本次 WSL git 连 127.0.0.1:443 黑洞挂起 3 轮排查才定位）；② WSL interop 启动 Windows 程序会继承 WSL 环境（HOME=/home/hermes）——Windows git 读用户配置时访问 `\\wsl.localhost` UNC 可能挂起，包装层须强制 HOME；③ cmd/wsl/bash/powershell 多层引号转义不可靠——复杂命令一律 Write 脚本文件再执行（本次 3 次转义踩坑后固化为脚本文件方案）。
- 停机原因：【任务完成，正常关闭】（上下文用量以任务窗口显示为准）。
- 交接提醒：① WSL 终端 git 已走 Windows git（GitHub 正常），WSL 原生 git 仍存在（/usr/bin/git，未删除，非 /mnt 仓库与脚本内 `command git` 会用到）；② gitwrap.ps1 依赖 C:\Users\Melody\AppData\Local\gitwrap.ps1（勿删）；③ 环境观察项：WSL 直接 exec git.exe 的网络挂起根因未深挖（被包装层规避），如未来失效可沿 GIT_CURL_VERBOSE 文件重定向调试。
