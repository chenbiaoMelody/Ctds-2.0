# Ctds-项目开发日志-26-09-12-0105

- 任务卡 / 需求编号：【WBS 2.4.10 数据库迁移工具链】（承接日志 26-09-11-2337；本会话前半段完成 2.4.9 补审②③④收尾与合并推送，后半段经编排师明确指令在同一会话启动 2.4.10——与"单会话单任务"条款的冲突由编排师裁决并留痕于此）
- 关联规格 / 剧本：制度依据 =《C-TDS项目总体实施计划与WBS》2.4.10 产出定义（"迁移脚本规范、版本化管理、示例迁移"，1 天）+ ADR-001（MySQL 8 冻结）+ 设计 docs/designs/WBS-2.4.10-{lofi,hifi}.md（PO 预授权确认，2026-09-12）+ ADR-009（新建规范载体）
- 会话目标：【2.4.10 全流程：设计 → PO 确认 → 编码 → 真实演练 → 4 视角评审 → 处置 → 待验收】
- 状态：【开发与评审闭环完成（分支 feat/C-2.4.10-db-migration，3 提交 c62930e/c0e2e6c/3325f3d；默认 profile 44 测试 2 跳全绿 + checkstyle 0 违规 + 条件测试连 Docker MySQL8 实测 2/2 + 全量门禁 GREEN）；**待 PO 业务验收 + 编排师合并裁决**】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. **两级设计 + PO 确认**：lofi/hifi 一次确认制提交（812f92f）；PO 会话声明"computer-use 可授权、本机 Docker 可用"并要求按判断原则复评裁决项 → AI 复评 6 项均无业务影响分歧项，按预授权惯例代录确认记录（c62930e）；
  2. **编码实现**（c0e2e6c，15 文件 +310）：① ADR-009 数据库迁移规范（十条规则，含"备选/可替换性"节过 adrFieldsCheck）；② example-service 引入 Flyway 四件套（flyway-core/flyway-mysql 11.7.2 + starter-jdbc 3.5.16 + mysql-connector-j 9.7.0，全 Boot BOM 管理，dependencies.md 逐行登记）；③ 示例迁移 V1 建表/V2 灌数据 + 只读端点 GET /api/v1/demo-notes（四层分层合规）；④ **无库兼容机制**：默认 profile 排除 DataSourceAutoConfiguration（B3 兼容性决策——starter-jdbc 上 classpath 会激活数据源自动配置，既有集成测试曾因此 3 个失败，排除后恢复全绿）；⑤ 条件集成测试 FlywayMigrationTest（环境变量 CTDS_IT_MYSQL_URL 门控，无库自动跳过）；
  3. **Docker 真实演练（B9 四步全过）**：专用容器 ctds-mysql-demo（mysql:8.0，端口 3307，口令 ctds-demo 为一次性演练凭据）——①空库启动 V1/V2 依次应用（history 两行 success=1）②服务 mysql profile 启动 + curl 端点返回封套 2 行中文数据完好 ③重启 no-op（日志"Schema up to date. No migration necessary"）④篡改已应用脚本 → 启动失败报 checksum mismatch（fail-fast 实证）。Docker Desktop 经预授权由 AI 启动；
  4. **4 视角独立评审（串行，全部成功）**：①规格与设计符合性=通过（7×P3 文档同步级）②安全供应链=需修复后通过（P2×1+P3×4）③一致性重复=通过（7×P3）④测试质量=需修复后通过（P2×1+P3×3）；
  5. **评审处置**（3325f3d，8 文件 +53/-21）：**P2-1**（②）demo-notes 端点补 `@RequirePermission("demo.read")` + application.yml 演示权限映射同步授予（user/admin 均加 demo.read，与既有端点安全基线一致——示例服务是后续业务模板，缺口不能复制）；**P2-2**（④）条件测试补端点真实调用断言（200 + 2 行按 id 升序 + 标题契约 + 未认证 401/code 1000C0002 双向）；P3×14：ADR-009 三处措辞（history 表测试断言除外/main 源集不 import Flyway/生产 TLS 要求）、hifi 七处同步（封套 message 笔误更正、B5/B6/B7 口径、脚本契约、边界值标注）、dependencies.md 去"同上"逐行写实、DemoNoteView 改 Long id、target 演练残留清理；
  6. **验证闭环**：默认 profile 44 测试（2 跳：条件测试无库跳过）全绿 + checkstyle 0 违规 + 条件测试连真实 MySQL 2/2 绿 + 全量门禁 GREEN（PASS=7 FAIL=0，adrFieldsCheck 9 ADR 全过）。
- **下一步**：【①PO 业务验收（演示：容器 ctds-mysql-demo 已在跑（3307），`cd services/example-service && mvn spring-boot:run -Dspring-boot.run.profiles=mysql`（配 CTDS_DB_URL=jdbc:mysql://localhost:3307/ctds_demo?useSSL=false&allowPublicKeyRetrieval=true、CTDS_DB_USER=root、CTDS_DB_PASSWORD=ctds-demo）→ 浏览器/curl 访问 http://localhost:8080/api/v1/demo-notes（带 X-Ctds-Subject: u-1、X-Ctds-Roles: user 头）看 2 行迁移数据；默认 profile 启动验证无库照常起）；②验收通过后编排师裁决合并 feat/C-2.4.10-db-migration（3 提交）入 main 并推送 origin；③合并后下一任务 = 2.4.11 测试基建-集成测试（Testcontainers，可承接本任务条件测试升级与 out-of-order 覆盖）——另起新会话。】
- **关键决策**：
  1. **选 Flyway、不建 common 模块、默认 profile 无库兼容**——三项核心判定沿"技术栈平庸化/最小实现/保护既有演示"原则，PO 以"判断原则+预授权"下放（判定留痕 lofi"问题确认"节）；备选 Liquibase/强制接库均无业务可见差异或纯负面；
  2. **无库兼容的实现机制**：默认 profile 显式排除 `DataSourceAutoConfiguration`（application.yml 注释留痕），mysql profile 以 `exclude: ""` 覆盖清空——评审①独立反查 Boot 3.5.16 全部相关自动配置确认充分（JdbcTemplate/事务/Flyway 自动配置均 @ConditionalOnSingleCandidate(DataSource) 或等价条件，无遗漏激活）；配置契约表 message 笔误等 7 处文档级偏差由评审①抓出并已同步；
  3. **条件集成测试沿 2.4.7"真实中间件在配了它的环境跑"策略**：默认门禁环境无库自动跳过（44 跑 2 跳全绿），真实验证靠环境变量驱动的 Docker 实测（本次 2/2 绿）——2.4.11 Testcontainers 建成后升级为容器化自动执行（ADR-009 §5 升级路径已写明）；B5 端点断言在评审④指出"映射断裂"后已补入条件测试（P2-2）。
- **文件指针**：本日志；此前日志 26-09-11-2337（2.4.9 收尾）；分支 feat/C-2.4.10-db-migration = 740e778 → c62930e → c0e2e6c → 3325f3d；规范 docs/adr/ADR-009-数据库迁移规范.md；设计 docs/designs/WBS-2.4.10-{lofi,hifi}.md；依赖登记 docs/dependencies.md（Maven 表新增 4 行）；门禁报告 scripts/gates/reports/gate-report-20260912-004*.md（本地，gitignore）；评审返回原文见会话记录（①agent_7b6364de/②agent_a26dc2bb/③agent_19b21567/④agent_74bb1a2e，要点已摘录本日志）。
- **补正说明**：① hifi 原稿 FlywayMigrationIT 名称、B5 计划测试列、封套 message=null、脚本契约文案均为编码/评审期更正，更正过程已在 hifi 各处行内留痕；② 评审②曾发现 target/classes 残留演练篡改版 V2 副本导致条件测试一度 checksum 失败——根因是"还原源文件后未触发增量资源拷贝"，清 target 资源目录后恢复，教训见下。

## 任务记录（一次成文，落盘核对后即为定稿）

### 业务可读交付说明

- **完成了什么功能**：给平台建立了"数据库结构变更管理"的标准做法——以后每次改表都写一个带版本号的 SQL 文件，随代码一起评审，系统启动时自动按顺序执行并留痕，谁改过、改没改对、改到哪一版全部机器说了算，杜绝人工改表不可追溯的问题。规范已固化为 ADR-009；示例服务完成演示（建表→灌数据→查询接口），并做了四步真机演练（含"有人偷偷改了已执行的脚本，系统会拒绝启动"的安全演示）。原有演示功能零影响：不配数据库时服务照常启动。
- **如何演示**：见"下一步-①"，两条演示线（带库/不带库启动）+ 一个查询接口。
- **注意事项**：① 演练容器 ctds-mysql-demo（端口 3307，口令 ctds-demo）为本任务一次性凭据，仅限本机演示，验收后可删除（`docker rm -f ctds-mysql-demo`）；② 生产环境数据源必须启用 TLS（ADR-009 §5）；③ 验收剧本建议：无数据库节点，无需更新，如 PO 认可可增补"迁移演示"节点。

### 任务记录 ｜ 26-09-12 01:0x ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：承接 2337 日志 → 2.4.9 收尾（补审②③④+合并推送 740e778+分支清理）→ 编排师指令同会话启动 2.4.10 → WBS/ADR/环境调研（本机无 MySQL、有 Docker Desktop 未运行）→ 两级设计起草提交 → PO 预授权复评（6 项无业务分歧）→ 建分支编码 → starter-jdbc 激活数据源自动配置致既有测试 3 失败 → 默认 profile 排除 DataSourceAutoConfiguration 修复 → surefire 不拾取 *IT 命名 → 更名 FlywayMigrationTest → 启动 Docker Desktop（预授权 Start-Process，5 秒就绪，发现既有 sc-mysql 占 3306）→ 专用容器 3307 演练四步全过 → 依赖登记 4 项 → 全量门禁 GREEN → 提交 → 串行派发 4 视角评审（全成功）→ 统一处置 P2×2+P3×14 → 复验（44 跑 2 跳全绿 + 条件测试 2/2 + 门禁 GREEN）→ 提交 3325f3d → 日志落盘。
- 验证结果：默认 profile 44 测试（2 跳）全绿 / checkstyle 0 违规 / 条件测试连 Docker MySQL8 2/2 绿（迁移+端点双向）/ 全量门禁 PASS=7 FAIL=0 GREEN。
- 问题与教训：① **starter-jdbc 上 classpath 即激活 DataSourceAutoConfiguration**——"加依赖"不等于"零行为变化"，B3 兼容性必须靠显式排除 + 既有全量测试回归验证，评审①的独立反查（javap 逐类核注解）提供了"充分性"证据；② **surefire 默认不拾取 *IT 命名**——条件门控测试若叫 *IT 会在 mvn test 中静默消失形成"假覆盖"，命名 *Test + @EnabledIfEnvironmentVariable 才是 mvn test 闭环的组合；③ **演练篡改残留会以 checksum 失败的形式反噬后续验证**——target/classes 的资源副本不随源文件还原而自动刷新，篡改类演练后必须清构建产物；④ 演示服务加端点必须同步挂权限注解（②P2）——示例即模板，安全基线缺口会被后续业务复制；⑤ 本会话跨 2.4.9 收尾与 2.4.10 全程，自估上下文用量约 30%，仍在安全区，但印证"跨任务会话宜尽早拆分"教训（2.4.9 部分已在上一份日志闭环，跨度可接受）。
- 停机原因：【无停机——2.4.10 开发评审闭环完成，待人工验收节点】。
- 交接提醒：① 您的验收点 = 业务验收（演示步骤见上）→ 合并裁决（feat/C-2.4.10-db-migration 3 提交 fast-forward 入 main + 推送 origin）；② 验收通过后 2.4.10 关闭，下一任务 2.4.11 测试基建-集成测试（另起会话冷启动）；③ 容器 ctds-mysql-demo 验收后可删，Docker Desktop 由您决定是否关闭。
