# Ctds-项目开发日志-26-09-12-1625

- 任务卡 / 需求编号：【WBS 2.2.6 制品与镜像流水线（nightly 构建、镜像 tag、SBOM 产出；本地脚本版）】（承接日志 26-09-12-1316 续点"下一任务 2.5.1"；本会话冷启动时发现 2.5.1 的 WBS 前置 2.2.6 从未执行，AskUserQuestion 获编排师裁决"先补 2.2.6"，任务随即切换为本工作包）
- 关联规格 / 剧本：制度依据（基建任务，无 C-x.y 规格）=《C-TDS项目总体实施计划与WBS》2.2.6 产出定义 + ADR-001/002 + 章程 4.1；设计 docs/designs/WBS-2.2.6-{lofi,hifi}.md（PO 确认，2026-09-12，AskUserQuestion 即时选定"确认进入编码"；hifi 含 §7 补正说明两批）；规范 docs/adr/ADR-012-制品与镜像流水线规范.md（§7 验收意见待 PO 签署）
- 会话目标：【2.2.6 全流程：冷启动核对 → 编排师裁决 → 设计 → PO 确认 → 编码 → 实测 → 4 视角评审 → 处置 → 待验收】
- 状态：【**开发评审闭环完成，待 PO 业务验收与编排师合并裁决**（分支 feat/C-2.2.6-artifact-pipeline = 4cba882 → 1f6f05a → e917584，3 提交，基于 main=32dad3c）】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. **冷启动核对与前置裁决**：2.4.12 已关闭（main=32dad3c）；核查发现 2.5.1 的 WBS 前置 2.2.6 全库零完成记录（无 SBOM/nightly 产物、无 CI 平台配置、日志仅作为依赖提及）→ AskUserQuestion 裁决**"先补 2.2.6"**（本地脚本版，nightly 调度/远程推送能力受限部分登记 PENDING-CI）；2.5.1 顺延至后续会话；
  2. **两级设计 + PO 确认**（4cba882）：lofi 六项判定（本地脚本版/CycloneDX 双插件+2 新依赖/镜像 tag 三段式/ADR-012 载体/实跑验证/门禁先行全绿才打包）+ hifi 行为清单 N1–N7、契约表、边界值 B-1~B-7——AskUserQuestion 获编排师**即时选定"确认进入编码"**（确认记录已签署非代录）；
  3. **编码实现**（1f6f05a，11 文件）：scripts/pipeline/{nightly-build.ps1,image-tag.ps1,README.md} + ADR-012 + dependencies.md 登记 2 行（cyclonedx-maven-plugin 2.9.3 命令行调用不进生命周期、@cyclonedx/cyclonedx-npm 6.0.1 devDep）+ gates-config.json secretsExcludePaths 追加 build-output + .gitignore；
  4. **实测四轮迭代**（详见"问题与教训"）：第 1 轮解析错误（Markdown 反引号转义）→ 第 2 轮 N1 误报全缺失（EAP=Stop + stderr）→ 第 3 轮**假 PASS**（Write-Output 污染返回值 + 门禁崩溃 + vite stderr 假失败）→ 第 4 轮修复后 6 阶段全 PASS；B-3 失败即停双边界实测（N2 语法错误停 + N4 类型错误停，均退 1、PARTIAL 报告留现场）；SBOM 内容抽查命中已登记依赖（后端 bcprov-jdk18on、前端 element-plus）；提交后门禁独立复跑 GREEN（gate-report-20260912-160829）；
  5. **4 视角独立评审（串行，全部成功）**：①规格与设计符合性=需修复后通过（P2×1+P3×2）②安全供应链=通过（依赖双注册表独立复测核验一致、无注入面、secretsExcludePaths +build-output 判定不构成绕过；P3×2）③一致性重复=需修复后通过（P2×2 文档漂移+P3×3）④测试质量=通过（独立复跑 tag/重算 sha256/复核 SBOM 全对上，B-2/B-4 有自然证据；P3×3）；
  6. **评审处置**（e917584）：①P2 N3-2"可运行 jar"标注进汇总报告（实测验证生效）；③P2 hifi N5-2 措辞修正（两段独立 npx 命令）+ 输出目录契约补 logs/（ADR-012 §3.4 与 hifi §2 同步）；②P3×2 N4 失败路径补 Set-Location 消 cwd 残留 + SHA256SUMS 防篡改边界登记 ADR-012（PENDING-CI）+ connector-sdk 占位标注进报告；①③P3 G-3 同秒冲突序号首撞 -1 与设计对齐；③P3 N2-2 措辞（-ReportPath 直落盘等价复制）；④P3 B-7 lock 不同步假设留痕 pipeline README；**不采纳留痕**：③P3 Add-Stage/报告脚手架与 run-gates 同构——下次任一触碰时抽 scripts/common（登记 hifi §7）；
  7. **处置后复验**：nightly 全流程 6 阶段 PASS（N2 门禁 168s GREEN）+ tag 冒烟（1f6f05a-dirty，dirty 因处置提交未入时点属预期）+ 报告含新标注。
- **下一步**：【①PO 业务验收（演示：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts/pipeline/nightly-build.ps1` 一条命令约 5 分钟看 6 阶段 PASS + 打开 build-output/<最新时间戳>/nightly-report.md 看五件套清单 + 在 sbom/backend/bom.json 检索 bouncycastle 证明材料清单真实；**注意用最新时间戳目录，勿拿 20260912-153935（旧脚本"假 PASS"轮产物）演示**）；②验收通过后编排师裁决合并（feat/C-2.2.6-artifact-pipeline 3 提交基于 main=32dad3c，可 fast-forward 入 main + 推送 origin，走 Watt Toolkit + D:\Soft\Git 惯例）；③合并后下一任务 = 2.5.1 容器化部署模板（前置 2.2.6 本任务已补齐，另起新会话冷启动）；④2.4.10 遗留容器 ctds-mysql-demo 仍待您处置（与本任务无关）。】
- **关键决策**：
  1. **先补 2.2.6 再 2.5.1**（编排师裁决）：WBS 前置关系不被跳过；本地脚本版落地，nightly 定时调度/远程制品库推送/CI 触发登记 PENDING-CI 待 2.5.x 接入时平移——与 frontendE2E PENDING-CI 同口径；
  2. **SBOM 工具 = CycloneDX 双插件**：业界事实标准、官方维护、Apache-2.0、版本锁定入仓可复现；maven 插件**全坐标带版本命令行调用不进 build 生命周期**（避免每次 mvn package 都出 BOM 的开销，根 pom 零改动）；Syft 弃（需运行时拉外部镜像，供应链面大、版本不可锁定）；
  3. **镜像 tag 三段式** `ctds/<模块>:<版本>-<时间>-<git短哈希>[-dirty]`：同时回答哪个版本/何时构建/哪份代码；image-tag.ps1 为唯一规范出处（-All 供 2.5.1 消费）；connector-sdk 占位（ADR-002 独立仓库策略）；
  4. **门禁先行、全绿才打包**：nightly N2 子进程复用 run-gates.ps1 零行为改动，门禁已跑过的测试打包阶段 -DskipTests 跳过——不重复跑测试也不绕过门禁；
  5. **secretsExcludePaths 追加 build-output**（红线 3 敏感点，留痕充分）：实测发现 secretsScan 逐文件 ReadAllBytes 会读到 nightly 正在写的阶段日志（独占冲突崩溃误报），且对 gitignore 构建产物扫描只会假阳性——与 node_modules 排除先例（b7330f5）同口径，任何检测阶段行为不变，留痕 ADR-012 §5 + hifi §7；
  6. **验证方式 = 实跑演练 + 失败即停边界实测**（流程编排类无单测框架口径，与 2.2.2 门禁同惯例）：全流程 PASS 一轮 + B-3 双边界（N2/N4 各停对位置）+ SBOM 抽查命中 + sha256 重算一致。
- **文件指针**：本日志；此前日志 26-09-12-1316（2.4.12 关闭）；分支 feat/C-2.2.6-artifact-pipeline = 4cba882 → 1f6f05a → e917584；规范 docs/adr/ADR-012-制品与镜像流水线规范.md；设计 docs/designs/WBS-2.2.6-{lofi,hifi}.md（hifi §7 两批补正说明）；流水线 scripts/pipeline/{nightly-build.ps1,image-tag.ps1,README.md}；登记 docs/dependencies.md（+2 行）；门禁 scripts/gates/{gates-config.json（仅 secretsExcludePaths +build-output）,run-gates.ps1（零改动）}；门禁报告 scripts/gates/reports/gate-report-20260912-160829.md（本地 gitignore）；nightly 实测产物 build-output/20260912-155059（全流程 GREEN）/155723 与 160058（B-3 双边界 PARTIAL）/161937（处置后 GREEN，本地 gitignore）；评审返回原文见会话记录（①agent_7bf6eb2a/②agent_3d0f4dda/③agent_f9e8aa36/④agent_c369ef14，要点已摘录本日志）。
- **补正说明**：① 本任务承接路径与 1316 日志"下一任务 2.5.1"不同——冷启动核对发现 2.5.1 前置 2.2.6 未执行，经编排师裁决改为先补 2.2.6，属计划内顺延非擅自改范围；② 第一轮 nightly 曾产出"假 PASS"报告（20260912-153935，旧脚本三缺陷所致），该目录产物不可作验收证据，教训已固化进脚本注释与 hifi §7；③ 设计确认记录中 hifi 交付物清单"根 pom（+cyclonedx-maven-plugin）"在确认后修正为"根 pom 不改"（插件改命令行调用），属确认后技术路径细化，已留痕 hifi 状态行。

## 任务记录（一次成文，落盘核对后即为定稿）

### 业务可读交付说明

- **完成了什么功能**：给项目装了一条"出厂包装线"——一条命令（nightly-build.ps1，约 5 分钟）自动完成：先跑全套质量门禁（全绿才继续）→ 后端 8 个模块打包收 jar（可运行的 example-service 在报告里特别标注）→ 前端页面构建收产物 → 自动生成两份"第三方组件材料清单"（SBOM：后端、前端各一份，JSON+XML 双格式，附指纹对账文件）→ 按统一规则算出每个模块将来的镜像名字 → 出一份业务可读的汇总报告。这是信通院测评与供应链安全的硬要求，也是后续容器化部署（2.5.1）的地基。镜像命名规则固化为 ADR-012。
- **如何演示**：① 仓库根目录执行 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/pipeline/nightly-build.ps1` → 结尾看 `[NIGHTLY] PASS`；② 打开 `build-output/<最新时间戳>/nightly-report.md` → 眯环境信息、六阶段 PASS 表、34+ 文件产物清单；③ 用编辑器在 `sbom/backend/bom.json` 里搜 `bouncycastle`、在 `sbom/frontend/bom.json` 里搜 `element-plus`——都能搜到，证明"材料清单"真实列出了第三方组件；④ 验收剧本建议：增补"制品包装线演示"节点（hifi §5 有业务语言文案），既有节点无需改。
- **注意事项**：① 本地版能力边界：定时调度与远程制品库推送登记 PENDING-CI（待 2.5.x CI 接入平移），当前为手动一条命令；② build-output/ 目录为本地产物不入库，验收演示请用最新时间戳目录（20260912-153935 为修复前旧产物）；③ 本任务只定镜像命名规则并算名字，**不构建镜像**（Dockerfile 与 K8s 编排属 2.5.1）；④ 镜像名末尾 -dirty 表示"构建时代码有未提交改动"，提交干净后自动消失。

### 任务记录 ｜ 26-09-12 16:25 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：冷启动核对（发现 2.5.1 前置 2.2.6 未执行）→ AskUserQuestion 裁决"先补 2.2.6" → 建分支起草两级设计 → PO 即时确认 → 编码（nightly 六阶段+tag 工具+ADR-012+README+登记+gitignore）→ 实测四轮迭代（反引号转义→EAP=Stop stderr 陷阱→假 PASS 三缺陷→全绿）→ B-3 双边界实测 → 提交（4cba882/1f6f05a）→ 门禁独立复跑 GREEN → 串行 4 视角评审（全成功，②④独立复测通过）→ 统一处置 P2×3+P3 采纳项（e917584）→ 处置后复验全流程 PASS → 日志落盘。
- 验证结果：nightly 全流程 6 阶段 PASS（处置前后各一轮，N2 门禁 299s/168s 均 GREEN）/ B-3 双边界停对位置退 1 / image-tag 单模块+全清单退 0 格式合规（含 -dirty）/ SBOM 双生态抽查命中已登记依赖 + sha256 重算一致（评审④独立复测）/ 提交后全量门禁独立复跑 GREEN（gate-report-20260912-160829，PASS 无 FAIL）。
- 问题与教训：① **Markdown 反引号是 PowerShell 双引号字符串的转义雷**（"```" 与 `` `" `` 混用连环解析错，逐处改单引号）；② **PS 5.1 下 EAP=Stop + 原生命令 stderr = 终止性 NativeCommandError**（vite 构建成功却"失败"；探测统一改 cmd /c 内合并 stderr）；③ **Select-Object -First 1 会掐断原生命令管道使 $LASTEXITCODE=-1**（N1 曾全数误报，全量收割替代）；④ **函数内 Write-Output 污染返回值 → 布尔判定数组恒真 → fail-fast 静默失效**（第一轮"假 PASS"根因，进展输出一律 Write-Host——这条与 2.4.12 的"vi.waitFor 消时序脆弱"同级重要）；⑤ **门禁全仓扫描会撞上自家流水线正在写的日志**（独占冲突崩溃，以排除 gitignore 产物目录方式根治并留痕）；⑥ 失败即停测试要选"门禁放行、仅下游能抓"的缺陷（语法错误会被 lint 先抓，类型错误才到 N4）。
- 停机原因：【无停机——2.2.6 开发评审闭环完成，待人工验收节点；上下文自估约 45%（趋势参考，以任务窗口为准），接近停机线故本日志从速落盘】。
- 交接提醒：① 您的验收点 = 2.2.6 业务验收（演示见上，约 5 分钟）→ 合并裁决（feat/C-2.2.6-artifact-pipeline 3 提交基于 main=32dad3c，可 fast-forward 入 main + 推送 origin）；② 验收通过后 ADR-012 §7 验收意见表签署随合并提交入 main；③ 下一任务 = 2.5.1 容器化部署模板（前置 2.2.6 已补齐；其 WBS 另一隐含输入是各模块 Dockerfile，属该任务本体），另起新会话冷启动；④ 本会话上下文已接近 50% 停机线，**请在本会话完成验收裁决后关闭**，合并与推送若未完成则由新会话承接；⑤ 2.4.10 遗留容器 ctds-mysql-demo 仍待您处置。
