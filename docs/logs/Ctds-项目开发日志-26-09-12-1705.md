# Ctds-项目开发日志-26-09-12-1705

- 任务卡 / 需求编号：【WBS 2.5.1 容器化部署模板（各模块容器化 + K8s 编排模板）】（承接本会话前段任务 2.2.6（日志 26-09-12-1625）：2.2.6 已 PO 验收通过、fast-forward 合并入 main（bfb53d1）并推送 GitHub；编排师**显式授权同会话续做 2.5.1**（章程"单会话单任务"惯例豁免，本行即留痕），并确认窗口用量 14.9% 远低于 50% 停机线）
- 关联规格 / 剧本：制度依据（基建任务，无 C-x.y 规格）=《C-TDS项目总体实施计划与WBS》2.5.1 产出定义 + ADR-001 + ADR-012（2.2.6 镜像命名/产物契约）；设计 docs/designs/WBS-2.5.1-{lofi,hifi}.md（PO 确认 2026-09-12，AskUserQuestion 即时选定"确认进入编码"；hifi 含 §7 补正说明）+ ADR-013（规范载体）
- 会话目标：【2.5.1 全流程：前置核对 → 两级设计 → PO 确认 → 编码 → 实测 → 4 视角评审 → 处置 → 待验收】
- 状态：【**开发评审闭环完成，待 PO 业务验收与编排师合并裁决**（分支 feat/C-2.5.1-container-templates = b2aa67d → f0d5675 → dafe89d，3 提交，基于 main=bfb53d1）】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. **2.2.6 收尾**：ADR-012 §7 验收意见表签署"验收通过，合并" + 设计确认记录补验收行 → fast-forward 合并入 main（bfb53d1）→ 经 D:\Soft\Git 推送 origin 成功（Watt Toolkit 通道）——2.2.6 正式关闭；
  2. **2.5.1 前置核对**：deploy/ 现状（仅 sql/）、example-service 端口 8080 默认 profile 无库自足（2.4.10 B3）、前端纯静态、本机 kubectl v1.36.1 client-only、docker 29.7.2 可用、基础镜像直拉成功；
  3. **两级设计 + PO 确认**（b2aa67d）：lofi 六项判定（容器化对象=可运行 2 箱/Dockerfile 消费 2.2.6 产物不自带构建/基础镜像显式 tag 禁 latest/K8s 模板 4 份+占位符注入归 2.5.2/ADR-013 载体/单箱冒烟+离线校验集群演练归 2.5.2）——AskUserQuestion 获编排师**即时选定"确认进入编码"**（确认记录已签署非代录）；
  4. **编码实现**（f0d5675，15 新增文件）：后端 Dockerfile（temurin:17-jre + 非 root + MaxRAMPercentage=75 + COPY 预构建 jar）与白名单 .dockerignore；前端 Dockerfile（nginx:1.29-alpine + nginx.container.conf SPA fallback）与白名单；deploy/k8s/ 4 份 YAML（占位符/tcpSocket 探针/资源基线）+ kustomization.yaml + README；ADR-013；dependencies.md 新增"容器基础镜像"节（2 镜像登记）；
  5. **实测**：基础镜像直拉成功（未用镜像源）；双镜像构建成功（**uid 1000 被基础镜像默认用户占用 useradd 退 4，实测改 uid 1001** 并三处文档同步）；双容器冒烟全过（后端 404 应答/启动 2.4s/日志仅 curl 触发的预期 WARN；前端 200 + SPA /login 200）；`kubectl create --dry-run=client` 需连 API 行不通**实测改道 `kubectl kustomize` 离线校验**（kustomization.yaml 为新增交付物）4 资源全识别；全量门禁 GREEN（PASS=9 FAIL=0）；
  6. **4 视角独立评审（串行，全部成功）**：①规格与设计符合性=需修复后通过（P2×2+P3×1）②安全供应链=通过（docker inspect 实证非 root 生效、dockerignore 白名单无泄漏、try_files 无穿越风险；P2×1 与①重合+P3×3）③一致性重复=需修复后通过（P2×1 同 uid+P3×5）④测试质量=通过（**独立复跑双容器冒烟全过**+镜像 User 实证；P3×2）；
  7. **评审处置**（dafe89d）：①②③P2 dependencies.md temurin 行 uid 笔误更正 1001 并注明原因；①P2 hifi 补正说明留痕通配 jar 名（无 plain.jar 双匹配面评估维持通配）；①P3 backend limits 补 OOM 兜底注释；②P3 backend Deployment 补 `runAsNonRoot: true`；②P3 hifi B-7 措辞更正（apply 不报错、失败在 ImagePullBackOff）；③④P3 注入口径改"按 image: 字段行定位"+Deployment 注释去除占位符字面量防全文替换误伤；③P3 label 统一与 frontend 非 root **登记 2.5.2**；③P3 base/overlay 不采纳留痕；
  8. **处置后复验**：kubectl kustomize 退 0（占位符仅存 image: 字段行×2、runAsNonRoot 生效）+ 全量门禁 GREEN（PASS=9 FAIL=0）。
- **下一步**：【①PO 业务验收（演示：`mvn -B -ntp package -DskipTests` 与 `npm run build` 产物已在位可跳过 → `docker build` 两镜像（tag 用 scripts/pipeline/image-tag.ps1 输出）→ `docker run` 两容器 → 浏览器开 http://localhost:18081 看平台前端 + curl http://localhost:18080 有应答 → `docker ps` 双箱运行；验收剧本建议见 hifi §5）；②验收通过后编排师裁决合并（feat/C-2.5.1-container-templates 3 提交基于 main=bfb53d1，可 fast-forward 入 main + 推送 origin）；③合并后下一任务 = 2.5.2 一键部署工具链 v1（依赖 2.5.1，另起新会话冷启动——届时启用集群演练口径并处理 2.5.2 登记项：frontend 非 root/label 统一/占位符注入脚本）。】
- **关键决策**：
  1. **容器化对象口径=可运行制品 2 个**：common/* 是 jar 库由服务携带不单独成箱，connector-sdk 沿 ADR-012 占位——"各模块容器化"防膨胀解释，ADR-013 §3.1 固化；
  2. **Dockerfile=打包说明书非构建说明书**：容器内自构建需重新下载全部 Maven/npm 依赖（aliyun/npmmirror 配置不可进容器），与 2.2.6"流水线产构件、下游消费"契约同构；多阶段自构建留观察项随 CI 再评估；
  3. **基础镜像 eclipse-temurin:17-jre + nginx:1.29-alpine**：官方、显式 tag 禁 latest（沿 ADR-010 精神）、dependencies.md 新设"容器基础镜像"节登记（非 Maven/npm 依赖单独成节）；
  4. **探针 tcpSocket 而非 HTTP**：后端业务端点有鉴权（401），HTTP 探针假红；tcpSocket 只验端口监听，与 2.5.3 监控职责分离；
  5. **镜像注入=IMAGE_PLACEHOLDER 占位符**：kustomize overlay 弃（动态 tag 污染工作区，ADR-013 §2）；注入脚本归 2.5.2，注入口径=按 image: 字段行定位（评审查出的全文替换误伤面已在 YAML 注释层消除）；
  6. **K8s 校验离线化**：create --dry-run=client 需 API 连接（本机无集群），kustomize 纯离线可用且 kustomization.yaml 本就是主流交付物（2.5.2 可 apply -k）——校验口径降级留痕，schema 级校验顺延 2.5.2 集群演练。
- **文件指针**：本日志；此前日志 26-09-12-1625（2.2.6 关闭，同会话前段任务）；分支 feat/C-2.5.1-container-templates = b2aa67d → f0d5675 → dafe89d；规范 docs/adr/ADR-013-容器化部署模板规范.md；设计 docs/designs/WBS-2.5.1-{lofi,hifi}.md（hifi §7 补正说明）；容器 services/example-service/{Dockerfile,.dockerignore}、frontend/{Dockerfile,.dockerignore,nginx.container.conf}；编排 deploy/k8s/（4 YAML+kustomization+README）；登记 docs/dependencies.md（"容器基础镜像"节）；门禁报告 scripts/gates/reports/（本地 gitignore，处置后 GREEN 一份）；评审返回原文见会话记录（①agent_ff6ad537/②agent_71b37a81/③agent_6c34b686/④agent_4f9c3395，要点已摘录本日志）。
- **补正说明**：① 本日志会话内包含两个任务卡（2.2.6 收尾 + 2.5.1 全流程），2.2.6 部分以 26-09-12-1625 日志为准，本日志只记其收尾动作；同会话双任务系编排师显式授权（见任务卡行）。② 实测中 Dockerfile 曾用 uid 1000 失败（useradd 退 4），改 uid 1001 后构建通过，教训：**新版 eclipse-temurin（Ubuntu 底座）自带 uid 1000 默认用户 ubuntu**。

## 任务记录（一次成文，落盘核对后即为定稿）

### 业务可读交付说明

- **完成了什么功能**：给平台的两个"能跑起来"的软件各自写了"集装箱装箱说明书"——后端服务装进标准 Java 运行箱（非 root 运行、内存感知、自动随容器内存限配额）、前端页面装进标准网页服务器箱（刷新深链接不 404）；再写了一套 Kubernetes"机房摆放图纸"（每个箱子开几份、崩了自动拉起、机器不够怎么排队），模板齐全可通过离线校验，等下一任务把镜像名字填进去就能整体上架。规范固化为 ADR-013，两个基础镜像（Java 运行环境、网页服务器）已核验登记。
- **如何演示**：① 两个镜像已构建（tag 按 2.2.6 命名规则自动生成，含 -dirty 属本地预期）；② `docker run -d --name demo-f -p 18081:80 ctds/frontend:<tag>` → 浏览器开 http://localhost:18081 见平台页面，开 http://localhost:18081/login 刷新也不 404；③ `docker run -d --name demo-b -p 18080:8080 ctds/example-service:<tag>` → `curl http://localhost:18080/` 有应答（404 属正常，服务在应答）；④ `kubectl kustomize deploy/k8s` 输出 4 份资源——图纸可校验；⑤ 测完 `docker rm -f demo-f demo-b`。
- **注意事项**：① 装箱前须先有构建产物（后端先 mvn package、前端先 npm run build——即 nightly 流水线的 N3/N4 产物），否则 docker build 明确报错，属设计内防呆；② K8s 模板里镜像名是占位符，未注入不可直接部署（失败在调度侧 ImagePullBackOff），注入工具链属 2.5.2；③ 集群真实部署演练属 2.5.2 口径，本任务只做单箱实测+离线校验；④ 验收剧本建议：增补"容器化模板演示"节点（hifi §5 有文案），既有节点无需改。

### 任务记录 ｜ 26-09-12 17:05 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：2.2.6 收尾（签署→合并 bfb53d1→推送）→ 编排师授权同会话续做 2.5.1 → 前置核对 → 建分支起草两级设计 → PO 即时确认 → 编码（Dockerfile×2+dockerignore 白名单+nginx conf+K8s 4+1 份+README+ADR-013+基础镜像登记）→ 实测（uid 1000 被占退 4 改 1001、双镜像构建、双容器冒烟、kustomize 离线校验改道）→ 提交 b2aa67d/f0d5675 → 门禁 GREEN → 串行 4 视角评审（全成功，②④独立复测含复跑容器冒烟）→ 统一处置（dafe89d）→ 复验全绿 → 日志落盘。
- 验证结果：双镜像构建成功（tag 符合 ADR-012 三段式含 -dirty）/ 双容器冒烟全过（本会话实测+评审④独立复跑双确认）/ kubectl kustomize 离线校验 4 资源退 0（处置前后各一轮）/ 全量门禁 GREEN（PASS=9 FAIL=0，处置前后各一轮）/ 基础镜像直拉成功。
- 问题与教训：① **新版 eclipse-temurin 镜像自带 uid 1000（ubuntu 用户）**——useradd 显式 uid 1000 必退 4，非 root 用户改用 1001；② **kubectl create/apply --dry-run=client 仍需 API 连接做资源映射**（无集群时不可用），纯离线校验用 kubectl kustomize；③ **Dockerfile 双阶段内 echo $? 被 tail 管道吃掉**（build 失败但 echo 0，应 set -o pipefail 或不用管道收尾）——实测时已用镜像存在性二次确认兜住；④ 注入式占位符的注释里不要出现占位符字面量（防工具全文替换误伤），契约写"按字段行定位"。
- 停机原因：【无停机——2.5.1 开发评审闭环完成，待人工验收节点；上下文自估约 32%（趋势参考，以任务窗口为准——编排师口径 14.9%+本任务增量），安全区】。
- 交接提醒：① 您的验收点 = 2.5.1 业务验收（演示见上，约 3 分钟）→ 合并裁决（feat/C-2.5.1-container-templates 3 提交基于 main=bfb53d1，可 fast-forward 入 main + 推送 origin，走 Watt Toolkit + D:\Soft\Git 惯例）；② 验收通过后 ADR-013 §7 验收意见表签署随合并提交入 main；③ 下一任务 = 2.5.2 一键部署工具链 v1（依赖 2.5.1），另起新会话冷启动——届时处理本任务登记项：frontend 非 root 硬化、label 取值统一（ctds-frontend vs frontend）、占位符注入脚本、集群部署演练；④ 2.4.10 遗留容器 ctds-mysql-demo 仍待您处置。
