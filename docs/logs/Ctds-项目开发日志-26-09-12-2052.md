# Ctds-项目开发日志-26-09-12-2052

- 任务卡 / 需求编号：**WBS-2.5.2 一键部署工具链 v1**（任务行：docs/C-TDS项目总体实施计划与WBS.md L214——"部署脚本 + runbook（开发/测试环境演练通过）"，依赖 2.5.1 已满足；1 会话）
- 关联规格 / 剧本：上级规范 ADR-012（镜像命名）、ADR-013（容器化模板，本任务补正为 §8 五条）；设计 docs/designs/WBS-2.5.2-lofi.md（七判定）/-hifi.md（行为清单 A/B/C/D + 契约 + 边界值 + §七实测补正五条）
- 会话目标：一键部署脚本 + runbook + 2.5.1 登记项五项收尾（frontend 非 root/label 统一/注入脚本/集群演练/OpenAPI schema 校验），开发/测试环境演练通过
- 状态：【**开发+评审+处置+复验全部完成，待 PO 业务验收与合并裁决**；终态部署保持运行（报告 204926），演示入口 http://localhost:30081 / :30080】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. **冷启动核对**：2.5.1 交付物（deploy/k8s 占位符口径、image-tag.ps1 契约）、环境探测（Docker 29.7.2 + kubectl v1.36.1 client-only 无集群）；
  2. **两级设计一次确认**（063dd0b）：lofi 七判定 + hifi 行为清单，AskUserQuestion 获编排师"确认进入编码"（含 D2 开启 Docker Desktop 内置 K8s 授权）；
  3. **编码**（4e1c08e 等 7 提交）：scripts/deploy/deploy.ps1（自检/副本行级锚定注入/NodePort 副本 patch/镜像装载/服务端 schema 校验/apply+rollout/分离式 port-forward/冒烟/业务可读报告/-Teardown）+ 两份 README + runbook + frontend 非 root 三件套（nginx.main.conf 全主配置 /tmp 收敛 + listen 8080 + USER 101）+ K8s 模板修订（label 统一 ctds-backend/ctds-frontend、frontend 8080、runAsNonRoot）+ ADR-013 §8 补正；
  4. **集群开通**：settings-store.json 键名 `KubernetesEnabled`（首试 `EnableKubernetes` 被 4.88 判 unknown，WebSearch 查证后改键名 + `docker desktop start`）；desktop-control-plane Ready（v1.36.1，kind 模式）；
  5. **实测演练链 9 轮**（细节见"任务记录"）：5 个设计外边界逐一实测暴露并修复（分钟 tag 回退/-dirty 哈希解析/kind 镜像隔离/USER 数字化/NodePort 不映射），后 3 轮全链 PASS + 幂等 PASS + Teardown PASS；
  6. **4 视角串行评审**：①规格与设计符合性=需修复后通过（P2×2+P3×8）②安全供应链=通过（P3×4，独立复测含 docker build --check 与线上核验）③一致性重复=需修复后通过（P2×1 与①重合+P3×5）④测试质量=需修复后通过（P2×3+P3×3，独立复测 curl/pods/id/镜像 tag/锚定计数/门禁报告全过）；
  7. **统一处置**（7 文件 +56/-24）：P2 全修（ADR-013 §8 补第 5 条访问口径改道、报告补 A1 自检行、Teardown 落盘 teardown-report.md）+ P3 择纳（PID 回收校验进程名 kubectl 防重用误杀、tag 正则收紧、报告末行 [DEPLOY] 字面、文档计数/契约表/注释对齐）；server_tokens 与 seccompProfile/readOnlyRootFilesystem 硬化登记后续任务（不扩本任务需求，留痕）；
  8. **处置后复验全绿**：部署 PASS（报告含 A1 行 + 末行 [DEPLOY] PASS）→ Teardown PASS（teardown-report.md 落盘）→ **空场 Teardown PASS**（NotFound 视为成功退 0，边界值末行动态证据）→ 终态部署 PASS 保活 → 门禁 **PASS=9 FAIL=0 GREEN**。
- **2.5.1 登记项五项全部闭合**：①frontend 非 root（uid 101 实证）②label 统一（四处一致实测）③注入脚本行级锚定（注释字面量不命中静态+动态双证）④集群部署演练（Docker Desktop 内置 K8s 全链 9 轮）⑤OpenAPI schema 校验（每轮 `kubectl apply --dry-run=server`）。
- **下一步**：【①**PO 业务验收**：浏览器开 http://localhost:30081（平台前端）与 /login（深链接）、http://localhost:30080（后端应答 404 正常）、看 build-output/deploy/204926/deploy-report.md；验收时部署与 port-forward 保持运行（沿"验收保活"教训），验收通过后可 `deploy.ps1 -Teardown` 清场；②验收通过 → 编排师合并裁决（feat/C-2.5.2-oneclick-deploy 9 提交基于 main=6e021fb，可 fast-forward + 推送 origin，Watt Toolkit + D:\Soft\Git 惯例）；③下一任务 2.5.3 监控告警基座（依赖 2.5.1），新会话冷启动——本机内置 K8s 集群可直接复用。】
- **关键决策**：
  1. **演练集群 = Docker Desktop 内置 Kubernetes**（零新增工具，PO 在 D2 确认授权重启；集群保留供 2.5.3）；
  2. **访问通道 = 分离式 kubectl port-forward**（实测 kind 模式不映射 NodePort 后改道；NodePort patch 保留对映射型集群有效；"分离式"回应 lofi D6"短进程易断"顾虑——ADR-013 §8.5 / hifi §七.4 双留痕）；
  3. **镜像供给 = 现算现查 + 同 git 短哈希回退**（分钟级 tag 时间戳的消费者防御，回退认领打印留痕；同哈希保证镜像与代码状态一致）；
  4. **K8s 模板零回归**：所有环境差异（NodePort/镜像名）只在 build-output 副本内发生，base 模板保持 ClusterIP + 占位符原状（沿 ADR-013 副本注入口径）。
- **文件指针**：本日志；此前日志 26-09-12-1900（2.5.1 收尾补录，本任务续点来源）；设计 docs/designs/WBS-2.5.2-{lofi,hifi}.md；规范 ADR-013 §8（五条补正）；实现 scripts/deploy/{deploy.ps1,README.md}、deploy/{runbook.md,k8s/*}、frontend/{Dockerfile,nginx.main.conf,nginx.container.conf,.dockerignore}、services/example-service/Dockerfile；分支 feat/C-2.5.2-oneclick-deploy（9 提交）；报告档案 build-output/deploy/<时间戳>/{deploy-report.md,teardown-report.md}（gitignore 目录，本机档案）。

## 任务记录（一次成文，落盘核对后即为定稿）

### 业务可读交付说明

- **完成了什么功能**：给平台装了一部"上架电梯"——过去把软件搬进 Kubernetes 机房要懂一串命令行操作，现在一条命令（`deploy.ps1`）自动完成"体检（环境自检）→ 复印图纸（模板副本）→ 填集装箱号（镜像名注入）→ 搬货上架（装载+部署+等就绪）→ 验货（自动冒烟）→ 出报告"，再配一本不开代码也能看懂的部署手册（runbook）。顺带把上一任务（2.5.1）登记的五件收尾事全部办结：前端箱改为非 root 运行（更安全）、机房图纸命名统一、镜像号的"按行填入"防呆机制、真实集群整链演练、图纸的正规校验。全程零新增依赖。
- **如何演示**：①（若已清场）`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\deploy\deploy.ps1` 约 2~3 分钟见 `[DEPLOY] PASS`；②浏览器开 `http://localhost:30081` 见平台首页，开 `/login` 刷新不 404；`http://localhost:30080/` 显示 404 属正常（后端活着且有鉴权）；③`build-output/deploy/204926/deploy-report.md` 可看本轮部署的完整体检报告；④演示完 `scripts\deploy\deploy.ps1 -Teardown` 一键清场。**当前集群内有一套在跑的终态部署（供验收，勿清）**。
- **注意事项**：① 本分支净增约 870 行（设计/文档约占六成，代码以 433 行 deploy.ps1 为主），单一任务卡整体交付沿 2.5.1 惯例，PR 行数口径提请您在合并裁决时知悉；② Docker Desktop 内置 Kubernetes 已开启（设置键 KubernetesEnabled），演练后保留供 2.5.3，如需关闭在 Docker Desktop 设置操作；③ sc-mysql/sc-minio/sc-redis 三容器随 Docker 重启已自动恢复运行；④ ctds-mysql-demo 容器在重启后保持停止状态（原待您处置项，现状即安全态，删除仍由您决定：`docker rm ctds-mysql-demo`）；⑤ 本机现存多个任务镜像 tag，可按需 `docker rmi` 清理。

### 任务记录 ｜ 26-09-12 20:52 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：冷启动核对 → 两级设计+PO 确认 → 编码 → 开通内置 K8s（键名踩坑一次）→ 演练链 9 轮（5 个实测边界逐一修复）→ 提交 9 个 commit → 串行 4 视角评审 → 统一处置 → 复验全绿 → 日志落盘。
- **演练链与三轮 PASS 归属**（评审④要求写明）：run1 EXIT=2（镜像自检拦截：分钟 tag 差）→ run2 EXIT=1（rollout 失败：工作区 -dirty 致哈希解析错+误认领旧镜像）→ run3 EXIT=1（ImagePullBackOff：kind 模式镜像隔离）→ run4 EXIT=1（CreateContainerConfigError：USER 用户名不被 runAsNonRoot 认可）→ run5 EXIT=1（smoke 000：NodePort 不映射 localhost）→ run6 EXIT=2（提交后未重建镜像的自检拦截——**退出码 2"镜像缺失+构建指引"分支的动态证据**）→ **run7 PASS（报告 201356，首跑）** → **run8 PASS（报告 201532，幂等复跑）** → Teardown PASS（stdout+后续版本已补报告）→ **final PASS（报告 201703，Teardown 后重新部署）**；处置后再验：部署 PASS（204804）→ Teardown PASS（**teardown-report.md 落盘**）→ 空场 Teardown PASS（NotFound 退 0）→ 终态 PASS（204926 保活）。frontend 单箱预验四条输出：`docker inspect` User=101；容器内 `id`=uid 101(nginx)；`netstat -tln` 仅 8080；curl `/`=200、`/login`=200。
- 验证结果：处置后复验 = 部署 PASS×2（204804/204926）+ Teardown PASS×2（含空场 NotFound 退 0）+ 集群内双容器非 root（uid 101/1001）+ 锚定计数每文件恰 1（评审④独立 grep 复证）+ 门禁 **PASS=9 FAIL=0 GREEN**（2049 部署后轮）；4 视角评审结论①需修复后通过②通过③需修复后通过④需修复后通过，P2×5 全部修复、P3 择纳/留痕（评审②安全供应链独立复测含 docker build --check 双镜像零告警、线上镜像 tag 与命名规范核对一致）。
- 问题与教训：① **Docker Desktop 4.88 内置 K8s 三个坑**：设置键名是 `KubernetesEnabled`（旧名 EnableKubernetes 被判 unknown settings 静默不生效）；kind 模式有独立 containerd，本地镜像必须显式装载（docker save → ctr import）否则全体 ImagePullBackOff；NodePort 不映射到宿主 localhost，宿主访问用分离式 port-forward；② **kubelet runAsNonRoot 只认数字 uid**——镜像 USER 写用户名触发 CreateContainerConfigError（2.5.1 的 `USER appuser` 是隐性哑雷，2.5.2 实测引爆）；③ **分钟级时间戳 tag 的消费纪律**：现算现查必须配"同 git 短哈希回退"，且哈希解析先剥 -dirty 后缀（脏工作区会让 tag 变形，工具链消费者须防御）；④ 评审④指出"无档案行为"（Teardown 原不落报告）——运维工具链的关键行为都应留档，已补。
- 停机原因：【无停机——任务开发评审闭环完成，待人工验收节点；上下文自估约 55%（趋势参考，以任务窗口为准），接近 50% 线——**如您启用了后续会话，2.5.3 请新会话冷启动**】。
- 交接提醒：① 您的验收点 = 2.5.2 业务验收（演示见上，约 3 分钟）→ 合并裁决（9 提交可 fast-forward 入 main + 推送 origin，走 Watt Toolkit + D:\Soft\Git 惯例）；② 验收期间部署与转发专线保持运行，验收通过后 `-Teardown` 清场（或保留给 2.5.3 用）；③ 后续观察项（已留痕不阻塞）：自检"集群不可达"分支故障注入演练择机补做；server_tokens/seccompProfile/readOnlyRootFilesystem 硬化登记 2.5.3+；Invoke-Native PS5.1 模式第三次出现时提取公共库 scripts/common/；④ ctds-mysql-demo 待您决定是否删除。
