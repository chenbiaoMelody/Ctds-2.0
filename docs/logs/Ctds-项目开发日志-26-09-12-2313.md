# Ctds-项目开发日志-26-09-12-2303

- 任务卡 / 需求编号：**WBS-2.5.3 监控告警基座**（任务行：docs/C-TDS项目总体实施计划与WBS.md L215——"指标采集、基础看板、告警路由"，依赖 2.5.1 已满足；1 会话）
- 关联规格 / 剧本：规格基线 = WBS 任务行 + 本卡两级设计（沿 2.5.1/2.5.2 惯例，编排师 2026-09-12 会话确认"2"沿用）；剧本更新在交付时一并提议
- 会话目标：冷启动承接日志 26-09-12-2105 续点 → 处置遗留容器 → 集群/交付物核对 → 依赖核验报批 → 两级设计；本日志落盘时点停在 **lofi 待 PO 方向确认**
- 状态：【**进行中——lofi 已落盘待方向确认（6 问）；未进入 hifi/编码**】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**（23:03 首版 + 23:13 补记）：
  0. **补记（23:13）**：PO 对 lofi 六问回复"**都按推荐**"（方向确认通过）→ lofi 状态行签署 → 高保真 `docs/designs/WBS-2.5.3-hifi.md` 落盘（定稿确认稿，待四问确认后编码）；确认清单：①契约值照 hifi §1~§7 ②Grafana 匿名只读+admin 出厂默认 ③应用未部署时提示先跑 deploy.ps1 不代部署 ④测试先行两步走。
  1. **遗留处置**：`ctds-mysql-demo` 容器经核查（2.4.10 一次性演练容器，2.4.11 起被一次性测试容器替代，仓库现行链路零依赖）按编排师授权条件删除，`docker ps -a` 无残留；
  2. **开工核对**：分支 `feat/C-2.5.3-monitoring-baseline` 已建（基于 main=3c7cb3f）；集群 desktop-control-plane Ready（v1.36.1）且 `kubectl get all --all-namespaces` 无 ctds 应用资源（2.5.2 清场干净）；ADR-013（§53/§8.5）、runbook、deploy/k8s 模板已回读；
  3. **依赖核验（报批材料）**：`prom/prometheus:v3.13.3`（3.x 当前 LTS）、`prom/alertmanager:v0.34.0`、`grafana/grafana:13.2.1` 三镜像 **docker pull 实测入库**（digest 留痕于设计 §2）；Maven 侧 spring-boot-starter-actuator + micrometer-registry-prometheus 拟由 Boot 3.5.16 BOM 托管（实施时 dependency:tree 实测补留痕）；
  4. **lofi 落盘并提交**：`docs/designs/WBS-2.5.3-lofi.md`（提交 18e3b76）——范围三要素 + 依赖申报 5 项 + 三决策对比表（推荐：朴素清单三件套 / 仅平台服务指标 / 独立 monitoring.ps1）+ 行为清单 B1~B10 + 异常边界 6 条 + PO 问题确认清单 6 问。
- **下一步**：PO 逐条回复 lofi §6 六问（部署形态 A/B/C、指标范围 A/B、交付通道 A/B、演练短暂故障接受与否、超 400 行单卡交付接受与否、依赖 5 项批准与否）→ 按裁决出 `docs/designs/WBS-2.5.3-hifi.md`（接口契约表 + 看板区块定义 + 脚本验收判据 + Actuator 暴露与鉴权放行口径）→ hifi 定稿确认后编码（先写失败测试）。第一个动作 = 读 lofi §6 六问的裁决结果，更新 lofi 状态行。
- **关键决策**：
  1. 监控栈组件版本取 **Prometheus 3.13.3 LTS**（基座求稳，不追 3.14 普通版）——决策+理由在设计 §2；
  2. 镜像核验口径：本机直连 registry-1.docker.io 不通（实测超时），以 **Docker Desktop 经加速器 pull 实测成功** 为准（daocloud 通道对 Grafana 大镜像卡死，改 1ms 加速域名拉取后 retag 回官方坐标，digest 同一）——新教训待记忆沉淀；
  3. lofi 推荐"独立 monitoring.ps1 不动 deploy.ps1"（零回归风险），最终以 PO 裁决为准。
- **文件指针**：本日志；上游日志 26-09-12-2105（2.5.2 收尾）；`docs/designs/WBS-2.5.3-lofi.md`（待确认）；`docs/C-TDS项目总体实施计划与WBS.md` L215；`docs/adr/ADR-013-容器化部署模板规范.md`；`docs/dependencies.md`（审批后登记）；`scripts/deploy/deploy.ps1`、`deploy/runbook.md`、`deploy/k8s/`（2.5.2 交付物参照）；分支 feat/C-2.5.3-monitoring-baseline（18e3b76）。
- **补正说明**：无。

## 任务记录（一次成文，落盘核对后即为定稿）

### 业务可读交付说明

- **完成了什么功能**：本会话是 2.5.3 的开工准备——把上一任务留下的"待决容器"按您的授权清掉；确认机房（集群）与上一任务的工具箱都完好；调研并核验了监控要用的三个软件（Prometheus/Alertmanager/Grafana，全部官方出品、实际下载成功）；写好监控方案的"方向征求意见稿"（低保真设计），等您拍板方向后再出定稿和编码。
- **如何演示**：暂无可演示功能（尚未编码）。可复核：`docker images` 可见三个监控镜像；`kubectl get nodes` 集群 Ready；分支上 18e3b76 即设计文档。
- **注意事项**：① 设计稿 §6 有 **6 个问题等您逐条回复**（都给了推荐项，可直接"都按推荐"）；② Grafana 镜像约 1.4GB 已占本机磁盘；③ 三个监控镜像已入库但**未部署**（等设计定稿才动集群）。

### 任务记录 ｜ 26-09-12 23:03 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：冷启动承接（2105 日志）→ 删 ctds-mysql-demo（授权条件核查后）→ 建分支 + 集群/交付物核对 → 依赖核验三镜像 pull 实测（含 daocloud 卡死换 1ms 通道）→ lofi 落盘提交（18e3b76）→ 本日志落盘。
- 验证结果：`docker rm ctds-mysql-demo` 成功且无残留；`kubectl get nodes` Ready + `kubectl get all -A` 无 ctds 资源；三镜像 `docker images` 在库（digest：6976aa8a…/690c7b52…/f772d434…）；`git log` 18e3b76 随分支提交。
- 问题与教训：① daocloud 镜像加速器对 Grafana 大镜像拉取卡死（50 分钟无进展），**显式走 docker.1ms.run 加速域名秒级完成**——不同加速器稳定性差异大，大镜像要有"换通道"预案；② `docker manifest inspect` 走 CLI 直连在本机网络必超时，核验一律以 `docker pull` 实测为准。
- 停机原因：【等待人工确认——lofi 六问待 PO 裁决（两级设计门禁的"低保真方向确认"），确认前不进 hifi/编码】。
- 交接提醒：① 六问都有推荐项，编排师可整体裁决"按推荐"；② 确认后同会话或新会话续接均可（本日志续点已写到"可直接开工"粒度）；③ 依赖 5 项批准后记得要求我把登记写入 docs/dependencies.md 并随分支提交。
