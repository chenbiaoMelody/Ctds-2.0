# Ctds-项目开发日志-26-09-13-0156

- 任务卡 / 需求编号：**WBS-2.5.3 监控告警基座**（任务行：docs/C-TDS项目总体实施计划与WBS.md L215——"指标采集、基础看板、告警路由"；1 会话）
- 关联规格 / 剧本：需求基线 = WBS 任务行 + 两级设计（lofi 六问"都按推荐" + hifi 定稿"确认"，签署均在设计文档内）；剧本 = deploy/runbook.md（§7 监控章节为本次新增）
- 会话目标：承接日志 26-09-13 前夜冷启动续点（日志 26-09-12-2313）——依赖报批→两级设计→测试先行→编码→集群实测→四视角评审→处置复验，交付待 PO 业务验收
- 状态：【**编码与评审完成，待 PO 业务验收与合并裁决**；上下文任务窗口显示用量约 25%（AI 自估 ~23 万 tokens，权威值以任务窗口为准），安全区】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**（8 个提交，7a5e28c → 8b8cdf9）：
  1. **测试先行**：MetricsEndpointTest 三断言（prometheus 200 含 JVM/HTTP 指标、health 200、env 404 守卫）先 RED（7a5e28c，404 为正确缺口）后 GREEN（2fa6dd6）；
  2. **指标暴露实现**：pom 加 actuator + micrometer-registry-prometheus（Boot BOM 托管）、application.yml 最小暴露面、backend 部署模板三条抓取注解；全模块 49/49 + checkstyle 过；
  3. **监控栈交付**（748ebb4）：deploy/k8s-monitoring/ 8 份清单（注解发现+RBAC+两告警规则+路由+四区块看板）、monitoring.ps1（自检/镜像装节点/apply/rollout/指标冒烟/看板冒烟/告警演练四步含静默往返/Teardown/失败强制恢复副本安全网）、ADR-014、dependencies.md 登记 5 项、runbook §7；
  4. **集群实测修补 ×6**（3a78195）：namespace 字典序竞态先单独 apply、RBAC 三件套（kind 默认 RBAC 拒 list pods）、prometheus.yml 补 alerting 投递段、JSON 数字无引号/alertname 键名正则、PS5.1 LF 行尾 @() 续行拆分（改 -f 格式化+转 CRLF）、冒烟轮询重试+silence POST 补 Content-Type——**全流程九步 PASS + Teardown/重部署双向实测 PASS**；
  5. **四视角评审与处置**（8b8cdf9）：①规格设计 fail（P2 hifi 漏签）②安全供应链 pass ③一致性 fail（P2 固定端口 vs 随机口）④测试质量 fail（P2 HighErrorRate 零验证）→ 3 个 P2 全修（hifi §11 确认记录+§12 八项补正、S4 双规则装载 health=ok 校验、固定端口补正留痕）+ P3 择纳 9 项（S1 端口占用自检、S6b 改轮询、S6d 校验 curl 码防假 PASS、$node 字符集校验、rollout 失败补 events、完整 digest 入登记簿、Explore/Ctrl+C 边界登记 ADR-014 §4、注释改英文、runbook/scripts README 口径）→ **处置后全流程复验九步 PASS**。
- **下一步**：PO 业务验收（演示入口见交付说明；验收剧本无需更新结构，runbook §7 即操作手册）→ 验收通过后签署 lofi/hifi 验收行 → fast-forward 合并入 main → 推送 origin。第一个动作 = 编排师确认验收结论；若打回按新会话冷启动承接本日志。
- **关键决策**：
  1. Prometheus 取 3.13.3 LTS 不追 3.14（基座求稳）；2. Grafana 匿名 Viewer + admin 出厂默认（口令零入库，生产化接配置中心）；3. monitoring.ps1 独立于 deploy.ps1（零回归）；4. 告警 v1 界面可见不外发（真实渠道留生产任务）；5. 告警演练=缩 0 制造真实下线（已获 PO 授权）。
- **文件指针**：本日志；上游日志 26-09-12-2313（设计阶段）；分支 feat/C-2.5.3-monitoring-baseline（8b8cdf9，未合并）；设计 WBS-2.5.3-{lofi,hifi}.md（hifi 含 §11 确认记录 + §12 补正说明）；ADR-014；deploy/k8s-monitoring/（8 清单+README）；scripts/deploy/monitoring.ps1；deploy/runbook.md §7；docs/dependencies.md（5 行）；实测档案 build-output/monitoring/20260913-014732/monitoring-report.md（gitignore 本机）。
- **补正说明**：日志时间戳 01:56 = 落盘时点；2.5.3 全程在本会话完成（依赖核验→设计→编码→评审→处置），2313 日志的"待确认"续点已全部关闭。

## 任务记录（一次成文，落盘核对后即为定稿）

### 业务可读交付说明

- **完成了什么功能**：平台有了自己的"仪表盘+警报器"——后端服务的运行指标（存活/JVM 内存/请求速率/错误率）被自动采集进 Prometheus；Grafana 打开 `http://localhost:30082` 免登录即看《C-TDS 平台基础看板》四区块；两条告警规则（服务下线、5xx 错误率超 5%）经 Alertmanager 路由，本期告警在界面可见、可分组可静默（对外发送留生产任务）；`monitoring.ps1` 一键部署/验证/清场，内置一次真实告警演练（缩 0→响铃→恢复→解除）并落盘报告。
- **如何演示**：① 浏览器打开 `http://localhost:30082`（若转发已停，先跑 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\deploy\monitoring.ps1`，约 3 分钟，结尾 `[MONITOR] PASS`）——看板四区块有数据、服务存活显示"运行中"；② 最新报告 `build-output/monitoring/<时间戳>/monitoring-report.md` 九步全 PASS 含演练留痕；③ 想亲眼看告警：把 `scripts\deploy\monitoring.ps1` 重跑一遍，看控制台"alert FIRED"→"resolved"。
- **剧本是否需要更新**：已同步——`deploy/runbook.md` 新增 §7"监控告警基座怎么看"（部署/看结果/排查表/清理），`scripts/deploy/README.md` 补监控入口。
- **注意事项**：① 监控栈与应用生命周期独立：`deploy.ps1 -Teardown` 清应用不动监控，`monitoring.ps1 -Teardown` 清监控不动应用；② 集群当前处于"应用+监控都已部署"的绿态，可直接演示；③ 数据存 Pod 本地盘，Pod 重建指标清零（开发基线边界，ADR-014 §4）；④ Grafana admin 留出厂默认首登强制改密，仓库零口令。

### 提交前自检（章程附录 B1 对应）

1. 两级设计门禁：lofi 确认（"都按推荐"）+ hifi 确认（"确认"）均签署入库（hifi §11）；实现与设计逐条一致，8 处实测补正留痕（hifi §12）✅
2. 映射表：lofi B1~B10 → hifi §1~§9 契约 → MetricsEndpointTest/脚本九步 → runbook §7，见 hifi §8（按 S1~S7 对齐）✅
3. 复用声明：Invoke-Native/Stop-PortForwardPids/Write-TextFile 沿 deploy.ps1 同款（独立脚本系 PO 3A 裁决）；规格外实现声明：3 项保护性行为已在 hifi §12-8 登记 ✅
4. 本地检查：`mvn test`（example-service）49/49、`checkstyle:check` 过；monitoring.ps1 全流程九步 PASS ×2（含 Teardown 往返）；报告 20260913-014732 全 PASS ✅
5. 高风险点：密钥零入库（评审②全量 grep 无命中）；RBAC 最小化（只读 pods）；actuator 最小暴露面（env=404 守卫测试）；演练兜底（失败强制恢复副本）✅
6. 验收剧本：runbook §7 即剧本，已随分支提交 ✅
7. 交付说明：见上 ✅

### 任务记录 ｜ 26-09-13 01:56 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：测试先行 RED→GREEN → 监控栈 8 清单+脚本+ADR-014+登记+runbook → 集群实测 6 修补 → 4 视角评审（串行）→ 3 P2 + 9 P3 处置 → 复验九步 PASS。
- 验证结果：mvn 49/49 + checkstyle 过；monitoring-report.md 九步 PASS（S1 端口自检/S4 双规则 health=ok 均生效）；Teardown 清场验证（namespace 无资源）+ 重部署 PASS；分支 8b8cdf9 共 8 提交未合并。
- 问题与教训：① kind 集群 RBAC 默认拒绝 list pods——vanilla Prometheus 注解发现必须配最小 RBAC；② prometheus.yml 缺 alerting 段则规则"评估但不投递"（假静默）；③ PS 5.1 + LF 行尾会把 `@()` 内"字符串 + 变量"续行拆成两个元素（本机 Write 工具产 LF 文件，转 CRLF 或避免 + 续行）；④ kubectl apply -f 目录按文件名字典序，namespace.yaml 排最后须先单独 apply；⑤ @SpringBootTest 默认禁用全部 metrics 导出器（@AutoConfigureObservability 才开）且 scrape 端点 MOCK 环境不映射；⑥ hifi 定稿确认须当时回写设计文档，事后补录会被评审抓 P2（2.4.5 同款教训二次发生）。
- 停机原因：【等待人工确认——待 PO 业务验收与合并裁决；上下文约 25%，安全区；验收后同会话可继续合并推送，或另开会话承接本日志续点】。
- 交接提醒：① 演示入口 http://localhost:30082（当前绿态在跑）；② 合并裁决沿用 2.5.2 惯例：验收签署随分支提交后 fast-forward 合并、推送走 D:\Soft\Git + Watt Toolkit；③ 四视角评审为单轮，如需第二轮按升级流程另派。
