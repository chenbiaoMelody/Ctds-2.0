# Ctds-项目开发日志-26-09-13-1256

- 任务卡 / 需求编号：**WBS-3.1.2 主体数据模型与注册服务（第一阶段：两级设计门禁）**——承接 `docs/logs/Ctds-项目开发日志-26-09-13-1221.md` 续点（"合并后 WBS 顺序下一包 = WBS-3.1.2，实施前须走两级设计门禁"），本会话冷启动复述后直接进入设计起草。
- 关联规格 / 设计：`docs/specs/C-1.1-主体注册与实名认证.md` V1.0（已确认）；本包产出 `docs/designs/WBS-3.1.2-lofi.md` + `docs/designs/WBS-3.1.2-hifi.md`（两级一并提交、一次确认，章程 2.6.3 口径）。
- 会话目标：【设计起草（lofi+hifi）→ 展示核对 → PO 确认（五问裁决+签署）→ 编码 → 测试 → 提交评审】
- 状态：【**两级设计已起草落盘并随分支提交，待 PO 确认（确认记录表未签署 = 禁止进入编码）**】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. 冷启动（README + 1221 日志 + 规格核对 + WBS 表核对 3.1.x 段）：确认 3.1.1 全链路关闭（main=a7dcad9 已推送），本包 = WBS-3.1.2（主体库表、注册/变更/注销服务，依赖已满足，会话预算 1）；
  2. 材料收集（探索子智能体，业务可读结论带回）：kms 为最贴近的业务服务模板（四层+ArchUnit+Testcontainers-MySQL+Flyway 先例）；错误码已占段 1000/1001/1002（KMS 与幂等锁共用）/1003，**主体服务下一段 = 1004**，下一个 ADR = **016**；`spring-boot-starter-validation` 无先例须走核验+登记；common-crypto/auth/idempotency/logging 的对外签名与用法示例已核。
  3. 起草两级设计：lofi（服务落位 `services/subject-service` + 复用判定表 + 做什么 7 条 + 待确认问题 5 个）；hifi（行为清单 B1~B10 + 库表 DDL 三表 + REST 接口契约 3 端点 + 错误码表 1004 两码 + 边界值表 10 行 + 依赖清单）。
  4. 分支 `feat/C-1.1-主体数据模型与注册服务` 提交（设计两份 + 本日志）。
- **下一步**：【向 PO 展示两级设计全文与五个待确认问题（①注册页面归属建议随 3.1.3 ②影像存储采 SM4 加密落库 ③撤销=复用记录重置待认证 ④WBS"注销"按规格非目标收敛不做 ⑤validation 依赖按 PO 预授权引入），**第一个动作 = 等 PO 签署确认记录表（lofi 五问答复 + hifi 全文确认，可一次签署）**；确认后同会话或新会话进入编码（B1~B10 顺序：骨架→建表→注册→幂等→撤销→查询→状态服务→错误码→权限审计→登记），测试先行映射规格行为 1/4 的 Given/When/Then。】
- **关键决策**：
  1. 服务落位：新建 `services/subject-service`，3.1.3/3.1.4/3.1.5 同服务扩展（同档案同状态机，拆分属过度设计）；
  2. 错误码段占用：**1004**（1001/1002/1003 已占用），以 ADR-016 留痕（沿 ADR-015 惯例）；
  3. 唯一性双保险：`uk_uscc` 唯一索引兜底 + 服务层预检；重报（驳回后/撤销后）= **复用主体记录重置状态**，一行一代码终身唯一（lofi Q3 建议 A，待 PO 裁决）；
  4. 影像存储介质建议：SM4 加密落库 LONGBLOB（lofi Q2 建议 A，待 PO 裁决；结论供 3.1.3 执行，本包无 L4 字段）；
  5. 设计只从规格派生：WBS"变更/注销服务"按规格口径收敛为"撤销重报"（注销=非目标 7），登记为待确认问题 4 而非私自定。
- **文件指针**：本日志；上游日志 `docs/logs/Ctds-项目开发日志-26-09-13-1221.md`（3.1.1 收尾）；规格 `docs/specs/C-1.1-主体注册与实名认证.md`；本包设计 `docs/designs/WBS-3.1.2-lofi.md`、`docs/designs/WBS-3.1.2-hifi.md`；实现模板参照 `services/kms/`（结构/pom/配置先例）；依据 `docs/C-TDS项目总体实施计划与WBS.md` 3.1 段、ADR-005/006/007/008/009/015。

## 任务记录（一次成文，落盘核对后即为定稿）

### 业务可读交付说明

- **完成了什么功能**：把"主体注册与实名认证"规格中的注册环节翻译成了可直接开工的工程设计——主体数据库三张表（主体表/流转留痕表/申请编号序号表）、三个后端接口（注册/进度查询/撤销）、五个需要项目主导者裁决的业务口径问题（页面归属、影像存储方式、撤销语义、注销边界、校验组件引入）。
- **如何演示**：打开 `docs/designs/WBS-3.1.2-lofi.md` 看"做什么/不做什么/待确认问题"（业务语言），`WBS-3.1.2-hifi.md` 看接口契约与边界值（含请求响应示例）；确认签署后进入编码。
- **有无注意事项**：①设计确认记录表未签署前**禁止编码**（章程 2.6 两级设计门禁）；②五个待确认问题均附建议项与理由，可整批"按建议"裁决；③1004 错误码段与 ADR-016 为本包新占用，须随编码留痕。

### 提交前自检（B1，设计阶段裁剪口径）

1. 设计门禁：本包两级设计已起草，**待 PO 确认**（本包交付物即设计文档本身）；
2. 规格映射：hifi 行为清单 B1~B10 逐条标注规格出处（行为 1/2/4/5 对应条款）；计划测试列映射规格 Given/When/Then；
3. 复用声明：common 五组件复用判定表（lofi），未新造平行能力；规格外实现声明：空（设计未超出规格行为清单；WBS 措辞收敛已登记待确认问题 4）；
4. 本地检查：docs-only 变更（设计 2 新文件 + 本日志），Markdown 目视核对，无代码无门禁触发面；
5. 高风险点：并发唯一性（双保险设计）、L4 介质（建议落库加密，待裁决）、无代码故无加解密/事务实现风险；
6. 验收剧本更新建议：剧本 C-1.1 的 S1/S2 步骤中界面元素以本设计接口契约为基准可在编码交付后一并核对修订（本阶段无剧本变更）；
7. 业务可读交付说明：已附（上节）。

### 任务记录 ｜ 26-09-13 12:56 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：冷启动复述 → 材料收集（子智能体探索隔离，带回 10 项业务可读结论）→ 起草 lofi+hifi → 分支 `feat/C-1.1-主体数据模型与注册服务` 提交（设计两份 + 本日志）。
- 验证结果：docs-only 变更；设计与规格逐条映射核对一致（行为 1/4 全覆盖，行为 2/3/6 触发端明确划归 3.1.3/3.1.4/3.1.5）；错误码段 1004 与 ADR-016 编号经代码/ADR 目录实测核验。
- 问题与教训：①WBS 3.1.2 产出定义"注册/变更/注销服务"与规格口径存在措辞差（变更=撤销重报、注销=非目标），按"设计只从规格派生"处理并留待确认问题，不私自收敛；②规格行为 1 第 5 条"撤销重报"与行为 4 状态机最小集之间存在"撤销后状态"缺口，已在设计显式登记待裁决，禁止实施包私自增删状态。
- 停机原因：【设计门禁等人工确认——PO 签署前禁止编码（章程红线）；上下文自估约 22%（任务窗口权威值为准），安全区】。
- 交接提醒：①PO 确认时对 lofi 五问逐条表态（或整批"按建议"），并签 hifi 确认记录表；②确认后编码须测试先行（先写失败测试映射规格 Given/When/Then）；③`spring-boot-starter-validation` 编码会话履行核验+登记义务（PO 预授权不免核验与登记）；④本机集群绿态保留勿动。
