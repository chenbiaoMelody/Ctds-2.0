# Ctds-项目开发日志-26-09-13-1337

- 任务卡 / 需求编号：**WBS-3.1.2 主体数据模型与注册服务（第二阶段：设计确认 + 编码交付）**——承接 `docs/logs/Ctds-项目开发日志-26-09-13-1256.md` 续点（两级设计待 PO 确认），本会话编排师裁决"按建议，认可hifi全文"后代录确认记录并完成编码。
- 关联规格 / 设计：规格 `docs/specs/C-1.1-主体注册与实名认证.md` V1.0；设计 `docs/designs/WBS-3.1.2-lofi.md`（Q1~Q5 均采建议，代录留痕）、`docs/designs/WBS-3.1.2-hifi.md`（B1~B10 全文确认）。
- 会话目标：【设计确认 → 编码（B1~B10）→ 本地全绿 → 分支提交 → 交付说明】
- 状态：【**编码完成、本地验证全绿（26/26 测试 + checkstyle 零违规），已随分支提交，待 4 视角评审**】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. PO 裁决代录：lofi 五问"按建议"+ hifi 全文认可，写入两份设计确认记录表；
  2. 编码 B1~B10 全部落地：新建 `services/subject-service`（四层结构 + ArchUnit 分层守卫）；Flyway `V1__create_subject_tables.sql` 三表（subject / subject_status_log / subject_daily_seq，库 ctds_subject）；注册/重报/撤销/进度查询应用服务与 MySQL 仓储；`@Idempotent(key=uscc)` 防重复提交；错误码段 **1004** 两码；RBAC 三权限点 + 审计联动；`spring-boot-starter-validation` 核验（dependency:tree 实测 3.5.16）+ 登记 `docs/dependencies.md`；
  3. 契约固化：新建 `docs/adr/ADR-016-主体服务契约.md`（1004 段占用、状态机锁定、库表、撤销重报口径、幂等边界观察项）；
  4. 测试：单元 10（业务规则分支）+ 集成 13（Testcontainers-MySQL 全链路：校验逐字段/唯一性/幂等双跑/并发 20 线程恰一份/撤销重报复用编号/脱敏/权限三态/审计留痕/唯一索引兜底）+ ArchUnit 3 = **26 全绿**；checkstyle 零违规；根 pom 注册模块全仓编译通过。
- **下一步**：【按章程 §5 提交 4 视角评审（规格与设计符合性/安全供应链/一致性重复/测试质量），评审材料 = 本分支 diff + 设计两份 + 规格映射；评审通过后走 PO 业务验收（验收剧本 C-1.1 的 S1 前置步骤可用本服务接口实测：注册→查询→撤销→重报链路）与 ff 合并推送（第一个动作 = `git log feat/C-1.1-主体数据模型与注册服务` 核对提交后由编排师发合并裁决）。WBS 下一包 = 3.1.3 实名认证集成（两级设计门禁，承接 ADR-016 §2.3 影像加密落库口径与注册页面原型归属裁决）。】
- **关键决策**：
  1. 实现与 hifi 逐条一致（B1~B10 对照）；实现中发现并留痕一个设计级边界：**幂等键=信用代码时，TTL 窗口内同码再次注册返回首次结果而非 1004B0001 拒绝**——规格行为 1 第 2 条与第 3 条在无客户端请求令牌时不可区分，V1.0 取"窗口内复用、窗口外拒绝"，登记 ADR-016 §2.5 观察项（精确区分需规格变更流程），测试用例已按此口径校准（重报/重复拒绝用例经仓储直造数绕开幂等缓存）；
  2. 撤销重报按 Q3-A 落地：撤销 = 撤销留痕（状态保持待认证），"最新留痕 = 撤销备注"为撤销标记契约（SubjectStatusService 常量 + 仓储写方法强制携带 StatusTransition 参数实现留痕不可绕过）；
  3. 集成测试环境问题修一处：同类用例共享容器库，`countAll()==0` 断言改为前后对比（非产品缺陷）。
- **文件指针**：本日志；上游日志 `docs/logs/Ctds-项目开发日志-26-09-13-1256.md`（设计阶段）；设计 `docs/designs/WBS-3.1.2-lofi.md`、`WBS-3.1.2-hifi.md`；代码 `services/subject-service/`（主代码 13 文件 + 测试 3 文件）；契约 `docs/adr/ADR-016-主体服务契约.md`、`docs/dependencies.md`（validation 行）；依据 规格 C-1.1、WBS 3.1 段、ADR-005/006/007/009/015。

## 任务记录（一次成文，落盘核对后即为定稿）

### 业务可读交付说明

- **完成了什么功能**：主体注册服务上线——申请人可提交注册（必填与格式逐字段校验、统一社会信用代码全平台唯一、重复点击不重复建档、返回申请编号）、按申请编号查询进度（联系电话脱敏、全程状态流转留痕）、撤销申请后重新注册（同一申请编号，信息更新）；主体状态机与流转留痕底座就绪，认证（3.1.3）、审核（3.1.5）后续接入。
- **如何演示**：`POST /api/v1/subject/registrations` 提交注册（applicant 角色）→ `GET .../{申请编号}` 查进度（电话 138\*\*\*\*1234 脱敏、流转记录四要素）→ `POST .../{申请编号}/cancellation` 撤销 → 再次注册同码 = 同编号重报；未带身份头 401、错误角色 403；集成测试 26 用例即演示脚本（`SubjectRegistrationIntegrationTest`）。
- **有无注意事项**：①验收剧本 C-1.1 的 S1/S2 中"注册/进度查询"步骤现可实测，界面元素名称仍以 3.1.3 交付为准；②幂等窗口边界见 ADR-016 §2.5（窗口内重报返回首次结果），如需"每次重报明确拒绝"须走规格变更；③变更/注销边界：本包按规格交付"撤销重报"，注销未做（非目标 7）。

### 提交前自检（B1）

1. 设计门禁：lofi+hifi 已获 PO 确认（确认记录表已代录签署，2026-09-13）；实现与设计逐条一致（B1~B10 对照，偏差仅"实现发现的设计级边界"1 项，已留痕 ADR-016 §2.5）；
2. 规格映射：规格行为 1 四条验收标准 + 行为 4 三条验收标准 → hifi 行为清单 B3~B7 → 测试用例（单元 10 + 集成 13）逐一对应；剧本步骤映射：S1 幕注册/查询步骤 ↔ 集成测试同名场景；
3. 复用声明：common-errorcode/logging/auth/idempotency 五组件 + kms 结构先例 + Testcontainers 基座，检索过程见设计 lofi 复用判定表；规格外实现声明：空（撤销标记契约、校验异常补充 handler 均为设计内条目）；
4. 本地检查：`mvn -pl services/subject-service -am test` EXIT=0（26/26）；`mvn -pl services/subject-service checkstyle:check` EXIT=0；validation 依赖 dependency:tree 核验 3.5.16 并登记；
5. 高风险点自查：并发（唯一索引兜底 + 幂等，20 线程并发用例实测恰一份）、事务（create/resubmit 同事务落档案与留痕）、加解密（本包无 L4 字段，未触碰密码学）、性能（非网关/检索/计量敏感路径，暂缓基准）；
6. 验收剧本更新建议：剧本 S1 幕步骤 1~3（注册/查询）可标注"接口已可实测"；无需结构性修订；
7. 业务可读交付说明：已附（上节）。

**规模留痕**：本包有效变更约 1500 行（新建服务模块的骨架文件占大头），超出 PR ≤400 行约定——属单任务卡内聚交付（拆分将破坏"测试先行映射整包规格"的闭环），建议评审按模块整体受理；如需拆分，评审可要求先行合并 B1/B2 骨架提交。

### 任务记录 ｜ 26-09-13 13:37 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：设计裁决代录 → 编码 B1~B10（服务模块/建表/服务/幂等/撤销/查询/错误码/权限审计/依赖登记）→ ADR-016 契约固化 → 26 测试全绿 + checkstyle 清零 → 分支提交。
- 验证结果：全部本地门禁绿（编译/测试/checkstyle）；集成测试覆盖规格行为 1/4 全部验收标准 + 幂等并发 + 权限三态 + 审计留痕。
- 问题与教训：①规格行为 1 第 2/3 条在无请求令牌设计下的不可区分性应在 hifi 评审期识别（本包实现期发现，ADR-016 留痕观察项）——后续设计评审把"幂等键 vs 业务唯一性规则"交叉检查列入习惯；②集成测试跨用例共享容器库，隔离断言要用范围收窄（uscc 级）而非全表断言。
- 停机原因：【任务卡编码阶段交付完成，等待编排师组织 4 视角评审；上下文自估约 40%（任务窗口权威值为准），安全区】。
- 交接提醒：①评审材料 = 本分支全部提交 + 设计两份 + ADR-016；②评审通过后 PO 业务验收可用集成测试场景作演示脚本；③WBS 下一包 3.1.3 先走两级设计门禁（注册页面原型按 Q1 裁决随其交付）；④本机集群绿态与 Docker Desktop 保持运行（Testcontainers 依赖）。
