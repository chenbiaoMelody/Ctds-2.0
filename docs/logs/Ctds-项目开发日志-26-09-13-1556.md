# Ctds-项目开发日志-26-09-13-1556

> **补正说明一（内容修正时点 26-09-13 15:50，自 1517 重命名）**：1517 版落盘后、验收演示准备中发现本会话引入的取号回归——`LAST_INSERT_ID()` 在"当日首次插入"路径不被赋值，返回连接池残留值（演示实测首号 `S20260913000000`，应为 000001；补测试 `dailySeqFirstTakeReturnsOneAndIncrements` 首跑即抓到连接池残留非零值的更坏变体），修法改为新插入路径写 `LAST_INSERT_ID(1)`、重复键路径写 `LAST_INSERT_ID(seq_val + 1)`（两条路径均产生本连接确定值），测试增至 **30/30**。另：演示实测确认 ADR-016 §2.5 边界的实际后果——**撤销后 600s 幂等窗口内重报只返回首次结果、重报不真正执行**（须重启服务/等窗口过后重报才落留痕），已在验收演示口径标注。
>
> **补正说明二（内容修正时点 26-09-13 15:56，自 1550 重命名）**：编排师于 26-09-13 15:5x 回复"**验收通过**"（8 步演示链路此前已实测全符合预期），本版补录验收记录；合并推送结果见后续关闭补正。

- 任务卡 / 需求编号：**WBS-3.1.2 主体数据模型与注册服务（第三阶段：4 视角评审 + 打回项修复）**——承接 `docs/logs/Ctds-项目开发日志-26-09-13-1337.md` 续点（待 4 视角评审），本会话编排师确认后串行派发 4 视角评审、汇总裁决材料、修复可自主处置的打回项。
- 关联规格 / 设计：规格 `docs/specs/C-1.1-主体注册与实名认证.md` V1.0；设计 `docs/designs/WBS-3.1.2-lofi.md` / `-hifi.md`；契约 `docs/adr/ADR-016-主体服务契约.md`（本会话补记 §2.4/§2.5/新增 §2.6）。
- 会话目标：【4 视角评审 → 打回项修复 → 本地全绿 → 分支提交】
- 状态：【**验收通过（编排师 26-09-13 15:5x），合并推送执行中；两项遗留事项（主体类型重报默认不放开、§2.6 越权缺口）已按默认口径关闭入 ADR-016 留痕，幂等窗口规格侧留痕仍待变更流程**】

## 验收记录（26-09-13 15:56 补录）

- 验收人：编排师（项目主导者，兼任 PO 业务验收）；验收方式：本机 8080 演示链路 8 步（未登录 401 / 注册取号 / 重复点击幂等 / 查询脱敏+流转 / 撤销 / 重复撤销拒绝 / 重报同编号 / 错误角色 403），AI 先行实测全符合预期后交付编排师，编排师回复"验收通过"。
- 遗留事项处置：①主体类型重报 = 维持默认（不随重报变更，ADR-016 §2.4）；②越权缺口 = 维持 ADR-016 §2.6 登记（裁决前不对多用户开放，3.1.3 前裁决）；③幂等窗口规格侧留痕 = 待变更流程，不阻塞合并。

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. 4 视角评审串行完成（子智能体独立审阅，材料 = 提交 8ffdadd diff + 设计两份 + 规格 + ADR-016）：视角 1 规格与设计符合性 = **打回（轻度，2 项）**；视角 2 安全供应链 = 有条件通过（1 条件项）；视角 3 一致性重复 = 有条件通过（1 必修项）；视角 4 测试质量 = 有条件通过（1 打回项）。共同评价：测试基盘扎实、common 复用到位、无密钥泄露、SQL 全参数化，问题集中在 4 个可修点；
  2. 打回项修复全部落地（见"任务记录"），新增 3 条集成测试 + 1 处单测断言强化，**29/29 全绿**（原 26 + 新 3）+ checkstyle 零违规，已随分支提交；
  3. ADR-016 补记：§2.4 重报可变字段集合显式枚举（不含主体类型）、§2.5 幂等窗口后果披露与缓解口径（含配置键 `ctds.idempotency.default-expire-seconds`，经 IdempotencyProperties 源码核验）、新增 §2.6 对象级越权缺口登记（待 PO 裁决）。
- **下一步**：【编排师复核本修复提交（第一个动作 = `git log feat/C-1.1-主体数据模型与注册服务` 核对）并裁决两项待决事项：① 重报是否放开主体类型修改（本会话默认按已确认设计不放开，若放开须补设计变更留痕）；② §2.6 越权缺口处置（3.1.3 前加归属校验，或演示期"单租户信任网关"口径回写规格——裁决前本服务不得对多用户开放）。两项均裁决后走 PO 业务验收（集成测试场景即演示脚本：注册→查询→撤销→重报）→ ff 合并推送。另：幂等窗口口径的规格侧留痕与验收剧本同步，待 PO 走变更流程确认后补记（ADR-016 §2.5 已声明）。WBS 下一包 = 3.1.3 实名认证集成（两级设计门禁）。】
- **关键决策**：
  1. 主体类型重报处置：编排师两项 AskUserQuestion 未应答，按最佳判断取"**对齐已确认设计**"（重报不更新主体类型，SubjectRegistrationService.resubmit 取 current.subjectType() + 仓储 UPDATE 移除 subject_type 列）——定性为"实现回归契约"而非代选；若 PO 放开，走设计变更流程补 hifi 行 126 留痕即可恢复；
  2. 越权缺口不私自补码（章程红线 2/原则 5）：登记 ADR-016 §2.6 留待 3.1.3 前裁决，裁决前不开放多用户；
  3. 取号原子化：`INSERT ... ON DUPLICATE KEY UPDATE seq_val = LAST_INSERT_ID(seq_val + 1)` + `SELECT LAST_INSERT_ID()`（hifi 所称"MySQL 主流写法"）+ 方法级 `@Transactional` 绑定连接（LAST_INSERT_ID 为连接级，无事务则两语句可能异连接）；另封顶 999999 超限抛 INTERNAL_ERROR（%06d 溢出会产生 15 位编号破坏 S\d{14} 格式）；
  4. 建议项处置：撤销重报 0 行分支文案失真、脱敏上收 common、审计断言锚定本次实例、流转时间要素断言等**非阻塞建议未改**（登记于评审报告，随 3.1.3/3.1.5 或下一个自然修改窗口处理）。
- **文件指针**：本日志；上游日志 `docs/logs/Ctds-项目开发日志-26-09-13-1337.md`（编码交付）；代码 `services/subject-service/`（本会话改 SubjectJdbcRepository / SubjectRegistrationService / 两测试文件）；契约 `docs/adr/ADR-016-主体服务契约.md`；设计 `docs/designs/WBS-3.1.2-lofi.md` / `-hifi.md`；依据 规格 C-1.1、章程 §5。

## 任务记录（一次成文，落盘核对后即为定稿）

### 4 视角评审结论汇总

| 视角 | 结论 | 关键发现 |
| --- | --- | --- |
| 1 规格与设计符合性 | 打回（轻度） | ①重报更新 subject_type 超出 hifi 可变字段集合（未声明的第二处设计偏差）；②幂等窗口偏差留痕不充分（后果未披露、无测试锚定、规格侧零留痕） |
| 2 安全供应链 | 有条件通过 | 对象级越权未防护（A 可查/撤 B 的申请编号）——定性为规格/设计缺口须走变更流程；另有电话无格式校验等 6 建议 |
| 3 一致性重复 | 有条件通过 | 取号非原子（两条独立语句可并发同号，且撞 uk 后误译"该主体已注册"）——违背 hifi"原子取号"契约 |
| 4 测试质量 | 有条件通过 | "唯一索引兜底转 1004B0001"无服务流内测试（并发用例被内存幂等串行化，触达不了 catch 分支） |

评审建议项（非阻塞，登记待办）：①resubmit 0 行分支误用"不可撤销"文案（并发删除竞态场景，语义错位）；②脱敏工具第二个使用方出现前上收 common；③审计断言按 subjectNo 锚定本次实例（当前取首条可能命中旧事件）；④流转留痕"时间"要素无断言；⑤电话无格式校验 + 短输入脱敏暴露面；⑥archunit-junit5 补登记 dependencies.md；⑦jacoco 覆盖率门禁缺位（项目级）。

### 本会话修复明细（对应评审打回项）

1. **取号原子化**（视角 3 必修项）：`SubjectJdbcRepository.nextDailySeq` 改 LAST_INSERT_ID 模式 + `@Transactional`；封顶 999999；附并发不同信用代码 20 线程取号不重复集成测试 `concurrentDistinctUsccRegistrationsGetDistinctSubjectNos`；
2. **重报回归契约**（视角 1 打回项 1）：resubmit 保留 `current.subjectType()`，仓储 UPDATE 移除 subject_type；单测 `rejectedSubjectResubmitsReusingSameRow` 强化为 GOV 类型命令注册 ENTERPRISE 主体断言类型保留；
3. **兜底转换测试**（视角 4 打回项）：新增 `duplicateKeyInRepositoryTranslatedToBusinessErrorNotDbDetails`（直插同 uscc 后经 `repository.create()` 触达 catch 分支，断言 BizException 1004B0001 固定文案）；
4. **幂等窗口固化**（视角 1 打回项 2 部分）：新增 `idempotencyWindowReturnsFirstResultEvenWhenPayloadDiffers`（同码异内容两次注册返回首次结果，ADR-016 §2.5 回归锚点）；ADR-016 §2.5 补后果披露与缓解口径；
5. **越权缺口登记**（视角 2 条件项）：ADR-016 新增 §2.6，处置留待 PO 裁决。

### 提交前自检（B1）

1. 设计门禁：本会话修复均属"实现回归已确认设计"或评审登记事项，无新设计决策（LAST_INSERT_ID 写法即 hifi"原子取号"契约本义）；ADR-016 补记为评审留痕非设计变更；
2. 规格映射：新增测试均映射评审要求锚点；规格行为 1/4 验收标准映射不变；
3. 复用声明：无新依赖、无新组件；规格外实现声明：空；
4. 本地检查：`mvn -pl services/subject-service -am test` EXIT=0（29/29）；`mvn -pl services/subject-service checkstyle:check` EXIT=0；
5. 高风险点自查：并发（取号原子化 + 20 线程双并发用例：同码恰一份 / 异码编号不重复）、事务（nextDailySeq 事务绑定连接）、加解密（未触碰）、性能（未触碰敏感路径）；
6. 验收剧本更新建议：无（行为口径未变，仅缺陷修复与测试加固；幂等窗口口径留痕待 PO 变更流程）；
7. 业务可读交付说明：评审发现的问题已全部修复或登记，主体注册服务行为口径与此前交付说明一致；两项待裁决事项（主体类型重报放开与否、越权缺口处置）需编排师/PO 决定。

### 任务记录 ｜ 26-09-13 15:17 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：4 视角评审串行派发与汇总 → 两项业务裁决以 AskUserQuestion 提请未应答 → 按最佳判断执行可自主处置修复（4 打回项全修复）→ ADR-016 补记 → 29/29 测试 + checkstyle 全绿 → 分支提交。
- 验证结果：全绿（编译/29 测试/checkstyle 零违规）；新增并发取号、兜底转换、幂等窗口三条集成测试实证。
- 问题与教训：①AskUserQuestion 未应答时不阻塞——按"对齐已确认设计优先、缺口登记不补码"两条原则自主处置并在日志声明，裁决权保留 PO；②评审前自检单声称"偏差仅 1 项"与事实不符（漏了 subject_type）——自检单的"偏差声明"应有测试/代码级核对支撑，不能凭实现期印象；③内存幂等把并发用例串行化导致兜底分支测试失明——并发测试设计要区分"幂等串行化路径"与"仓储层竞态路径"。
- 停机原因：【评审 + 修复 + 验收演示准备完成，等待编排师本机验收（演示口径见交付答复：URL http://localhost:8080，8 步链路，服务保持运行）与两项待决裁决；上下文自估约 30%（任务窗口权威值为准），安全区】。
- 交接提醒：①两项待裁决：主体类型重报（默认不放开）、§2.6 越权缺口（裁决前不对多用户开放）；②演示库 = 本机 sc-mysql 容器 `ctds_subject` 库（Flyway 自动迁移，演示前可整库重建重置）；③演示用服务为内存幂等模式，**重启即清缓存**——撤销后立即重报须先重启服务或等 600s 窗口过（ADR-016 §2.5 实测确认）；④规格侧幂等窗口留痕 + 验收剧本同步走变更流程后补记；⑤非阻塞建议清单见本日志评审汇总节，勿散失；⑥本机 Docker Desktop 保持运行（Testcontainers 依赖）。
