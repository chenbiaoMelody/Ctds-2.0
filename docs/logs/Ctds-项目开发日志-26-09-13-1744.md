# Ctds-项目开发日志-26-09-13-1744

- 任务卡 / 需求编号：**WBS-3.1.3 实名认证集成（OCR 证照识别 + 法人核验服务对接与封装）**——承接 `docs/logs/Ctds-项目开发日志-26-09-13-1558.md` 续点（下一包 3.1.3 另会话冷启动），本会话完成：冷启动 → 三项前置事项 → 两级设计门禁（一次确认）→ 编码实现 → 本地全绿 → 分支提交。
- 关联规格 / 设计：规格 `docs/specs/C-1.1-主体注册与实名认证.md` **V1.1**（本会话升版：幂等窗口口径留痕）；设计 `docs/designs/WBS-3.1.3-lofi.md` / `-hifi.md` / `WBS-3.1.3-原型-注册与认证.html`（PO 已确认，五问均采建议）；契约 `docs/adr/ADR-016-主体服务契约.md`（§2.1/§2.3/§2.5/§2.6 补记）、`docs/adr/ADR-008-标准适配层契约.md`（§7 变更补记：新增 CERTIFICATION 域）。
- 会话目标：【前置裁决留痕 → 两级设计确认 → 编码 → 测试全绿 → 分支提交（评审前状态）】
- 状态：【**WBS-3.1.3 编码交付完成，分支 `feat/C-1.1-auth-integration`（00ace94 设计包 + 917209b 代码），subject-service 51/51 + std-adapter 15/15 测试全绿、checkstyle 双模块 0 违规；待 4 视角评审与编排师验收，未合并 main**】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. 三项前置事项（编排师 26-09-13 会话确认"哪个方案业务上更合理选哪个 + 本会话处理遗留"）：① ADR-016 §2.6 越权缺口**已裁决采方案①**（subject 加申请人列 + 归属断言；理由：多主体平台的 L4 档案最小授权是业务底线，"单租户信任网关"回写规格等于固化安全缺口）；② 幂等窗口口径规格侧留痕（规格 C-1.1 升 V1.1，行为 1 第 3 条补"窗口内返回首次结果、重报不真正执行"披露）+ 剧本 S1 步骤 2 同步（原预期与实测不符处已对齐）+ ADR-016 §2.5 标记闭环；
  2. 两级设计门禁：lofi/hifi/静态 HTML 原型同批提交，PO 回复"**五问均采建议**"（认证失败态=超限只锁定不转态、真实 Vue 页面随 3.1.5、ADR-008 本包补记、存量回填 legacy-demo、影像 ≤5MB jpg/jpeg/png），一次确认口径生效；
  3. 编码全量落地（见"任务记录"）：std-adapter 认证域、V2 迁移、认证服务与 6 端点、OwnershipGuard、错误码扩展；
  4. 本地验证：`mvn -pl services/subject-service,std-adapter -am test` EXIT=0（**subject-service 51/51 + std-adapter 15/15**）；`mvn -pl services/subject-service,std-adapter checkstyle:check` EXIT=0。
- **下一步**（可直接开工粒度）：承接本日志续点，第一动作 = 对提交 917209b 串行派发 **4 视角评审**（材料 = 提交 00ace94+917209b diff + 设计三份 + 规格 V1.1 + ADR-008/016），按 3.1.2 先例：汇总裁决 → 可自主处置修复 → 全绿 → 提请编排师本机验收（接口级演示 + 原型走查）→ ff 合并 main 推送。评审关注点提示：①渠道调用留痕已扩展至 OCR 调用（hifi 库表节仅写核验记录——偏差见关键决策 3）；②核验发起未挂幂等（重试须真执行，防刷靠当日上限）；③`CertChannelUnavailableException` 专类出站映射 503（绕全局 S 型脱敏，common 零改动）。
- **关键决策**：
  1. **§2.6 越权缺口裁决 = 方案①**（编排师预授权"选业务上更合理的"，判定留痕见 ADR-016 §2.6）：多主体平台业务本质 + L4 档案最小授权底线；
  2. **认证失败态口径**（lofi Q1 采建议 A）：超限只做当日锁定（计数拒绝）不转态；"认证失败"仅由申请人主动"结束认证"触发；CERT_FAILED→重新核验通过→待审核路径打通——规避"次日自动恢复需定时任务回拨状态"的复杂度；
  3. **渠道调用留痕扩展**：`cert_verification_log.verify_type` 增加 `OCR_LICENSE`（规格行为 7 第 3 条"任一次渠道调用留痕三要素"的完整覆盖；hifi 库表节原文只写核验记录，属实现口径补全，ADR-016 §2.3 已同步，非规格变更）；
  4. **渠道异常出站口径**：`CertChannelUnavailableException`（BizException 子类）+ 本地 `@Order(HIGHEST_PRECEDENCE)` 处理器 → 503 + 1004S0001 + "认证服务暂不可用，请稍后重试"，绕开全局 S 型文案脱敏（否则规格要求的明确业务错误会被"系统繁忙"吞掉）；common-errorcode 零改动；
  5. **核验发起不挂 @Idempotent**：失败重试必须真执行（幂等窗口会吞重试），防重复提交靠当日 5 次上限 + 渠道留痕兜底（hifi 接口契约已声明）；
  6. **渠道技术异常不留审计**：规格仅要求"拒绝留痕"（越权）与"渠道调用留痕"（CHANNEL_ERROR 行已落）；auditRecorder 只在越权 DENIED 时触发——单测相应断言为"不落审计"。
- **文件指针**：本日志；上游日志 `docs/logs/Ctds-项目开发日志-26-09-13-1558.md`；分支 `feat/C-1.1-auth-integration`（00ace94 / 917209b）；代码 `services/subject-service/`（新增 application/CertificationService 等 13 文件、infrastructure/CertificationJdbcRepository、interfaces/CertificationController、V2 迁移；改 Subject/SubjectJdbcRepository/SubjectRegistrationService/SubjectErrorCodes/application.yml/pom）、`std-adapter/src/main/java/com/ctds/std/certification/`（4 文件 + 单测）；契约 `docs/adr/ADR-008`/`ADR-016`；设计三份 `docs/designs/WBS-3.1.3-*`；规格 V1.1 与剧本 V1.1。

## 任务记录 ｜ 26-09-13 17:44 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：冷启动复述续点获确认 → §2.6 裁决留痕（ADR-016）→ 幂等窗口规格 V1.1 + 剧本同步 → 两级设计 + 原型起草并提交（00ace94）→ PO"五问均采建议"签署留痕 → std-adapter 认证域 + subject-service 认证全量编码 → 测试三轮修复全绿 → ADR 双补记 → 分支提交（917209b）。
- 验证结果：`mvn -pl services/subject-service,std-adapter -am test` EXIT=0（66/66，含 Testcontainers 集成 4 类：全链路/密文落库/越权双向/渠道异常 503）；checkstyle 双模块 0 违规。修复过程：①测试确认值须取 OCR 回填值（模拟渠道固定要素口径）而非注册值——恰验证了"以 OCR 回填为准"业务规则；②verifications 计数漏过滤 verify_type（OCR 调用留痕也算入）——补 `verify_type='LEGAL_PERSON'` 过滤；③awaitEvent 审计断言需按 outcome+actor 双过滤（跨用例审计文件累积）。
- 问题与教训：①本地仓库残留 std-adapter 旧 SNAPSHOT——单模块 `-pl` 不带 `-am` 时用旧构件假编译（报"程序包不存在"实为旧包缺类），两模块联动一律带 `-am`；②Mock 渠道固定要素 vs 测试多用例唯一信用代码存在张力：确认环节以 OCR 回填值为准（业务正解）而非注册值，测试设计要跟随业务口径而非迁就造数便利；③S 型错误码全局脱敏与"渠道不可用需明确文案"冲突——专类异常 + 高优先级本地处理器是零改 common 的最主流解法（沿 3.1.2 ValidationExceptionHandler 先例）。
- 停机原因：【编码交付完成、待 4 视角评审（下一会话或本会话续接派发）；上下文自估约 40%（任务窗口权威值为准），接近停机线，按 1.3 规则完成本最小步骤（日志落盘+提交）后停机——建议评审与验收在新会话冷启动执行】。
- 交接提醒：①验收演示 = 接口级（沿 3.1.2 先例）+ 原型走查（`docs/designs/WBS-3.1.3-原型-注册与认证.html` 浏览器直开）；**演示环境须先配 SM4 密钥文件**：设 `CTDS_SM4_KEY_FILE` 指向 `*.keys` 文件（内容格式 `subject-cert-material=Base64(16字节密钥)`，密钥零入库）或运行期设 `CTDS_CRYPTO_KMS_BASEURL` 接本机 KMS；②模拟渠道规则：文件名含 `A1` → 识别成功固定要素（蓝天数据/91330100MA27XW123X/张伟），其余 → 无法识别；身份证尾号 `8` → 核验不通过；`ctds.std.certification.mock.channel-error=true` → 注入渠道异常；③归属断言演示口径：换 X-Ctds-Subject 头即 403（reviewer 角色豁免可查）；④非阻塞观察项：并发双 PASS 理论窗口（状态门槛先读后写，演示期单人无风险，登记评审参考）；⑤dependencies.md 无需新增（本包仅内部模块依赖，无外部新包）。
