# Ctds-项目开发日志-26-09-13-1907

> **补正说明一（内容修正时点 26-09-13 19:07，自 1744 重命名；本会话未停机续做）**：1744 版按自估上下文约 40% 停机，编排师核对任务窗口权威值为 **24.6%**，指示继续——自估再次显著高估（教训重申：以窗口显示为准）。续做内容：**4 视角评审串行完成（视角1/2/3 有条件通过、视角 4 打回）→ 打回项与条件项修复全量落地（提交 8c2c200）→ subject-service 58/58 + std-adapter 15/15 全绿、checkstyle 双模块 0 违规 → ADR-016 §2.2/§2.3/§2.6 与 ADR-008 §7、hifi 补正说明同步**。评审记录与修复明细见文末新增节；**下一步 = 编排师本机验收演示 → ff 合并 main 推送**。
>
> **评审打回核心（视角 4，属真缺陷非测试问题）**：状态流转只落留痕**从不更新 `subject.status` 列**——核验通过/结束认证后库内主体仍"待认证"，审核端将永远查不到待审核主体；此前集成测试断言的"待审核"来自响应硬编码，绿灯失真。修复 = `appendTransition` 同事务更新状态列 + `WHERE status=from_status` 乐观门槛（并发重复流转 0 行即拒 1004C0002），并补持久化状态断言（红转绿验证闭环）。

- 任务卡 / 需求编号：**WBS-3.1.3 实名认证集成（OCR 证照识别 + 法人核验服务对接与封装）**——承接 `docs/logs/Ctds-项目开发日志-26-09-13-1558.md` 续点（下一包 3.1.3 另会话冷启动），本会话完成：冷启动 → 三项前置事项 → 两级设计门禁（一次确认）→ 编码实现 → 4 视角评审 → 打回项修复 → 本地全绿 → 分支提交。
- 关联规格 / 设计：规格 `docs/specs/C-1.1-主体注册与实名认证.md` **V1.1**（本会话升版：幂等窗口口径留痕）；设计 `docs/designs/WBS-3.1.3-lofi.md` / `-hifi.md` / `WBS-3.1.3-原型-注册与认证.html`（PO 已确认，五问均采建议；hifi 含"4 视角评审补正"块）；契约 `docs/adr/ADR-016-主体服务契约.md`（§2.1/§2.2/§2.3/§2.5/§2.6 补记）、`docs/adr/ADR-008-标准适配层契约.md`（§7 变更补记：新增 CERTIFICATION 域 + 接口补齐）。
- 会话目标：【前置裁决留痕 → 两级设计确认 → 编码 → 评审 → 修复 → 测试全绿 → 分支提交（待验收）】
- 状态：【**WBS-3.1.3 评审+修复完成：分支 `feat/C-1.1-auth-integration`（00ace94 设计 + 917209b 交付 + 8c2c200 评审修复），subject-service 58/58 + std-adapter 15/15 全绿、checkstyle 双模块 0 违规；待编排师验收演示，未合并 main**】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. 三项前置事项（编排师 26-09-13 会话确认"哪个方案业务上更合理选哪个 + 本会话处理遗留"）：① ADR-016 §2.6 越权缺口**已裁决采方案①**（subject 加申请人列 + 归属断言；理由：多主体平台的 L4 档案最小授权是业务底线，"单租户信任网关"回写规格等于固化安全缺口）；② 幂等窗口口径规格侧留痕（规格 C-1.1 升 V1.1，行为 1 第 3 条补"窗口内返回首次结果、重报不真正执行"披露）+ 剧本 S1 步骤 2 同步（原预期与实测不符处已对齐）+ ADR-016 §2.5 标记闭环；
  2. 两级设计门禁：lofi/hifi/静态 HTML 原型同批提交，PO 回复"**五问均采建议**"（认证失败态=超限只锁定不转态、真实 Vue 页面随 3.1.5、ADR-008 本包补记、存量回填 legacy-demo、影像 ≤5MB jpg/jpeg/png），一次确认口径生效；
  3. 编码全量落地（见"任务记录"）：std-adapter 认证域、V2 迁移、认证服务与 6 端点、OwnershipGuard、错误码扩展；
  4. 本地验证：`mvn -pl services/subject-service,std-adapter -am test` EXIT=0（**subject-service 51/51 + std-adapter 15/15**）；`mvn -pl services/subject-service,std-adapter checkstyle:check` EXIT=0。
- **下一步**（可直接开工粒度）：**请编排师本机验收演示**。演示准备（AI 已在日志交接节列明）：①启动前设 `CTDS_SM4_KEY_FILE` 指向 `*.keys` 文件（内容 `subject-cert-material=Base64(16字节)`，可运行 `mvn -pl services/subject-service spring-boot:run -Dspring-boot.run.profiles=mysql` 前置环境变量）或接本机 KMS；②演示链路（接口级，沿 3.1.2 先例）：注册 → 上传 A1.jpg（OCR 回填）→ 重复上传替换 → 确认（改码阻断/恢复）→ 核验通过（状态自动待审核，**直查库确认 status 列已更新**）→ 核验失败×5 → 第 6 次拒绝 → 结束认证 → CERT_FAILED 再核验通过 → 他人身份 400（出站与"不存在"同形）→ reviewer 豁免可查 → 原型走查（浏览器直开 `docs/designs/WBS-3.1.3-原型-注册与认证.html`）。验收通过后 ff 合并 main 推送（合并裁决由编排师发出）。
- **关键决策**：
  1. **§2.6 越权缺口裁决 = 方案①**（编排师预授权"选业务上更合理的"，判定留痕见 ADR-016 §2.6）：多主体平台业务本质 + L4 档案最小授权底线；
  2. **认证失败态口径**（lofi Q1 采建议 A）：超限只做当日锁定（计数拒绝）不转态；"认证失败"仅由申请人主动"结束认证"触发；CERT_FAILED→重新核验通过→待审核路径打通——规避"次日自动恢复需定时任务回拨状态"的复杂度；
  3. **渠道调用留痕扩展**：`cert_verification_log.verify_type` 增加 `OCR_LICENSE`（规格行为 7 第 3 条"任一次渠道调用留痕三要素"的完整覆盖；hifi 库表节原文只写核验记录，属实现口径补全，ADR-016 §2.3 已同步，非规格变更）；
  4. **渠道异常出站口径**：`CertChannelUnavailableException`（BizException 子类）+ 本地 `@Order(HIGHEST_PRECEDENCE)` 处理器 → 503 + 1004S0001 + "认证服务暂不可用，请稍后重试"，绕开全局 S 型文案脱敏（否则规格要求的明确业务错误会被"系统繁忙"吞掉）；common-errorcode 零改动；
  5. **核验发起不挂 @Idempotent**：失败重试必须真执行（幂等窗口会吞重试），防重复提交靠当日 5 次上限 + 渠道留痕兜底（hifi 接口契约已声明）；
  6. **渠道技术异常不留审计**：规格仅要求"拒绝留痕"（越权）与"渠道调用留痕"（CHANNEL_ERROR 行已落）；auditRecorder 只在越权 DENIED 时触发——单测相应断言为"不落审计"。
- **文件指针**：本日志；上游日志 `docs/logs/Ctds-项目开发日志-26-09-13-1558.md`；分支 `feat/C-1.1-auth-integration`（00ace94 / 917209b）；代码 `services/subject-service/`（新增 application/CertificationService 等 13 文件、infrastructure/CertificationJdbcRepository、interfaces/CertificationController、V2 迁移；改 Subject/SubjectJdbcRepository/SubjectRegistrationService/SubjectErrorCodes/application.yml/pom）、`std-adapter/src/main/java/com/ctds/std/certification/`（4 文件 + 单测）；契约 `docs/adr/ADR-008`/`ADR-016`；设计三份 `docs/designs/WBS-3.1.3-*`；规格 V1.1 与剧本 V1.1。

## 任务记录 ｜ 26-09-13 17:44 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：冷启动复述续点获确认 → §2.6 裁决留痕（ADR-016）→ 幂等窗口规格 V1.1 + 剧本同步 → 两级设计 + 原型起草并提交（00ace94）→ PO"五问均采建议"签署留痕 → std-adapter 认证域 + subject-service 认证全量编码 → 测试三轮修复全绿 → ADR 双补记 → 分支提交（917209b）。
- 验证结果：`mvn -pl services/subject-service,std-adapter -am test` EXIT=0（66/66，含 Testcontainers 集成 4 类：全链路/密文落库/越权双向/渠道异常 503）；checkstyle 双模块 0 违规。修复过程：①测试确认值须取 OCR 回填值（模拟渠道固定要素口径）而非注册值——恰验证了"以 OCR 回填为准"业务规则；②verifications 计数漏过滤 verify_type（OCR 调用留痕也算入）——补 `verify_type='LEGAL_PERSON'` 过滤；③awaitEvent 审计断言需按 outcome+actor 双过滤（跨用例审计文件累积）。
- 问题与教训：①本地仓库残留 std-adapter 旧 SNAPSHOT——单模块 `-pl` 不带 `-am` 时用旧构件假编译（报"程序包不存在"实为旧包缺类），两模块联动一律带 `-am`；②Mock 渠道固定要素 vs 测试多用例唯一信用代码存在张力：确认环节以 OCR 回填值为准（业务正解）而非注册值，测试设计要跟随业务口径而非迁就造数便利；③S 型错误码全局脱敏与"渠道不可用需明确文案"冲突——专类异常 + 高优先级本地处理器是零改 common 的最主流解法（沿 3.1.2 ValidationExceptionHandler 先例）。
- 停机原因：【编码交付完成、待 4 视角评审（下一会话或本会话续接派发）；上下文自估约 40%（任务窗口权威值为准），接近停机线，按 1.3 规则完成本最小步骤（日志落盘+提交）后停机——建议评审与验收在新会话冷启动执行】。
- 交接提醒：①验收演示 = 接口级（沿 3.1.2 先例）+ 原型走查（`docs/designs/WBS-3.1.3-原型-注册与认证.html` 浏览器直开）；**演示环境须先配 SM4 密钥文件**：设 `CTDS_SM4_KEY_FILE` 指向 `*.keys` 文件（内容格式 `subject-cert-material=Base64(16字节密钥)`，密钥零入库）或运行期设 `CTDS_CRYPTO_KMS_BASEURL` 接本机 KMS；②模拟渠道规则：文件名含 `A1` → 识别成功固定要素（蓝天数据/91330100MA27XW123X/张伟），其余 → 无法识别；身份证尾号 `8` → 核验不通过；`ctds.std.certification.mock.channel-error=true` → 注入渠道异常；③归属断言演示口径：换 X-Ctds-Subject 头即 403（reviewer 角色豁免可查）；④非阻塞观察项：并发双 PASS 理论窗口（状态门槛先读后写，演示期单人无风险，登记评审参考）；⑤dependencies.md 无需新增（本包仅内部模块依赖，无外部新包）。

## 4 视角评审记录（26-09-13 补正说明一新增）

| 视角 | 结论 | 关键发现 |
| --- | --- | --- |
| 1 规格与设计符合性 | 有条件通过 | hifi 三处计划测试未兑现；1004C0002 未入 hifi 错误码表；cert_verification_log 表结构与 hifi 定稿漂移；核验响应硬编码状态值掩盖库内状态不更新（与视角 4 交叉印证）；边界值"transition 状态门槛"无真实实现 |
| 2 安全与供应链 | 有条件通过 | **中危**：归属断言先查后判形成申请编号存在性侧信道（建议统一 404 或 PO 接受）；建议：文件名无长度校验、容器超限落 500、RuntimeException 全转渠道异常、legacy-demo 占位口径、测试身份证号无"虚构"标注。供应链声明核对 = 通过（零新外部依赖，L4 加密/密钥零入库/注入面/审计全达标） |
| 3 一致性重复 | 有条件通过 | **必修**：上传响应缺 hifi 契约字段 materialId/recognizable；渠道异常根因被吞（无日志）。**条件**：ADR-016 §2.2 缺 CERT_FAILED→PENDING_REVIEW 边补记；SHA-256 自行摘要 vs ADR-006 SM3 唯一入口裁决；业务代码 import Mock 类 + 硬编码渠道标识/流水号（违反"只见接口"）；Clock 双时间源分裂。复用判定：requireSubjectNo/operator/audit 应上收本服务公共支撑类（第二使用方条件已触发）——本轮未做，登记 3.1.4 前完成 |
| 4 测试质量 | **打回** | **必修：全套测试无"持久化状态"断言，状态流转从不更新 subject.status 列（真缺陷，绿灯失真）**；CERT_FAILED 再核验全链路/重传重置确认/存量回填断言/他人端点全量越权/无脏数据直查库/影像解密往返等计划测试缺失 |

### 本轮修复明细（提交 8c2c200）

1. **状态流转落库 + 乐观门槛**（视角 4 必修）：`SubjectJdbcRepository.appendTransition` 同事务 `UPDATE subject SET status WHERE id=? AND status=from`（0 行 → 1004C0002）+ 落留痕；集成测试补直查库持久化状态断言（PENDING_REVIEW/CERT_FAILED）；
2. **SM3 唯一入口**（视角 3）：`content_sha256`→`content_sm3`（Sm3Service.digestHex），ADR-006 Q2"文件完整性国密摘要"裁决兑现；
3. **渠道出口取标识**（视角 3）：`CertificationStandardApi.channelCode()` + `OcrRecognition.channelRequestNo`，业务代码去 Mock import 与硬编码"MOCK-OCR"（ADR-008 §7 补记）；
4. **防存在性探测**（视角 2 中危）：归属不匹配出站统一 400 + 1000C0003（与"不存在"同形），DENIED 审计保留（ADR-016 §2.6 补正）；
5. **异常治理**（视角 2/3）：渠道异常根因 `log.error` 后转译；IllegalArgumentException 重抛不转译；双计时统一 Instant；
6. **身份证校验位**（视角 1）：GB 11643 mod 11-2 验证，测试号码改为程序校验的合法虚构号（110101199001011229 / 110101199001011288）；
7. **契约字段补齐**（视角 1/3）：上传响应补 materialId/recognizable；文件名 ≤256 校验；容器超限映射业务文案（独立 `CertificationExceptionHandler`）；
8. **Clock 统一**（视角 3）：SubjectStatusService/SubjectRegistrationService 注入 Clock，时间源全服务一致；
9. **测试补齐**（视角 4 F2~F8）：CERT_FAILED 再核验全链路、重传重置确认、他人全端点 400、无脏数据直查库、影像解密往返、留痕三要素断言、状态门槛负向、legacy-demo 回填、guard 调用 verify、extensionOf 边界；
10. **文档同步**：ADR-016 §2.2（CERT_FAILED→PENDING_REVIEW 边 + 门槛口径）、§2.3（SM3/uk_subject_type/verify_type）、§2.6（404→400 补正）；ADR-008 §7（接口补齐）；hifi 补正块（六条）；错误码表 1004C0002 入表。

**登记未修（观察项，随 3.1.4/3.1.5 处理）**：①requireSubjectNo/operator/audit 三处工具方法上收本服务公共支撑类（3.1.4 前完成）；②cert_verification_log 并发核验 TOCTOU 上限+并发−1 穿透口径（3.1.5 多角色前补并发用例并在 ADR 登记）；③核验业务不通过审计记 SUCCESS+reason=verify_failed 的粒度口径；④ApiResult 404 语义（1000C0003 出站 400 非 404）为平台既有口径，未改。

### 任务记录 ｜ 26-09-13 19:07 ｜ 补正说明一 ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：4 视角评审串行派发（子智能体独立审阅，串行单发）→ 汇总 4 份报告 → 打回项与可自主处置条件项全量修复 → 73/73 全绿 + checkstyle 0 违规 → ADR/hifi 补记 → 分支提交。
- 验证结果：`mvn -pl services/subject-service,std-adapter -am test` EXIT=0（73/73，含持久化状态/无脏数据/解密往返新断言）；checkstyle EXIT=0。
- 问题与教训：①**绿灯失真最危险形态 = 断言了"响应里的硬编码值"而非"库里的真实状态"**——状态机类功能测试必须至少一条直查持久层断言；②上下文自估 40% vs 窗口权威值 24.6%，自估再次高估近一倍——停机决策只认窗口显示值；③身份证校验位手算出错，用 awk 脚本程序化生成测试号码（信计算不信手算）；④Mockito 打桩 replaceMaterial 默认返回 null 导致 NPE——打桩返回值要符合契约语义（带 id 回填）。
- 停机原因：【评审+修复完成、待编排师本机验收演示；上下文自估约 40%（**以任务窗口权威值为准**），安全区】。
- 交接提醒：①演示前必配 `CTDS_SM4_KEY_FILE`（*.keys）或 KMS 环境变量，否则上传报系统繁忙（密钥不可用 fail-fast 是预期行为）；②演示身份证号须用校验位合法的虚构号（如 110101199001011229 通过 / 110101199001011288 触发不通过——尾号 8 且校验位合法）；③他人身份探测出站已与"不存在"同形（400+1000C0003），演示口径按"无法区分存在性"讲解；④观察项清单勿散失（见登记未修节）；⑤本机 Docker Desktop 保持运行（Testcontainers）。

> **补正说明二（验收关闭，26-09-13 19:29）**：编排师于 26-09-13 19:2x 回复"**验收通过**"（接口级 10 步演示链路 + 原型走查，AI 先行实测全符合预期后交付）。本版补录验收记录；随后执行验收记录提交 → `git merge --ff-only` main → `git push origin main`。

## 验收记录（26-09-13 19:29 补录）

- 验收人：编排师（项目主导者，兼任 PO 业务验收）；验收方式：接口级演示链路 + 静态原型走查（演示口径见"下一步"节），AI 先行实测全符合预期后交付编排师，编排师回复"验收通过"。
- 遗留事项处置：①观察项四条（工具方法上收/并发 TOCTOU 口径/审计粒度/1000C0003 出站 400）已登记，随 3.1.4/3.1.5 处理；②真实 Vue 页面随 3.1.5 交付（lofi Q2 裁决）；③真实认证渠道替换 = std-adapter Bean 注册替换点（ADR-008 §7）。

## 当前续点（终版）

- **已完成**：WBS-3.1.3 全链路（设计门禁 → 编码 → 4 视角评审 → 打回项修复 → 验收通过 → 合并推送）——状态以补正说明三的推送结果为准。
- **下一步**：下一包 **WBS-3.1.4 政务 CA 接入适配**（另会话冷启动）：承接输入 = ADR-008 §7（认证域政务 CA 方法扩展）、ADR-016 §2.3（政务 CA 材料表随 3.1.4 建表）、规格行为 6（政务 CA 不走 OCR/法人核验、验证通过直接待审核、仍须人工审核——§7 Q2 已裁决）、验收剧本 S3（模拟渠道政务证书规则 A3 有效/A4 过期）；另承接 3.1.3 观察项①（工具方法上收）。
- **关键决策**：见前文关键决策 1~6 与评审修复明细（共 10 项），不再重复。
- **文件指针**：本日志；上游 `docs/logs/Ctds-项目开发日志-26-09-13-1558.md`；分支 `feat/C-1.1-auth-integration`（00ace94/917209b/8c2c200）；代码 `services/subject-service/`、`std-adapter/`；契约 ADR-008/ADR-016；设计三份 `docs/designs/WBS-3.1.3-*`；规格 C-1.1 V1.1 + 剧本 V1.1。

> **补正说明三（全链路关闭，26-09-13 19:4x）**：验收记录随 main 提交 751056f → `git merge --ff-only` main（343d10e..751056f）→ `git push origin main` **PUSH-EXIT=0，origin/main=751056f**。WBS-3.1.3 全链路（设计门禁→编码→评审→修复→验收→合并推送）关闭。**下一包 = WBS-3.1.4 政务 CA 接入适配，另会话冷启动**（设计输入见"当前续点（终版）"节）。
