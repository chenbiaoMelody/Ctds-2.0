# Ctds-项目开发日志-26-09-13-1955

- 任务卡 / 需求编号：**WBS-3.1.4 政务 CA 接入适配（政务 CA 证书接入通道，收口 std-adapter）**——承接 `docs/logs/Ctds-项目开发日志-26-09-13-1930.md` 终版续点（下一包 3.1.4 另会话冷启动），本会话完成：冷启动复述获编排师确认（"启动；两处遗留按建议处置"）→ 遗留处置 → 观察项①上收 → 两级设计起草提交，**停下待 PO 设计确认**。
- 关联规格 / 设计：规格 `docs/specs/C-1.1-主体注册与实名认证.md` V1.1（行为 6 / 行为 7）；设计 `docs/designs/WBS-3.1.4-lofi.md`（五问均附建议）/ `-hifi.md`（定稿草案）；契约输入 `docs/adr/ADR-008-标准适配层契约.md` §7、`docs/adr/ADR-016-主体服务契约.md` §2.1/§2.3；剧本 S3 + 附录 A。
- 会话目标：【冷启动+遗留处置 → 观察项① → 两级设计 PO 确认 →（确认后续会话/续接）编码 → 评审 → 测试全绿 → 验收合并】
- 状态：【**两级设计已提交待 PO 确认（未签署 = 禁止编码）；观察项①已完成（1b57374）；分支 `feat/C-1.1-gov-ca`（1b57374 重构 + 5b7e674 设计），未合并 main**】

## 当前续点（新会话从这里继续；续接会话承接本块另建新日志）

- **已完成**：
  1. 遗留处置（编排师授权"按建议处置"）：① 1930 日志补正说明三时点校准（19:4x→19:30）随 main 提交 **be3051e** 并推送成功（PUSH-EXIT=0，先例同 ccd4963）；② `docs/designs/A1.jpg` 经查验**为 ZCode 应用窗口截图（误存，非证照影像）**，已移出仓库至 `E:\AI Projects\ZCode\A1-截图-原docs-designs误存.jpg`（未删除，编排师可自行取回）；
  2. 观察项①（3.1.4 视角评审登记"3.1.4 前完成"）：`requireSubjectNo`/`operator()`/`audit` 三处同型工具方法上收 `SubjectOpsSupport`（application 包 @Component，两服务注入委托；纯重构零行为变化）——提交 **1b57374**，`mvn -pl services/subject-service,std-adapter -am test` EXIT=0（**73/73 全绿**：subject 58 + std 15）+ checkstyle EXIT=0；
  3. 两级设计同批起草提交（**5b7e674**）：lofi（场景判定/做什么 8 条/不做什么/五问含建议）+ hifi（行为清单 9 项/库表零迁移/接口契约/1004B0007/std-adapter 扩展/配置项/边界值/映射表）。
- **下一步**（可直接开工粒度）：**等编排师（兼任 PO）对两级设计签署确认**（lofi/hifi 确认记录表；五问口径见下"关键决策"）。确认后（同会话续接或新会话冷启动均可）：第一动作 = 按章程 4"测试先行"先写政务域失败测试——① `MockCertificationChannelTest` 补政务规则四例（A3 通过/A4 过期/其余拒绝/channel-error）；② `GovCaCertificationIntegrationTest` 骨架（A3 全链路含直查库状态断言）；随后实现 `CertificationStandardApi.verifyGovCaCertificate` + `GovCaVerification`、`CertificationService.submitGovCaCertificate` 与互斥门槛、`CertificationProfile` 扩展（remainingAttemptsToday 改 Integer）、1004B0007 入 `SubjectErrorCodes`；三项 ADR 补记（ADR-008 §7 / ADR-016 §2.1 / §2.3）随实现提交。
- **关键决策**（设计五问，均采建议口径——**待 PO 签署生效**）：
  1. **材料存储复用 `cert_material` 表**（material_type 新增值 GOV_CA_CERT，零 DDL 零迁移）；ADR-016 §2.3 预留的"政务 CA 材料表建表"收敛为"复用+新类型值"随包补记——两表结构完全同构，新建平行表违背 ADR-016 §3"不建平行表"约束精神；`ocr_*` 列名语义泛化为"渠道原始要素"，不重命名（3.1.3 冻结契约）；
  2. **单端点一步验证**（POST `/certification/gov-ca-certificate`，multipart）：政务流程无 OCR 回填核对环节，拆两步无业务语义；
  3. **政务验证失败不设次数上限**（规格行为 6 未定义，最小实现不加严；与行为 3 法人核验 5 次上限是两条行为）；
  4. **证书文件参数**：≤2MB、cer/crt/pem、重复提交替换保留最近一次（配置参数可调）；
  5. **通道互斥双向同码 1004B0007**"主体类型与认证流程不匹配"（规格行为 6 第 1 条+"全程不出现"验收标准的强制口径）；
  6. **verify_type=GOV_CA / counted 恒 0 / 结论枚举沿用 PASS-FAIL-CHANNEL_ERROR**；渠道校验职责全归渠道（证书解析/证书链本地校验不做——收口纪律）。
- **文件指针**：本日志；上游日志 `docs/logs/Ctds-项目开发日志-26-09-13-1930.md`（终版续点）；分支 `feat/C-1.1-gov-ca`（1b57374 / 5b7e674）；设计 `docs/designs/WBS-3.1.4-lofi.md` / `-hifi.md`；规格 `docs/specs/C-1.1-主体注册与实名认证.md` V1.1；剧本 `docs/manuals/C-1.1-主体注册与实名认证-验收剧本.md`（S3）；契约 `docs/adr/ADR-008`（§7）/ `ADR-016`（§2.1/§2.3）；代码基座 `services/subject-service/`（SubjectOpsSupport 新增、两服务改造）、`std-adapter/src/main/java/com/ctds/std/certification/`。

## 任务记录 ｜ 26-09-13 19:55 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：冷启动（README + 1930 日志）→ 复述续点四要素与两处遗留请裁决 → 编排师"启动；按建议处置"→ 遗留处置两件 → WBS 3.1.4 分支 + 设计输入回读（规格行为 6 全文/剧本 S3/ADR-008 §7/ADR-016 §2.3/认证代码七文件）→ 观察项①重构（新支撑类 + 两服务改造 + 测试构造器同步）→ 全绿验证 + 提交 → 两级设计起草 + 提交 → 本日志落盘停机。
- 验证结果：`mvn -pl services/subject-service,std-adapter -am test` EXIT=0（**73/73**）；`mvn -pl services/subject-service,std-adapter checkstyle:check` EXIT=0；main 推送 PUSH-EXIT=0（be3051e）。
- 问题与教训：①A1.jpg 实为应用窗口截图误存 designs/（人眼验收时注意：未跟踪文件先看内容再定处置）；②sed 批量替换后 Edit 工具因文件变更拒绝写入——**先 sed 后 Edit 的混合编辑要重读文件**；③测试直接 new 服务时构造器变更必须同步测试（本次用 perl 多行匹配一次改三处）。
- 停机原因：【两级设计待 PO 签署（章程 2.6 设计门禁：未确认 = 禁止编码）；上下文自估约 15%（以任务窗口权威值为准），安全区——本次停机为流程等待非资源停机】。
- 交接提醒：①PO 确认口径建议"**五问均采建议**"或逐问给意见（lofi 待确认 5 问 + hifi 即视为定稿）；②确认后编码第一动作 = 测试先行（见"下一步"）；③演示素材准备：政务证书模拟文件命名 **A3.cer（有效）/ A4.cer（过期）**（文件内容任意，模拟渠道按文件名匹配——与 A1 同构）；④登记观察项②③（并发 TOCTOU / 审计粒度）归 3.1.5 前处理，本包不重复登记；⑤本机 Docker Desktop 保持运行（Testcontainers 集成测试用）。
