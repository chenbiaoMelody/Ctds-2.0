# Ctds-项目开发日志-26-09-14-0007

> 时间戳 = 本日志落盘时点（2026-09-14 00:07；会话跨零点，设计确认与编码发生在 2026-09-13 晚间）。

## 当前续点（终版）

- **已完成**：WBS-3.1.5 主体审核后台（审核工作台 + 真实 Vue 页面 + 后端审核端点）全链路——冷启动承接 2130 日志 → 两级设计一次确认（PO 回复"三问均采建议"：A admin 兼审核员 / A 证书原件不提供 / 全量 Vue 范围）→ 测试先行编码（后端 + 前端）→ 4 视角评审串行完成（视角 1 打回 3 必修 / 视角 2 通过 / 视角 3 打回 3 必修 / 视角 4 打回 2 必修）→ 打回项与建议项全量修复 → 全绿提交（056bb2e）。**待编排师验收演示，未合并 main**。
- **下一步**（可直接开工粒度）：**请编排师本机验收演示**。前置：①subject-service 走 mysql profile 连本机库（Docker Desktop 保持运行）；②配 `CTDS_SM4_KEY_FILE`（*.keys，格式 subject-cert-material=Base64(16字节)）；③前端 `cd frontend && npm run dev`（5173 代理 /api → 8080）。演示链路（界面级，沿 3.1.3/3.1.4 先例）：① 演示登录（admin 兼审核员，登录后菜单见"主体入驻"与"主体审核"）→ ② 主体入驻注册企业主体 → 认证页上传 A1.jpg（OCR 要素自动回填核对表单）→ 确认提交 → 法人核验通过 → 状态"待审核" → ③ 另注册政府部门主体上传 A3.cer → 直接"待审核" → ④ 侧边栏"主体审核"→ 清单见两主体 → 点开政务主体：注册信息/证书验证段/无核验段/流转留痕可见 → 执行"通过"→ 状态"已入驻"、留痕"审核通过"→ ⑤ 企业主体执行"驳回"（理由必填，空理由被拦）→ 登录态切申请人视角（或直接查该主体认证页）可见驳回理由 → ⑥ 接口级复验（可选）：无权限角色调审核端点 403；并发/门槛由测试锚定。验收通过后 ff 合并 main 推送（合并裁决由编排师发出）。
- **关键决策**：
  1. 设计三问采建议（演示角色 admin 兼审核员 / 政务证书原件不提供下载 / 申请人侧 Vue 页面随本包全量交付），PO 已确认（lofi+hifi 一次确认）；
  2. 分页落位：领域层不依赖 common-pagination（LayerRulesTest 架构门禁），仓储收敛为 `countByStatus` + `findByStatus(offset,limit)`，PageResult 组装归应用层——hifi 已补正留痕；
  3. 状态门槛拒绝补 DENIED 审计（hifi E3 兑现）+ 拒绝态负向红锚（删 `requirePendingReview` 单测必红）——3.1.4"门槛必须有拒绝态负向用例"教训在本包自查中被评审视角 4 再次抓到（初版无锚），教训延续有效；
  4. 驳回理由：业务上限走配置参数（默认 200，硬上限 251=留痕列 256 减前缀），DTO 512 传输兜底——三层口径已在 hifi 契约表补记。
- **文件指针**：本日志；上游 `docs/logs/Ctds-项目开发日志-26-09-13-2130.md`；分支 `feat/C-1.1-review-workbench`（7641b56/2a7fcc7/c956559/056bb2e）；代码 `services/subject-service/`（ReviewController/ReviewService/ReviewQueueItem/ReviewActionResult/dto/ReviewRejectionRequest + SubjectRepository 分页方法 + SubjectOpsSupport.normalizeFileName + CertificationProperties）、`frontend/src/`（api/client.ts、api/subject.ts、constants/subject.ts、views/review/*、views/subject/*、router）；设计 `docs/designs/WBS-3.1.5-lofi.md`/`-hifi.md`/`-原型-审核工作台.html`；ADR-016（§2.1/§2.2/§3 补记）；规格 C-1.1 V1.1 行为 5。

## 任务记录 ｜ 2026-09-14 00:07 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode（GLM-5.3-Flash）

- 做了什么：冷启动承接 3.1.4 续点 → 设计输入回读（规格行为 5 / ADR-016 §2.2 / 剧本 S3 / 前端基建 / common-pagination / TriggerRole 预留）→ 两级设计起草（lofi 三问 + hifi 十项行为清单 + 线框原型）→ PO"三问均采建议"一次确认 → 测试先行（ReviewServiceTest + SubjectOpsSupportTest + ReviewIntegrationTest 先行，随后实现；前端 5 个 spec + 4 页面 + api 封装）→ 全绿提交 → 4 视角评审串行派发（单发串行）→ 汇总四报告 → 必修 8 项（去重后）+ 建议 10 项全量修复 → 全绿提交。
- 验证结果：`mvn -pl services/subject-service,std-adapter -am test` EXIT=0（**subject 96/96 + std 20/20**，含审核集成 8 例：通过流转直查库+留痕四要素+时间落值/驳回必填/驳回流转/状态门槛四态拒绝+DENIED 审计/并发单胜出/清单过滤分页/越权 403+审计/文件名归一化两入口）；checkstyle 双模块 0 违规；前端 vitest **58/58** + eslint 0 + vue-tsc 0 + vite build 通过。
- 4 视角评审结论：视角 1 规格设计符合性=打回（必修 3：详情页缺注册信息分区/OCR 回填缺失与文案失实/E3 DENIED 审计缺失）；视角 2 安全供应链=**通过**（0 必修；建议 3 项全落实或登记）；视角 3 一致性重复=打回（必修 3：注册信息+留痕分区/机构类型缺失+hifi 两处补正；ADR-016 补记建议全采纳）；视角 4 测试质量=打回（必修 2：requirePendingReview 无红锚/B5 第四态+E3 三方缺口；"删门槛测试依旧绿"盲区复现）。**必修全修；建议项落实 8 项、登记观察项 3 项（见下）；hifi 补正 4 处 + ADR-016 三节补记。**
- 问题与教训：①3.1.4 的"门槛必须有拒绝态负向用例"教训在 3.1.5 初版仍失守（requirePendingReview 无红锚，删掉判断集成测试仍绿——响应由仓储乐观门槛兜出同码 1004C0002）——同一形态的盲区会跨包复发，红锚检查应成为写门槛测试时的固定自检项；②集成测试断言审计事件须按 targetId（申请编号）过滤，否则同 auditDir 前序用例同类事件会"顶替"通过；③负向 UI 断言（not.toContain）要与动态文案错开（留痕备注含"政务 CA 证书验证"字样导致断言撞车）；④领域层分页：横切组件不进 domain（架构门禁），原始类型参数 + 应用层组装是本仓库的正解；⑤离线 `-o` 模式会用到 .m2 里落后的 std-adapter 旧快照（缺 GovCaVerification），同仓库多模块迭代期间测试一律带 `-am` 在线跑。
- 停机原因：【编码+评审+修复完成、待编排师本机验收演示；上下文自估约 40%（以任务窗口权威值为准），按 1.3 完成本最小步骤（日志落盘+提交）后停机；建议验收在新会话冷启动执行，或编排师指示本会话续做合并推送】。
- 交接提醒：①演示前置与链路见"当前续点（终版）"下一步节；②登记未修观察项（随后续包处置）：清单查看无审计留痕（与档案查看既有口径一致，L4 回溯价值评估后可随 2.5.x 监控基座或真实渠道替换任务补）、ReviewService.requireSubject 与 CertificationService.requireSubject 同型上收（沿 SubjectOpsSupport 先例，3.1.6 或后续包）、QueueView 清单排序断言与 E2 的 HTTP 层用例（低风险）、政务写放大监控已登记升级 2.5.x（3.1.4 交接项延续）、requireCertFile/requireImage 参数化评估结论=未复用不参数化（本包审核端无第二使用方，维持现状留痕）；③剧本更新建议（随交付说明提交 PO 审批）：剧本头关联规格同步 V1.1、S2 步骤 7 补幂等窗口判定说明（重报落在注册后 600s 内返回首次结果）、S1 步骤 9 可见性已随流转留痕分区兑现无需改步骤；④dependencies.md 零变更（common-pagination 为内部模块；前端零新 npm 依赖）；⑤Flyway 零迁移（无 V3），本机库无需重建；⑥审批收口：ADR-016 补记沿 3.1.4 先例随包留痕，无需单独审批。
