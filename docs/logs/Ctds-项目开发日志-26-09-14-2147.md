# Ctds-项目开发日志-26-09-14-2147

> 时间戳 = 本日志落盘时点（2026-09-14 21:47）。

## 当前续点（终版）

- **已完成**：WBS-3.1.6「C-1.1 测试补全与集成」**补独立评审③（一致性与重复）+ ④（测试质量）**——冷启动承接 26-09-14-1513 日志续点（③④ 首轮因评审子智能体配额耗尽降级为主智能体自检留痕，待配额恢复补独立评审）→ 编排师选定"补独立评审③④" → 并行派发两个独立子智能体只读评审 → 两份独立报告：③【有条件通过：必修 1 + 建议 5 + 观察 6】、④【有条件通过：必修 1 + 建议 4 + 观察 8】→ 主智能体逐条回读源码核实两处必修成立 → **修复 2 项必修（仅文档层）+ §8 T6 错误码措辞补"政务 2MB"** → 建议/观察项登记 → 提交。**代码/测试零改动**（既有门禁结论继续有效）。**待编排师验收演示 + 合并裁决。**
- **下一步**（可直接开工）：**请编排师本机验收演示**。前置：①本机 Docker Desktop 保持运行（sc-mysql/sc-minio/sc-redis）；②后端 8080（mysql profile + `CTDS_SM4_KEY_FILE=E:\AI Projects\ZCode\Ctds-2.0\subject-demo.keys`）；③前端 `cd frontend && npm run dev`（5173）。演示链路 = 验收剧本 S1/S2/S3（AI 已用 Playwright 全链路走查 14/14；演示数据 S20260914000001~4 已造好：蓝天企业已入驻、星河企业已驳回、市大数据管理局已入驻、过期证书局待认证）。验收通过后由编排师发出合并裁决，再执行 ff 合并 main + 推送（合并推送不代决）。
- **关键决策**：
  1. **独立评审结论以独立评审为准**（hifi §9.1）——两处必修均为**文档层更正**，不改代码/测试，保持既有门禁结论有效；
  2. S1 上收范围维持仅 `ReviewService`/`CertificationService`（hifi §4 已确认设计），`SubjectRegistrationService` 同型内联重复**不在本包扩围**，登记后续去重项（避免无设计的规格外改动，原则 5）；
  3. `counted = 1` 谓词与 `conclusion = 'FAIL'` 冗余（存活突变）**不在本包删改**（删谓词属实现变更，须走设计/验证，非评审修复范围），登记观察项；
  4. 建议/观察项统一登记，随交接提醒移交后续包处置。
- **文件指针**：本日志；上游 `docs/logs/Ctds-项目开发日志-26-09-14-1513.md`（3.1.6 主体）；分支 `feat/C-1.1-test-completion`（本会话提交位于 `8818394` 之上）；设计 `docs/designs/WBS-3.1.6-hifi.md`（§2 T8、§8 T6、§9.1/§9.2 本会话修正）；被评审变更集 = `git diff main...feat/C-1.1-test-completion`（25 文件 / +884 -60）。

## 任务记录 ｜ 2026-09-14 21:47 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode

### 做了什么（业务可读）

WBS-3.1.6 的编码与前三项评审（①②）已在上一会话完成，只剩"一致性与重复""测试质量"两项评审因评审子智能体配额耗尽、临时降级为 AI 自检。**本会话不写新功能，只补这两项独立评审**：由两个互不相干的评审子智能体各自独立通读本轮全部改动（884 行新增/删减、25 个文件），分别从"有没有重复代码 / 前后是否一致"与"测试是否真的能挡住缺陷"两个角度审查并给出结论。

两份独立评审均判**有条件通过**：代码功能层面未发现业务回归，但各抓出 1 项必须更正的问题（**均为文字记载失准，不涉及代码**）：

1. **③ 抓出**：设计文档把注册服务里的重复代码说成"合理独立（只校验格式、不查库）"，实际它同样做了"查库"，属本轮去重未覆盖的历史重复 → 已更正文字并登记为后续去重项。
2. **④ 抓出**：某条"渠道异常不占当日额度"的测试，文档写的"故意改坏代码就会变红"的手法其实改坏后测试**也不会变红**（真正能触发变红的改法是另一种）→ 已把该说明更正为真实可验证的写法，并把"计数参数在该路径下冗余"登记为观察项。

其余建议与观察（并发用例缺失败方错误码断言、某测试用绝对日期、前端某处 100 毫秒等待等）已逐条登记，移交后续工作包，避免本包越界改动。

### 验证结果

- **本会话零代码/行为改动**（仅改设计文档 + 新增本日志），故不重跑门禁：沿用上一会话实测结论——后端 `mvn -pl services/subject-service,std-adapter -am test` = subject **111/111** + std **20/20**；前端 vitest **68/68** + eslint 0 + vue-tsc 0；run-gates **GREEN（PASS=9 FAIL=0）**；三幕剧本 Playwright 走查 **14/14**。
- 独立评审方式：两个独立子智能体**并行只读**评审（read/search/lsp，禁改文件），耗时 188s / 191s。
- 两处必修**主智能体逐条回读源码独立复核成立**：`SubjectRegistrationService.java:89-91` / `:107-109`（三段式同型内联）；`CertificationJdbcRepository.java:103-110`（`conclusion = ? AND counted = 1 AND created_at >= ?`，conclusion 固定 `FAIL`）+ `CertificationService.java:424-431`（窗口起点 `LocalDate.now(clock).atStartOfDay()`）。

### 评审结论（独立，替代首轮降级自检）

- 视角③（一致性与重复）：**有条件通过** → 必修 1（已修复）+ 建议 5 + 观察 6。
- 视角④（测试质量）：**有条件通过** → 必修 1（已修复）+ 建议 4 + 观察 8。
- 与首轮自检差异：③"SubjectRegistrationService 属合理独立"、④"T8 红锚有效"两处判定**不成立**（以独立评审为准，已更正）；其余自检结论经独立复核成立。详见 `docs/designs/WBS-3.1.6-hifi.md` §9.1/§9.2。
- 本会话另行更正 §8 T6 错误码措辞（补"政务 2MB"，视角③建议⑤，零成本准确性修正）。

### 问题与教训

1. **自检替代独立评审会漏**：首轮自检把"同型重复"误判为"合理独立"、把"无效红锚"误判为"有效"，两处均被独立评审推翻——印证章程"独立评审不可降级替代"，降级只能是应急留痕、必须回补。
2. **红锚声明须先证明"破坏后真的红"**：T8 教训——当查询含两个谓词且二者在可达路径下相关时，单独破坏其一可能不可观测（存活突变）；写"删锚验证"列时应明确"破坏哪个谓词、为何可观测"。
3. **存量注释错码会被新增测试"打脸"**：`SubjectOpsSupport:38` 注释写 1000C0002、实抛 1000C0001（`ErrorCodes.PARAM_INVALID`），本轮新增测试锚定真实错误码后偏差显性化；沿 3.1.x 先例登记处置，不在本包顺手改。

### 停机原因

【独立评审③④补录 + 两处必修修复 + 建议/观察项登记完成；本会话工作完整收口，按 1.3 完成本最小步骤（提交 + 日志落盘）后停机。**待编排师本机验收演示 + 合并裁决**。】

### 交接提醒

① 演示前置与链路见"当前续点·下一步"；② **登记观察项（随后续包处置）**：**本轮新增**——`SubjectRegistrationService.cancel/detail` 同型内联去重、`SubjectOpsSupport:38` 注释错码、`countFailuresSince` 的 `counted = 1` 与 `conclusion = 'FAIL'` 冗余（存活突变，评估删谓词或补计数位隔离用例）、T5a/T5b 败者错误码族断言缺失（含政务败者死锁 500 未转译）、T3 绝对历史日期时钟耦合、`ReviewServiceTest#approveUnknownSubjectNo` 弱透传断言、并发栅栏 `ready` 未 `await`、F1 `setTimeout 100ms` 时序、`RegisterView` try/catch 吞任意拒绝、`ReviewIntegrationTest.queueList` 与存量内联重复、前端 `inputByLabel`/`fillAllFields` 跨 spec 重复；**上一会话延续**——清单查看无审计留痕、T7 exact-FQN 守卫盲区、认证页加载瞬间企业分支闪现、common-idempotency 计时敏感用例 TTL 放宽；③ 剧本更新建议（随交付说明提交 PO 审批）：3.1.5 交付建议的两项（剧本头关联规格同步 V1.1、S2 步骤 7 幂等窗口判定说明）仍待 PO 审批，本包界面核对修订已留痕；④ dependencies.md 零变更；⑤ Flyway 零迁移（无 V3）；⑥ ADR-016 补齐七字段属门禁红灯校正（§1~§3 决策未动）；⑦ **本会话仅文档层更正（hifi §2/§8/§9），不改实现，门禁结论沿用**。
