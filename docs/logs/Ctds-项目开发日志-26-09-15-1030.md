# Ctds-项目开发日志-26-09-15-1030

> 时间戳 = 本日志落盘时点（2026-09-15 10:30）。

## 当前续点

- **已完成**：WBS-3.1.6 编排师本机验收演示——冷启动承接 26-09-14-2251 日志续点 → 编排师选定"先准备本机验收环境" → 环境拉起（sc-mysql/minio/redis 在跑；后端 8080 = `java -jar subject-service-2.0.0-SNAPSHOT.jar --spring.profiles.active=mysql` + `CTDS_DB_PASSWORD` + `CTDS_SM4_KEY_FILE=subject-demo.keys`；前端 vite 5173；接口冒烟权限门/清单/代理链路全通）→ **补建演示上传文件**（走查临时件已删，重建 `build-output/demo-files/A1.jpg·A3.cer·A4.cer`，模拟渠道按文件名标记匹配）→ 编排师界面走查三幕：S1 口径指导（管理员兼任审核员=lofi Q1 采 A 既定口径；张伟合法身份证号 `330100199001010095`）→ S2 全程（5 次失败+第 6 次当日锁定 → AI 直插等效"跨日恢复"（5 条 FAIL 时间戳挪昨日）→ 换合法数据核验通过转待审核 → 空理由驳回被拒 → 填理由驳回 → 申请端直达 URL 可见驳回理由）→ S3（政府部门+A3 直接待审核、A4 过期拒绝、政务档案通过）→ 走查中提出**两项注册问题**（联系电话无格式校验；管理员账号语义无指引）→ 编排师**三项业务裁决**（电话=手机+座机双格式；账号=界面加指引说明、选填与否入规格一并裁定；时序=登记后继续验收）→ 落盘任务笔记 `docs/tasks/C-1.1-注册字段格式与指引-规格变更待立卡-2026-09-15.md` → **编排师宣布"验收通过"**（2026-09-15 10:3x）。
- **下一步**（可直接开工）：**请编排师发出合并裁决**。裁决后执行：`git merge --ff-only feat/C-1.1-test-completion`（main）→ `git push origin main`（推送失败按 Watt Toolkit 先例重试）→ 删除任务分支；本会话或新会话执行均可（新会话冷启动承接本日志）。**随后一包 = WBS-3.1.7「C-1.2 业务规格与验收剧本」（签发/验证/吊销/互认，纯文档，另会话开工）**；验收中登记的两项问题 + 直达 URL 入口缺口 + 3.1.6 既有观察项清单（见 2251 日志"交接提醒②"）随 V1.2 规格变更任务卡处置。
- **关键决策**：
  1. **"审核员"不新增独立角色**——管理员兼任为 3.1.5 lofi 问题 1 采 A 的既定设计，验收界面疑问按口径解释，零改动；
  2. **S2"次日恢复"用库改时间戳等效**（沿 3.1.6 走查先例）：不真实改系统日期，将 5 条计数 FAIL 记录 `created_at` 挪至昨日，界面核验一次通过即等效验证"锁定按日自动恢复"；
  3. **两项注册问题不在 3.1.6 分支上改**（原则 5 最小实现 + 本包定位测试补全）：规格未定义电话格式规则、管理员账号语义写在规格字段清单但界面未透出——统一入 C-1.1 V1.2 规格变更任务卡，裁决与影响清单已落 `docs/tasks/`（见上）；
  4. **演示数据保留、环境关停与数据清理由编排师另行指示**（沿 3.1.6 lofi Q4 口径）：本机今日新增 S20260915000001~07 七个主体（含 1 已驳回/1 锁定待认证/若干未认证残留）。
- **文件指针**：本日志；上游 `docs/logs/Ctds-项目开发日志-26-09-14-2251.md`（验收前置与观察项清单）、`26-09-14-1513.md`（3.1.6 主体）；任务笔记 `docs/tasks/C-1.1-注册字段格式与指引-规格变更待立卡-2026-09-15.md`；剧本 `docs/manuals/C-1.1-主体注册与实名认证-验收剧本.md`（V1.1）；分支 `feat/C-1.1-test-completion`（`f774663`，待 ff 合并）。

## 任务记录 ｜ 2026-09-15 10:30 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode

### 做了什么（业务可读）

把 WBS-3.1.6 的**人工验收演示**走完并拿到通过结论：给没有编程经验的验收人准备好可点的环境（数据库/后端/前端三层拉起、演示数据与"执照/证书"上传文件补建），然后按三幕剧本逐步陪同走查——正常入驻（S1）、核验失败与恢复含重试上限（S2）、政务 CA 通道（S3）。过程中回应了三个走查疑问（审核入口在哪、次日恢复怎么演、驳回理由去哪看），并把发现的两个注册表单问题（联系电话乱填也能过、管理员账号没人看得懂）经编排师裁决后登记入册，留待规格变更修复。

### 验证结果

- 三幕走查全部通过，**编排师宣布验收通过**（2026-09-15）。
- 走查中所有"异常拦截"均按规格现势生效：信用代码不一致阻断、法人不一致阻断、当日第 6 次锁定、空理由驳回拒绝、无权限角色拒绝——非缺陷，逐条向验收人解释后确认。
- 环境链路实测：`/api` 代理 → 8080 → sc-mysql `ctds_subject` 库，冒烟 `code:"0"`；错误角色请求返回 `1000C0005 无权限`（后端鉴权边界正常）。
- 本会话未改动任何代码/测试（仅任务笔记 + 本日志入库）。

### 问题与教训

1. **交付验收材料必须持久**：走查用的 A1/A3/A4 上传文件是临时件"用毕即删"，导致人工验收时无文件可传、临时重建——以后凡剧本含文件上传，演示文件交付时放持久目录并写进剧本附录或交接提醒。
2. **"等效替代"要在验收前向验收人声明**：S2 次日恢复的库改时间戳口径，昨天走查记录里写了但验收指引没前置说明，编排师在审核清单前卡了一轮；今后演示指导里凡"不真实操作"的等效口径需前置标注。
3. **界面语义透出不足会被验收当作缺陷**：管理员账号字段规格里有定义、界面上一字未提，业务人合理理解为"平台管理员账号"——登记为 V1.2 变更项。
4. **合法身份证号现算现验**：剧本附录 A 未附具体号码，AI 按 GB 11643 校验位计算给出 `…0028`（尾 8 触发失败）与 `…0095`（通过），不凭记忆（沿"信计算不信记忆"方法论）。

### 停机原因

【验收演示完成、"验收通过"结论与三项裁决已落盘；本会话工作收口，按 1.3 完成当前最小步骤（日志+笔记提交）后停机。待编排师发出合并裁决。】

### 交接提醒

① 合并裁决待发出；执行序=ff 合并 main + 推送 + 删分支（合并不代决）；② 下一包 WBS-3.1.7（C-1.2 规格与剧本，另会话冷启动）；③ **V1.2 变更任务卡待立**：电话双格式、管理员账号指引/选填裁定、"我的主体"入口缺口一并裁定（详见任务笔记）；④ 本机环境进程（后端 8080 / 前端 vite 5173）仍在运行，演示数据 S20260915000001~07 与 `build-output/demo-files/` 去留由编排师指示（关停清理 AI 可代办）；⑤ 3.1.6 既有观察项清单沿用 2251 日志"交接提醒②"，未在本会话处置；⑥ 剧本 V1.1 零改动；dependencies.md 零变更；Flyway 零迁移。
