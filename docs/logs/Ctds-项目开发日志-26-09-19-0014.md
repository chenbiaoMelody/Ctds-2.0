# Ctds-项目开发日志-26-09-19-0014

> 时间戳 = 本日志落盘时点（2026-09-19 00:14；复评动作实际发生于 2026-09-18 22:5x~23:5x，跨零点落盘）。
> 任务性质：**卡 1 ｜ WBS-2.2.7 门禁运行器健壮性 —— 第 2 轮 4 视角复评（评审循环 2/3）**（单会话单任务；只读评审 + 复评留痕，未修改任何被评代码）。
> 上游：`Ctds-项目开发日志-26-09-15-1631.md`（第 2 轮修复 R1~R9 与交接提醒）；任务卡"卡 1 第 2 轮修复记录"节。

## 当前续点

- **已完成**：**卡 1 第 2 轮 4 视角复评通过**，汇总裁决 = ① 有条件通过（保留条件=selftest 动态反向探针，**已由本会话当日执行闭环**）② 通过 ③ 通过 ④ 通过；R1~R9 经四位独立评审逐条核对**全部确认落地**，第 1 轮 9 个命中点全部确认关闭：
  1. **动态验证三件套（主会话执行，补评审①保留条件）**：selftest 全量 `40 断言全绿、真实退出码 0`；**独立反向探针**（S1 断言期望码 0→99 → `SELFTEST FAIL - 40 assertion(s), 1 failure(s)` 且失败沙箱保留；恢复后复跑全绿，探针沙箱已清理、脚本已复原）——证明自检可被证伪、不造假绿；交付态全量门禁复跑（RunLabel `20260918-230903-7008`）= **RED/退出码 1，唯一 FAIL=frontendTest**（F4 首转超时），随即 `npm run test` 复跑 **68/68、退出码 0** → 定性为债务 **DB-02 已登记偶发**，非本轮回归；secretsScan 547 文件 0 命中、护栏 0 hits、后端三阶段+frontendLint 全 PASS。
  2. **复评新发现（无必修项）**：评审②——目录枚举失败可致入库文件逃逸 unreadable 检测、>1MB 入库文件静默绕过扫描（并入 **DB-18**）；评审③——`-1631` 正文一处 RunLabel 误引（已按补正说明③处理）、"464 系笔误"定性不准（同补正）；评审④——R2"FAIL+ERROR→1"组合场景与 R5 非法字符跳过路径**无自动锚点**（登记 **DB-17**，补 S8+注入用例约 30 行）、FAIL 明细中文乱码（**DB-19**）；评审①——报告正文与实际退出码极窄竞态、护栏生命周期绑定 secretsScan 阶段（随 DB-07/README 收敛，不阻塞）。
  3. **留痕收口**：任务卡新增"卡 1 第 2 轮 4 视角复评结论"节 + 状态行改"复评已通过，合并与提交待编排师指示"；债务账 DB-01/DB-16 转**待验证**（合并入库后关闭）、新登记 **DB-17/18/19**、关闭统计改 19 条、进度快照"当前阻塞"行同步；`-1631` 追加补正说明③。

- **下一步**（可直接开工）：
  1. **编排师裁决两件事**：① 卡 1 提交与推送（建议提交信息：`fix(gates): WBS-2.2.7 门禁运行器第 2 轮修复（R1~R9）+ 第 2 轮 4 视角复评通过`，须包含本日志与全部文档留痕，AGENTS §6 日志与代码同库提交）；② 变更体量约 640 行超出 AGENTS §6"≤400 行"指引——按"单卡单目标"不拆分合并，或拆"运行器修复"与"自检脚本"两个 PR（任务卡 ：154 已披露）。
  2. 合并入库后：债务账 **DB-01/DB-16 关闭**（填关闭时间+提交号）。
  3. 之后按既有顺序推进 **卡 3**（AUD-03 演示环境加固，需编排师挑演示之外时点重启 8080 后端）→ **卡 4**（事务边界+身份证校验）→ **卡 2**（前端测试稳定性，可与 3.1.7 并行）；**DB-17** 建议作小卡紧随 DB-16 关闭后接续（selftest 补 S8+R5 注入用例）。

- **关键决策**：
  1. **复评通过的裁决口径**：四位评审无必修项、无打回；评审①的保留条件（动态反向探针）不在评审会话内做（其只读约束），而由主会话按交接提醒执行并留痕——探针同时验证了"断言真实求值、失败显式红、失败时沙箱保留"三条 harness 承诺。
  2. **全量门禁 RED/1 不判为本卡问题**：唯一 FAIL=frontendTest F4 用例，与 DB-02 登记的偶发缺陷完全吻合，且复跑 68/68 证实偶发；门禁对已知偶发正确抓红恰是本卡要保证的行为。不据此推迟复评结论。
  3. **复评建议级发现一律登记债务而非当场修改**：单会话单任务 + 只读评审纪律——本会话不改被评代码（反向探针对 selftest 的临时改动已复原，`git status` 核对一致），新发现以 DB-17/18/19 立账。
  4. **历史日志错误按补正说明机制处理**：`-1631` 的 RunLabel 误引与"464 笔误"定性以补正说明③收口，正文不改动（AGENTS §1.4）。

- **文件指针**：
  - 复评结论与证据：`docs/tasks/C-TDS-AUD-P0-处置任务卡-2026-09-15.md`（"卡 1 第 2 轮 4 视角复评结论"节）；`docs/status/进度看板与债务登记簿.md`（DB-01/16/17/18/19 行、关闭统计、进度快照、变更记录）；`gate-report-20260918-231227.md`（交付态复跑，RED/1 归因 DB-02）；
  - 被评对象（未改动，待提交）：`scripts/gates/{gates-config.json,run-gates.ps1,selftest.ps1,README.md}`；
  - 上游日志：`docs/logs/Ctds-项目开发日志-26-09-15-1631.md`（含本次新增补正说明③）、`-1525.md`（第 1 轮评审结论）、`-1511.md`；
  - 补充实测：`npm run test` 复跑 68/68（输出留本会话临时文件，结论已记入本日志与债务账）。

## 任务记录 ｜ 2026-09-19 00:14 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：WBS-2.2.7（第 2 轮复评）

### 做了什么（业务可读）

上一会话把"体检仪"修好了但还没人复核。本会话请了**四位独立评审员**分别从"是否按图纸施工""有无新的安全隐患""台账之间对不对得上""自检本身可不可信"四个角度复核，并亲自做了三件机器验证：

1. **证明"体检仪的自检"不是摆设**：故意把自检的一条标准改错再跑——自检立刻报错（`40 条断言，1 条失败`）且保留现场；改回来复跑全绿。这说明自检的每条标准都是真的在被核对，不是走过场。
2. **复跑一次完整门禁**：结果亮红灯——但唯一不合格项正是债务账里早就登记过的"前端测试偶尔心跳不齐"（DB-02），紧接着单独复跑前端测试 68/68 全绿，证实是老毛病偶发，不是这轮修复弄坏的。同时确认：密钥扫描 547 个文件 0 命中、后端全部检查通过——**门禁在真实场景下正确地抓了红灯，行为符合承诺**。
3. **汇总裁决并落账**：四位评审一致确认九项修复全部真实落地、第一轮的九条意见全部关闭。评审过程中发现的三类新小问题（两处自检盲区、三类扫描死角、报告乱码）都已登记为新债务 DB-17/18/19，待后续小卡处理，不阻塞本卡。

### 验证结果

| 验证 | 命令/方式 | 结果 |
| --- | --- | --- |
| selftest 全量 | `powershell ... selftest.ps1`（真实退出码经 `$LASTEXITCODE` 转发核实） | `SELFTEST PASS - 40 assertion(s), 0 failure(s)`，退出码 0，沙箱自清理 |
| 独立反向探针 | 临时改 S1 断言 `-eq 0`→`-eq 99` 后复跑；恢复后复跑 | 改坏 → `SELFTEST FAIL - 40 assertion(s), 1 failure(s)`、失败沙箱保留；恢复 → 全绿、退出码 0、探针沙箱清理、脚本复原（`git status` 与探针前一致） |
| 交付态全量门禁 | `run-gates.ps1 -Scenario round2-recheck-20260918` | RED、退出码 1；唯一 FAIL=frontendTest（DB-02 偶发）；secretsScan 547 文件 0 命中；护栏 `0 hits against 518 git-tracked path(s)`；报告 `gate-report-20260918-231227.md` |
| 前端测试复跑（定性） | `npm run test` | 68/68、退出码 0 → 证实 DB-02 偶发定性 |
| 复评派发 | 4 个独立只读评审子智能体（串行） | ①有条件通过（保留条件已闭环）②通过 ③通过 ④通过；无必修项 |

### 问题与教训

1. **评审③靠报告头字段反查出台账引用错误**（RunLabel 误引）——这恰说明 R6 的"报告可复核字段"设计有效：第三方可以用报告本身证伪台账。
2. **"修了"不等于"测了"**：R5（非法配置整段跳过）修法正确却零验证锚点，被评审④点名——修复清单的每一项都应同步问"用什么锚点防它退回去"，这条已固化为 DB-17。
3. **红灯要先定性再下结论**：全量门禁 RED/1 出现时，先查 FAIL 归属（frontendTest=DB-02 已登记偶发）+ 复跑证实，才判定"非本轮回归"——避免"看到红就慌"或"看到红就掩盖"两个方向的错误。
4. **历史数字必须带时点口径**："464"与"463"都是真实测量值（不同时点），"笔误"的定性反而失准——与 -1631 教训#4（历史数字不可直接引用）同源。

### 停机原因

【卡 1 第 2 轮 4 视角复评完成且通过，留痕全部落盘（任务卡/债务账/补正说明/本日志）。下一步"编排师裁决提交推送与体量拆分"属人工输入，本会话按纪律在此收口。上下文用量：自估约 4~5 成（权威值以任务窗口显示为准，AGENTS §1.2.3）。】

### 交接提醒

① **卡 1 状态 = 复评通过、待合并**：提交/推送/合并由编排师指示；建议提交信息见"下一步 1"；提交须包含 `scripts/gates/` 4 文件 + 任务卡/债务账/两份日志（-1631 补正 + 本日志）。
② **两项待裁决**：变更体量约 640 行是否拆 PR（任务卡"变更体量说明"）；DB-17 是否作小卡紧随 DB-16 关闭后立卡。
③ **合并后动作**：债务账 DB-01/DB-16 填关闭时间+提交号关闭。
④ **未触碰**：被评四文件、业务代码、`pom.xml`/依赖、Flyway 迁移、8080 后端进程、演示数据、`CHG-C-1.1-V1.2`、`feat/C-1.1-V1.2-...` 分支；反向探针对 selftest 的临时改动已复原。
⑤ **门禁当前真实状态**：因 DB-02 偶发，最近一次全量门禁为 RED/1——红灯归属卡 2（前端测试稳定性），不阻塞卡 1 合并裁决，但编排师知悉即可。
