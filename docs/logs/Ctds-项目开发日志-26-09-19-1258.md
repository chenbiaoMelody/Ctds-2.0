# Ctds-项目开发日志-26-09-19-1258

> 时间戳 = 本日志落盘时点（2026-09-19 12:58）。任务性质：**卡 2 ｜ WBS-3.1.6b 前端测试稳定性修复（对应债务 DB-02）**（多任务会话第 4 段=最后一段，编排师显式授权沿 26-09-12 先例；本段完成即会话收口）。
> 上游：`Ctds-项目开发日志-26-09-19-1227.md`（卡 4）。

## 当前续点

- **已完成**：卡 2 全链路完成（修复 → 连跑验证 → 2 个独立评审 → 采纳建议 → 复验），**待编排师验收合并**：
  1. **修复内容**：`frontend/src/router/router.spec.ts` 顶层**静态导入** `../views/review/QueueView.vue`（/review 懒加载组件）——vite-node 首次转换成本先于一切用例与超时窗口完成，F4 用例（admin 直连 /review 放行）不再承担首转耗时，消除"第一次红、第二次绿"偶发；vitest 受限重试方案**未采用**（无必要，测试文件留痕注释；先例=QueueView.spec.ts 的静态导入）。
  2. **实现演进（评审驱动）**：初版为用例体内 `await import` 预热（5 连跑 68/68，其中前 3 次与后端 `mvn -B -ntp test` BUILD SUCCESS 并行窗口重叠）；评审B 建议 1 指出"用例体内预热仍计入 30s 超时窗口，顶层静态导入才是真正移出"——采纳后改为顶层静态导入并复验 3 连跑绿（两方案累计 8 连跑 68/68、退出码 0，全超卡面"连续 5 次"门槛）。
  3. **独立评审（2 个合并视角智能体，改动面仅 +4 行按比例配置，留痕供编排师裁决是否回补 4 会话形态）**：①+③（规格符合性+一致性）【通过】——diff 仅此一处、预热与懒加载同一模块标识符、注释与行为逐条相符、无"应预热而未预热"遗漏；②+④（安全供应链+测试质量）【有条件通过】——供应链零新增（spec 文件不进构建图）、断言未空洞化、无假绿路径；建议 1（顶层导入）已采纳，建议 2（关闭留痕如实标注证据边界）已写入台账。
  4. **台账**：DB-02 → **待验证**（关闭条件实测达成；证据边界如实注记：连跑为经验证据非机制证明——失败明细因 DB-19 乱码从未可读，"首转超时"为推断，随手清 DB-19 后可复验红灯原文）。**注**：本分支基于 main，台账仅改 DB-02 行不动统计行——三支卡分支各自演化台账，**合并时需人工并账**（见交接提醒①）。

- **下一步**（可直接开工，新会话冷启动）：
  1. **编排师验收与合并裁决**：三支分支待合并（建议顺序与并账要点见交接提醒）。
  2. 合并后关闭 DB-02/DB-03/DB-04/DB-05（台账已备好关闭条件与证据）；随后按裁决推进 DB-17~21 小卡与 3.1.7。
  3. **演示环境**：8080/5173 仍在运行（卡 3 起拉起，含回环加固）；卡 4 改动需重新打包部署后才反映到演示后端。

- **关键决策**：
  1. **预热落位=文件顶层静态导入**（评审B 建议，优于初版用例体内预热）：静态导入在 vitest 模块转换阶段完成、不占任何用例超时窗口，且仓库已有同款先例；两版累计 8 连跑全绿留痕。
  2. **不设 vitest 受限重试**：卡面措辞"确有必要时"——预热已使 8 连跑全绿，重试非必要且会引入"重试后通过须单独标注"的额外口径；测试文件注释留痕该取舍。
  3. **关闭留痕如实降格**（评审B 建议 2）：DB-02 的关闭证据标注为"经验证据满足台账自设门槛"，不写"机制上已消除"——历史红灯明细因 DB-19 乱码从未被读懂，"首转超时"是从注释推断的病因；清 DB-19 后应复验一次红灯原文。

- **文件指针**：
  - 本次变更：`frontend/src/router/router.spec.ts`（顶层静态预热 + F4 用例注释）；台账 DB-02 行；
  - 上游：`-1227`（卡 4）、`-1107`（卡 3+卡 1 收尾）；任务卡 `docs/tasks/C-TDS-AUD-P0-处置任务卡-2026-09-15.md` 卡 2 节；
  - 证据：/tmp/card2-run{1..8}.log（8 次 68/68）、/tmp/card2-mvn-parallel.log（后端并行窗口 BUILD SUCCESS）。

## 任务记录 ｜ 2026-09-19 12:58 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：WBS-3.1.6b（卡 2）

### 做了什么（业务可读）

前端测试"偶尔心跳不齐"的病根：审核清单页面是"用时才加载"的，第一次跑测试时测试工具要现做这个页面的"翻译"，赶上和其他测试挤在一起就要好几秒，把导航断言拖红；第二次跑翻译已缓存就绿了。修复 = 在测试文件开头就先把页面"翻译"好（静态导入），翻译成本彻底挪出被测环节。连续跑 8 次全部 68/68 全绿（其中 3 次故意和后端全量测试同时跑），远超验收要求的 5 次。

### 验证结果

| 验证 | 结果 |
| --- | --- |
| 前端连跑 | 8 次 `npm run test` 全部 `68 passed (68)`、退出码 0（用例内预热版 5 次 + 顶层静态导入版 3 次） |
| 并行条件 | 前 3 次与后端 `mvn -B -ntp test` 同时运行（后端 BUILD SUCCESS：111+15+49 全绿，main 态） |
| 独立评审 | ①+③合并视角【通过】；②+④合并视角【有条件通过】（建议 1 已采纳并复验、建议 2 已写入台账） |

### 问题与教训

1. **"移出被测行为"要移干净**：用例体内预热只是把成本移出导航断言，仍在超时窗口内；评审指出顶层静态导入才是彻底解法——修稳定性问题时问一句"这个成本现在落在哪个窗口里"。
2. **病因是推断不是实证**：DB-02 的"首转超时"来自注释与乱码报告的拼图，从未读到红灯原文——修好后应顺手清了 DB-19（乱码）再回头验证一次病因诊断。
3. **三支分支并行的台账演化**：每支卡分支各自带着台账前进，合并时同文件必冲突——本卡刻意只改自己的债务行，并账动作留给合并环节（交接提醒①）。

### 停机原因

【卡 2 完成 = 编排师指定的"继续卡 2、3、4"全部执行完毕。上下文自估约 8.5 成（权威值以任务窗口显示为准，AGENTS §1.2.3），已达停机线——本日志落盘、提交推送（或留待网络恢复的推送指令）后本会话收口。后续：三支分支验收合并（新会话）。】

### 交接提醒

① **三支分支待合并（均未推送成功的可能：GitHub 通道 11:3x 起中断，推送重试见②）**：`fix/AUD-03-demo-loopback-hardening`（c1bd6eb）→ `fix/AUD-04-05-tx-id-validation`（0397553，底座=main）→ `fix/WBS-3.1.6b-frontend-test-stability`（底座=main）。**合并顺序建议卡 3 → 卡 4 → 卡 2**：三者台账（本文件）各自演化，后合者需手工并账（以最后状态为准：DB-01/16 已关闭+DB-03 待验证+DB-20、DB-04/05 待验证+DB-21、DB-02 待验证；关闭统计行以合并完成后的实况重写）。前端 `router.spec.ts` 仅卡 2 触碰、`CertificationService` 系列仅卡 4 触碰、配置与文档仅卡 3 触碰——代码文件无冲突。
② **推送状态**：卡 3/卡 4/卡 2 分支若仍未推送，命令 = `git push origin fix/AUD-03-demo-loopback-hardening fix/AUD-04-05-tx-id-validation fix/WBS-3.1.6b-frontend-test-stability`（卡 4/卡 2 分支已在本地就绪）。
③ **验收要点**：卡 3 = 同机走查剧本 V1.2（环境在跑）+ 可选跨机实测；卡 4 = 审查回滚测试与并发断言（或重打包后演示 15 位号被拒）；卡 2 = 抽查测试稳定性。
④ **遗留推送注意**：卡 4 分支曾短暂含卡 3 提交后被重置回 main 底座（0397553 单提交干净）；若远端曾收到带 c1bd6eb 的旧引用需 force-with-lease——实际未推送成功过，无需处理。
