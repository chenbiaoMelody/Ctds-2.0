# Ctds-项目开发日志-26-09-19-1714

> 时间戳 = 本日志落盘时点（2026-09-19 17:14）。任务性质：**CHG-C-1.1-V1.2 代码实施（变更单 §4 第④步，测试先行）**。
> 上游：`Ctds-项目开发日志-26-09-19-1601.md`（设计确认收口，裁定代码实施另会话）。

## 当前续点

- **已完成**（CHG-C-1.1-V1.2 代码实施全链，分支 `feat/C-1.1-V1.2-registration-fields-and-progress`）：
  1. **冷启动**：读 README + 日志 `-1601`，推送状态核实（main 与分支 `f6f8a85` 均已在 GitHub，交接提醒②闭环）；经编排师问答裁决本会话 = **V1.2 代码实施**（备选 3.1.7 未选）。
  2. **后端测试先行（RED）→ 实现（GREEN）**：`SubjectRegistrationServiceTest` 新增 9 用例（电话双格式正反、管理员账号 64/65/汉字边界锁定、progress 六分支：五字段最小出参/小写带空格归一/17 位参数错误不查库/编号不存在同文案无审计/凭证不符 DENIED 留痕/驳回理由）；`SubjectRegistrationIntegrationTest` 新增 7 用例（HTTP 层电话拒绝/座机通过/progress 成功五字段+无敏感字段+SUCCESS 审计/归一/统一文案/DENIED 审计/缺参与 17 位参数错误/驳回理由/未认证 401）。
  3. **后端实现**：`requireValid` 电话双格式 `^(1[3-9]\d{9}|0\d{2,3}-\d{7,8})$`（删 32 长度检查）；`RegisterRequest.contactPhone` 换 @Pattern 同口径；新增 `progress(subjectNo, rawUscc)`（凭证断言取代归属断言、统一文案防枚举、审计 SUCCESS/DENIED）+ `ProgressResult`；`SubjectRegistrationController` 增端点 `GET /{subjectNo}/progress?uscc=`（@RequirePermission("subject.read")）；`ProgressView` 五字段 DTO；`ValidationExceptionHandler` 补 `ConstraintViolationException` 分支。
  4. **前端测试先行（RED）→ 实现（GREEN）**：新增 `ProgressQueryView.spec.ts`（9 用例）、`RegisterView.spec.ts` 增 4 用例（placeholder 文案/非法格式红锚/座机通过/指引小字）、`router.spec.ts` 增 1 用例（menuOrder=7/icon=Search/无权限点/菜单最大序号锚）；实现 `ProgressQueryView.vue`（双凭证表单+结果卡+驳回 alert+前往认证页）、`RegisterView.vue` 两处增量（电话 placeholder+格式规则、管理员账号常驻指引）、`api/subject.ts`（`fetchRegistrationProgress`+类型）、`router/index.ts` 路由；`MainLayout.spec.ts` 菜单数 4→5/6→7（设计内同步）。

- **下一步**（可直接开工）：
  1. **人工验收演示**（编排师安排会话）：按剧本 V1.2 S1-1/S1-2/S1-11/S2-7 走查电话双格式、进度查询、驳回理由；演示环境启动命令与既有口径不变（后端 `mvn spring-boot:run`，前端 `npm run dev`）。
  2. **四视角评审**（提交后章程流程）：规格与设计符合性/安全供应链/一致性重复/测试质量。
  3. 旁支：3.1.7（C-1.2 业务规格与验收剧本）、DB-17~21 小卡待立，先后由编排师指示。

- **关键决策**（均已在交付说明/代码注释留痕）：
  1. **GET 查询参数校验通道**：类级 `@Validated` + `@RequestParam(required=false)` + `@NotBlank`，缺参/空参统一走校验通道（400 逐字段文案，防落入全局兜底 500 失真）；uscc 18 位格式校验**复用既有 `USCC_PATTERN` 在 service 层执行**（trim+upper 归一**之后**校验）而非参数注解 @Pattern——hifi §4"小写带空格匹配成功"要求归一先于格式判定，注解校验原始串无法表达；"既有 18 位 USCC 正则"字面即复用常量。技术裁决在 AI 链路内完成，人只认业务行为（17 位/缺参 → 400 参数错误文案）。
  2. **rejectReason 语义实现**：status=REJECTED 时取 `findLatestTransition` 且 `toStatus==REJECTED` 的流转备注（状态机保证 REJECTED 态最新流转必为转驳回），非驳回态恒 null——与 hifi §3"最近一条转已驳回流转备注"逐字对应。
  3. **集成测试造数须过 `appendTransition` 状态门槛**（UPDATE 带 from_status 前置）：驳回态造数按真实状态机路径两步推进（PENDING_CERT→PENDING_REVIEW→REJECTED），不走"单步直插"捷径。
  4. **门禁脚本环境受阻的处置**：`run-gates.ps1` secretsScan 阶段被演示进程写锁的 `logs/app.log`（git 已忽略）阻断 ReadAllBytes 异常；不改门禁脚本（红线 3）、不动演示进程，改用脚本同款 8 条正则对 16 个待提交文件等效扫描（0 hits）——正式门禁在干净环境运行无此文件锁问题。

- **文件指针**：
  - 分支 `feat/C-1.1-V1.2-registration-fields-and-progress`：`f6f8a85`（设计确认）→ 本次提交（代码实施，见 git log）；
  - 设计契约：`docs/designs/CHG-C-1.1-V1.2-hifi.md`（§2/§3/§4 编码契约）/ `-lofi.md`；
  - 规格与剧本：`docs/specs/C-1.1-主体注册与实名认证.md` V1.2、`docs/manuals/C-1.1-主体注册与实名认证-验收剧本.md` V1.2；
  - 变更单：`docs/tasks/CHG-C-1.1-V1.2-规格变更单-2026-09-15.md`（§4 第④步本次完成）；
  - 上游日志：`docs/logs/Ctds-项目开发日志-26-09-19-1601.md`。

## 任务记录 ｜ 2026-09-19 17:14 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：CHG-C-1.1-V1.2（代码实施）

### 做了什么（业务可读）

注册页的"联系电话"从随便填升级为只能填两种格式：11 位手机号（如 13800001234）或"区号-座机号"（如 0571-87654321），填错当场红字提示；"管理员账号"输入框下方新增常驻说明小字，解释该填什么。另新增"入驻进度查询"侧边栏菜单页：凭注册时返回的申请编号 + 统一社会信用代码两样凭证，可随时查询自己的入驻状态；被驳回的会直接显示驳回理由，并有"前往认证页"按钮一站式跳转。查询失败统一提示"未查询到匹配的申请"（不泄露哪一项填错），全部查询留痕可回溯。

### 验证结果

| 验证 | 结果 |
| --- | --- |
| 后端 subject-service 全模块测试 | **129 tests，0 失败**（含本次新增单测 9 + 集成 7；集成走 Testcontainers MySQL 实库） |
| 后端 checkstyle | BUILD SUCCESS |
| 前端全量测试 | **81/81 通过**（含本次新增 14 用例） |
| 前端 ESLint | 通过（0 报错） |
| 密钥扫描 | 门禁脚本被演示进程文件锁阻断（环境因素，见关键决策 4）；同款规则等效扫描 16 个待提交文件 **0 hits** |
| 测试先行留痕 | 单测 RED（ProgressResult 编译失败）→ GREEN；前端 RED（组件缺失 4 failed）→ GREEN |

### 问题与教训

1. **集成测试 uscc 序号撞号**：新用例选 30~36 区间与既有并发用例 21~40 撞号（4 个用例连带失败）；教训：测试数据段位分配应有登记（现 1~13、21~40 已占用，本次起用 41~47），后续新增用例先 grep 既有 `uscc(` 分配。
2. **GB 32100 字符集排除 I/O/S/V/Z**：凭证不符用例初版用 `MA27X8ZZZZ`（含 Z）被格式校验先拦，断言误报——测试数据必须用合法格式的"另一个号码"，而非非法字符凑数。
3. **前端菜单数断言属路由表联动锚点**：新增菜单须同步 `MainLayout.spec.ts` 两处计数（本次已同步并加"入驻进度查询"渲染断言）。

### 停机原因

【编排师裁定的任务卡（变更单 §4 第④步代码实施）已完成：测试先行实现 + 全量验证 + 本日志落盘。上下文自估约 4~5 成（权威值以任务窗口显示为准）。】

### 交接提醒

① **推送**：本分支新提交（代码实施 + 本日志）需推送 GitHub（Watt Toolkit 须在运行）；若落盘时推送未成功，推送是下一会话第一动作。
② **人工验收演示**：剧本 V1.2 的 S1-1（合法手机/座机）、S1-2（非法格式红锚）、S1-11（进度查询入口与结果）、S2-7（驳回理由与凭证不符）待走查；剧本是否需更新 = 无（V1.2 已同步本次行为）。
③ **评审**：按章程提交后进入 4 视角评审；PR 描述需含自检单与四项声明（复用声明/规格外实现声明=空/本地检查摘要/高风险自查）。

## 补正说明（2026-09-19 20:3x，4 视角评审③发现，原文不改）

1. **测试分项计数笔误**："验证结果"表与正文写"新增单测 9 + 集成 7""前端新增 14 用例"——实际为**集成 +9**（电话正反 2 + 进度查询 7，评审④核实），**前端 +13**（ProgressQueryView 8 + RegisterView 4 + router 1；MainLayout 为既有用例断言同步非新增）。总数结论不受影响：后端 129（111+18）、前端 81（68+13），双方独立复跑全绿。
2. 同会话交接提醒①的推送、人工验收（三组）与评审收口，详见后续日志 `-2007`。
