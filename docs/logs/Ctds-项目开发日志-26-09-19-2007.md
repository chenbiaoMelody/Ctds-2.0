# Ctds-项目开发日志-26-09-19-2007

> 时间戳 = 本日志落盘时点（2026-09-19 20:07）。任务性质：**CHG-C-1.1-V1.2 人工验收支持与收口**（同会话承接 `Ctds-项目开发日志-26-09-19-1714.md` 的验收交接段）。

## 当前续点

- **已完成**：
  1. **验收受阻诊断**：编排师按验收指引走第二/三组时，进度查询页两轮返回"未查询到匹配的申请"。接口直测证实根因——**演示后端是 15:43 启动的旧版本进程**（新查询端点 17:14 才交付），路由级 404 也返回 code `1000C0003`（文案"资源不存在"），前端按码归一显示成"未查询到匹配的申请"，误导验收方向。
  2. **演示环境重启部署**：停旧进程 → 重打包 jar（18:38）→ 按制度经环境变量注入数据库口令重启（口令实时取自 sc-mysql 容器，零落盘）；沉淀启动脚本 `build-output/start-demo-backend.ps1`（纯 ASCII，独立进程不随会话退出）。
  3. **编排师人工验收通过**（三组增量走查）：①注册页电话双格式与管理员账号指引；②入驻进度查询已入驻态（蓝天，`S20260914000001`）+ 前往认证页跳转；③已驳回态驳回理由全文（星河，`S20260914000002`）与错误凭证统一拒绝。
  4. **文档收口**（分支提交）：变更单 §4 补"第④步完成 + 人工验收通过"进度记录（五步全部完成）；剧本维护说明追加"CHG-C-1.1-V1.2 界面核对修订留痕"（界面与剧本预期一致，业务步骤与判定标准零改动，版本号维持 V1.2）。

- **下一步**（可直接开工，**建议新会话**）：
  1. **4 视角评审**（章程提交后流程）：对分支 `feat/C-1.1-V1.2-registration-fields-and-progress`（`9a0c13e` + 本次 docs 提交）派发规格与设计符合性/安全供应链/一致性重复/测试质量四视角（子智能体单发串行，注意限流与配额区分）；评审通过后走 PR 合并 main。
  2. 或按编排师指示改做 **WBS-3.1.7（C-1.2 业务规格与验收剧本）** / DB-17~21 小卡。
  3. 旁支：演示环境启动方式已沉淀（见关键决策 2），下次演示前直接复用。

- **关键决策**：
  1. **"旧后端 404 被前端归一为业务文案"不判为产品缺陷**：正确部署下路由级 404 不可达；已在记忆与剧本留痕，并将"前端可按 message 细分系统错误"登记为评审观察项（最小实现纪律，不在验收现场加码）。
  2. **演示后端启动姿势标准化**：`java -jar … --spring.profiles.active=mysql` 必须 `CTDS_DB_PASSWORD` 环境注入（默认空=启动失败）；脚本用 `docker exec sc-mysql printenv MYSQL_ROOT_PASSWORD` 实时取密码。
  3. **PowerShell 5.1 编码教训**：UTF-8 无 BOM 的 .ps1 含中文注释时按 GBK 误读导致解析坏（docker 输出取值为 null）——演示/工具脚本一律纯 ASCII 注释；已写入持久记忆。
  4. **驳回理由展示含前缀"审核驳回："**：系统留痕原文（审核服务既定口径），剧本 S2-7"理由全文"判定不受影响，界面核对留痕如实登记、不改判定。

- **文件指针**：
  - 分支 `feat/C-1.1-V1.2-registration-fields-and-progress`：`9a0c13e`（代码实施）→ 本次 docs 收口提交（变更单进度 + 剧本界面核对留痕 + 本日志）；
  - `docs/tasks/CHG-C-1.1-V1.2-规格变更单-2026-09-15.md`（五步全部完成）；`docs/manuals/C-1.1-主体注册与实名认证-验收剧本.md`（V1.2 + 界面核对留痕）；
  - `build-output/start-demo-backend.ps1`（演示后端启动脚本，不入库目录）；
  - 上游日志：`Ctds-项目开发日志-26-09-19-1714.md`（代码实施）、`…-1601.md`（设计确认收口）。

## 任务记录 ｜ 2026-09-19 20:07 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：CHG-C-1.1-V1.2（人工验收支持与收口）

### 做了什么（业务可读）

验收时进度查询页面提示"未查询到匹配的申请"，排查确认是演示环境还在运行改动前的旧版后端服务，并非功能错误。将新版后端重新打包、按密钥不落盘的规矩重启部署后，编排师重走三组验收全部通过：注册电话只能填手机或"区号-座机"两种格式并有说明文字；凭申请编号和信用代码能查到自己的入驻状态（已入驻/已驳回均正确，被驳回的能看到驳回理由全文）；查询信息填错时只提示"未查询到匹配的申请"、不泄露细节。验收通过后，把结论登记进变更单（五步流程全部完成）和验收剧本（界面与剧本一致的核对记录）。

### 验证结果

| 验证 | 结果 |
| --- | --- |
| 编排师人工验收 | **三组通过**（注册页增量 / 已入驻查询 / 驳回理由与错误凭证） |
| 接口直测三场景 | 已入驻五字段、已驳回含理由全文、错误凭证统一 `1000C0003`——全部符合 hifi §3/§4 |
| 演示环境 | 后端独立进程运行新版本（18:49 起），前端 5173 热更新无感 |
| 文档 | 变更单 §4 五步闭环留痕；剧本界面核对留痕（零改动） |

### 问题与教训

1. **改代码后演示后端必须重打包+重启**：旧进程无新端点，且路由级 404 与业务"未查询到"共码（1000C0003），前端统一文案放大了误导——演示前一律实测端口/端点再开走查（已入持久记忆）。
2. **PowerShell 5.1 读 UTF-8 无 BOM 中文注释 .ps1 会解析坏**：密码注入脚本两次取空即此因，改纯 ASCII 后一次通过（已入持久记忆）。
3. **测试数据段位要登记**（承接 1714 日志教训）：本次验收用演示库既有主体（非新造数据），规避了撞号问题；后续新造演示数据先查库再定值。

### 停机原因

【编排师裁决的验收已通过，文档收口与日志落盘完成。上下文自估约 6~7 成（权威值以任务窗口显示为准），按 50% 停机纪律在收口动作完成后即停，4 视角评审留下会话。】

### 交接提醒

① **推送**：分支 docs 收口提交与 main 台账提交需推送 GitHub（Watt Toolkit 在运行即推）。
② **下一会话首选**：4 视角评审（分支已具备全部评审输入：代码 + 两级设计 + 规格/剧本 V1.2 + 自检数据）；评审通过后 PR 合并 main（DB-11 部分关闭项不受影响）。
③ **3.1.7 / DB-17~21** 先后由编排师指示。
