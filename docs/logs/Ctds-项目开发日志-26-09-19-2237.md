# Ctds-项目开发日志-26-09-19-2237

> 时间戳 = 本日志落盘时点（2026-09-19 22:37）。任务性质：**DB-17~21 清债小卡（交付+评审+合并）**（编排师指示"启动 DB-17~21 小卡任务"，同会话多任务授权；承接 `-2052`）。

## 当前续点

- **已完成**：
  1. **冷启动与立卡**：承接 `-2052` 续点；编排师两项裁决（①五条全做=多任务单会话授权，沿卡 2/3/4 先例；②DB-21 规格补正一并做，PO 审批以裁决留痕）。任务卡 `docs/tasks/DB-17-21-清债小卡-2026-09-19.md` 落盘（5 卡节+免设计声明+批次声明：一支分支、一次评审覆盖）。
  2. **五卡实施**（分支 `fix/DB-17-21-gates-and-alignment`，提交 `2b5a3ab`/`6a0fcdc`/`ee4f39d`/`4cd36b3`）：卡 17=selftest 增 S8（FAIL+ERROR 并存→退出码 1+`-> RED (also`）+S9（goals 注入 `&`→ERROR 含 "NOT executed"）；卡 18=secretsScan 三类静默逃逸显性化（enumGap ERROR/2、oversized/binary 入库列名、PASS 细分）；卡 19=前端段 UTF-8 解码两行；卡 20=`LoopbackBindingGuardTest`（绑定层断言+对照组）；卡 21=DTO 收紧仅 18 位+hifi B6 补正说明+规格行为 7 例外条款与验收量词限定。
  3. **批次验证**：selftest 全绿（初版 49 断言×3 次）；subject-service **134/134**（Testcontainers 集成 0 跳过）；DB-20 负向复验（0.0.0.0→红→恢复→绿）；DB-19 注入实测（中文断言失败→报告 `gate-report-20260919-211427.md` 逐字可读→用例 git 恢复）；交付态全量门禁 **GREEN/0**（`gate-report-20260919-212651.md`）。
  4. **4 视角评审（串行独立智能体，循环 1/3）**：①通过（3 建议）②通过（1 建议）③**有条件通过**（2 必修：README SKIP 语义句失实、卡 18 把注入态误标"交付态"）④通过（3 建议，含反向探针独立复验可证伪性、负向复验独立重放）。
  5. **评审修复**（提交 `63b34be`）：P2-1 README 三处（SKIP 三类语义+退出码 2 清单补 enumGap+变更留痕补 DB-18/19 行）；P2-2 卡 18 观察标注更正；采纳 7 条建议（含 ④P3-1 S8 补注入物锚定断言 49→**50**，复跑全绿）；留档观察项 4 条（enumGap 绝对路径/前端 allowlist 无锚点/报告 ANSI 码/SERVER_ADDRESS 边界）随任务卡评审节留痕。
  6. **合并与收口**：`--no-ff` 合并入 main（`2d80249`）；修复后全量门禁复跑 **GREEN/0**（`gate-report-20260919-223011.md`）；台账收口（DB-17~21 五行关闭+关闭统计 12 条+质量基建行九场景 50 断言+变更记录一行）。

- **下一步**（可直接开工）：
  1. **推送 main**（若本日志提交未随推送成功则为第一动作）：GitHub 走 Watt Toolkit + `D:\Soft\Git\bin\git.exe` 直调通道（记忆先例），推送 `7f430c1..` 后删除分支 `fix/DB-17-21-gates-and-alignment`（本地+远端）。
  2. **3.1.7（C-1.2 数据产品目录 业务规格与验收剧本）**——任务卡待编排师立卡指示（规格先行流程）。
  3. 观察项沉淀（非阻塞）：任务卡评审节 4 条 + 变更单 §5 观察项清单，随相应任务顺带。

- **关键决策**：
  1. **单批次一支分支一次评审**（任务卡头表声明）：五卡合计有效变更 <400 行、DB-17/18/19 同一脚本域，按卡节各自留痕降低核对成本——编排师"五条全做"授权下的执行解释。
  2. **④P3-1 采纳导致断言数 49→50**：S8 归因链与 S5/S6 同档（锚定注入物），README/任务卡/台账计数三处同步——数字一致性（评审③重点）优先于"少改文件"。
  3. **④P3-2/②P3-1 不改代码、留档观察**：前端 allowlist 锚点与本卡交付物承诺无关（扩范围）；enumGap 路径形态泄露面可忽略——避免为 P3 在评审循环内夹带代码改动引发重评。

- **文件指针**：
  - main：合并提交 `2d80249`（分支五提交已并入）；任务卡 `docs/tasks/DB-17-21-清债小卡-2026-09-19.md`（执行记录+批次验证+评审结论+留档观察项）；台账 `docs/status/进度看板与债务登记簿.md`（DB-17~21 行+统计+质量基建+变更记录）；规格 `docs/specs/C-1.1-主体注册与实名认证.md`（行为 7 例外+V1.2 补正版本行）；hifi `docs/designs/WBS-3.1.3-hifi.md`（B6 补正）；README `scripts/gates/README.md`（SKIP 语义/场景表/留痕）；本日志；
  - 证据报告（不入库，本机）：`scripts/gates/reports/gate-report-20260919-211427.md`（DB-19 注入）、`-212651.md`/`-223011.md`（交付态 GREEN×2）；
  - 上游日志：`-2052` → 本日志。

## 任务记录 ｜ 2026-09-19 22:37 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：DB-17~21（清债小卡批次）

### 做了什么（业务可读）

偿还了上一阶段"体检仪改造"和"演示加固"过程中记账欠下的五笔技术债：①给门禁自检补上两条只有手工演示背过的规矩（真红灯不被环境错误掩盖、查出非法配置绝不执行命令），现在每次自检自动验证且断言从 40 条增至 50 条；②密钥扫描的三个"查无此人"死角全部显性化——打不开的目录当错误上报，超大文件和二进制文件里的入库文件逐条列名供人工复核；③门禁报告抓到红灯时中文明细不再乱码（实测注入中文失败后逐字可读）；④给演示服务"只许本机访问"的开关装上机器守卫（配置改坏测试当场红）；⑤把身份证号三处互相矛盾的说明书统一成与服务层实现完全一致的口径（18 位、校验位错误码 1000C0001、并发败者留痕随事务回滚的例外写入规格）。全程 4 路独立评审（其中一致性评审抓出两处交付文档失实，已更正），评审通过后合并入主分支。

### 验证结果

| 验证 | 结果 |
| --- | --- |
| selftest 全量 | `SELFTEST PASS - 50 assertion(s), 0 failure(s)`（评审修复后终态；4 个评审智能体/主会话共 6 次运行一致） |
| subject-service 全量 | 134/134（Testcontainers 集成真实执行 0 跳过，含新守卫测试 2 例） |
| DB-19 注入实测 | `gate-report-20260919-211427.md` FAIL 明细中文逐字可读 |
| 交付态全量门禁 | 评审前后各一次均 **GREEN/0**（`PASS=10 FAIL=0 SKIP=2 ERROR=0`；报告 `-212651`/`-223011`） |
| 4 视角评审 | ①通过 ②通过 ③有条件通过（2×P2 已修复）④通过——循环 1/3 |
| 合并 | `2d80249` 入 main（--no-ff）；台账/任务卡/规格/README 收口 |

### 问题与教训

1. **台账编辑误删行（本会话最重教训）**：给台账"变更记录表"追加新行时，Edit 以既有行（15:5x 行）整行做 old_string，new_string 只写了新行——**该行被静默替换删除**；靠"先恢复、后核验行序"两步补救（恢复 15:5x 行→发现 20:0x 行被排到新行之后→python 交换复位→Read 复核 89~92 行序）。**教训：对台账/变更单等"逐行追加"场景，old_string 必须用"锚点行+新行拼接"或改后立即 Read 核验目标区行序**；该教训与"-2052 日志指针失实"同属"台账写入后不核验"一类，本次升级为"行级核验"。
2. **S8/S9 首跑 4 断言失败**：未接管 `$env:JAVA_HOME`，本机真实 JDK 压过配置假路径致 Maven 段真跑 mvn——照 S3 同构模式接管后全绿。教训：selftest 新场景凡涉及 Maven 段，env 接管是必备件（已在任务卡卡 17 记录）。
3. **Edit 工具对未 Read 文件拒绝编辑**（DTO 首次 Edit 被拒）——先 Read 后 Edit 的流程约束有效，无实际损失。

### 停机原因

【编排师指示的 DB-17~21 小卡批次已全部完成：五卡交付+批次验证+4 视角评审（循环 1/3 通过）+评审修复+合并入 main+台账与任务卡收口+本日志落盘。上下文自估约 8.5 成（权威值以任务窗口显示为准），按 50% 停机纪律本会话必须收口；推送若未完成则为新会话第一动作，3.1.7 留新会话。】

### 交接提醒

① **推送**：本日志与台账收口提交（`docs(status)`）如未随合并推送成功，新会话第一动作 = Watt Toolkit + `D:\Soft\Git\bin\git.exe` 推 main、删分支 `fix/DB-17-21-gates-and-alignment`（本地+远端）。
② **演示环境**：本批次零业务代码改动（DTO 注解收紧对演示无感——剧本无 15 位输入步骤），运行中的演示进程无需重启。
③ **下一会话**：3.1.7（C-1.2 业务规格与验收剧本）待编排师立卡；4 条留档观察项见任务卡评审节，随相应任务顺带、不单独立卡。
