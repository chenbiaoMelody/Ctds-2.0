# Ctds-项目开发日志-26-09-20-2130

> 时间戳 = 本日志落盘时点（2026-09-20 21:30）。任务性质：**WBS-3.1.8（DID 密钥管理与签发服务）低保真确认回写 + 高保真落盘**（编排师"四问均采建议口径"确认 + 指示"先清 DB-22 再进高保真"；承接日志 `-1955`）。

## 当前续点

- **已完成**：
  1. **裁决回写**：`docs/designs/WBS-3.1.8-lofi.md` 确认记录节签署（轮次 1 确认，Q1~Q4 均采建议 A，2026-09-20），并补"裁决口径落点"段；任务卡执行记录"状态/决策点"两行同步转已裁决。
  2. **高保真落盘**：`docs/designs/WBS-3.1.8-hifi.md`（编码契约）——总体方案（新建 `services/did` + KMS 扩展 SM2 托管与签名 + subject-service 触发衔接；端口 8080/8081/8082 全回环）、数据结构（`ctds_did` V1 两表 + DID 文档 JSON + KMS V2 增列）、行为清单 B1~B7（逐条对应规格验收标准 + 计划测试）、接口契约表（DID 四端点 + KMS 两端点 + 触发衔接）、错误码 1005 段六码、边界值与异常行为、配置项表、测试计划与达标线、变更影响声明（ADR-017 新增 / ADR-015 补记 / ADR-016 §2.7 补记）、实施前置检查项三项。
  3. **台账收口**（随本分支提交）：进行中/待编排师两行 + 裁决记录追加 3.1.8 行 + DB-22 关闭行/关闭统计（见 `-2120` 交接） + 变更记录两行。

- **下一步**（可直接开工，**被高保真确认门禁阻塞**）：
  1. 编排师确认 `docs/designs/WBS-3.1.8-hifi.md`（签署"确认记录"节 = 编码契约）。
  2. 确认后编码会话冷启动：实测 kms 默认 profile 启动 → 补 kms 回环绑定 8081 + 重建构件 → 落 ADR-017 / ADR-015 补记 / ADR-016 §2.7 补记 → `services/did` 骨架 + Flyway V1 → 测试先行（RED）→ 实现（GREEN）→ 自检单 → 4 视角评审。

- **关键决策**：
  1. **高保真即编码契约**：本表把 Q1~Q4 建议口径展开为可编码细节（库表唯一守卫 `guard_key`、接口/错误码/配置/测试锚点）；任何实现与已确认 hifi 不一致 = 打回项。
  2. **吊销/重签写操作随 Q4-A 落入本包**：`did_operation_log` 单表以 nullable 列承载签发四要素与吊销五要素；`did_identity.guard_key` 用"非吊销行=subject_no、吊销行=NULL"的唯一索引实现"一主体同期唯一有效/待签发 + 吊销行可累积历史"（MySQL 唯一索引允许多 NULL 的既有语义，无新增依赖）。
  3. **DID 服务不重复校验主体状态**：主体是否"已入驻"由触发方（subject-service）单一事实源保证，避免两服务复制状态机（登记于 ADR-017）。
  4. **演示注入（剧本 S1-5）零新增代码路径**：以"重复调用触发接口"重放命令作为注入方式（交付说明给出），最小实现、零新增攻击面。

- **文件指针**：
  - 设计：`docs/designs/WBS-3.1.8-lofi.md`（已确认）、`docs/designs/WBS-3.1.8-hifi.md`（待确认，本卡核心交付）
  - 任务卡：`docs/tasks/WBS-3.1.8-DID密钥管理与签发服务-2026-09-20.md`
  - 规格/剧本：`docs/specs/C-1.2-分布式数字身份DID.md`；`docs/manuals/C-1.2-分布式数字身份DID-验收剧本.md`
  - 契约：ADR-015/016（§2.7、§6）/005/006/007/008/009/010（实施时落 ADR-017 + 补记）
  - 台账：`docs/status/进度看板与债务登记簿.md`
  - 上游日志：`-1955`（立卡+lofi）→ `-2120`（DB-22 修复，同批执行）
  - 分支：`feat/C-1.2-DID密钥管理与签发服务`（本卡 docs 提交，另含台账收口）

## 任务记录 ｜ 2026-09-20 21:30 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：WBS-3.1.8 低保真确认 + 高保真落盘

### 做了什么（业务可读）

承接您"四问均采建议口径"的裁决，把方向落成**定稿说明书**（高保真设计 = 编码契约）：身份标识长什么样（`did:ctds:<申请编号>.<签发序号>`，重签换新号）、身份说明书包含哪些公开要素（公钥/算法/主体绑定/解析入口，绝不含隐私与私钥）、身份档案怎么落库（一张身份表 + 一张操作留痕表，用数据库唯一约束保证"一个主体同期只有一份有效身份"）、服务之间怎么握手（审核通过提交后调用签发、失败留"待签发"可重试、私钥永远不离开密钥保险柜）、对外接口与报错文案、要补哪些测试。同时按您指示先清了 DB-22（见 `-2120`）。

### 验证结果

| 验证 | 结果 |
| --- | --- |
| 规格符合性 | hifi 行为清单 B1~B7 逐条挂规格行为 1（规则 1~7、验收标准 1~5）与行为 4（规则 1~4）；无规格外新增业务承诺 |
| 上下游一致性 | Q1~Q4 口径落点与 lofi 确认记录一致；错误码 1005 段顺延、ADR 补记项、剧本 S1-5 注入方式均登记 |
| 代码改动 | 零（本卡为设计文档，未编码） |

### 问题与教训

1. **分支基线错位**（本会话实际发生）：新建 `fix/DB-22-…` 时误从当前 feat 分支起，导致修复分支带上了 3.1.8 文档提交——已用 `rebase --onto origin/main` 纠正。**教训：新建分支前先确认 `git branch --show-current` 与目标基线；fix 一律 `git checkout -b fix/… origin/main`。**
2. **高保真把"留痕"做成单表 nullable 列**而非两张表：签发四要素与吊销五要素共享 `did_operation_log`，减少表数量、留痕口径一处维护（沿"最小实现"）。

### 停机原因

【3.1.8 低保真确认回写 + 高保真落盘完成；**下一步被人工确认门禁阻塞**（章程 2.6.1：高保真未经 PO 确认不得进入实现）。上下文自估约 4.5 成（权威值以任务窗口显示为准），按 50% 纪律本会话收口】

### 交接提醒

① **等确认**：`docs/designs/WBS-3.1.8-hifi.md`（回复"确认 hifi 全文"即签署 = 编码契约；或逐条意见）。
② 确认后编码会话第一动作 = 实测 kms 默认 profile 启动 → 补 kms 回环绑定 8081 + 重建构件（hifi §10）。
③ DB-22 修复分支已随本会话提交（`fix/DB-22-test-two-clocks`），待 4 视角评审 + 编排师验收合并。
④ Docker 桌面 + sc-mysql/sc-minio/sc-redis 已拉起；演示后端未启动（8080/8081/8082 无监听）。
