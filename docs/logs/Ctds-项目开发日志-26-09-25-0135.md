# Ctds-项目开发日志-26-09-25-0135

- 任务卡 / 需求编号：**WBS-3.1.10**（跨空间身份互认接口 · **4 视角评审 + 验收走查 + 合并收口会话**）——`docs/tasks/WBS-3.1.10-跨空间身份互认接口-2026-09-24.md`
- 关联设计 / 契约：`docs/designs/WBS-3.1.10-hifi.md`（编码契约）/ `-lofi.md`；`docs/adr/ADR-008-标准适配层契约.md` §9、`docs/adr/ADR-017-DID服务契约.md` §8
- 会话目标：编排师指示"**进行 4 视角评审**"（后续追加"两项裁决"→"开始走查，完成后确认无问题继续进行提交和合并任务"）→ 冷启动 → 四视角独立只读评审 → 必修项修复 → 验收走查 S4 1~5 → 提交 → 合并入 main → 收口留痕
- 状态：✅ **已完成（WBS-3.1.10 交付并合并闭环）**——4 视角评审循环 **1/3**（四视角"有条件通过" + 必修 R1 台账用例计数 561→557 修复 + 两项裁决落地）+ **剧本 S4 走查 1~5 全通过** + 提交 `058f9ae`/`c7c5402` + `--no-ff` 合并 `2c714f2` + 推送 main；本会话**零代码/依赖/门禁配置变更**（唯一环境动作 = 重打构件并重启演示服务）

> **补正说明（2026-09-25 01:4x，同一会话内）**：本日志起草后，编排师即下达对任务卡 §七 7.5 两项裁决的裁定（会话回复"**1.不拆分；2.可以。**"）→ 已据实更新"下一步"第 1 条（裁决事项转为"验收走查"）并在"关键决策"补记第 6 条；裁决留痕同时写入任务卡 §七 7.5（裁决表 + 留痕）与台账变更记录新增行。其余内容不改。
>
> **补正说明二（2026-09-25 02:1x，同一会话内）**：编排师下达"开始走查 + 无问题即提交合并"后，本日志已追加"验收走查与提交"节；其中"未完成（待编排师确认）"三步经编排师回复"**继续**"后**已全部执行完毕**（推送分支 + 合并入 main `2c714f2` + 收口文档提交），详见该节末尾的完成留痕与任务卡 §九。
>
> **补正说明三（2026-09-25 02:3x，同一会话内）**：收口提交后复核环境发现 **Docker Desktop 已停止**（docker API 管道不可达、`java` 进程数 **0**、8080/8081/8082 **无监听**）→ 三容器与三服务均已随之下线，**非本会话操作所致**（本会话仅重启过 did 服务与临时启动过 example-service）。① **走查证据仍然有效**：五步与留痕核对均在环境在线时（01:42）实测完成，原始输出已入任务卡 §八；② 本日志"下一步"第 4 条"演示环境已运行新构件（did 服务 PID 17824）"**已失效**（该进程已不存在）；③ **续做前需先启动 Docker Desktop（三容器）+ 三服务（did/subject/kms，均须带环境变量 `CTDS_DB_PASSWORD`）**，再按需重跑 S4 走查。本节其余内容不改。

## 当前续点（新会话从这里继续；续接会话承本块另建新日志）

> **分支与主库**：本包已提交并合并入 main——分支 `feat/C-1.2-跨空间身份互认接口`（`058f9ae` / `c7c5402`，已推送，按惯例保留）；**`main` = `2c714f2`（合并提交，已推送）**；工作树干净。新窗口第一步：`git checkout main` + `git pull`（若需续做 3.1.11 则从最新 main 新建 `feat/*` 分支）。

- **已完成**：
  1. **冷启动核验**：`AGENTS.md` → 最新日志 `-0102`（编码交付交接）→ 任务卡 → hifi/lofi → ADR-008 §9 / ADR-017 §8 → 规格 C-1.2 行为 5 → 剧本 S4 + 附录 A 组 E1；分支 = `feat/C-1.2-跨空间身份互认接口`，基线 `f246b25`；
  2. **交付态门禁报告复核**（评审第 0 步）：`scripts/gates/reports/gate-report-20260925-011559.md` 头三行 = `ExitCode 0` / `PASS=10 FAIL=0 SKIP=2 ERROR=0 PENDING=9` → 交付态 **GREEN 有效**（SKIP=2 = DB-18 binary 行 + 演示环境占用日志行 9 个，逐条列名）；
  3. **四视角只读评审**（源码 19 个新增/修改文件逐份比对 + 报告/报告文件级实测复核 + 反向证据核对）：结论 = ①②③④ **各有条件通过**（详见任务卡 §七）；
  4. **必修 1 项（P2 账实）修复**：台账"质量基建状态"后端用例数 **561 → 557**（561 误含 example-service 已删除类 `DiagEndpointTest` 1 例、subject-service 已删除类 `InternalAdmissionIntegrationTest` 3 例的陈旧 surefire 报告；正确 = 256 + 50 + 24 + 142 + 85）；同批补注：门禁 SKIP 口径、任务卡 §六 体量统计口径（非空行：28 文件 1609 行/1367 非空行；代码改动 8 文件 +46/−54；仓库全量 13 文件 +138/−73）；
  5. **落盘更正**：台账（快照三行 + 质量基建行 + 变更记录新增两行）/ 任务卡（新增 §七"4 视角评审与修复（循环 1/3）"共 5 小节 + §四 评审与验收行 + §六 口径补注）/ 上游日志 `-0102` 加"补正说明二"（1.4.2 口径：历史日志只读，错漏以补正标注）；
  6. **两项裁决落地留痕**：编排师回复"1.不拆分；2.可以。" → 任务卡 §七 7.5 改为裁决表（D1/D2 + 裁决留痕 + 验收走查口径）+ 台账变更记录新增一行 + 本日志补正说明；**评审循环 1/3 就此闭环**。

- **下一步**（可直接开工粒度）：
  1. ✅ **（已完成）验收走查 + 提交 + 合并**：S4 步骤 1~5 全通过（见下方"追加"节）；提交 `058f9ae`（交付）/ `c7c5402`（走查与提交留痕）；`--no-ff` 合并 `2c714f2` 并推送 main；
  2. **（下一步，新会话）3.1.11（DID 管理界面）立卡**：待编排师指示（WBS 行 243，含吊销二次确认界面强制项）；立卡前建议从最新 `main` 新建分支；
  3. **（下一步）§七 7.3 建议项（8 条）吸收**：非阻塞，建议在 3.1.12 或相应包吸收（其中 5 条为 ADR-008 §9 / ADR-017 §8 的一两句话补登记，成本极低，可随下次契约补记一并做）；
  4. **（提醒）演示环境口径**：重启服务须带环境变量 `CTDS_DB_PASSWORD`（漏设会因 Flyway 连接失败而启动失败）；**当前演示环境已下线**（Docker Desktop 停止 + 三服务停止，见补正说明三）——续做/演示前先启动 Docker Desktop（`sc-mysql`/`sc-minio`/`sc-redis`）再启动 subject(8080)/kms(8081)/did(8082)，example-service 未常驻（探活核对时临时 :8083 后已停止）。

- **关键决策**：
  1. **必修项定性为"账实"而非"代码缺陷"**：全量源码与测试复核未发现功能/安全/一致性缺陷，唯一必修项是台账用例计数与自身口径不符 → 修复方式 = 更正台账 + 逐模块复核证据入 §七 R1（含三类已删除类实测 `src/test` 无源文件）；
  2. **不重跑交付态门禁**：本会话仅文档更正（台账/任务卡/日志），零代码与依赖变更 → 沿 3.1.9 收口 `docs(status)` 提交先例不触发门禁复跑（如需复跑由编排师指示）；
  3. **体量裁决建议不拆分（已被编排师采纳为 D1）**：协议收口 / 三端点 / 留痕表 / 装配替换 / 契约留痕互为前提，拆分将产生不可独立运行的中间态 → 沿 WBS-2.2.7"单卡单目标"先例；
  4. **验收标准 4 的映射判定成立**：本包交付后 did 域已开放，该条转为"代理域承载"（互认占位答复契约保留于 std-adapter + `StdCapabilitiesIntegrationTest` 1003C0001 封套断言），规格原文以"协议实现交付前"为前提；
  5. **未改任何交付物代码**：建议项（8 条）一律只在任务卡 §七 7.3 登记，不在本会话顺手改（最小实现 + 避免评审会话夹带变更）；
  6. **两项裁决（编排师 2026-09-25）**：**D1 体量不拆分**（单卡单目标；拆分将产生不可独立运行的中间态，本次验收按整体受理）；**D2 设计自冲突处置视为设计门禁内闭环**（取严 + T11 原文未动 + 如实登记 = 非静默偏离，无需 PO 二次确认）。二者与必修项 R1 修复共同构成"评审循环 1/3 闭环"。

- **文件指针**：
  - 分支：`feat/C-1.2-跨空间身份互认接口`（基线 `f246b25`；未提交）
  - 评审记录：`docs/tasks/WBS-3.1.10-跨空间身份互认接口-2026-09-24.md` **§七**（7.1 结论 / 7.2 必修 R1 / 7.3 建议 8 条 / 7.4 已核关键证据 / 7.5 裁决项）+ §四 评审与验收行 + §六 口径补注
  - 台账：`docs/status/进度看板与债务登记簿.md`（快照"进行中""待编排师""质量基建状态"三行 + 变更记录新增两行：评审循环 1/3、两项裁决落地）
  - 上游日志（补正）：`docs/logs/Ctds-项目开发日志-26-09-25-0102.md`（"补正说明二"）
  - 门禁报告：`scripts/gates/reports/gate-report-20260925-011559.md`（交付态 GREEN，本次复核）
  - 被评审代码/测试：`std-adapter/src/main/java/com/ctds/std/did/`（11 类 + 样例资源）/ `services/did/src/main/java/com/ctds/did/`（10 类 + V3 迁移）/ `services/example-service/.../StdAdapterPlaceholderConfig.java`；测试 `std-adapter/src/test/java/com/ctds/std/did/`、`services/did/src/test/java/com/ctds/did/{DidInteropIntegrationTest,application/DidInteropServiceTest,architecture/InteropSeamTest}.java`
  - 走查材料：`build-output/demo-files/did-interop/`（gitignored）
  - 上游日志：`-0102`（编码交付）← `-0022`（编码启动交接）← `-2347`（设计一次确认）

---

## 任务记录 ｜ 2026-09-25 01:2x~01:3x ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：WBS-3.1.10（4 视角评审）

### 做了什么（业务可读）

您指示对本包（跨空间身份互认接口）进行 **4 视角评审**。我先确认了"交付态门禁是绿的"（报告头三行），再把这次交付的每一行代码和每一个测试用例过了一遍，从四个独立角度挑问题：**功能是否符合规格与已确认设计、有没有安全或供应链风险、有没有重复和不一致、测试是否真的能证明事情对了**。

**评审结果（业务语言）**：

1. **功能对得上**：规格里"跨空间互认"的四条业务规则和三条验收标准，逐条都能在系统里找到对应实现和对应测试——对端来访的"三查"（签名、对端是否吊销、绑定是否成立）顺序与口径正确；每次结论都留痕且**原文一个字不存**；"我们去对端办事"能按本空间当前状态给出与之一致的结论；而且**通道取不到数时会如实说"不可用"，不会假装成"不通过"**。第四条验收标准（能力未开放时的答复）因为本包把能力"开通"了，已转为由其它未开放能力承载的回归性证明——这一点在评审里确认过是成立的。
2. **安全面没有新风险**：没有引入任何新的第三方组件（锁定清单文件未改动），用到的国密算法仍然只走平台唯一入口；样例里只有公钥和签名，**没有任何私钥材料**（自动化扫描 0 命中，制作期私钥已销毁、临时目录已确认无残留）；留痕表结构上就存不了原文，测试还做了"库表 + 日志双向扫描 0 命中"的反向验证。
3. **发现并修好了一处"账实不符"**：台账里写"后端用例 561 例"，我逐个模块数了测试报告文件，发现这是**多算了 4 例**——来自两个早已删除的旧测试类残留在构建目录里的旧报告。正确数字是 **557 例**（本次新交付贡献的 32 例不变）。已把台账、任务卡和上一份开发日志都更正并留痕。
4. **测试是可信的**：关键的两条"防回退守卫"做过故意破坏试验（把占位实现偷偷加回来、把实现类名偷写进业务服务），**两次都立刻变红**，说明守卫真的能拦住；集成用例是真跑了 9 个（不是被跳过）。
5. **另有 8 条改进建议**（不阻塞验收）：例如把"出向验证入参错误码与旧接口略有不同"这件事写进契约文档、把"演示样例公开等于演示凭证公开"写进诚实边界、给样例的自洽性再加一条自动校验等；都已登记在任务卡里，建议后续包一并吸收。

**需要注意的三件事**：① 我**没有改动任何交付代码**——评审只做文档更正与记录，避免评审会话夹带变更；② 评审发现的两项需要您拍板：**体量约 1443 行超 400 行指引是否拆分**（我建议不拆，理由：本包是一个目标，拆开会留下跑不起来的中间状态）、**设计中一处自相冲突的处置（已回写并登记）能否直接视为门禁内闭环**；③ 代码尚未提交，等您指示。

### 验证结果

| 验证 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 冷启动与分支 | `git branch --show-current` / `git status --short` / `git log --oneline` | `feat/C-1.2-跨空间身份互认接口`（基线 `f246b25`）；工作树 = 编码交付 + 本次文档更正，未提交 |
| 门禁报告复核（评审第 0 步） | 读 `scripts/gates/reports/gate-report-20260925-011559.md` 头三行 | `ExitCode 0` / `PASS=10 FAIL=0 SKIP=2 ERROR=0 PENDING=9` → 交付态 **GREEN 有效** |
| 用例计数复核（R1 证据） | 逐模块读 `surefire-reports/*.txt`（`Tests run:` 首行）并按"是否含已删除类陈旧报告"分类 | common/std-adapter 256、example-service 51（50 新鲜 + `DiagEndpointTest` 1 陈旧）、kms 24、subject-service 145（142 + `InternalAdmissionIntegrationTest` 3 陈旧）、did 97（85 + `DidResolutionVerificationIntegrationTest` 11 + 一次性生成器 1 陈旧）→ **剔除陈旧后后端 557** |
| 陈旧报告对应源文件不存在 | `Test-Path` 三个类源文件 | 三者均 `False` = 确为已删除类的残留报告 |
| 新增文件体量口径复核 | `git ls-files --others --exclude-standard` + 逐文件行数/非空行数 | 28 文件 **1609 行（含空行）/1367 非空行** → §六 "约 1365 行" = 非空行口径，成立但需注明 |
| 改动体量复核 | `git --no-pager diff --numstat` | 13 文件 **+138/−73**；其中代码 8 文件 **+46/−54**（文档 5 文件 +92/−19 不计入交付体量） |
| 私钥残留与样式扫描 | `build-output` 递归搜 `*private*`/`*key*` + 源码 64 位 hex 样式扫描（排除 target） | **无残留**；源码命中仅 2 个既有测试向量文件（非本包） |
| 集成用例真跑证据 | `services/did/target/surefire-reports/*.txt` | `DidInteropIntegrationTest` **9 run / 0 skipped**；`InteropSeamTest` 2/2；`DidInteropServiceTest` 7/7 |
| 文档更正自检 | 回读台账三行 + 任务卡 §七 + 上游日志补正说明 | 三处编号/数字一致（557、报告号、剔除清单） |

### 问题与教训

1. **"用例总数"必须写清统计口径，且口径要跨模块一致**：本次必修项的根因不是算错，而是**同一次统计里对三个模块用了两种口径**（did 剔陈旧、example/subject 未剔）。教训：计数类留痕应附"口径 + 剔除清单"，并在每次重构时对**所有模块**执行同一规则（可在台账行内固定写法）。
2. **删除测试类后要清理构建目录**：三个已删除类（`DiagEndpointTest` / `InternalAdmissionIntegrationTest` / `DidResolutionVerificationIntegrationTest`）的 surefire 报告长期残留在 `target/`，是本次账实偏差的物证。教训：删除测试类后建议 `mvn clean`（或至少删除对应报告）再统计；长期可考虑在门禁/统计脚本里按"报告对应的源文件是否存在"过滤。
3. **评审会话零代码变更**：本次评审发现 8 条建议，一律只登记不改动——避免"评审"与"改进"混在一个会话，保持评审对象可复现（评审对象 = 明确快照/工作树 + 报告号）。
4. **陈旧证据也可能"看起来新鲜"**：评审中不能只信台账数字，要回到文件级实测（逐份报告 + 源文件存在性）才能定性。

### 停机原因

【**阶段性完成（未触停机线）**：WBS-3.1.10 的 4 视角评审（循环 1/3）已闭环——交付态门禁报告复核有效、四视角结论与证据入任务卡 §七、唯一必修项（台账 561→557）已修复并同步三处留痕。剩余动作（**编排师验收走查 + 提交 + 合并**）属**新工作项/需人工参与**，故交接新会话（两项裁决已由编排师于本会话内下达并落盘）。**上下文用量**：AI 自估约 **30%**（口径以任务窗口显示的实际用量为准；本会话为只读评审 + 文档更正，用量高于一般文档会话，未触 §1.3 的 50% 停机线）。**未提交、未推送**（提交待编排师指示；本包变更与日志同库，提交时一并提交）。】

### 交接提醒

① **新窗口第一步 = 切到最新 main**：`git checkout main` + `git pull`（本包已合并，**main = `2c714f2`**；工作树干净）；**门禁无需复跑**（交付态 GREEN `gate-report-20260925-011559.md`；本包合并未改代码/依赖/门禁配置）；3.1.11 起新任务时从最新 main 新建 `feat/*` 分支；
② **两项裁决已落地（无需再问）**：D1 体量**不拆分** / D2 设计自冲突处置**视为设计门禁内闭环**（留痕见任务卡 §七 7.5 + 台账变更记录 + 本日志补正说明）；
③ **验收走查已完成**（剧本 S4 步骤 1~5 全通过，明细见下方"追加"节与任务卡 §八）；**后续重打构件重启服务时须带环境变量 `CTDS_DB_PASSWORD`**（`application-mysql.yml` 设计为"口令只经环境变量注入"，漏设会启动失败）；
④ **红线复述**：本会话未动代码/依赖/门禁配置；样例私钥不入库（已核无残留）；互认协议实现只落 std-adapter（两条守卫可证伪）；未夹带 DB-23/24/26 与 3.1.11；
⑤ **建议项 8 条**在任务卡 §七 7.3，非阻塞，建议后续包吸收（其中 5 条只是 ADR 补一两句话）；
⑥ **3.1.11（DID 管理界面）立卡**：待编排师指示（WBS 行 243）；剧本界面核对修订归 3.1.11 交付后一次完成（本包不改变业务判定标准，正文无需修改）。

---

## 追加：验收走查与提交（2026-09-25 01:4x~02:0x，同一会话内；编排师指令"开始走查，完成后确认无问题继续进行提交和合并任务"）

### 做了什么（业务可读）

走查 = 按验收剧本 S4 的五个步骤，在真实运行的服务上把"跨空间身份互认"跑一遍，看结果是否符合剧本写的预期：

1. **先留证据证明"新功能确实还没上线"**：重启前访问互认接口是 404（旧构件里没有这个功能），既有的"解析 DID"接口仍然正常（200）。
2. **重新打构件并重启服务**：重启后系统自动建好了互认留痕表（新的数据库迁移生效）。
3. **五步走查全部符合预期**：有效来访 → 通过；对端已吊销 → 不通过（原因：状态核验失败）；签名被篡改 → 不通过（原因：签名核验失败）；本空间有效 DID 出向验证 → 通过；本空间已吊销 DID → 不通过（原因：已吊销）。
4. **留痕核对**：5 次结论各留一行（来访 3 行 / 出向 2 行），四要素齐全；并且**样例原文在库表和日志里一个字都没有**（自动化扫描 0 命中）。
5. **演示页语义核对**：临时启动探活服务，"跨空间身份互认"已由"未开放"变为"**已开放**"（另两个能力域仍如实显示未开放），核对后已关掉这个临时进程。
6. **提交**：本包（代码 + 测试 + 六份文档 + 台账 + 两份会话日志）已提交到分支，提交号 **`058f9ae`**（43 个文件，+2035/−73）。

**走查中发现的一件事（不是代码问题，但很重要）**：重启服务时必须显式带数据库口令环境变量（系统按红线要求"口令只走环境变量、不写进代码"，默认空口令直接启动失败）；第一次重启漏带导致启动失败，补上后 10 秒内启动成功。已写进交接提醒，后续演示重启照此办理。

**两点需要您知悉**：① 剧本 S4 步骤 4~5 用的是演示库里现存的 DID（有效一个 / 已吊销一个），不是剧本 S1/S3 新签发的蓝天、云栖 DID——本次没跑 S1/S3，但**判断标准完全一致**（有效→通过、已吊销→不通过），结论不受影响；② 推送分支与合并入 main 这两步被工具要求您人工确认，两次等待授权超时未执行，故**第 6 步（提交）已完成、第 7~8 步（推送 + 合并）待您确认后执行**。

### 验证结果

| 验证 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 环境核验 | `docker ps` + 8080/8081/8082 `/actuator/health` | 三容器 Up；三服务均 200 |
| 重启前基线 | `GET /api/v1/did-interop/samples` / `GET /api/v1/did/{did}` / `SHOW TABLES` | 互认端点 **404**、既有解析 200、**无** `did_interop_log` 表 → 反证替换生效 |
| 打构件 | `mvn -B -ntp -pl std-adapter,services/did,services/example-service -am package -DskipTests` | **BUILD SUCCESS / exit 0**；内含互认类与资源（jar 条目核验） |
| 重启 | 停旧进程（PID 36192/412）→ 启动新构件 → 健康检查 | 首启失败（漏 `CTDS_DB_PASSWORD`，Flyway 连接 1045）→ **补设后 10 秒内 200**；V3 自动建表 `did_interop_log` ✓ |
| S4 步骤 1~5 | 请求体取自 `build-output/demo-files/did-interop/requests/`（出向为内联 JSON） | PASS / FAIL+REVOKED / FAIL+SIGNATURE_INVALID / PASS / FAIL+REVOKED **五步全对** |
| 留痕四要素 | `SELECT id,direction,peer_space,did,result,reason,occurred_at FROM did_interop_log` | 5 行（3 INBOUND + 2 OUTBOUND），六列齐备 ✓ |
| 原文零命中 | 库表 `LIKE '%临江港航%'` + `services/did/logs/*.log` 全文扫描 | 库表 **0** / 日志 **0**；64 位 hex 命中 5 处 = **Testcontainers 容器 ID**（非密钥材料） |
| 探活（T12 业务可见） | example-service 临时 `:8083` → `GET /api/v1/std-capabilities` | interconnect=false / **did=true（"跨空间身份互认能力已开放（演示期模拟对端，业务口径方法）"）** / evidence=false；**临时进程已停止** |
| 提交 | `git add -A` + `git commit` | **`058f9ae`**（43 文件 +2035/−73；工作树干净） |

### 提交与合并（编排师回复"继续"后**已全部执行完毕**）

1. ✅ `git push origin feat/C-1.2-跨空间身份互认接口`（`058f9ae` / `c7c5402`，exit 0）；
2. ✅ 合并前核验 `main = origin/main = 3c7ec99` 且为分支基线（`git merge-base --is-ancestor` 通过 → 无分叉无冲突）、工作树干净 → `git checkout main` + `git merge --no-ff` → **合并提交 `2c714f2`**（双亲 `3c7ec99` + `c7c5402`）→ `git push origin main`（exit 0）；
3. ✅ 收口文档提交：台账快照（**L1-3 10/73**、V1.0 约 **38/136 ≈ 28%**、进行中清零、下一包 3.1.11、待编排师改写 + 变更记录合并收口行）、任务卡状态闭环（§四 + 新增 §九 交付闭环）、本日志上述留痕（收口提交见台账变更记录末行）。

> **过程留痕（如实登记）**：前两次执行推送/合并时，工具层要求人工授权且**等待授权超时**，故当时未执行、未做任何绕过（红线：不绕过确认）；编排师回复"继续"后三步一次执行成功。**惯例偏离一件**：合并动作在同一会话内执行（先例为"合并另开会话"），理由 = 编排师当次明确指示连续完成走查/提交/合并，已在台账变更记录登记。
