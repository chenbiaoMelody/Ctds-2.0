# Ctds-项目开发日志-26-09-25-1137

- 任务卡 / 需求编号：**WBS-3.1.11**（DID 管理界面 · **4 视角评审会话（循环 1/3）**）——`docs/tasks/WBS-3.1.11-DID管理界面-2026-09-25.md`
- 关联设计 / 契约：`docs/designs/WBS-3.1.11-hifi.md`（§1~§11，本会话只读核对）/ `WBS-3.1.11-lofi.md` / `WBS-3.1.11-原型-DID管理界面.html`；`docs/adr/ADR-017-DID服务契约.md` §9；评审规程 `docs/manuals/ai-review/README.md` + `cards/01-规格符合性.md`~`04-测试质量.md`
- 会话目标：编排师"继续任务" → 冷启动复述续点（含分支/工作树/门禁报告的**实测**核对）→ 编排师再回"继续任务" → 按续点开工：**4 视角独立评审 → 结论留痕 → 日志落盘**
- 状态：✅ **4 视角评审循环 1/3 完成**——**②安全与供应链 PASS / ③一致性与重复 PASS / ④测试质量 PASS；①规格与设计符合性 FAIL**（必修 **R1**、**R2** + 登记更正 **R3**）→ **循环 1/3 未通过，必修项修复归新会话**（修复与评审须分离）；**本会话零代码变更、零依赖变更、门禁配置零改动、未复跑门禁**（仅改文档）

## 当前续点（新会话从这里继续；续接会话承本块另建新日志）

> **分支与主库**：本会话在 **`feat/C-1.2-DID管理界面`**（评审基线 HEAD = `ff6b07c`，编码提交 `7ede686`；`main` = `7534f11` **未动**）。新窗口第一步：`git checkout feat/C-1.2-DID管理界面` + `git log --oneline -3` 核验 → **直接开修 R1/R2**（见下方"下一步"第 1 条，含文件路径与第一个动作）。

- **已完成**：
  1. **冷启动核验（含实测偏差说明）**：读 `AGENTS.md` → 最新日志 `-1057` → 任务卡 → 台账 → 评审规程与 4 张评审卡 → hifi §1~§11；实测分支 `feat/C-1.2-DID管理界面`、工作树**干净（0 变更）**、`main` = `7534f11`（与日志记载一致）；**HEAD 实测 = `ff6b07c`**（日志正文写 `7ede686`，其"补正说明"已登记其后两笔文档提交 `29e7351`/`ff6b07c` → 属已留痕延伸，非偏差）；门禁报告 `-104829`（交付态）与 `-110605`（复跑）均在位；向编排师复述"当前任务 + 续点四要素"后获"继续任务"；
  2. **评审材料准备**：任务卡 §一~§六 + hifi §1~§11（行为 B1~B15 / 契约表 2.1~2.2 / 库表 / 错误码 / 配置 / 前端界面说明书 6.1~6.5 / 边界 E1~E25 / 测试锚点 T1~T17 / 演示口径 §9 / 前置检查 §10 / 实施补正 P1~P7）+ ADR-017 §9 + `cards/01~04` 清单全文；
  3. **证据采集（探索隔离，3 个只读子智能体并行）**：①后端实现证据（端点/权限/开关/字段/SQL/依赖/错误码/分层）②测试质量证据（用例清单/双向可证伪/空洞断言/计数脆弱/卫生/异常覆盖）③前端实现与一致性证据（API 模块与角色头/常量收口/两页交互/路由字段/vite 代理/复用与重复）；
  4. **主评审人 git 与源码实测复核（补子智能体无 shell 的限制）**：`git diff 7534f11..HEAD --stat`（40 文件）→ `show --stat 7ede686`（36 文件 / +3143 −34）→ **敏感面 `--name-only` 实测为空**（`scripts/gates`、`frontend/package.json`、`frontend/package-lock.json`、`docs/dependencies.md`、`DidErrorCodes.java`、`resources/db`）→ 硬编码凭据/密钥**敏感串复扫 0 命中** → 源码抽样核 `goDemo()` / `route.query.did` / 状态文案散写 / 三态样式 / 路由字段 / ADR 章节（§9 在位）；
  5. **4 视角结论（循环 1/3）**：②③④ **PASS**、① **FAIL**。必修 **R1**：演示入口未按 hifi §6.3 带 `?did=`（`IndexView.vue:97-99` 无 query；`DemoView.vue:33` 读取能力闲置；spec 仅断言路由名）；必修 **R2**：未达成 hifi §6.4"禁止页面内散写字面量"（`IndexView.vue:37-39/370`、`DemoView.vue:239` 硬编码状态中文，且筛选项"待签发"与 `constants/did.ts` 的"待签发（记录中间态）"**口径漂移**）；**R3**：体量实测 **36 文件 / +3143 −34**（原登记"31 文件 / +3073 −16"、日志"35 文件"）。**P1~P7 偏离全部裁决"如实登记 + 平台口径/数据模型优先"，可接受**；建议 **S1~S9**（非阻塞）登记；
  6. **留痕回填**：任务卡 **§七 4 视角评审记录**（7.1 结论总览 / 7.2 必修 R1~R3 / 7.3 视角①对账与偏离裁决 / 7.4 ②③④已核证据 / 7.5 建议 S1~S9 / 7.6 方法与限制）+ §四"评审与验收"行更新 + §六"实测复核更正"行；台账「进行中 / 待编排师 / 下一包」三行更新 + 变更记录新增 1 行；本日志。

- **下一步**（可直接开工粒度）：
  1. **（新会话首选）修复 R1 + R2（前端 2 文件 + 2 spec + 可选 1 守卫）**：
     - **R1**：`frontend/src/views/did/IndexView.vue` 的 `goDemo()`（`:97-99`）改为带参跳转——列表行"演示与验证"按钮传该行 `did`（`router.push({ path: '/did/demo', query: { did } })`，无目标 DID 时省略 query）；`frontend/src/views/did/IndexView.spec.ts:307-314` 用例增断言"跳转携带 `query.did` = 该行 DID"；（若判定应保持"手输"，则须改 `hifi §6.3` 并重新确认——**不得静默偏离**）
     - **R2**：`frontend/src/views/did/IndexView.vue:37-39`（筛选项）、`:370`、`frontend/src/views/did/DemoView.vue:239` 改为引用 `frontend/src/constants/did.ts` 的 `RECORD_STATUS_LABELS`；若筛选下拉需短文案，**在常量文件新增短文案常量**并注明用途；同步 `IndexView.spec.ts` / `DemoView.spec.ts` 断言；
     - 完成后本地门禁：`cd frontend; npm test` + `npm run lint`（目标：仍全绿且用例数不回退），再跑 `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\gates\run-gates.ps1`（**修复涉及代码 → 门禁须复跑**）；
  2. **（修复后）重审视角①（循环 2/3）**：对象 = 修复后的工作树 + 新门禁报告；重点核对 R1/R2 是否闭环、是否引入新的设计偏离；
  3. **（重审通过后）编排师走查（剧本 S1/S2/S3 界面）**：`mvn -B -ntp -pl services/did -am package -DskipTests`（**必须重打构件**）→ 起 Docker 三容器 → 三服务（**均须带 `CTDS_DB_PASSWORD`**；did 额外带 `CTDS_DID_DEMO_SIGNATURE_ENABLED=true`）→ 前端 `npm run dev`（5173，**禁止 `--host`**）；
  4. **走查前须先处置"演示根密钥丢失"**（见"问题与教训 1"；**未获裁决前不要写演示库**）；
  5. （走查材料）建 `build-output/demo-files/did-management/`（gitignored）：界面走查步骤卡 + 演示库现状说明 + 未登记 DID 样例 `did:ctds:S20260925999999.1`；
  6. （走查通过后）提交合并按编排师指示（`--no-ff` 入 main + 推送）；**剧本修订待 PO 审批**。

- **关键决策**：
  1. **视角①判 FAIL 而非"有条件通过"**：R1 是"设计与实现不一致"（hifi §6.3 明文写入 `?did=` 带入，实现与 spec 均未承载）、R2 是"设计强制条款未达成且口径漂移"——按 `cards/01` 检查清单第 2/3 条属打回项（章程打回口径），不降级为建议；
  2. **评审与修复合分离**：按 `docs/manuals/ai-review/README.md` 与编码日志交接"必修项修复后新会话处理"，本会话只出结论与留痕、**不动代码**——避免同窗口既当运动员又当裁判；
  3. **R3 体量登记就地更正**：更正属"留痕准确性"而非代码缺陷，且评审会话有权纠正登记 → 任务卡 §六 新增"实测复核更正"行并注明"以本行为准"，原值保留可追溯（不擦除历史）；
  4. **P1~P7 偏离全部接受**：逐条核出"如实登记 + 平台口径/数据模型优先"（含 P1 HTTP 400 与主体服务既有同码一致、P2 S 类文案平台统一遮蔽、P3 原字面断言不可达、P4 E8 数据模型上不可达、P7 ADR §9 实测在位）——**登记即闭环，无需 PO 二次确认**（不改变业务判定）；
  5. **不启动修复/不走查/不动环境**：本会话目标止于评审（编排师续点即"下一步 = 4 视角评审"）；未启服、未打构件、未碰演示库；根密钥裁决仍挂起；
  6. **未复跑门禁**：交付态 GREEN + 文档入库后复跑 GREEN 均为原始证据，本会话零代码/配置/依赖改动 → 沿交接提醒执行，避免无意义重复运行（**修复会话必须复跑**）。

- **文件指针**：
  - 分支 / 提交：`feat/C-1.2-DID管理界面`（评审基线 HEAD `ff6b07c`；编码提交 `7ede686`；本会话评审文档提交见文末）；基线 `main` = `7534f11`（未动）
  - 本卡：`docs/tasks/WBS-3.1.11-DID管理界面-2026-09-25.md`（**§七 4 视角评审记录（新增）** + §四 评审与验收行 + §六 实测复核更正行）
  - 设计：`docs/designs/WBS-3.1.11-hifi.md`（§1~§11；本会话只读，**未改**——P3 建议"补替代关系说明"待修复会话一并处理）/ `-lofi.md` / `-原型-DID管理界面.html`
  - 契约：`docs/adr/ADR-017-DID服务契约.md` §9（实测在位：第 144 行）
  - 剧本：`docs/manuals/C-1.2-分布式数字身份DID-验收剧本.md`（顶部修订留痕在第 14 行；**待 PO 审批**）
  - 台账：`docs/status/进度看板与债务登记簿.md`（快照 进行中 / 待编排师 / 下一包 三行 + 变更记录新增行）
  - 评审规程：`docs/manuals/ai-review/README.md`、`cards/01-规格符合性.md`、`02-安全与供应链.md`、`03-一致性与重复.md`、`04-测试质量.md`
  - 待修复代码（R1/R2）：`frontend/src/views/did/IndexView.vue`（`:37-39`、`:97-99`、`:370`）、`frontend/src/views/did/DemoView.vue`（`:239`）、`frontend/src/constants/did.ts`（常量源）；相关 spec：`frontend/src/views/did/IndexView.spec.ts`（`:307-314` 及筛选/解析断言）、`DemoView.spec.ts`、`DidSourceGuard.spec.ts`
  - 后端（本会话未动，仅核对）：`services/did/.../{interfaces/DidManagementController,interfaces/DidDemoSignatureController,application/DidManagementQueryService,application/DidDemoSignatureService,infrastructure/DidJdbcRepository,infrastructure/DidKmsHttpClient,domain/DidRepository,domain/DidKmsClient,interfaces/dto/*}.java`、`resources/application.yml`
  - 门禁报告（gitignored）：`scripts/gates/reports/gate-report-20260925-104829.md`（交付态）、`-110605.md`（复跑）
  - 上游日志：`docs/logs/Ctds-项目开发日志-26-09-25-1057.md`（编码交付）← `-0946`（立卡 + 设计一次确认）← `-0135`（3.1.10 合并收口）

---

## 任务记录 ｜ 2026-09-25 11:1x~11:4x ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：WBS-3.1.11（4 视角评审循环 1/3）

### 做了什么（业务可读）

您说"继续任务"，我先按规矩冷启动（读上一份日志、任务卡、设计与台账，并**实测核对**分支/工作树/门禁报告），向您复述了续点；您再次回"继续任务"，于是我按续点做了 **4 位独立评审员对"上一会话交付的 DID 管理界面"的会审**。

**评审是怎么做的**：4 个视角各自独立按既定评审清单逐条核对——①规格与设计是否逐条对上 ②安全与供应链 ③一致性与重复 ④测试质量。证据由 3 个"只查不写"的独立助手带回（每条都带文件与行号），关键结论（是否偷偷改了依赖清单、错误码定义、门禁配置、是否有硬编码密钥、变更到底多大）由我**亲自用 git 命令复核**，不看助手转述。

**会审结论**：**4 个视角里 3 个通过，1 个不通过**——
- **通过（②安全供应链 / ③一致性 / ④测试质量）**：没有硬编码密钥或真实凭据（专门扫描 0 命中）；没有自行实现密码学（签名全部走 KMS）；**没有新增任何第三方依赖**（前端依赖清单一个字没改，后端只加了一个我们自己的内部分页模块）；**没有偷偷改错误码定义、数据库迁移或门禁配置**；演示代签入口**出厂关闭**且关闭时**不会调用 KMS**、也不会暴露"某个 DID 存不存在"；所有查询语句都是参数化的（无注入风险）；页面上**没有任何私钥**（只有密钥引用），验证留痕**不存业务原文**；测试方面**没有空洞断言**，"吊销要填理由""二次确认点取消"这类关键防误操作路径都有正反两面对照用例，源集扫描守卫也是正反可证伪的。
- **不通过（①规格与设计符合性）——两项必须先修**：
  1. **R1**：设计写明"从列表进入演示页时，DID 输入框要自动带入那张 DID"，实际跳转时**没带**，运营进页面后仍需手工粘贴（功能能用，但与已确认的设计不符，且界面里"读取该参数"的能力被闲置）；
  2. **R2**：设计强制要求"状态/结果/原因文案集中在一处常量里、页面里不许写死"，实际页面里仍有 3 处写死中文；更要紧的是**同一个"待签发"状态，筛选框里叫"待签发"、列表标签里叫"待签发（记录中间态）"，两种说法**；
  3. 另外更正了一处**登记数字**（R3）：本次交付的实际体量是 **36 个文件、新增 3143 行、删除 34 行**，而任务卡原登记"31 文件 / +3073 −16"、日志写"35 文件"——三处不一致，已在任务卡改成实测值并注明来源。

**对原设计的 7 处偏离（P1~P7）怎么看**：**全部判定"可接受"**——每一条都是"如实登记 + 服从平台统一口径/数据模型现实"，例如"入口未启用"这个错误按平台规则返回 400（设计稿写 404，但全平台同码都是 400，单独放行会破坏一致性）、系统级错误对外的提示被平台统一改写（不暴露内部实现）、"待签发"记录本身还没有 DID 编号所以按空态处理等。**没有一条属于偷偷改需求**。

**这次评审没动任何代码**（评审与修改必须分开，避免自己判自己），也**没有启动服务、没走查、没碰演示数据**；除更正登记数字外，只改了文档（任务卡、台账、本日志）。**下一会话**再修 R1/R2 并请①号评审员复看。

### 验证结果

| 验证 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 冷启动核验 | `git branch --show-current` / `git status --porcelain` / `git log --oneline -5` / `git log -1 main` | 分支 `feat/C-1.2-DID管理界面`；工作树 **0 变更**；HEAD `ff6b07c`；`main` = `7534f11`（未动） |
| 门禁报告在位性 | 列 `scripts/gates/reports/` | `gate-report-20260925-104829.md`、`-110605.md` 均在位（本会话按交接提醒**未复跑**） |
| 变更面复核 | `git diff 7534f11..HEAD --stat` / `--name-only` | 40 文件；**敏感面实测为空**（`scripts/gates`、`frontend/package.json`、`frontend/package-lock.json`、`docs/dependencies.md`、`DidErrorCodes.java`、`resources/db` 零改动） |
| 体量实测（R3 依据） | `git show --numstat 7ede686` | **36 文件 / +3143 −34**（非 docs 35 文件 / +3131 −34；docs 1 文件 = 台账 +12） |
| 硬编码凭据/密钥复扫 | `Select-String` 对 40 个变更文件扫 `password=` / `AKIA…` / `BEGIN … PRIVATE KEY` / `secret=` / 内网地址模式 | **0 命中** |
| 路径冲突核对 | 读 `DidManagementController` / `DidResolutionController` + 集成用例 | 字面量段 `/records`、`/verification-logs` 优先于 `/{did}` 模板；用例 `recordsPathDoesNotShadowResolutionEndpoint` 锚定 |
| 前端实现抽样 | 读 `IndexView.vue:92-110`、`IndexView.spec.ts:296-315`、`DemoView.vue:9-33/56-63/100-157/239`、`router/index.ts`、`vite.config.ts` | 核出 **R1**（`goDemo()` 无 query，spec 仅断路由名）与 **R2**（状态文案散写 + "待签发"口径漂移） |
| 设计/契约留痕在位性 | 读 `ADR-017` 章节 + 剧本顶部留痕 + 台账环境风险行 | ADR-017 **§9 在位（第 144 行）**；剧本修订留痕在第 14 行；台账"环境风险"行在位 |
| 4 视角结论 | 按 `cards/01~04` 逐条核对（3 子智能体采证 + 主评审人复核） | ②PASS / ③PASS / ④PASS / **①FAIL（R1、R2；R3 已就地更正）** |
| 本会话改动面自检 | `git status`（待提交） | **仅 3 个文档**：任务卡、台账、本日志——**零代码、零配置、零依赖** |

### 问题与教训

1. **演示根密钥丢失（继承上会话，仍待裁决）**：`%TEMP%\ctds-kms-root-key.txt` 不存在 → 既有 3 条演示 DID 的**代签**不可用（解析/验证/吊销/重签/重试不受影响）。台账"环境风险"行已列三选项（**推荐 A**：用"重签"生成新一代 DID 作代签演示目标）。**未获裁决前不要写演示库**；**本包代码与测试不依赖该数据**，不影响评审与修复。
2. **登记数字会"过期"**：编码会话的体量统计在提交前完成，之后又追加了文件/文档，导致"31 文件 / +3073 −16"、"35 文件"与实际提交"36 文件 / +3143 −34"三处不一致。**教训**：体量登记与日志交接的**最终数字必须以提交后的 `git show --numstat` 实测为准**（本次已按此更正并保留原值可追溯）。
3. **"设计写了的细节"最容易漏**：R1（`?did=` 带入）与 R2（文案收口）都属于"设计文档某一句话"级别的要求，实现时因功能可用而未落地，**测试也只测了"能跳转/能显示"，没测"跳转带了什么/文案出自哪里"**。**教训**：界面类交付在测试锚点上要**逐条把设计里的"细节状语"变成断言**（带什么参数、文案取自哪个常量），否则设计条款会静默蒸发。
4. **同一状态两种说法是真实风险**：筛选项"待签发" vs 标签"待签发（记录中间态）"——对运营而言是"同一件事两个名字"，走查时容易被当成 Bug 提问。**教训**：文案收口不是洁癖，是**演示与验收的一致性前提**。
5. **探索隔离 + 主审实测的组合有效**：3 个子智能体带回带行号证据（主上下文只留结论），而"是否改动依赖/错误码/门禁/迁移"这类**归属判定**由主评审人用 git 实测兜底（子智能体无 shell）——**证据不因工具限制而失真**。
6. **评审不修代码是正确的**：本轮视角①的两项必修都属前端小改，但若在同一会话内顺手修，就失去了"独立评审"的意义（自己判自己）。按规程打回、**新会话修复 + 重审**。

### 停机原因

【**评审工作项完成（4 视角结论 + 必修 R1~R3 留痕 + 文档回填 + 提交）**；按 §1.3 停机：完成"评审留痕与提交"这一最小步骤后立即停止，**未开启新工作项**（R1/R2 修复按规程归新会话，避免同一窗口既评审又修复）。**上下文用量**：AI 自估 **约 40%**（口径以任务窗口显示的实际用量为准；本会话含 hifi 全文 + 4 张评审卡 + 任务卡 + 3 路子智能体证据 + git 实测复核，属"评审类"高读取量会话）。**分支与主库**：`main` 未动；本会话评审文档提交随分支入库（编码日志同库提交惯例）。**环境未启**（未起容器/服务，未打构件，未碰演示库）。】

### 交接提醒

① **新窗口第一步**：`git checkout feat/C-1.2-DID管理界面` + `git log --oneline -3` 核验 → **直接开修 R1/R2**（动作清单见本日志"下一步"第 1 条；R1 改 `IndexView.vue:97-99` + spec 断言；R2 改 `IndexView.vue:37-39/370`、`DemoView.vue:239` 引用 `constants/did.ts` 并统一"待签发"口径）；
② **修复会话必须复跑门禁**（涉及代码）：`cd frontend; npm test; npm run lint` → `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\gates\run-gates.ps1`；目标 = 前端用例数不回退（当前 14 文件 / 109 例）+ 全仓 GREEN；
③ **修复后重审视角①（循环 2/3）**，对象 = 修复工作树 + 新门禁报告；循环上限 3 轮；
④ **待裁决一件**：演示根密钥丢失三选项（台账"环境风险"行；**未裁决前不要写演示库**）；
⑤ **待 PO 审批一件**：剧本 S1 步骤 1 / S2 步骤 1、5 界面名称核对修订（业务判定未变）；
⑥ **走查前**：`mvn -B -ntp -pl services/did -am package -DskipTests`（**必须重打构件**）+ 三服务（均带 `CTDS_DB_PASSWORD`；did 另带 `CTDS_DID_DEMO_SIGNATURE_ENABLED=true`）+ 前端 5173（**禁止 `--host`**）；
⑦ **建议项 S1~S9（非阻塞，未在本会话实施）**：S1 守卫注释写明"中文私钥不扫"的边界；S2 `PageData<T>` 前端类型收口；S3 `handleOperationError` 范式说明；S4~S8 测试加固（反向探针 / 全元素白名单 / 业务码断言 / 计数隔离）；S9 前端分页越界与留痕空态 + 并发幂等，随 3.1.12 吸收；**另**：hifi §11 P3 行末补"T17③ 原文已被本行替代"的替代关系说明（可在修复会话一并回填）；
⑧ **红线复述**：本会话零代码变更、零依赖变更（前端依赖清单零改动）、零错误码/枚举/库表/迁移变更、零门禁配置改动；未启服务、未写演示库、未碰其它模块与其它任务卡。
