# Ctds-项目开发日志-26-09-25-1518

- 任务卡 / 需求编号：**WBS-3.1.12**（C-1.2 测试补全与集成 · **立卡会话**）——`docs/tasks/WBS-3.1.12-C-1.2测试补全与集成-2026-09-25.md`
- 关联设计 / 契约：`docs/designs/WBS-3.1.12-lofi.md`（**Q1~Q5 待确认** + 体量预估）、`docs/designs/WBS-3.1.12-hifi.md`（**编码契约，待确认**）；规格 `docs/specs/C-1.2-分布式数字身份DID.md`（行为 1~5 + §4 非目标 + §6.1 边界声明）；上游卡 `WBS-3.1.8/3.1.9/3.1.10/3.1.11`；`docs/adr/ADR-001/005/010/015/017`
- 会话目标：编排师指令"**立卡**"（承接 WBS 行 244「C-1.2 测试补全与集成 / 测试达标、与连接器注册联调」）
- 状态：✅ **立卡完成，待编排师一次确认**——任务卡 + lofi（Q1~Q5 + 体量预估）+ hifi（编码契约 §1~§11）三文档落盘；台账快照与变更记录更新；分支 `feat/C-1.2-测试补全与集成`（自 `main` = `593bc26`）；**本会话零代码变更、零依赖变更、零门禁配置改动、未推送远端**

## 当前续点（新会话从这里继续；续接会话承本块另建新日志）

> **分支与主库**：本会话在 **`feat/C-1.2-测试补全与集成`**（自 `main` = `593bc26`，含 3.1.11 合并 `ad6a447`）；`main` 本会话未动。
> **新窗口第一步**：`git checkout feat/C-1.2-测试补全与集成` + `git log --oneline -2` + `git status --short` 核验 → **仅当编排师已确认 Q1~Q5 + 体量裁决后方可进入编码**；编码第一步 = hifi §10 前置检查项七项逐项实测留痕（含**覆盖率基线测量**与**变异试跑**）。

- **已完成**：
  1. **冷启动核验**：`main = origin/main = 593bc26`、工作树干净；读最新日志 `-1336`（3.1.11 合并收口）→ 台账 → **WBS 行 244** 定位本卡；
  2. **立卡前隔离探索（三路只读子智能体，主上下文只留结论）**：
     - **测试达标现状**：覆盖率/变异门禁**均 PENDING**（阈值已在 `scripts/gates/gates-config.json` 但未接入，DB-06 新登记）；覆盖率仅有 `3.1.8`/`3.1.9` 的**手工瞬时值**（did 行覆盖 92.8% → 94.0% → 94.8%，jacoco 0.8.12，未入库依赖，报告已清理）；**变异测试全仓零先例**；测试债务面 DB-06 / DB-07 / DB-14 / DB-23 仍在登记；
     - **连接器注册现状（本卡最大不确定点）**：`connector-sdk/` 仅 `.gitkeep`、**未纳入根 pom**、无 Java 源码/测试；`docs/specs/` **无 C-6.x 规格**；WBS `3.6.3 连接器注册与核验服务` 未开工且**不在本卡依赖链**（`3.1.9~3.1.11`）内；ADR-017 无"连接器"表述；**C-1.2 规格自张力** = §4 非目标第 5 项"连接器 DID 绑定归 3.6.3，**本规格不定义连接器行为**" vs §6.1"测试补全与集成（3.1.12，**含与连接器注册联调**）" → **执行对象不存在**；
     - **遗留项清点**：`3.1.11` §七 7.5 **S1~S10**（S9 卡面点名"随 3.1.12 吸收"）+ §七 7.7 的 S10；`3.1.10` §七 7.3 第 **7** 条（留痕表收敛，原文"建议 3.1.12 或统一审计视图阶段做收敛评估"）与第 **8** 条（互认测试加固 4 项）；`3.1.9` **无**遗留测试项；
  3. **落盘三份立卡交付物**：
     - **任务卡**（表头纪律声明 + §一 范围 + §二 Q1~Q5 + §三 映射表骨架 + §四 执行记录 + §五 自检单骨架 + §六 体量登记 + 边界复述）；
     - **lofi**（方向三件事 + 盘点结论摘要 + **Q1~Q5 选项与业务影响** + 体量预估与拆分建议）；
     - **hifi**（编码契约 §1~§11：达标口径与**固定执行命令** / 变异范围与优先级 / 盘点方法与映射表 / **后端补测 T1~T8** / **前端补测 F1~F5** / 防"凑数字"边界 / 联调六项链路 + **下游可消费性证明** / 遗留项逐条处置 / 边界值 E1~E6 / 门禁与零新增依赖声明 / 前置检查项七项 / 变更登记占位）；
  4. **分支与台账**：新建 `feat/C-1.2-测试补全与集成`（自 `593bc26`）；台账快照"进行中/待编排师/下一包"三行更新 + 变更记录新增立卡行；本日志。

- **下一步**（可直接开工粒度）：
  1. **（编排师）一次确认 Q1~Q5 + 体量裁决 D1**（回复格式建议："Q1 采 A / Q2 采 A / Q3 采 A / Q4 采 B / Q5 采 A" + D1 拆分与否）；确认后回填任务卡 §二 确认留痕 + lofi/hifi 两处「确认记录」表，并在**同一会话**内推送分支（沿 3.1.11 先例"设计确认后推送分支"）；
  2. **（确认后，另开会话）编码第一步** = `docs/designs/WBS-3.1.12-hifi.md` §10 前置检查项七项逐项实测留痕：① 冷启动读序 + 分支核验 ② Docker 三容器 + 三服务健康 200 + 前端 5173 ③ 基线用例数复核（后端 580 / 前端 113）④ **覆盖率基线测量**（`mvn -pl services/did org.jacoco:jacoco-maven-plugin:0.8.13:prepare-agent test …:report`）→ 回填 hifi §1.3 ⑤ **变异试跑单类评估耗时** → 定 §1.2 范围 ⑥ hifi §2 盘点回填 ⑦ `git log` 核验基线；
  3. **（编码中）测试先行约束**：每条补测先写用例 → 确认"对现状即绿或如实红"→ 用"删锚验证"制造必红 → 还原；**禁止弱化既有断言**（弱化 = 打回项）。

- **关键决策**：
  1. **"与连接器注册联调"列为 Q1 而非自行处置**：连接器注册**无规格/无实现/未开工** = 无契约可联调；若按其字面在 `std-adapter` 造模拟实现，属"在设计里私自补需求"（章程 2.6 / 红线 2），故只提建议（A：本卡做**接口级"下游可消费性"证明**，真联调顺延并**走变更流程**同步规格 §6.1 与 WBS 行 244），**由编排师裁决**；
  2. **达标只测量、不开门禁**（沿用 `3.1.6` 先例）：阈值接入属质量基建包 `2.2.8`（DB-06）职权，本卡不改 `gates-config.json` / `run-gates.ps1`（红线 3），实测数字与接入建议交其承接；
  3. **工具走"命令行临时注入"**：jacoco / pitest 不写入 pom 与 `docs/dependencies.md` → **零新增依赖**；前提是 PIT 首次执行需 Maven 从中央仓库拉取 `org.pitest:pitest-maven`（真实存在，ADR-001 §31 已列入质量工具链基线）——若编排师不接受任何下载，则退化为"只测覆盖率 + 变异登记顺延"（已写入 Q2）；
  4. **`vue-tsc` 4 处既有类型错误不并入**（建议 B）：属 C-1.1 域、跨域夹带违反最小实现与单卡单目标；
  5. **体量建议不拆分**（约 600~950 行超 400 行指引）：沿 `3.1.10`/`3.1.11` 先例报请裁决，并给出"拆 3.1.12a/3.1.12b"的备选方案；
  6. **本会话不推送远端**：沿 3.1.11 先例（**设计确认后**才推送分支），避免未确认设计提前进入远端。

- **文件指针**：
  - 分支：`feat/C-1.2-测试补全与集成`（自 `593bc26`；**本会话未推送**）
  - 本卡：`docs/tasks/WBS-3.1.12-C-1.2测试补全与集成-2026-09-25.md`
  - 设计：`docs/designs/WBS-3.1.12-lofi.md`（Q1~Q5）、`docs/designs/WBS-3.1.12-hifi.md`（编码契约 §1~§11）
  - 台账：`docs/status/进度看板与债务登记簿.md`（快照三行 + 变更记录新增行）
  - 上游参照：`docs/designs/WBS-3.1.6-{lofi,hifi}.md`（**同族先例**："测试补全与集成"卡的两级设计与验证方式）；`docs/tasks/WBS-3.1.11-DID管理界面-2026-09-25.md` §七 7.5/7.7（S1~S10）；`docs/tasks/WBS-3.1.10-跨空间身份互认接口-2026-09-24.md` §七 7.3（第 7/8 条）
  - WBS：`docs/C-TDS项目总体实施计划与WBS.md` 行 244（本卡）/ 行 301（3.6.3 连接器注册与核验服务，未开工）
  - 门禁配置（**本卡不改**）：`scripts/gates/gates-config.json`（阈值 `coreLineCoverage=80` / `overallLineCoverage=70` / `mutationKillRate=60`；`coverage` = PENDING-JACOCO、`mutationTest` = PENDING-TOOLCHAIN）
  - 上游日志：`docs/logs/Ctds-项目开发日志-26-09-25-1336.md`（3.1.11 走查验收 + 合并收口）← `-1321` ← `-1234` ← `-1205` ← `-1137` ← `-1057` ← `-0946`

---

## 任务记录 ｜ 2026-09-25 13:4x~15:1x ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：WBS-3.1.12（立卡）

### 做了什么（业务可读）

您说"立卡"，我把 **DID（C-1.2）这一块的"测试收尾包"立起来了**——这一包不新增任何业务功能，只做三件事：**把测试的窟窿补齐、把"测试到底达标没有"用真实数字证明、再把整条身份链路从头到尾联调跑通一遍**。

**1. 先摸清了现状**（我用三个"只读调研助手"分头查，只把结论带回）：
- **达标数字从来没算过**：质量门禁里"覆盖率"和"变异测试"两个刻度尺都还挂着"未接入"（阈值早就写好了：核心模块行覆盖 ≥80%、变异杀除率 ≥60%、整体 ≥70%，但机器还没装上）。历史上只有 9 月 20/22 日两次**手工**量过的数字（DID 模块约 93%~95%），且**变异测试一次都没跑过**。
- **前两包留下的"测试小尾巴"**：DID 管理界面（3.1.11）评审时登记了 10 条改进建议（S1~S10），其中**第 9 条本来就点名"随 3.1.12 吸收"**（比如：分页翻到超出范围、验证记录为空时的提示、并发重复操作会发生什么）；跨空间互认（3.1.10）也登记了 4 条"测试加固"项（比如：时间格式要钉死在"秒级标准格式"、日志里不能出现任何原始数据）。

**2. 发现一个必须请您裁决的问题（本卡最大的不确定点）**：WBS 上这一包的交付描述是"测试达标、**与连接器注册联调**"——可是我查下来，**"连接器注册"这个服务目前根本还不存在**：它的代码目录是空的、没有业务规格、WBS 里排在 3.6.3 且**还没开工**，也不在这一包的依赖清单里；而且 C-1.2 的规格本身就"打架"——一处写着"连接器的事归 3.6.3，本规格不管"，另一处又写着"3.1.12 含与连接器注册联调"。
→ 按规矩**我不能自己解释模糊需求**，所以我把它列成 **Q1** 请您选：
  - **A（我建议）**：本包只做"**DID 侧能被下游正常消费**"的接口级证明（即：模拟第三方"拿到 DID → 验证身份"的完整闭环）；真正跟连接器注册联调，等 3.6.3 做出来再补，并且**走一次文档变更流程**，把这个说法改一致。
  - **B**：本包先自己造一个"模拟连接器"再联调——但这等于在没规格的情况下自己造需求（违规），而且一包做不完。
  - **C**：本包缓一缓，先立"连接器业务规格"卡。

**3. 顺手把方案写细了**（另 4 个待确认问题一起请您一次拍板）：
- **Q2** 达标怎么测：建议用"临时工具命令行量一下"，**不装进项目、不改门禁**（门禁该由质量基建包 2.2.8 负责），量出的数字交给它；
- **Q3** 前面留下的测试尾巴吸收多少：建议"能归本卡的全收（S4~S10 + 互认 4 条加固），留痕表是否合并不做实现只出结论，另两条用文档补注收口"；
- **Q4** 前端还有 4 处**历史**类型报错（属于 C-1.1 的旧账）：建议**不夹带**，另立小卡；
- **Q5** 联调怎么证明：建议真实环境**跑一遍留痕**（不回填自动化资产）。
**另外**：这一包预估 600~950 行，超过 400 行的惯例线，请您一并裁决**拆不拆**（我建议不拆，理由写在 lofi §4）。

**这一轮没有碰代码**：没改程序、没装工具、没动门禁配置；只写了 3 份设计/任务文档、1 条进度账记录。分支建好了但**还没推到远端**（等您确认设计后再推，跟上一包的做法一致）。

### 验证结果

| 验证 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 冷启动核验 | `git checkout main` / `git status -sb` | `main == origin/main == 593bc26`；工作树干净 |
| WBS 定位 | 检索 `docs/C-TDS项目总体实施计划与WBS.md` | **行 244** = 「3.1.12 C-1.2 测试补全与集成 / 测试达标、与连接器注册联调 / 依赖 3.1.9~3.1.11 / 1 会话」 |
| 门禁阈值与状态 | 读 `scripts/gates/gates-config.json` | 阈值 `coreLineCoverage=80` / `overallLineCoverage=70` / `mutationKillRate=60`；`coverage` = **PENDING-JACOCO**、`mutationTest` = **PENDING-TOOLCHAIN** |
| 连接器注册现状 | 三路只读探索（目录/规格/WBS/ADR 交叉核对） | `connector-sdk/` 仅 `.gitkeep`（未纳入根 pom）；`docs/specs/` 无 C-6.x；WBS 3.6.3 未开工；ADR-017 无相关表述 → **无可执行对象** |
| 遗留项清点 | 读 3.1.11 §七 7.5/7.7、3.1.10 §七 7.3 | S1~S10 逐条 + 互认第 7/8 条逐条（处置已写入 hifi §7） |
| 测试资产拓扑 | 文件清点 | `services/did` 测试 18 文件（16 测试类 + 2 支撑类）；`views/did` 3 个 spec；`std-adapter` 测试 6 文件 → 作为 T/F 清单落点 |
| 分支 | `git checkout -b feat/C-1.2-测试补全与集成` | **已建**，基线 `593bc26`；工作树仅 3 个新文档（未提交、未推送，落盘后随本会话提交） |
| 变更面自检 | `git status --short` | 仅 **3 个新增文档**（任务卡 + lofi + hifi）+ 台账/日志改动；**零代码变更、零依赖变更、零门禁配置改动** |

### 问题与教训

1. **规格自张力 = 立卡必须先裁决的信号**：C-1.2 规格 §4 与 §6.1 对"连接器注册联调"的表述互斥，且执行对象不存在。**处置**：不自行解释、不自行造需求，直接列为 **Q1** 并给出三选项与业务影响。**教训**：立卡前必须做"依赖对象是否存在"的实测核对（本次若不做，会在编码会话才撞墙，浪费一轮）。
2. **隔离探索是立卡的正确姿势**：三路只读子智能体（测试达标现状 / 连接器注册现状 / 遗留项清点）合计约 2.5 分钟，主上下文只留结论，避免了通读全仓测试与规格。**教训**：大范围调研一律隔离，**结论入库（设计文档）而非留会话**。
3. **"达标"必须先划清工具边界**：覆盖率/变异门禁的接入是**另一个包（2.2.8 / DB-06）**的职权，若本卡顺手接入即是"越界 + 改门禁配置（红线 3）"。**处置**：本卡只测量 + 交接入建议（沿 3.1.6 先例）。**教训**：涉及"质量数字"的包，务必先确认"测量"与"门禁接入"的归属。
4. **不要把跨域旧账夹进来**：`vue-tsc` 4 处报错属 C-1.1 域 → 建议不并入（Q4=B）。**教训**：立卡时对"顺手能修"的项要克制，跨域即出界。
5. **时间戳口径**：本会话跨约 1.5 小时（探索 + 撰写），日志按**落盘时点**命名（`-1518`），任务卡内引用已同步更正。**教训**：跨长时段会话中，先写文档后写日志时须回填/校正日志编号引用。

### 停机原因

【**立卡工作项完成（三文档 + 台账 + 分支）**；按 §1.3 停机：完成"落盘并提交留痕"这一最小步骤后立即停止，**未开启编码**（须先经编排师一次确认）。**上下文用量**：AI 自估 **约 35%**（口径以任务窗口显示为准；本会话含三路探索结论 + 三份文档撰写）。**本会话零代码变更、零依赖变更、零门禁配置改动**；未起停服务/容器、未写演示库；**分支未推送**。】

### 交接提醒

① **待办 = 一次确认**：Q1~Q5 + 体量 D1（回复格式建议："Q1 采 A / Q2 采 A / Q3 采 A / Q4 采 B / Q5 采 A" + 拆分与否）；确认后回填任务卡 §二 与 lofi/hifi 确认记录表，并**在同一会话推送分支**；
② **编码须另开会话**（单会话单任务）；编码第一步 = hifi §10 七项前置检查（**含覆盖率基线测量与变异试跑**）；
③ **Q1 若选 A**：请安排"**走变更流程**同步 C-1.2 规格 §6.1 与 WBS 行 244 表述"（消除规格自张力；本卡不擅自改规格）；
④ **Q2 若选 A**：需接受 Maven 首次拉取 `org.pitest:pitest-maven`（**不写入仓库依赖文件**）；若不接受，退化为"只测覆盖率 + 变异登记顺延"；
⑤ **演示环境**：本会话未起停服务；编码/联调会话需重启三容器 + 三服务（did 带 `CTDS_DID_DEMO_SIGNATURE_ENABLED=true`）并**沿用同一把演示根密钥**；
⑥ **硬边界（编码会话必守）**：零新增依赖 / 零门禁配置改动 / 零新增错误码、状态枚举、库表、迁移 / **补测只允许加强断言**；发现真缺陷**停止上报**（不擅自修）；
⑦ **红线复述**：本会话未动其它模块与其它任务卡；未实现规格外功能；未在设计中补需求（连接器注册缺口走 Q1 裁决 + 变更流程）。
