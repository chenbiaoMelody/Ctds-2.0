# Ctds-项目开发日志-26-09-25-1527

- 任务卡 / 需求编号：**WBS-3.1.12**（C-1.2 测试补全与集成 · **设计一次确认会话**）——`docs/tasks/WBS-3.1.12-C-1.2测试补全与集成-2026-09-25.md`（本会话回填 **§二 确认留痕** + §四 状态/设计确认会话行 + §六 D1 待补说明）
- 关联设计 / 契约：`docs/designs/WBS-3.1.12-lofi.md`（§3.1 确认留痕）、`docs/designs/WBS-3.1.12-hifi.md`（「确认记录」+ 设计裁决行 → **编码契约生效**）
- 会话目标：编排师答复立卡待确认问题（"Q1：A / Q2：A / Q及Q4：按推荐 / Q5：A"）→ 落确认留痕 → 推送分支
- 状态：✅ **编码就绪（设计一次确认通过）**——**Q1=A / Q2=A / Q3=A / Q4=B / Q5=A**（均采建议口径）；**D1 体量拆分待补裁决**（建议不拆分，**不阻塞编码**）；三处确认留痕落盘；分支已推送；**本阶段零代码变更、零依赖变更、零门禁配置改动**

## 当前续点（新会话从这里继续；续接会话承本块另建新日志）

> **分支与主库**：本会话在 **`feat/C-1.2-测试补全与集成`**（自 `main` = `593bc26`；**确认后已推送远端**）；`main` 未动。
> **新窗口第一步（编码会话冷启动）**：`git checkout feat/C-1.2-测试补全与集成` + `git log --oneline -3` + `git status --short` 核验 → **直接按 `docs/designs/WBS-3.1.12-hifi.md` §10 前置检查项七项逐项实测留痕**（① 冷启动读序与分支核验 ② Docker 三容器 + 三服务健康 200 + 前端 5173 ③ 基线用例数复核（后端 580 / 前端 113）④ **覆盖率基线测量** ⑤ **变异试跑单类评估耗时** ⑥ hifi §2 盘点回填 ⑦ 基线核验）→ 之后按 §2 盘点结果定稿 T/F 清单并**测试先行**开工。

- **已完成**：
  1. **编排师裁决落痕（Q1~Q5 均采建议口径）**：**Q1=A**（本卡只做"测试达标 + DID 侧可被下游消费的接口级联调"；连接器注册真联调顺延至 `3.6.3` 后并由编排师安排走变更流程同步规格 §6.1 与 WBS 行 244）；**Q2=A**（jacoco/pitest 命令行临时注入，不改 pom / `gates-config.json` / `dependencies.md`，数字与接入建议交 `2.2.8`/DB-06；接受 Maven 首次拉取 `org.pitest:pitest-maven`）；**Q3=A**（吸收 3.1.11 S4~S10 + 3.1.10 §7.3-8；§7.3-7 只出评估结论；S1/S3 文档补注）；**Q4=B**（`vue-tsc` 4 处既有类型错误不并入，跨域另立小卡）；**Q5=A**（真实环境一次性走查留痕，演示数据保留至验收后清理）；
  2. **确认留痕三处落盘**：任务卡 §二（逐条裁决 + 确认后状态）+ §四 状态行改"编码就绪"与新增"设计确认会话"行 + §六 D1 待补说明；`WBS-3.1.12-lofi.md` 新增 **§3.1 确认留痕**（含 D1 待补说明）；`WBS-3.1.12-hifi.md`「确认记录」表签署 + 设计裁决行改"编码契约自确认起生效"；
  3. **台账同步**：快照"进行中"行改"编码就绪（设计一次确认通过）"、"待编排师"行 ① 改"已闭环 + 待补 D1 + Q1 的变更流程附加事项"；变更记录新增本阶段行；
  4. **推送分支**：`git push -u origin feat/C-1.2-测试补全与集成`（沿 `3.1.11`/`3.1.10`"设计确认后推送分支"先例）。

- **下一步**（可直接开工粒度，**另开会话**）：
  1. **编码会话冷启动**：读 `AGENTS.md` → 最新日志（本日志）→ 任务卡 → `WBS-3.1.12-hifi.md`/`lofi.md`；`git checkout feat/C-1.2-测试补全与集成` + `git log --oneline -3` + `git status --short`；
  2. **第一步动作（可照抄）**：在 hifi §10 逐项执行并留痕——① 冷启动读序 ② `docker ps` 三容器 + `GET /actuator/health` 三服务（kms 8081 / did 8082 / subject 8080，did 需带 `CTDS_DID_DEMO_SIGNATURE_ENABLED=true`）+ 前端 5173 ③ 基线用例数（`mvn -B -ntp test` 全仓 + `npm test`）④ 覆盖率基线：`mvn -B -ntp -pl services/did -am install -DskipTests` 后 `mvn -B -ntp -pl services/did org.jacoco:jacoco-maven-plugin:0.8.13:prepare-agent test org.jacoco:jacoco-maven-plugin:0.8.13:report` → 回填 hifi §1.3 ⑤ 变异试跑：`mvn -B -ntp -pl services/did org.pitest:pitest-maven:mutationCoverage -DtargetClasses=com.ctds.did.application.DidIssuanceService`（单类评估耗时后定 §1.2 范围）⑥ 按 hifi §2 回填映射表"现有锚点/缺口"⑦ `git log` 核验基线；
  3. **测试先行**：按盘点结果定稿 T1~T8 / F1~F5 → 先写用例（确认"对现状即绿或如实红"）→ **用 hifi 的"删锚验证"制造必红 → 还原** → 再改实现（仅 F5 类型收口属源码重构，行为零变化）。

- **关键决策**：
  1. **D1（体量拆分）未获裁决 → 如实标注"待补裁决"，不代为决定**：按 lofi §4 建议口径（**不拆分**）推进，**不阻塞编码**（沿 `3.1.10`/`3.1.11` 先例，体量裁决可在编码交付前补登）；若后续补裁决为"拆分"，则按 `3.1.12a`/`3.1.12b` 切分并相应拆用设计；
  2. **Q1=A 的"真联调顺延"须由编排师走变更流程落地**：本卡**不改规格、不改 WBS**（红线：需求变化一律走变更流程），仅在设计中如实登记"连接器注册当前无规格/无实现/未开工"这一事实与顺延口径；
  3. **确认后即推送分支**（沿先例）：设计一经确认即为编码契约，分支推送便于后续编码会话与评审在同一远端基线上工作；
  4. **本阶段不开始编码**：章程一"单会话单任务"，且编码须冷启动并按 hifi §10 逐项留痕，故编码归新会话。

- **文件指针**：
  - 分支：`feat/C-1.2-测试补全与集成`（`edfbd6e` 立卡 + **`acb668d` 确认留痕**；**已推送**，基线 `main` = `593bc26`）
  - 本卡：`docs/tasks/WBS-3.1.12-C-1.2测试补全与集成-2026-09-25.md`（**§二 确认留痕** + §四 状态/设计确认会话行 + §六 D1 说明）
  - 设计：`docs/designs/WBS-3.1.12-lofi.md`（**§3.1 确认留痕**）、`docs/designs/WBS-3.1.12-hifi.md`（**编码契约已生效**：§1~§11，含 T1~T8 / F1~F5 / E1~E6 / 前置检查项）
  - 台账：`docs/status/进度看板与债务登记簿.md`（快照"进行中/待编排师"两行 + 变更记录新增行）
  - 上游参照：`docs/designs/WBS-3.1.6-{lofi,hifi}.md`（同族先例）；`docs/tasks/WBS-3.1.11-*.md` §七 7.5/7.7；`docs/tasks/WBS-3.1.10-*.md` §七 7.3
  - 门禁配置（**本卡不改**）：`scripts/gates/gates-config.json`
  - 上游日志：`docs/logs/Ctds-项目开发日志-26-09-25-1518.md`（立卡）← `-1336`（3.1.11 合并收口）← `-1321` ← `-1234` ← `-1205` ← `-1137` ← `-1057` ← `-0946`

---

## 任务记录 ｜ 2026-09-25 15:2x ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：WBS-3.1.12（设计一次确认）

### 做了什么（业务可读）

您对立卡时提的 5 个问题给了裁决（都按我的建议），我把裁决**逐条记进了任务卡和两份设计文档**，并把这一包正式转为"**可以开工**"状态：

| 问题 | 您的裁决 | 这意味着编码时会怎么做 |
| --- | --- | --- |
| Q1 连接器注册联调 | **按建议 A** | 本包只证明"**DID 能被下游正常消费**"（第三方视角：拿到 DID → 验证身份，走通完整闭环）；真正跟连接器注册联调，等 3.6.3 那个服务做出来再补 |
| Q2 达标怎么测 | **按建议 A** | 用临时工具量覆盖率与变异（**不装进项目、不改门禁**），量出的数字交给质量基建包 2.2.8 去装门禁 |
| Q3 遗留测试尾巴 | **按建议 A** | 3.1.11 的 S4~S10 + 互认的 4 条加固全收；"留痕表要不要合并"只出结论不动数据库 |
| Q4 前端 4 处历史报错 | **按建议 B** | 不夹带（属 C-1.1 域），另立小卡 |
| Q5 联调怎么证明 | **按建议 A** | 真实环境跑一遍并留痕，演示数据留到验收后再清理 |

**还有一件事需要您补一句**：我在立卡时另请裁决的 **D1（这一包预估 600~950 行，超过 400 行的惯例线，拆不拆）**，这次回复里没有涉及。我已**如实标注"待补裁决"**，并按建议口径（**不拆**）继续推进——**这不影响开工**（上一包、上上包也都是先开工、后补体量裁决）。您方便时补一句"不拆"或"拆"即可。

**这一轮同样没有碰代码**：只改了任务卡、两份设计和进度账，然后把分支推到了远端（这样接下来的编码会话和评审都在同一个基线上工作）。

### 验证结果

| 验证 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 确认留痕落盘 | 编辑核对（任务卡 §二/§四/§六 + lofi §3.1 + hifi 确认记录） | **三处齐备**（Q1~Q5 逐条 + D1 待补说明 + 确认后状态"编码契约生效"） |
| 台账同步 | 编辑核对（快照两行 + 变更记录一行） | 进行中 = "编码就绪（设计一次确认通过）"；待编排师 = ① 已闭环 + **待补 D1** + Q1 变更流程附加事项 |
| 分支推送 | `git push -u origin feat/C-1.2-测试补全与集成` | **成功**：远端**新建分支**（`* [new branch] feat/C-1.2-测试补全与集成`）；`git fetch` 后本地与 `origin/feat/C-1.2-测试补全与集成` **零差异** |
| 变更面自检 | `git status --short` | 仅 **5 个文档**（任务卡 / lofi / hifi / 台账 / 本日志）；**零代码变更、零依赖变更、零门禁配置改动** |
| 编码契约状态 | hifi「确认记录」表 + 设计裁决行 | **已签署生效**（Q1~Q5 采建议口径；D1 未涉及已如实标注） |

### 问题与教训

1. **裁决项与待裁决项必须分开登记**：编排师本次回复覆盖 Q1~Q5，但 **D1 未被涉及**。**处置**：不明示"已确认"、不代为决定，单独标注"**待补裁决（不阻塞编码）**"并说明当前推进口径与替代方案。**教训**：一次确认回复中的**未覆盖项**要逐项核对，宁可重复提示，不可默认通过。
2. **"按推荐"式回复要逐条对号入座**：回复写作"Q及Q4：按推荐"，存在笔误可能（Q3 与 Q4）。**处置**：按上下文对号（Q3=A、Q4=B，与建议口径一致），并在留痕中写明**逐条结论**（而非只写"按推荐"），便于日后核对。**教训**：确认留痕须落到"编号 → 具体口径"的可核验粒度。
3. **确认后立即推送分支**（沿先例）：设计即契约，远端基线一致可避免编码会话基于陈旧基线开工。
4. **不越界改规格**：Q1=A 中"真联调顺延"的落地方式是**由编排师走变更流程同步规格 §6.1 与 WBS 行 244**；本卡只在设计中如实登记事实与口径，**不擅自修改规格文档**（红线 2/4）。

### 停机原因

【**设计一次确认工作项完成（确认留痕三处 + 台账 + 分支推送）**；按 §1.3 停机：完成"推送分支"这一最小步骤后立即停止，**未开启编码**（编码归新会话，须冷启动并按 hifi §10 逐项留痕）。**上下文用量**：AI 自估 **约 40%**（口径以任务窗口显示为准；本会话累计含立卡探索与三份文档撰写 + 本次确认留痕）。**本阶段零代码变更、零依赖变更、零门禁配置改动**；未起停服务/容器、未写演示库。】

### 交接提醒

① **编码可直接开工（另开会话）**：冷启动 → `git checkout feat/C-1.2-测试补全与集成` → **hifi §10 七项前置检查**逐项实测留痕（含覆盖率基线测量与变异试跑）；
② **待补一件（不阻塞）**：**D1 体量拆分裁决**（建议不拆分；若补裁决为"拆"，按 `3.1.12a`/`3.1.12b` 切分并拆用设计）；
③ **Q1=A 的附加事项**：请编排师安排**走变更流程**同步 C-1.2 规格 §6.1 与 WBS 行 244 的表述（消除"规格不定义连接器行为 vs 3.1.12 含连接器注册联调"的自张力）；**本卡不改规格**；
④ **硬边界（编码会话必守）**：零新增依赖 / 零门禁配置改动 / 零新增错误码、状态枚举、库表、迁移 / **补测只允许加强断言**（弱化 = 打回项）；**只测量不开门禁**（数字与建议交 2.2.8）；发现真缺陷**停止上报**（不擅自修，红线 4）；
⑤ **环境**：编码/联调会话需重启三容器 + 三服务（did 带 `CTDS_DID_DEMO_SIGNATURE_ENABLED=true`）并**沿用同一把演示根密钥**；前端用 `localhost`（vite 监听 IPv6 回环）；
⑥ **补正说明（同日 15:3x，随一笔纯文字文档提交入库）**：本阶段留痕提交 = **`acb668d`**（5 文件 / +117 −8）；`git push -u origin feat/C-1.2-测试补全与集成` **成功**（远端新建分支，`git fetch` 后零差异），已于本日志"文件指针"与验证表回填；
⑦ **红线复述**：本阶段未动其它模块与其它任务卡；未实现规格外功能；未在设计/代码中补需求；未改门禁配置与规格文档。
