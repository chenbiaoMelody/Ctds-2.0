# Ctds-项目开发日志-26-09-25-2255

- 任务卡 / 需求编号：**WBS-3.2.1**（逻辑空间业务规格与验收剧本 · **立卡会话**）+ **清债小卡四张立卡**（DB-06 / DB-23 / DB-24 / 前端类型收口）——`docs/tasks/WBS-3.2.1-逻辑空间业务规格与验收剧本-2026-09-25.md`、`docs/tasks/DB-06-23-24-前端类型-清债小卡-2026-09-25.md`
- 关联文档：规格草案 `docs/specs/C-2.1-2.3-逻辑空间管理.md`（V0.9）；三份剧本 `docs/manuals/C-2.1-逻辑空间创建准入-验收剧本.md` / `C-2.2-空间成员与权限控制-验收剧本.md` / `C-2.3-空间隔离与策略继承-验收剧本.md`；台账 `docs/status/进度看板与债务登记簿.md`
- 会话目标：编排师回复"**1 和 2 都按推荐；3 中的几项待办请进行立卡**"（承接日志 `-2215` 交接提醒三件事）→ ① 下一包 3.2.1 立卡（任务卡 + 规格草案 + 三份剧本草案 + Q1~Q8 + D1）；② 四项长期待办立卡（一份清债小卡四张卡，口径 Q1~Q4 待确认）
- 状态：✅ **立卡完成**——五份新文档落盘（共 599 行）+ 台账同步（"下一包 / 待编排师 / DB-06·23·24 三行 / 裁决记录 / 变更记录"）；**两组待编排师确认**（3.2.1 §7 Q1~Q8 + D1；清债卡 Q1~Q4）；分支 `feat/C-2.1-逻辑空间业务规格与验收剧本` 已建（自 `cda446e`），**未推送**（沿 3.1.12"确认后推送分支"先例）；**本任务零代码变更、零依赖变更、零门禁配置改动**

## 当前续点（新会话从这里继续；续接会话承本块另建新日志）

> **分支与主库**：`main` = `origin/main` = `cda446e`（未动）；本批立卡文档在分支 **`feat/C-2.1-逻辑空间业务规格与验收剧本`**（未推送）。
> **新窗口第一步**：`git checkout feat/C-2.1-逻辑空间业务规格与验收剧本` + `git log --oneline -3` + `git status --short` 核验 → **若编排师已确认，第一步 = 回填裁决留痕（规格 §7 + 任务卡 §二 + 三份剧本版本行改"PO 已批准"）并推送分支**；未确认则只复述状态、不自行开工。

- **已完成**：
  1. 冷启动续接（读日志 `-2215` → 台账 → 用户指令解读：①=3.2.1 立卡、②=残留按推荐、③=四项待办立卡）；
  2. **只读素材盘点**（两路只读子智能体 + 本机实测）：WBS 行 251~257 / 行 154 / 行 6（WBS 版本 V1.2）；PRD V1.0 §5.2 行 109~111 + 行 232 术语、PRD V2.0 行 164/165；**既有资产核查：specs/manuals/adr 无 C-2.x 条目、代码侧无 space/tenant 实现与迁移（本域全新起点），唯一命名先例 ADR-005 `:27` `data-spaces`**；债务侧：gates-config 阈值已定但 `run-gates.ps1` **零引用**、`coverage/mutationTest/sast/dependencyScan` 均 PENDING、`dependencies.md` 无 jacoco/pitest/semgrep 登记；DB-23 造数已改应用时钟传参但**无任何时区偏移专项用例**、各 pom 无 surefire 配置；DB-24 现状与 ADR-016 §2.7 约束核实；**`npx vue-tsc -b` 本机实测退出码 2（4 处 TS2339）**；
  3. **3.2.1 三件套落盘**：任务卡（免设计声明 + Q1~Q8 + D1 + 映射表）+ 规格 **V0.9 草案**（216 行：8 条行为 + 逐条验收标准 + 非目标 8 项 + 边界声明 6 条 + §7 八决策点）+ 三份剧本（70/74/72 行，各三幕、界面入口占位）；
  4. **清债小卡落盘**（86 行，四张卡 + 待裁决 Q1~Q4）：卡 1 DB-06（质量度量门禁接入，**红线 3 → 卡即留痕载体**）/ 卡 2 DB-23（时区偏移回归固化 + 是否同批 DB-09）/ 卡 3 DB-24（kms 默认 profile 语义对齐 + 防御性回环）/ 卡 4 前端类型收口（**实测阻断 `npm run build` → 定级 S2**）；
  5. **台账同步**："下一包"行（3.2.1 已立卡）/"待编排师"行（① 决策点 + ② 清债卡口径）/ DB-06·DB-23·DB-24 三行（**新登记 → 已立卡**）/ 裁决记录 1 行 / 变更记录 1 行。

- **下一步**（可直接开工粒度）：
  1. **（待编排师确认）3.2.1 确认后动作**：回填裁决留痕（规格 §7"已裁决"块 + 任务卡 §二 + 三份剧本版本行改"PO 已批准"）→ `git push -u origin feat/C-2.1-逻辑空间业务规格与验收剧本`（沿"确认后推送分支"先例）→ 4 视角评审（规格与设计符合性 / 安全供应链 / 一致性重复 / 测试质量，纯文档包按 3.1.7 口径）→ 编排师验收；
  2. **（待编排师确认）清债卡执行**：确认 Q1~Q4 后按建议顺序**卡 4 → 卡 3 → 卡 2 → 卡 1** 逐卡执行（每卡独立分支 `fix/...` 或随卡声明、独立评审、独立合并，**单会话单卡**）；卡 4 第一步 = `npx vue-tsc -b` 复核 4 处错误现状 → 修 `inputByLabel` 返回类型 → 复测 `npm run build`；
  3. **（无确认时）**：不自行开工；3.2.1 分支保持未推送。

- **关键决策**：
  1. **免设计声明的适用**：3.2.1 = 纯文档类（规格与剧本即交付物，空间域设计归 3.2.2~3.2.6 两级设计门禁，沿 WBS-3.1.7 先例）；清债四卡 = S2/S3 债务与工具链/配置类（章程 2.6.3）；
  2. **红线 3 的留痕载体**：DB-06（门禁配置 + 工具链）与卡 4（若含 `frontendTypeCheck`）的变更以**清债小卡本身为留痕载体**（沿 DB-17~21 先例"scripts/gates/ 的修改属门禁变更，本卡即其留痕载体"），执行时另按卡内 ADR 要求留痕；
  3. **规格的诚实边界**：隔离 = **应用层强制 + 默认拒绝**，网络级隔离明确不在本规格（网关 3.5.2 承接）——避免规格越权承诺物理隔离；策略条目模型划归 3.2.5（本规格只定继承 / 覆盖 / 取严规则），防止与 C-4.x 两处各自定义策略模型；
  4. **错误码只预留段位**：空间域预留 **1006 段**（1000/1002/1003/1004/1005 已占用），码值随实施包设计——沿"一域一段"惯例，本规格不定码值；
  5. **剧本界面占位机制**：三份剧本先写业务步骤、界面入口占位，待 3.2.6 交付后按 C-1.2 同款"界面名称核对修订"一次性核对（业务判定不改，修订经 PO 批准留痕）；
  6. **编号口径如实登记**：WBS 清单（136 项）**无 2.2.8/2.2.9 行**（仅 AUD 卡"后续"表编号，同 2.2.7 已成卡先例）——已在清债卡表头与 DB-06 债务行注明，防追溯矩阵断链；
  7. **分支未推送**：沿 3.1.12 立卡先例（"未推送远端，待确认后按先例推送分支"）。

- **文件指针**：
  - 分支 / 提交：`feat/C-2.1-逻辑空间业务规格与验收剧本`（自 `cda446e`；提交号见文末"补正说明"；**未推送**）；`main = origin/main = cda446e`（未动）
  - 本批文档：`docs/tasks/WBS-3.2.1-逻辑空间业务规格与验收剧本-2026-09-25.md` / `docs/specs/C-2.1-2.3-逻辑空间管理.md` / `docs/manuals/C-2.1-...-验收剧本.md`、`C-2.2-...`、`C-2.3-...` / `docs/tasks/DB-06-23-24-前端类型-清债小卡-2026-09-25.md`
  - 台账：`docs/status/进度看板与债务登记簿.md`（"下一包 / 待编排师"两行 + DB-06/23/24 三行 + 裁决记录 + 变更记录）
  - 上游日志：`docs/logs/Ctds-项目开发日志-26-09-25-2215.md`（3.1.12 验收 + 合并收口 + 演示数据清理）

---

## 任务记录 ｜ 2026-09-25 22:2x~22:5x ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：WBS-3.2.1 立卡 + 清债小卡四张立卡

### 做了什么（业务可读）

您说"1 和 2 都按推荐；3 中的几项待办请进行立卡"，我据此做了两件立卡的事（**只写文档，没有动任何代码**）：

1. **下一包"逻辑空间业务规格与验收剧本"已立卡**：这一包要产出**一份覆盖三条 P0 需求的规格**（空间创建 / 成员与权限 / 隔离与策略继承）和**三份验收剧本**。我把草案全部写好了：规格草案列出 **8 条业务行为**（每条带"什么情况下应该怎样"的验收标准）和 **8 个需要您拍板的决策点**（比如：谁有资格建空间、成员怎么进空间、空间角色分几档、解散能不能反悔、下级策略能不能放宽上级红线等），三份剧本各三幕、可逐步执行。**您确认这 8 个决策点后，规格即转正式版**。
2. **四项长期待办已立成"清债小卡"（一份文件四张卡）**：① 把覆盖率 / 变异测试 / 静态扫描这三把"质量刻度尺"真正接入门禁（现在阈值写好了但根本没被读取）；② 把"时区差 8 小时会静默假绿"的测试隐患固化成自动化回归；③ 把 KMS 服务配置文件里"注释说能启动、实际起不来"的说法对齐事实；④ 修复前端 **4 处类型错误**——实测它们**已经导致前端无法打出生产包**（`npm run build` 失败），所以这张卡的优先级提为较高。每张卡都写清了"改什么、不改什么、怎么算完成"，并各留了一个待您确认的口径问题。

### 验证结果

| 验证 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 素材盘点 | 两路只读子智能体（逻辑空间素材 / 债务素材）+ 关键原文摘录 | WBS 行 251~257 / 154、PRD V1.0 行 109~111 / V2.0 行 164~165 均已核对入卡；specs/manuals/adr **无 C-2.x 条目**、代码侧无 space 实现（全新起点）；ADR-005 `:27` 为唯一命名先例 |
| 前端类型错误实测 | `cd frontend && npx vue-tsc -b` | **退出码 2**，恰 4 处 `TS2339`（`src/views/subject/CertificationView.spec.ts:195~198`）；`package.json` `build = vue-tsc -b && vite build` → **`npm run build` 被阻断**；实测后工作区零残留 |
| 门禁现状核验 | `gates-config.json` / `run-gates.ps1` / `dependencies.md` 只读核对 | `thresholds` 在脚本中**零引用**；`coverage/mutationTest/sast/dependencyScan` 均 `enabled:false` + PENDING；`dependencies.md` 无 jacoco/pitest/semgrep/owasp 条目 |
| DB-23 现状核验 | 全仓搜索 `user.timezone` / `GMT+12` / `ZoneId.of` + pom 核对 | `src/test` 与各 pom **零命中**（无时区偏移专项用例；根/模块 pom 均无 surefire 配置）；造数已改应用时钟传参（`LocalDateTime.now(clock)`） |
| 文档体量 | `wc -l`（6 份新文档） | 规格 **216** / 剧本 **70+74+72** / 3.2.1 任务卡 **81** / 清债小卡 **86** = **599 行**（任务卡 §五 已按实测回填） |
| 台账同步核验 | 编辑后锚点复查 | "下一包 / 待编排师"两行、DB-06/23/24 三行（新登记→**已立卡**）、裁决记录与变更记录各 1 行均已落位 |
| 红线自查 | 全程实测 | **零代码变更、零依赖变更、零门禁配置改动、零库表/迁移/错误码码值**；未碰其它任务卡；规格条目全部由 PRD + WBS + 既有 ADR 派生，未私自补需求 |

### 问题与教训

1. **"待办"立项前必须实测现状**：三张债务卡的卡面若只抄台账描述会失真——实测发现"阈值写了但脚本根本没读"（比台账表述更具体）、"4 处类型错误实际阻断生产构建"（比'告警'严重，据此把卡 4 定级 S2）、"各 pom 无 surefire 配置"（决定 DB-23 建议改走 offset Clock 形态）。**教训：立卡时的"现状实测"一节要写实测证据，不写转述。**
2. **编号断链风险**：`WBS-2.2.8/2.2.9` 在 WBS 清单（136 项）里**不存在**，只在 2026-09-15 的 AUD 处置卡"后续"表里——若直接引用会造成追溯矩阵断链。已在清债卡表头与 DB-06 债务行注明口径，是否正式并入 WBS 清单留编排师裁决。
3. **规格边界要先划清再写行为**：逻辑空间最容易"顺手承诺"物理隔离与策略语言——本次把"隔离=应用层强制""策略模型归 3.2.5"写进非目标与边界声明，避免规格越权（沿 3.1.10"模拟对端"同款诚实边界手法）。
4. **控制台中文乱码老坑复现**：`grep`/控制台输出中文为乱码但文件无损——核验一律用编辑器读取（沿用既有教训）。

### 停机原因

【**3.2.1 立卡 + 清债小卡四张立卡完成**；按 §1.3 停机：两组确认均待编排师（3.2.1 §7 Q1~Q8 + D1；清债卡 Q1~Q4），**不自行开工、分支不推送**。**上下文用量**：AI 自估约 **40%**（口径以任务窗口显示为准）。**本任务零代码变更、零依赖变更、零门禁配置改动**；多任务单会话沿显式授权先例（编排师当次指示），立卡与执行分离——任何一张卡的**执行**须另开会话。】

### 交接提醒

① **两组待确认（本次会话的唯一阻塞项）**：
   - **3.2.1**：规格 §7 **Q1 创建资格（建议 A：ADMITTED 自助创建）** / **Q2 参与方范围三档（建议 A）** / **Q3 角色三档（建议 A）** / **Q4 状态机与解散不可逆（建议 A）** / **Q5 红线不得放宽 + 冲突取严（建议 A）** / **Q6 隔离=应用层强制、可见≠可访问（建议 A）** / **Q7 资源名 `data-spaces` + 错误码预留 1006 段（建议 A）** / **Q8 剧本界面占位（建议 A）** + **D1 不拆分**——**全部建议采 A**，一句"都按建议"即可生效（规格转 V1.0、剧本同批批准）；
   - **清债小卡**：**Q1 DB-06 执行形态（建议 A：命令行形态，零 pom 变更）** / **Q2 DB-23 offset Clock 用例 + 同批做 DB-09（建议同批）** / **Q3 DB-24 注释对齐（建议 B）+ 同批补三服务防御性回环（建议同批）** / **Q4 卡 4 含 `frontendTypeCheck` 门禁段（建议含）**；
② **分支未推送**：`feat/C-2.1-逻辑空间业务规格与验收剧本`（含本批全部文档与台账），确认后第一步推送；
③ **清债卡执行顺序建议**：卡 4（放行前端构建）→ 卡 3（DB-24）→ 卡 2（DB-23，可含 DB-09）→ 卡 1（DB-06，最重）；每卡独立会话、独立评审、独立合并；
④ **环境与残留**：三容器 + 三服务在线（8080/8081/8082 健康 200）、前端 5173 未运行；根 `target/precheck/` 余 2 个被占用文件（28KB），按推荐留待停 did 服务时 `rm -rf`；
⑤ **红线复述**：本任务零代码、零依赖、零门禁配置改动；立卡不等于执行——**执行任何卡须另开会话并经编排师开工指示**；规格与剧本未经确认前均为"草案"口径，不得作为实现依据。