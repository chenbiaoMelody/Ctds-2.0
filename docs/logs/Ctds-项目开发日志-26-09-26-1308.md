# Ctds-项目开发日志-26-09-26-1308

- 任务卡 / 需求编号：**清债卡2｜DB-23 时区偏移窗口回归固化 + 同批 DB-09 取号日期统一注入时钟**（Q2=A：offset Clock 注入用例 + 同批 DB-09）——清债小卡 `docs/tasks/DB-06-23-24-前端类型-清债小卡-2026-09-25.md` 卡 2 段（免设计卡）
- 关联文档：台账 `docs/status/进度看板与债务登记簿.md`（行 56 = DB-23、行 42 = DB-09）；上游日志 `docs/logs/Ctds-项目开发日志-26-09-26-1122.md`（卡3，本会话连做授权的卡序上游）
- 会话目标：承接"开工：清债四卡"连做授权 → 卡3 完成（见 `-1122`）→ **卡2 执行** → 4 视角评审 → 修复 → 日志落盘 → 续做卡1
- 状态：✅ **卡2 完成——编码 + 4 视角评审通过（循环 1/3，含修复与复审），待合并**

## 当前续点（若 50% 停机，新会话从这里继续）

> **分支**：`fix/DB-23-09-two-clocks`（HEAD = 修复提交，基于卡3 分支梢链式）；卡1 起分支自本分支梢。
> **下一步**：卡1（DB-06 质量度量门禁接入，Q1=A 命令行形态升级为门禁内调用）：① **覆盖率段**：jacoco 命令行形态定稿 + `coverage` 段真实运行 + 阈值判定（核心 ≥80% / 整体 ≥70%）+ 报告真实数值；② **变异段**：PIT 范围/预算定稿 + `mutationTest` 段真实运行 + 阈值判定（≥60%）；③ **sast/dependencyScan**：semgrep / OWASP DC 获取通道前置实测（docker pull 实测为准），不可达则如实登记"不接入 + 另立卡"；④ 新建 ADR（工具选型/执行形态/阈值口径/时长预算/豁免语义）+ config changeLog + dependencies.md 登记 + selftest 场景（含"阈值未达标必红"反向探针）；⑤ 独立计时与预算（unitTest 段 241~250s 贴线，度量段不得挤占）。第一个动作 = 冷启动读本日志 → jacoco/PIT 命令行形态设计（沿 3.1.12 实证：jacoco 0.8.12 / PIT CLI）→ 自卡2 分支梢建 `fix/DB-06-quality-metrics-gate`。

- **已完成**（卡2 全链）：
  1. 自卡3 分支梢建分支；读现状（`insertVerificationRows` 已经应用钟传参、`SubjectRegistrationService:204` 取号日期直连系统钟 = DB-09 现场、Clock bean = systemDefaultZone）；
  2. **编码**：新增 `TwoClocksOffsetIntegrationTest`（偏移钟两锚用例 + 落位自检 + 分叉自检）+ DB-09 一行（取号日期经注入时钟）；提交 `e11bb99`；
  3. **首轮调试三连**：①落位自检红（OCR 上传也写核验留痕行 → 自检加 verify_type='LEGAL_PERSON' 过滤）②首版偏移方向 +1 天使锚②探针失效（SQL NOW() 态锚 2 绿）→ 改向 -1 天（系统昨天 23:00）→ 反向探针三态闭环（绿→SQL NOW() 态 2/2 红→还原绿）③checkstyle 未用导入红 → 修；
  4. **验证**：两锚 2/2 绿；全量门禁 **GREEN**（`gate-report-20260926-121914.md`；unitTest 含 subject-service 全量 + 取号并发回归）；
  5. **4 视角评审（循环 1/3）**：①PASS（4 P3）②PASS（序号唯一性推理成立；移交观察 = 时间带盲区）③**有条件 PASS**（2 P2 = 守护边界超 claim / 锚①红路径描述失实）④PASS（固化有效性最终判定：**类级必红 24/24 成立**——锚②任意时带 2~3 路独立红）；
  6. **修复轮**：注释收敛（类级必红=锚②全天候；锚①仅 8~24 带；0~8 带 DB 容器钟 UTC 分叉塌陷如实标注）+ **DB-09 钉死断言**（subjectNo.startsWith 应用钟当天——固化延伸到产品侧）+ 规格编号勘误（行为 3 第 3 条）+ 双向同步义务声明 + USCC 常量化 + T1 指路注释；提交 `9943103` → **独立复审通过**（P2 双关闭、断言抓红能力真实、零副作用）；
  7. 小卡卡2 执行记录 + 本日志落盘。

- **下一步**（可直接开工粒度）：见上方"当前续点"——卡1 开工（四卡最后一卡，最重：1~2 会话量）。

- **关键决策**：
  1. **偏移方向 -1 天**：+1 天方向锚②期望落位与 NOW() 落点巧合重合（首跑实证）→ -1 天方向锚②全天候必红，类级固化成立；
  2. **落位自检口径比服务计数更严**（有界双窗 vs 开区间）：评审④指出正是这份"更严"在反向探针里接住了服务开区间计数的假通过——设计成立；
  3. **守护边界如实标注**：0~8 带锚①假绿为解析推演（四方评审一致），如实写进 javadoc 不冒充实测——防"表述超前于证据"；
  4. **DB-09 钉死断言采纳**：评审建议的低成本高价值增强（2 行把固化延伸到产品侧改动）。

- **文件指针**：
  - 分支 / 提交：`fix/DB-23-09-two-clocks`（`9943103` ← `e11bb99`）；上游 `fix/DB-24-kms-profile-alignment`（`8d143bc`）← `fix/frontend-typecheck-gate`（`a4518e1`）；`main = origin/main = cac0697`（未动）
  - 任务卡：`docs/tasks/DB-06-23-24-前端类型-清债小卡-2026-09-25.md`（卡2 执行记录；卡1 段 = 后续输入）
  - 门禁证据：`scripts/gates/reports/gate-report-20260926-121914.md`（GREEN）
  - 上游日志：`docs/logs/Ctds-项目开发日志-26-09-26-1122.md`（卡3）← `-1059`（卡4）← `-0944`（3.2.1 收口）

---

## 任务记录 ｜ 2026-09-26 13:0x ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：清债卡2 DB-23+DB-09

### 做了什么（业务可读）

1. **把"两个时钟"缺陷的回归测试从"碰运气"变成"任何时候都有效"**：此前核验记录的测试造数若被改回数据库时钟，测试只在本地凌晨 0~8 点才会亮红灯（其余时段静默假绿）；现在用一台"固定在昨天 23:00 的测试时钟"让分叉在任何时刻都存在，造数一改回数据库时钟、测试必红（已实测验证三态：正常绿 → 人为改回必红 → 还原绿）；
2. **给"申请编号的日期"换了统一时钟源**：注册取号的日期此前直连系统钟，与其他业务日期来源不一致（DB-09 债务）；现统一经注入时钟，生产环境行为零变化（同一时钟源）；
3. **给产品侧改动加了"钉死"断言**：申请编号的日期段被断言必须等于测试时钟的"今天"——将来有人把取号日期改回直连系统钟，测试立刻红；
4. **四份独立评审全部通过**（其中一致性评审发现两处注释把守护能力写超了实际——已修正并经独立复审确认；评审同时用数学推演确认了"任何时刻改回必红"这一固化承诺真实成立）。

### 验证结果

| 验证 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 偏移钟锚用例 | `mvn test -Dtest=TwoClocksOffsetIntegrationTest` | **2/2 绿** |
| 反向探针 | 造数临时改回 SQL NOW() → 重跑 | **Failures: 2（必红）** → 还原 → 全绿（三态闭环，实测时点 12:19 属 8~24 带） |
| 全量门禁 | `run-gates.ps1` | **GREEN**（`PASS=11 FAIL=0 SKIP=1 ERROR=0 PENDING=9`；既有用例零改动零删除、只增不减） |
| checkstyle | `mvn checkstyle:check -pl services/subject-service` | **0 violations** |
| 4 视角评审 | 独立子智能体 ×4 串行 | ①PASS ②PASS ③有条件 PASS（2 P2 已修复+复审通过）④PASS |

### 问题与教训

1. **探针自身的探针**：首版偏移方向（+1 天）下反向探针的锚②与 SQL NOW() 落点巧合重合、探针静默失效——**反向探针必须实测红，不能只靠推演写注释**（实测抓出来了）；
2. **服务计数口径与直觉相反**：当日计数是开区间（>= 今日零点，无上界）——注释里想当然的"BETWEEN 窗口"写错了红路径机制，评审③以 SQL 原文勘正——**注释描述机制前先读实现**；
3. **表述超前于证据是评审高频命中点**："任何真实时刻""两条必红"超 claim（0~8 带假绿未标注、实测时点未写）——本轮四视角中三个视角独立命中同一问题。

### 停机原因

【卡2 完成待合并；**不构成会话终点**——连做授权中，续做卡1。上下文用量：AI 自估约 35~40%（权威值以任务窗口显示为准，50% 停机线 ≈ 50 万 tokens）——卡1 体量 1~2 会话，若窗口权威用量已近 50% 则在卡1 的第一个原子动作（jacoco/PIT 形态设计落盘）后停机交接。卡2 零依赖引入、零迁移、既有用例零改动。】

### 交接提醒

① **卡2 状态**：评审通过待合并（分支 `fix/DB-23-09-two-clocks`）；四卡全到评审态后编排师统一指示合并；
② **卡1 开工条件**：小卡卡1 段（Q1=A 命令行形态；jacoco 0.8.12 / PIT CLI 沿 3.1.12 实证先例）；注意 unitTest 段时长贴线，度量段须独立计时预算；
③ **环境**：三容器 + 三服务在线未触碰；测试库 ctds_subject_two_clocks 随测试自动建删；
④ **红线复述**：卡2 改动 = 1 个新测试类 + 1 行业务代码（DB-09 裁决授权）+ 既有类 2 行注释；零依赖、零迁移、零门禁配置改动。
