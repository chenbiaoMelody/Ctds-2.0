# Ctds-项目开发日志-26-09-26-1822

- 任务卡 / 需求编号：**WBS-3.2.2 空间数据模型**（WBS 行 252；前置 3.2.1 已闭环）+ 会话前置动作 = 清债四分支清理（承接日志 `docs/logs/Ctds-项目开发日志-26-09-26-1735.md` 续点"待编排师指示 3.2.2 立卡；四分支清理留待下一会话"）
- 关联文档：任务卡 `docs/tasks/WBS-3.2.2-空间数据模型-2026-09-26.md`；两级设计 `docs/designs/WBS-3.2.2-lofi.md` / `-hifi.md`；规格 `docs/specs/C-2.1-2.3-逻辑空间管理.md` V1.0；分级规范 `docs/domain/数据分类分级规范.md` §6.1；台账 `docs/status/进度看板与债务登记簿.md`
- 会话目标：① 四分支清理；② 3.2.2 立卡（两级设计一并）；③ 经编排师确认后实现 + 验证 + 推送（WBS 预算 1 会话）
- 状态：✅ **①②③全部完成——3.2.2 实现交付、探针 7/7 全绿、分支已推送，待 4 视角评审与编排师验收**

## 当前续点（新会话从这里继续）

> **分支 `feat/WBS-3.2.2-空间数据模型`（自 `546365d`）已推送 origin**；提交两笔：`7a15026`（立卡三件套）+ `d5e7be4`（实现+文档回填）。**main = origin/main = `546365d` 未动**（本卡未合并）。
> **新会话第一动作**：冷启动 → **对 3.2.2 组织 4 视角评审**（评审对象 = 分支 `d5e7be4`；纯数据模型包，评审锚 = 任务卡 §三映射表 + hifi §1 逐列 + 探针测试）→ 修复循环 → 编排师验收。

- **已完成**（本会话全链）：
  1. **冷启动 + 编排师表决**：复述 `-1735` 续点四要素 → 编排师四问确认"**先清理分支，再立 3.2.2 卡**"；
  2. **四分支清理**：`fix/frontend-typecheck-gate` / `fix/DB-24-kms-profile-alignment` / `fix/DB-23-09-two-clocks` / `fix/DB-06-quality-metrics-gate` 本地 4 支 `git branch -d` 安全删除（均已合并入 main，`-d` 校验通过；远端本就无这些分支）；其余 `feat/*` 历史分支不在授权范围未动；
  3. **3.2.2 立卡**：素材盘点（WBS 行 252 + 规格 V1.0 全文 + subject 迁移/domain/配置/容器测试先例 + 分级规范 CAT 定义）→ 三件套落盘 `7a15026`（任务卡 + lofi + hifi 两级一并，章程 2.6.3 ≤1 天卡一次确认）→ **编排师四问表决：Q1+Q2 采建议 A（新建 space-service 模块+最小交付）/ Q3~Q8 均采建议 A / Q9 采建议 A（落级+回写规范）/ D1 不拆分豁免**→ 确认留痕回填三处；
  4. **实现**（`d5e7be4`，28 文件 +882 行）：模块骨架（pom 沿 subject 惯例 + 主类 + application.yml【默认 profile 排除 DataSourceAutoConfiguration + 回环 127.0.0.1——DB-24 同族安全默认，space 从出生继承】+ application-mysql.yml【端口 8083 / 库 ctds_space / Flyway】+ 根 pom modules 追加）；**V1 迁移六表**（space / space_name_lock / space_member / space_admission / space_policy / space_action_log，MySQL 8 InnoDB utf8mb4 注释齐备）；**domain 5 实体 record + 12 枚举**；**探针测试 SpaceMigrationIntegrationTest**（Testcontainers mysql:8.0 实跑，7 用例）；
  5. **验证全绿**：`mvn -pl services/space-service -am test` = **Tests run: 7, Failures: 0, Errors: 0**（探针 0 迁移冒烟 / 1 同 owner 同名拒绝 / 2 跨 owner 同名允许 / 3 解散名全平台锁定 / 4 成员唯一活跃+唯一所有者 / 5 策略载体唯一 / 6 注释与生成列 information_schema 核对）+ `checkstyle:check` = **0 违规**；
  6. **分级规范 §6.1 回写 5 行**（Q9-A：space 配置 CAT-01/L2、成员权限映射 CAT-02/L2、策略 CAT-01/L2 含"引用资源则就高"诚实条款、留痕 CAT-06/L3、名称锁定 CAT-01/L2）+ hifi 测试类名勘误注记（`SpaceMigrationIT`→`SpaceMigrationIntegrationTest`，沿 subject 先例 surefire 直跑）；
  7. **推送 + 台账**：分支推送 origin（新分支）；台账"进行中"（3.2.1 闭环记录降为历史明细）/"下一包"两行更新 + 尾部变更记录 1 行；本日志落盘。

- **下一步**（可直接开工粒度）：**3.2.2 的 4 视角评审**（新会话冷启动后由编排师发起；评审对象 = 分支 `feat/WBS-3.2.2-空间数据模型` HEAD `d5e7be4`）——评审后修复循环（≤3 轮）→ 编排师验收 → 编排师指示合并（合并时做树一致性核验免重复门禁，沿先例）。**待编排师知悉/裁决项**：① 本包无接口无页面，**无业务演示点**——验收形态建议 = 探针测试实录 + 表结构与 hifi §1 逐列核对（3.2.3 起才有可演示功能）；② 登记未立卡（沿 `-1735` 交接提醒③继续保留）：did/kms `LocalDateTime.now()` 直连 8 处、kms/did 回环守卫补课、example-service 默认 profile 无回环、dependencyScan（OWASP DC）、DB-07（CI）。

- **关键决策**：
  1. **三条唯一性规则 DB 硬兜底**（"安全断言必须代码强制"教训的数据库版）：同 owner 归一化名唯一 = `uk_owner_norm_name`；解散名全平台锁定 = `space_name_lock` PK（解散事务内写入）；同空间同主体唯一活跃成员 + 唯一活跃所有者 = **MySQL 8 生成列**（`active_flag`/`owner_uniq`，NULL 不参与唯一去重故终态行可共存）+ 双 UNIQUE 索引；策略载体同作用域同键唯一 ACTIVE = `scope_uniq` 生成列——均有反向探针（编排师 Q5/Q6-A 确认）；
  2. **策略表只建载体**（scope/entry_key/entry_value/is_redline/status + `platform_entry_id` 显式继承指向），条目语言与可配置项集合归 3.2.5——规格行为 7 规则 6"禁止两处各自定义策略模型"（Q4-A）；
  3. **准入单独立建表**（space_admission）："未确认邀请不产生成员关系"由准入单/成员关系两表分工保证，成员表只存生效关系（Q3-A）；
  4. **场景类型枚举 = 规格缺口处置**：规格行为 1 规则 2"取值口径见 §7 Q1"系悬空引用（Q1 实为创建资格裁决）——lofi"规格缺口声明"如实登记，按行文示例落 FINTECH/MEDICAL/OTHER 并经 Q8-A 确认，扩充须走变更流程；
  5. **分级落级回写规范属合规义务非需求变更**：规格合规锚点明文"CAT 归类在 3.2.2 建表落级前复核"→ hifi 落级表 + 规范 §6.1 补 5 行（Q9-A）。

- **实现期踩坑与修复**（全部测试代码层，迁移 SQL 与设计逐列一致零改动）：
  1. **Testcontainers 应用用户对新库无 CREATE 权限**——首轮 7 用例全 ERROR 的根因；沿 `SharedMySqlContainer` 先例改**容器 root 建库 + GRANT**（subject 模块先例已解决过同一问题，先例读取应在本卡编码前置清单）；
  2. **约束冲突探针须断言原始异常**：`exec` 助手包装 SQLException 会把 `SQLIntegrityConstraintViolationException` 藏进 IllegalStateException——assertThrows 失配；助手改直接上抛；
  3. `@Container` 静态字段由扩展自动启动，`@BeforeAll` 手动 start 冗余；
  4. SQL 占位符漏传参两处（探针 0/6）——vararg 助手方法的经典错。

- **文件指针**：
  - 分支 / 提交：`feat/WBS-3.2.2-空间数据模型`（`7a15026` 立卡 + `d5e7be4` 实现，已推送）；`main = origin/main = 546365d` 未动
  - 代码：`services/space-service/`（pom + 主类 + yml×2 + V1 迁移 + domain 17 文件 + 探针测试）；根 `pom.xml` modules +1 行
  - 文档：任务卡 + lofi/hifi（确认留痕已回填）；分级规范 §6.1；台账"进行中/下一包/变更记录"
  - 上游日志：`-1735`（清债四卡收口/分支清理交接）← `-1442` ← `-1308` ← `-1122` ← `-1059`

---

## 任务记录 ｜ 2026-09-26 18:22 ｜ 编排师：项目主导者（兼任） ｜ AI/模型：ZCode ｜ 卡号：WBS-3.2.2 空间数据模型

### 做了什么（业务可读）

1. **分支清理**：上一会话合并闭环的四张清债卡分支（前端类型 / DB-24 / DB-23+09 / DB-06）按惯例从本地删除（删除前逐支核验"已并入主干"，远端本就没有这些分支）；
2. **空间数据模型立卡与设计**：为逻辑空间域新建"数据地基"设计——独立服务模块 + 6 张表（空间主表含状态机、解散名称锁定、成员关系、准入申请/邀请单、策略条目载体、统一操作留痕）+ 5 个数据对象与 12 个状态枚举。设计一次讲清方向与细节（低保真+高保真一并），**您四问表决全部按建议口径确认**；如实登记了一处规格笔误（"场景类型取值见 Q1"实际悬空）并按规格行文示例落三值枚举请您确认；
3. **实现与验证**：建表脚本 + 数据对象 + 自动化探针测试全部落地；探针在真实 MySQL 容器里实证了三道"硬闸门"——同一所有者不能建两个同名活跃空间、解散过的名字全平台不能再占用、一个空间同时只能有一个"生效中"的所有者——任何绕过应用层的并发写入都会被数据库直接拒绝；7 项测试全过、代码风格检查 0 问题；
4. **合规落级**：按规格指派义务，把 6 张表的敏感级别复核后回写进《数据分类分级规范》清单（空间配置=L2、成员与权限映射=L2、操作留痕=L3 审计级）。

### 验证结果

| 验证 | 命令 / 方式 | 结果 |
| --- | --- | --- |
| 迁移 + 约束探针 | `mvn -B -ntp -pl services/space-service -am test`（Testcontainers mysql:8.0 实跑） | **Tests run: 7, Failures: 0, Errors: 0** |
| 代码风格 | `mvn -B -ntp -pl services/space-service checkstyle:check` | **0 violations** |
| 分支推送 | `git push -u origin feat/WBS-3.2.2-空间数据模型` | 新分支建立成功，本地远端一致 |
| 落级回写 | 分级规范 §6.1 | 补 5 行（CAT-01/L2 ×3、CAT-02/L2、CAT-06/L3） |

### 问题与教训

1. **先例的"第二层"要在编码前读完**：SharedMySqlContainer 建库必须用容器 root（应用用户无 CREATE 权限）这一关键细节在先例文件后半段——首轮探针 7 用例全 ERROR 才回读；"读先例"不能只读前 60 行；
2. **探针类测试的异常断言链路要通到底**：助手方法包装异常会让 `assertThrows` 拿到包装类型而非原始约束冲突——断言什么类型，链路上就不许包装；
3. 规格行文悬空引用（"见 Q1"实际未定义）按"缺口声明 + 决策点确认"处置，不在设计里私自补口径——本轮第二次实证该流程（第一次为 3.2.1 评审补正）。

### 停机原因

任务完成正常收工（上下文自估约 25%，未触发停机线）。

### 交接提醒

① **3.2.2 待 4 视角评审**：评审对象 = 分支 `feat/WBS-3.2.2-空间数据模型` HEAD `d5e7be4`；本包无业务演示点，验收形态建议 = 探针测试实录 + 表结构与 hifi §1 逐列核对；
② **本会话门禁口径**：本地模块级 test + checkstyle 全绿；**全仓门禁未跑**（本包新增独立模块不触碰既有模块，评审/合并前如需全仓门禁按惯例在验收环境跑）；
③ **登记未立卡**（沿 `-1735` 交接提醒③继续保留）：did/kms `LocalDateTime.now()` 直连 8 处；kms/did 回环守卫补课；example-service 默认 profile 无回环（space 已从出生继承回环，家族差距 +1）；dependencyScan（镜像化+离线 NVD）；DB-07（CI）未接入；
④ **环境**：三容器 + 三服务在线未触碰；本包服务未启动（无接口无业务，验收不需要起服务；`ctds_space` 库由 Flyway 首次启动自动建）；
⑤ **红线复述**：本会话零门禁配置改动、零既有服务改动、零新增依赖族（仅平台内新增自研模块 space-service）；错误码 1006 段仅预留未落码值；策略本体未定义（归 3.2.5）。
