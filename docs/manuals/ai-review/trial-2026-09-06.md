# AI 评审流水线首次试运行记录（演练任务：W1–W3 基建首批交付物）

- 日期：2026-09-06 ｜ 演练依据：WBS 2.3.2（4 视角评审智能体配置与试运行，用演练任务验证）
- 演练对象：`docs/04-AI工具白名单-评估与发布.md`、`docs/adr/ADR-001~005`、`scripts/gates/*`、`docs/manuals/ai-review/*`、`docs/templates/PR描述模板.md`、规则修改（章程 V2.3 / AGENTS.md V2.3 / README / 风险手册）
- 执行方式：4 个独立评审会话（新鲜上下文、只读），各加载 `cards/01~04` 评审卡；发现的问题由编码会话修复后复验。

## 会审结论

| 视角 | 首轮结论 | 修复后结论 | 发现与处置 |
| --- | --- | --- | --- |
| ① 规格符合性 | PASS（4×P2） | PASS | 签发待人工（转用户待办）；trial 记录悬空（本次落盘修复）；门禁"规格外"未声明（见下方声明）；顾问意见 D-2 代偿（已留痕，立项后补复核） |
| ② 安全与供应链 | PASS（4×P2） | PASS（4×P2 转跟踪） | 无密钥泄露/无自实现密码学/无未核验依赖；门禁自防篡改、正则盲区、*.lock 排除、MinGit 哈希校验 4 项列入跟踪清单（见下） |
| ③ 一致性与重复 | **FAIL**（2×P1+4×P2） | PASS | P1：风险手册残留"增量更新"旧口径、README 版本号陈旧（章程 V2.1→V2.3、PRD"评审中"→发布稿）——均已修复；P2：白名单误引"章程 8.4.3"→改 5.5-3、章程 V2.3 修订记录落点补全、3.2 表补 `docs/templates/` 登记、trial 悬空——均已修复 |
| ④ 测试质量 | PASS（5×P2） | PASS | 已修复：损坏配置退出码改 2、排除规则不再误伤 `.gitignore`、扫描跳过数入报告、补 archunit 待接项与分支覆盖字段；**核实不成立**：其报告的"AGENTS.md 头部仍 V2.2"经 grep 核验为误读（实际两处均 V2.3） |

**会审总结论：4/4 视角 PASS（③ 经一轮修复）。** 门禁复跑 GREEN（PASS=4 / FAIL=0 / PENDING=9，报告 `gate-report-20260906-091456.md`）。

## 规格外实现声明（响应评审①）

`structureCheck`、`devLogNamingCheck`、`adrFieldsCheck`、内置 secrets 扫描超出 WBS 2.2.2/2.2.4 字面产出（"流水线配置+阈值配置"），定性为：WBS 2.2.3（安全扫描）与 2.2.1（检查单固化）在**零外部工具依赖**约束下的最小落地形态，非范围膨胀；gitleaks 等正式工具待 ADR-001 批准后替换（已在 `gates-config.json` 标注）。

## 转入跟踪清单的问题（P2，不阻断）

| # | 问题 | 跟踪动作 |
| --- | --- | --- |
| T1 | 门禁配置与被管代码同仓、无完整性保护，本地可整体跳过 | CI 落地后由流水线侧固定版本重跑门禁；远程托管+分支保护后收敛 |
| T2 | 内置正则存在漏报（无引号口令、JWT/连接串、≥1MB 与二进制跳过） | ADR-001 批准后接入 gitleaks/Semgrep 替换 |
| T3 | `*.lock` 被排除在密钥扫描外 | 锁文件生成后复核该排除项 |
| T4 | MinGit 取自 npmmirror 镜像，未留哈希校验 | 补做发布版哈希比对并记入评估记录 |
| T5 | 白名单正式签发、ADR-001~005 批准均为待人工动作 | 见交付说明"需要您决策的事项" |
