# C-TDS 清债小卡任务卡（DB-06 / DB-23 / DB-24 + 前端类型收口，2026-09-25）

| 项 | 内容 |
| --- | --- |
| 来源 | 编排师 2026-09-25 会话指示"**3 中的几项待办请进行立卡**"（承接 WBS-3.1.12 合并收口会话日志 `docs/logs/Ctds-项目开发日志-26-09-25-2215.md` 交接提醒④）；对象 = 台账 `docs/status/进度看板与债务登记簿.md` **DB-06 / DB-23 / DB-24** 三行 + WBS-3.1.12 评审观察项"`vue-tsc` 4 处既有类型错误" |
| 级别与性质 | 四张**免设计**小卡（章程 2.6.2 免设计类：S2/S3 债务修复与工具链/配置类任务，无界面、无业务行为）→ 依该条免两级设计门禁（理由随卡声明） |
| 本文件性质 | **立卡（本会话）**；**执行归后续会话**（各卡可独立交付、独立评审；若单会话连做沿"多任务单会话显式授权"先例，逐卡落日志） |
| 通用出口 | 每卡：本地验证全绿（附命令与结果）→ 4 视角评审通过 → 合并入 main（编排师指示）→ 台账对应债务行更新（关闭必须填**关闭时间 + 证据**）→ 开发日志落盘 |
| 纪律声明 | ① **不得降低既有检查强度**（不移除检查项、不放宽阈值）；② 涉依赖引入须先核验官方注册表真实存在 + 报告审批 + 登记 `docs/dependencies.md`（AGENTS §3）；③ **卡 1 涉门禁配置变更 → 属架构变更流程（红线 3），本卡即其留痕载体**（沿 DB-17~21 清债小卡先例）；④ 卡 4 只动类型层，**行为零变化**；⑤ 不夹带其它卡、不碰 C-1.x/C-2.x 业务行为 |
| 建议执行顺序 | **卡 4**（最小、收益立见：放行前端生产构建）→ **卡 3**（DB-24，配置/文档对齐）→ **卡 2**（DB-23，测试固化，可与 DB-09 同批）→ **卡 1**（DB-06，最重，需 ADR 与选型） |
| 编号口径说明 | **WBS 清单（136 项）中不存在 2.2.8 / 2.2.9 行**——两编号仅见于 `docs/tasks/C-TDS-AUD-P0-处置任务卡-2026-09-15.md` 的"后续（P1/P2，另行立项）"表（同 2.2.7「门禁加固」已于 2026-09-15 成卡并闭环的先例）。本卡沿用 AUD 卡编号并在台账登记；**如需正式并入 WBS 清单，请编排师裁决后走变更流程**（不在本卡夹带） |

---

## 卡 1 ｜ DB-06 ｜ 质量度量门禁接入（覆盖率 + 变异 + 静态扫描）

| 项 | 内容 |
| --- | --- |
| 类型 | 质量基建实现（**S2 债务**；工具链 + 门禁配置变更 → **须 ADR + 白名单 + `dependencies.md` 登记**） |
| 依据 | 台账 DB-06 行（"三个质量刻度尺没装：覆盖率、变异测试、静态扫描均未接入，历次达标数字是临时手工测的，无法被重新算一遍"）；AUD 卡"后续"表 **WBS-2.2.8 质量度量门禁接入** = "JaCoCo + PIT（+ 视情况 CPD/Semgrep/OWASP DC），把覆盖率/变异阈值变为硬门禁（工具链变更 → ADR + 白名单 + `dependencies.md` 登记）" |
| **现状实测（2026-09-25，只读核验）** | ① `scripts/gates/gates-config.json` 已含 `thresholds`：`coreLineCoverage 80` / `overallLineCoverage 70` / `mutationKillRate 60`（**`run-gates.ps1` 中 `thresholds` 零引用 = 阈值从未被读取**）；② 阶段态：`coverage`（`enabled:false`，`PENDING-JACOCO`，tool `jacoco`）、`mutationTest`（`PENDING-TOOLCHAIN`，tool `pitest`）、`sast`（`semgrep`）、`dependencyScan`（`owasp-dependency-check`）；③ `run-gates.ps1:370-376` 为**通用循环**：凡 `enabled=false` 且 `status` 以 `PENDING` 开头者一律记 `PENDING`（无"假执行"检测）；④ `docs/dependencies.md` 无 jacoco / pitest / semgrep / owasp 字样（仅 cyclonedx 用于 SBOM）；⑤ 最新交付态报告 `scripts/gates/reports/gate-report-20260925-215307.md` 中 `coverage`/`mutationTest` 仍为 `PENDING`；⑥ 先例数字（3.1.12 实测）：did 行覆盖 **96.22%**、变异杀除率 **68.18%**（jacoco 0.8.12 / PIT CLI **命令行临时注入**，未改 pom 与门禁） |
| 交付物 | ① **覆盖率段实跑**：jacoco 执行形态定稿 + `coverage` 段真实运行 + 阈值判定（核心模块 ≥80% / 整体 ≥70%）+ 报告出真实数值；② **变异段实跑**：PIT 执行形态与**范围/预算**定稿 + `mutationTest` 段真实运行 + 阈值判定（≥60%）；③ **静态扫描（`sast` / `dependencyScan`）**：工具可获取性**前置核验**（本机镜像/包通道实测，见记忆 `docker-image-pull-channels` 教训：以实测为准）→ 接入 或 **如实登记"不接入 + 另立卡"**（禁止以 PENDING 冒充已接入）；④ **新建 ADR**（质量度量门禁接入：工具选型 / 执行形态 / 阈值口径 / 时长预算 / 豁免与"未开放"语义）+ `gates-config.json` `changeLog` 留痕 + `dependencies.md` 登记 + 白名单核验 |
| 边界 | 只改 `scripts/gates/`（配置 + 脚本 + selftest）、`docs/adr/`、`docs/dependencies.md`、以及**仅在插件形态需要时**的根 pom；**不改业务代码与业务测试用例**；**不得放宽** `secretsScan` / `devLogNamingCheck` / `adrFieldsCheck` 等既有强度 |
| 验收口径（= 台账关闭条件） | 门禁中覆盖率/变异**由 PENDING 变 PASS/FAIL 且报告出现真实数值**；`selftest.ps1` 增对应场景（含"阈值未达标必红"的反向探针，防"假执行/假绿灯"）；`run-gates.ps1` 全量 **GREEN** 且退出码语义不变（1=RED / 2=环境或配置错误 / 0=GREEN） |
| 前置 / 风险 | ① **`unitTest` 段时长贴线**：DB-25 已缓解至 241~250s（上限 600s，`run-gates.ps1:298`）→ 度量段须**独立计时与预算**，若需调上限**另走架构变更评估**（红线 3，不得由本卡夹带）；② PIT 耗时随模块增长显著 → 建议范围限定**核心模块**（`gates-config.json:12` 的 `coreModules` 现含 `did`）并按模块分段；③ semgrep / OWASP DC 获取通道未实测 → 核验前置；不可达则如实登记，**不得把"跑不起来"写成"未开放"** |
| 体量 / 建议会话 | ≈ 250~450 行（脚本段 + ADR + 配置 + selftest 场景）/ **1~2 会话**（覆盖率+变异 1 会话；静态扫描视选型另计） |
| **Q1（已裁决采 A）** | 覆盖/变异的**执行形态**：**A（建议）= 沿用 3.1.12 实证的"命令行调用"升级为门禁内调用**（零 pom 变更、可回退、与既有"未开门禁"口径平滑衔接）／ B = 引入 `org.jacoco:jacoco-maven-plugin` / `pitest-maven` 插件（需 pom 变更 + 依赖登记 + 白名单；**插件坐标以官方注册表核验为准**——评审循环 1 修复勘误：原稿"Maven-jacoco"非真实 Maven 坐标） |

---

## 卡 2 ｜ DB-23 ｜ 时区偏移窗口回归固化（"两把钟"测试面）

| 项 | 内容 |
| --- | --- |
| 类型 | 测试补强（**S3 债务**，免设计） |
| 依据 | 台账 DB-23 行（"DB-22 的'本地 0~8 点两钟分叉'复现靠**手工命令**（`-Duser.timezone=GMT+12`），若将来把测试造数回退为 SQL `NOW()`，在 +8 时区的门禁里只有恰好跑在 0~8 点才会红，其余时段静默假绿"）；DB-22 任务卡 4 视角评审④ **P2**（建议与 DB-09 产品侧"两把钟"统一收口） |
| **现状实测（2026-09-25，只读核验）** | ① 造数**已**改为经应用时钟传参：`CertificationIntegrationTest#insertVerificationRows` 现为 `LocalDateTime.now(clock).plusDays(dayOffset)`，`clock` 由 Spring 注入（`@Autowired private Clock clock;`）；② **无任何时区偏移专项用例**：全仓 `src/test` 与各 pom 中 `user.timezone` / `GMT+12` / `ZoneId.of` **零命中**（唯一命中为 `target/surefire-reports/*.xml` 的 JVM 环境记录，非配置）；③ **根 pom 与各模块 pom 均无 `maven-surefire-plugin` 配置** → 台账设想的"专用 surefire 执行"形态需**新增插件配置**（属架构变更留痕项） |
| 交付物 | 把"应用时钟与数据库时钟分叉"固化为**自动化回归**，至少覆盖两条"今日对照"锚用例（**当日第 6 次核验被拒 400 / 次日恢复放行**）。形态二选一（**Q2 已裁决采 A**）：**A（建议）offset `Clock` 注入用例**——用既有 Clock 注入点构造固定偏移（如 `Clock.fixed` 于"跨日边界±"）使"应用时钟的今天"与"数据库时钟的今天"分叉，断言判定仍按应用时钟成立（**零 pom 变更、离线可跑**）；B 专用 surefire 执行（`-Duser.timezone=GMT+12` 固化）→ 需新增插件配置（架构变更留痕）。**无论采哪种，必须含反向探针**：把造数改回 SQL `NOW()` 时新用例**必红**（否则等于没固化） |
| 边界 | `services/subject-service` 测试源集（采 B 时含根 pom 插件配置）；**零业务代码变更、零迁移、零依赖引入** |
| 验收口径（= 台账关闭条件） | 时区偏移窗口固化为自动化回归，至少两条"今日对照"锚用例在报告中可见；**反向探针实测必红并还原留痕**；subject-service 全量用例只增不减且全绿 |
| 前置 / 关联 | **与 DB-09（产品侧"两把钟"：取号日期与其他逻辑日期来源不一致，证据 `SubjectRegistrationService:153`）统一收口的建议**：本卡只做**测试面**固化；若采 A 形态，DB-09 的**产品侧统一注入时钟**可同批或紧随其后（二者共用同一 Clock 注入点）。**Q2 附带裁决项：是否同批做 DB-09？**（建议 **同批**：一处改动、一次评审、一次回归） |
| 体量 / 建议会话 | ≈ 100~200 行 / **1 会话**（含 DB-09 的产品侧改动则 ≈ 1 会话，风险 = 触及注册取号路径，须回归取号并发用例） |

---

## 卡 3 ｜ DB-24 ｜ kms 默认 profile 启动语义与回环约束

| 项 | 内容 |
| --- | --- |
| 类型 | 配置 / 文档对齐（**S3 债务**，免设计） |
| 依据 | 台账 DB-24 行（"`application.yml` 注明'默认 profile 无数据库照常启动'，但实测 `UnsatisfiedDependencyException`——排除 `DataSourceAutoConfiguration` 后无 `JdbcClient` Bean，而 `KeyJdbcRepository` 构造器硬依赖之。注释与事实不符"）；**约束（评审②P2-3）**："若采①（让默认 profile 真能空跑），**必须同时补默认 profile 的 `server.address: 127.0.0.1`**——kms/did 默认 profile 均无回环声明，一旦默认 profile 真能启动即以非回环地址暴露**未鉴权**的材料读取/签名/签发端点，违反 ADR-016 §2.7" |
| **现状实测（2026-09-25，只读核验）** | ① `services/kms/src/main/resources/application.yml`：注释写明默认 profile 无库照常启动 + `spring.autoconfigure.exclude` 含 `DataSourceAutoConfiguration`，`server` 段**只有 `port: 8081`、无 `address`**；② `application-mysql.yml` 有 `address: 127.0.0.1`（并注"演示期安全边界（ADR-016 §2.7）"）；③ `KeyJdbcRepository` 构造器 `public KeyJdbcRepository(final JdbcClient jdbc)` 硬依赖；④ **did 与 subject 的默认 profile 同样无 `address`**（同一潜在暴露面，同族欠账）；⑤ ADR-016 §2.7 已写明启动纪律"**必须带 `--spring.profiles.active=mysql` 启动（默认 profile 未绑定回环）**" |
| 交付物 | **Q3 已裁决采 B**：**B（建议）= 注释与事实对齐 + 登记启动口径**——把 kms `application.yml` 注释改为"默认 profile 须 mysql 才能启动"并在部署口径（README / ADR 补记）登记，风险 = 零（无行为变更、不扩大启动面）；A = **默认 profile 条件装配仓储**（让默认 profile 真能空跑）——**若采 A 必须同时补默认 profile `server.address: 127.0.0.1`**，且须补"未鉴权端点后果表征"评估与冒烟（ADR-017 §"内部无鉴权面后果表征"口径）。**另建议（无论 A/B）**：为 kms 默认 profile **显式补 `address: 127.0.0.1`** 作防御性声明（只增不减、无行为影响）；**是否同批为 did / subject 做同一处置，请一并裁决**（建议同批，消除同族欠账） |
| 边界 | `services/kms/src/main/resources/*.yml`（采 A 时涉 `services/kms` 装配类；一致性同批时动 did / subject 的 yml 与注释）；**零业务逻辑变更、零依赖变更** |
| 验收口径 | 该 profile 语义与文档**一致且可核对**：采 B = 注释/部署口径更正 + 启动纪律留痕，kms 既有用例全绿；采 A = 默认 profile **启动实测成功**（actuator `UP`）且 **netstat 实测仅回环**（`127.0.0.1`），mysql profile 行为**零变化**；两形态均须全仓门禁 **GREEN** |
| 体量 / 建议会话 | ≈ 30~120 行 / **0.5~1 会话** |

---

## 卡 4 ｜ 前端类型收口（`vue-tsc` 4 处既有类型错误，C-1.1 域）

| 项 | 内容 |
| --- | --- |
| 类型 | 类型层修复（**S2**——理由：**阻断前端生产构建**；免设计，无界面行为变更） |
| 依据 | `docs/designs/WBS-3.1.12-lofi.md:38`（该错误"**属 C-1.1 域**、3.1.11 已登记不夹带"，Q4 采 B）；WBS-3.1.12 §七 观察项；3.1.11 / 3.1.12 两度登记"建议另立前端小卡" |
| **现状实测（2026-09-25，只读核验）** | `cd frontend && npx vue-tsc -b` → **退出码 2**，4 处同码错误：`src/views/subject/CertificationView.spec.ts(195,50)/(196,54)/(197,51)/(198,50): error TS2339: Property 'value' does not exist on type 'VueNode<Element>'`（测试辅助函数 `inputByLabel`（:167）返回类型被推断为 `VueNode<Element>`，其 `.value` 断言不成立）；**`frontend/package.json` 的 `build` = `vue-tsc -b && vite build`** → **`npm run build` 当前失败**（业务影响：前端无法产出生产包）；核验后工作区未被改动（`git status` 空） |
| 交付物 | ① 修复 4 处类型错误（**仅类型层、行为零变化**：约束辅助函数返回类型或改为显式元素类型断言，二选一由实现会话按最小改动定）；② 复核 **`npm run build` 通过** + 前端既有 **118 例全绿** + `npm run lint` 0；③ **含（Q4 已裁决采建议）**新增门禁阶段 **`frontendTypeCheck`**（执行 `vue-tsc -b`）以防回归——属门禁配置变更，**留痕载体 = 本卡**（沿 DB-17~21 先例：卡即变更留痕，另配"强度只增不减"声明与自检场景） |
| 边界 | `frontend/`（测试辅助/断言文件 + 若含 ③ 则 `scripts/gates/`）；**不得顺手重构其它文件、不得改业务行为、不得引入新依赖** |
| 验收口径 | `npx vue-tsc -b` **退出码 0**；`npm run build` **成功**（产物 `dist/` 不入库）；前端 `npm run test` / `npm run lint` 全绿；含 ③ 时门禁报告出现 `frontendTypeCheck PASS` 行、`gates-config.json` `changeLog` 留痕，且**既有阶段强度不变** |
| 体量 / 建议会话 | ≈ 20~60 行（含门禁段 +30 行）/ **0.5~1 会话** |

### 卡 4 执行记录（2026-09-26，执行会话连做授权：编排师"开工：清债四卡"）

| 项 | 内容 |
| --- | --- |
| 分支 / 提交 | `fix/frontend-typecheck-gate`（自 `origin/main = cac0697` 建）：编码 **`6b85cbf`**（5 文件 +48/−6）→ 评审循环 1 修复 **`c758222`**（README 同步 V1.3）→ 复审尾巴 **`1a51eb8`**（selftest 头注释补 S10 行） |
| 修法 | 交付物①采任务卡方向一"约束辅助函数返回类型"：`inputByLabel` 内 `find` 显式泛型 `<HTMLInputElement>`（1 行），返回类型收窄为 `DOMWrapper<HTMLInputElement>`，4 处 TS2339 消除；运行时零变化（泛型编译期擦除）。交付物③走既有 npm 循环最小接入：config 新段 `goals: "run typecheck"` + `frontend/package.json` 增 `typecheck` script（= `vue-tsc -b`，门禁 npm 形态调用的必要最小配套，经视角①边界判定未越界）+ runner 前端循环数组加段 + selftest 关闭列表同步 |
| 验证结果 | `npx vue-tsc -b` 退出码 0；`npm run build` 成功（chunk 体积告警为既有提示）；`npm run test` **118/118** 全绿；`npm run lint` 0；`selftest.ps1` **57 断言 0 失败**（S10 红/绿双探针过，S1~S9 不受扰）；全量门禁 **GREEN**（`gate-report-20260926-101728.md`：`PASS=11 FAIL=0 SKIP=2 ERROR=0 PENDING=9`，`frontendTypeCheck PASS` 行出现，既有阶段强度不变，PENDING 9 项照旧） |
| 4 视角评审 | **循环 1/3 收口**：①规格符合性 **PASS**（2 P3）②安全供应链 **PASS**（1 P3，既有设计观察）③一致性重复 **有条件 PASS**（1 **P2**：`scripts/gates/README.md` 三处停在 V1.2 → 已修 `c758222`，**独立复审通过**：三处证据关闭、断言数 57 独立核算属实）④测试质量 **PASS**（3 P3）；P3 共 7 条均不阻断、登记不夹带 |
| P3 留痕（不阻断） | ①立卡时把可预见配套文件写入边界行；②S10 为接线探针非 vue-tsc 端到端证明（注释已如实声明）；③config workdir 不在 goals 白名单范围（既有设计）；④changeLog "FAIL/1" 措辞可补 RED 字样（下次触碰时顺手）；⑤"enabled 翻 false 报告行静默消失"为 npm/maven 各段共有系统性盲区，建议后续 selftest 增"真实 config enabled 静态断言"（另立卡裁决）；⑥真实 package.json 删 script 由真门禁 fail-closed 兜底；⑦config 与 selftest 须同 commit 演进（本次已同 commit） |
| 状态 | ✅ **编码交付 + 4 视角评审通过（循环 1/3，含修复与复审）——待合并**（合并授权待编排师统一指示；台账 DB 关闭与快照刷新随合并收口段执行） |
| 剧本是否需更新 | **不需要**（零业务行为变更：1 行类型泛型 + 门禁接线，无界面/接口行为） |

---

## 裁决汇总（**已确认**：2026-09-25 编排师"都按建议"；章程 2.6.3 一次确认口径）

| 编号 | 问题 | 建议口径 |
| --- | --- | --- |
| **Q1** | DB-06 覆盖/变异的**执行形态** | **A**：沿用 3.1.12 实证的命令行调用升级为门禁内调用（零 pom 变更、可回退） |
| **Q2** | DB-23 回归形态 **+ 是否同批做 DB-09**（产品侧"两把钟"） | **A**：offset `Clock` 注入用例（零 pom 变更）+ **同批做 DB-09**（共用 Clock 注入点，一次评审） |
| **Q3** | DB-24 处置形态 **+ 是否同批 did / subject** | **B**：注释与事实对齐 + 启动口径登记（零行为变更）；**同批**为三服务默认 profile 补防御性 `address: 127.0.0.1` |
| **Q4** | 卡 4 是否含 `frontendTypeCheck` 门禁阶段（防回归） | **含**（门禁配置变更以本卡为留痕载体，强度只增不减） |

> **确认留痕（2026-09-25，编排师会话回复"**都按建议**"）**：**Q1=A**（DB-06 采命令行形态，零 pom 变更）／ **Q2=A**（DB-23 采 offset `Clock` 注入用例）**+ 同批做 DB-09**（产品侧统一注入时钟）／ **Q3=B**（DB-24 采注释与事实对齐 + 登记启动口径）**+ 同批为 kms/did/subject 三服务默认 profile 补防御性回环**／ **Q4=含**（卡 4 新增 `frontendTypeCheck` 门禁段，留痕载体 = 本卡）——**四卡口径锁定**；**执行顺序建议 卡4 → 卡3 → 卡2 → 卡1，每卡另开会话并经编排师开工指示**（单会话单任务）；台账"待编排师"行已同步。

---

> **边界复述（红线自查）**：本文件只立卡不执行；四卡均**免设计**（章程 2.6.2 免设计类，理由随卡）且**不得降低既有检查强度**；卡 1 的门禁/工具链变更与卡 4 的门禁阶段新增**均属架构变更流程（红线 3）**，以本卡（及后续执行卡的评审记录）为留痕载体；涉依赖引入一律"注册表核验 + 审批 + `dependencies.md` 登记"；不碰业务行为与其它任务卡。