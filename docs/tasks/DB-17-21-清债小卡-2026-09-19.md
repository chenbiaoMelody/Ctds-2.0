# C-TDS 清债小卡任务卡（DB-17~21，2026-09-19）

| 项 | 内容 |
| --- | --- |
| 来源 | `docs/status/进度看板与债务登记簿.md` DB-17~DB-21 行（均登记为"新登记，待小卡"，出自卡 1 第 2 轮复评与卡 3/卡 4 评审发现） |
| 裁决（编排师，2026-09-19 会话留痕） | ① **五条全做**：多任务单会话授权（沿 2026-09-12 / 2026-09-19 卡 2/3/4 先例）；② **DB-21 本次一并做**：规格例外条款 = 把已实现且已人工验收的行为补写进规格，属规格补正而非新需求，随卡附补正记录、PO 审批以本裁决留痕 |
| 本文件性质 | 立卡 + 执行记录同文件（单会话单批次：5 个卡节、一支分支 `fix/DB-17-21-gates-and-alignment`、一次 4 视角评审覆盖 5 节、一次合并）。理由：五卡合计有效变更预计 <400 行，且 DB-17/18/19 同属 `scripts/gates/` 一个脚本域；按卡节各自留痕，不降低"单卡单目标"可核对性 |
| 通用出口 | 每节：本地验证全绿（附命令与结果）→ 4 视角评审通过 → 合并入 main → 台账对应债务行更新 + 会话开发日志落盘 |
| 纪律声明 | `scripts/gates/` 的修改属门禁变更（AGENTS §8 第 3 条），本卡即其留痕载体；**所有卡不得降低既有检查强度**（不移除检查项、不放宽阈值——本批次只增不删） |

---

## 卡 17 ｜ DB-17 ｜ selftest 补两个自动锚点（S8 + R5 注入用例）

| 项 | 内容 |
| --- | --- |
| 类型 | 工具链测试补强（S2 债务，免设计——门禁脚本缺陷修复，无界面/业务行为） |
| 依据 | 任务卡"卡 1 第 2 轮 4 视角复评结论"评审④ N1/N2：R2"FAIL+ERROR 并存→退出码 1"组合场景只有一次手工报告背书（`162911`），无自动断言；R5"非法配置整段跳过"零验证 |
| 交付物 | `scripts/gates/selftest.ps1` 增两场景：**S8**（坏名日志触发 devLogNamingCheck FAIL + 无效 JAVA_HOME 使三 Maven 阶段 ERROR → 断言退出码 1、verdict 含 `-> RED (also`，锚定"真红灯不被环境错误掩盖"）；**S9**（Maven `compile.goals` 注入 `&` → 断言 compile ERROR 且明细含 "NOT executed"，锚定 R5 整段跳过路径） |
| 边界 | 只改 `scripts/gates/selftest.ps1` 与 `scripts/gates/README.md`（场景表/计数同步，事实性文档随码更新）；不改 run-gates.ps1 业务逻辑、不改配置 |
| 验收口径 | `selftest.ps1` 全量（S1~S9）退出码 0、断言数 = 原 40 + 新增；S8/S9 断言锚定退出码与报告行文 |

### 卡 17 执行记录

| 项 | 内容 |
| --- | --- |
| 状态 | ✅ 已完成（selftest 全量 49 断言全绿） |
| 变更 | `scripts/gates/selftest.ps1`：头部说明增 S8/S9 两行；新增 **S8**（坏名日志 `devLogNamingCheck` FAIL + 无效 JAVA_HOME 使三 Maven 阶段 ERROR → 断言退出码 1、报告含两行证据、verdict 含 `-> RED (also`）与 **S9**（`compile.goals` 注入 `&` → 断言退出码 2、ERROR 行含 "stage SKIPPED, command NOT executed"、点名 `stages.compile.goals`）；各以 try/finally 接管 `$env:JAVA_HOME`（沿 S3 同构，防本机真实 JAVA_HOME 压过配置假路径）。`scripts/gates/README.md`：场景表增 S8/S9 两行、"七场景（S1~S7）"更正为"九场景（S1~S9，49 断言）" |
| 断言数 | 40 → **50**（S8 六条 + S9 四条），锚定 R2"真红灯不被环境错误掩盖"与 R5"非法配置不执行命令"两条业务承诺；S8 含注入物锚定（`zz-bad-log-name`，归因链与 S5/S6 同档——评审④ P3-1 采纳） |
| 实施坑 | 首跑 S8/S9 各 4 断言失败：S8/S9 未接管 `$env:JAVA_HOME`，本机真实 JDK 使 Maven 段真跑 `mvn`（fixture 无 pom → FAIL 而非预期 ERROR）。照 S3 模式接管后全绿——该坑本身证明 S3 的 env 接管模式是必要的 |
| 验证 | `SELFTEST PASS - 49 assertion(s), 0 failure(s)`（评审后含 S8 补断言复跑为 50，见修复记录节），退出码 0，沙箱自清理 |

---

## 卡 18 ｜ DB-18 ｜ 密钥扫描三类"静默逃逸"显性化

| 项 | 内容 |
| --- | --- |
| 类型 | 工具链缺陷修复（S2 债务，免设计） |
| 依据 | 评审② #1/#2 + 评审④ N3：①目录枚举失败（无权限）时整个子树不进扫描名单且不报"打不开"；②≥1MB 文件只进"未扫总数"不列名单；③NUL 二进制静默跳过——三者均违背"入库文件不得静默逃逸扫描"原则 |
| 交付物 | `scripts/gates/run-gates.ps1` secretsScan 段：①枚举失败逐路径收集，记 `secretsScan.enumGap | ERROR`（退出码 2）；②超大文件与二进制按 tracked/untracked 区分——tracked 的逐条列名（`secretsScan.oversized` / `secretsScan.binary` SKIP 行，注明 listed for manual review），untracked 计数；PASS 行"not scanned"细分计数 |
| 边界 | 只改 `run-gates.ps1`；`gates-config.json` 零改动（`.git`、reports 等已在排除项）；既有 SKIP/ERROR 语义不变（unreadable 逻辑不动） |
| 验收口径 | selftest S1~S9 全绿（既有场景不回归）；门禁全量运行正常（真实仓库无枚举失败，新行只在有内容时出现）；tracked 大文件/二进制在报告中可见名单 |

### 卡 18 执行记录

| 项 | 内容 |
| --- | --- |
| 状态 | ✅ 已完成（selftest 回归全绿 + 真实仓库全量门禁观察新行） |
| 变更 | `scripts/gates/run-gates.ps1` secretsScan 段：① 枚举失败经 `-ErrorVariable` 收集，逐路径记 `secretsScan.enumGap | ERROR`（退出码 2）；② ≥1MB 文件按 tracked/untracked 区分——tracked 逐条列名（`secretsScan.oversized | SKIP` 行，注明 listed for manual review），untracked 计数；③ NUL 二进制同构（`secretsScan.binary | SKIP` 行）；④ PASS 行"not scanned"由合并总数改为细分（excluded/oversized/binary/unreadable） |
| 语义边界 | 既有 SKIP/ERROR 语义零改动（`secretsScan.unreadable`/`secretsScan.skipped` 逻辑原样）；未动 `gates-config.json`（`.git`、reports 等已在排除项，二进制计数不被 git 内部对象污染）；检查强度只增不减（枚举失败从"静默"变"ERROR"，大文件/二进制从"只进总数"变"入库文件列名"） |
| 真实仓库观察 | **DB-19 注入实测同轮运行**（`gate-report-20260919-211427.md`：真实仓库、`-ConfigPath` 临时配置、frontendTest FAIL/退出码 1，观察时点 2026-09-19 21:14）：`secretsScan | PASS | Scanned 571 files, 0 hits (not scanned: 22071 excluded, 0 oversized, 9 binary, 1 unreadable)` + `secretsScan.binary | SKIP | 9 binary file(s) ... untracked: 9`——新行正常出现、SKIP 不影响退出码的结论由**交付态**运行 `gate-report-20260919-212651.md`（GREEN/0，SKIP=2）独立证实（两轮 scanned 数差 1 系演示进程 app.log 增长跨过 1MB 线与 npm 运行时缓存，tracked 数 540 两轮一致，非失实） |

---

## 卡 19 ｜ DB-19 ｜ 门禁 FAIL 明细中文乱码修复

| 项 | 内容 |
| --- | --- |
| 类型 | 工具链缺陷修复（S3 债务，免设计） |
| 依据 | 评审④ N4：npm 子进程输出未设解码编码，中文失败明细在报告中为乱码（红灯本身不丢，人工复核"为什么红"失效） |
| 交付物 | `run-gates.ps1` 前端段 ProcessStartInfo 显式设 `StandardOutputEncoding/StandardErrorEncoding = UTF-8`（Node.js/npm 输出恒为 UTF-8；Maven 段不动——JDK 17 中文 Windows 输出为平台编码，与默认解码一致，改了反而引入乱码） |
| 边界 | 只改前端段两行；不动 Maven 段、不动超时与结果处理逻辑 |
| 验收口径 | 注入一次真实前端测试失败（含中文断言文案）→ 门禁 frontendTest FAIL 明细在报告中为可读中文；恢复后全量复绿 |

### 卡 19 执行记录

| 项 | 内容 |
| --- | --- |
| 状态 | ✅ 已完成（注入实测：报告明细中文可读） |
| 变更 | `run-gates.ps1` 前端段 `ProcessStartInfo` 显式设 `StandardOutputEncoding/StandardErrorEncoding = UTF-8`（两行 + 注释说明为何 Maven 段不改） |
| 实测 | 注入含中文断言文案的临时失败用例（`expect({ 核验结论: '不通过', 原因: '身份证号校验位不正确' }).toEqual(...)`，验证后 git 恢复、不提交）→ 门禁 frontendTest FAIL → 报告 `gate-report-20260919-211427.md` 明细中**中文源码行与断言文案逐字可读**（连 vitest 的 `⎯` 分隔线 U+23AF 亦完整） |
| 边界澄清 | stdout 重定向查看时个别字符显示 `?` 属 PowerShell 5.1 显示层损耗（GBK 控制台转写），落盘报告文件本体（UTF-8 BOM）完好——验收口径锚定报告文件 |
| 留档观察 | 报告明细含 vitest 强制彩色输出的 ANSI 转义码（`[90m` 等）——非编码问题、不影响"为什么红"的人工识别，沿"复评建议级改进项"惯例留档，不阻塞 |
| 检查强度声明 | 仅增解码正确性，Maven 段（JDK 17 中文 Windows 平台编码输出，与默认解码一致）未动——改了反而引入乱码 |

---

## 卡 20 ｜ DB-20 ｜ 演示 profile 回环绑定机器守卫

| 项 | 内容 |
| --- | --- |
| 类型 | 测试补强（S3 债务，免设计） |
| 依据 | 卡 3 评审④重点 3：`application-mysql.yml` 的 `server.address=127.0.0.1` 全靠人工 netstat/curl 验证，配置被改或启动姿势变化无警报 |
| 交付物 | `services/subject-service` 新增绑定断言测试（约 30 行）：`ConfigDataApplicationContextInitializer` 加载真实 `application.yml` + `spring.profiles.active=mysql` → 注册 `ServerProperties` → 断言 `getAddress()` 非空且 `isLoopbackAddress()`；附默认 profile 对照（address 为 null=未绑定，证明断言真正绑定于 mysql profile 配置而非巧合） |
| 边界 | 只加测试文件；不改任何主代码/配置；不起真实服务器、不连数据库（Environment 绑定层断言） |
| 验收口径 | `mvn -B -ntp test`（subject-service）全绿；负向复验：临时把 yml 改为非回环 → 测试红、恢复 → 绿（防空洞断言） |

### 卡 20 执行记录

| 项 | 内容 |
| --- | --- |
| 状态 | ✅ 已完成（正负双向验证） |
| 变更 | 新增 `services/subject-service/src/test/java/com/ctds/subject/LoopbackBindingGuardTest.java`（约 55 行）：`ConfigDataApplicationContextInitializer` 加载真实 `application.yml` + 激活 mysql profile → 注册 `ServerProperties` → 主断言 `getAddress()` 非空且 `isLoopbackAddress()`；对照组断言默认 profile `getAddress()` 为 null（证明断言真由 mysql profile 配置驱动，非默认值巧合） |
| 正向 | `mvn -B -ntp test -Dtest=LoopbackBindingGuardTest` → `Tests run: 2, Failures: 0, Errors: 0` |
| 负向复验（防空洞断言） | 临时把 `application-mysql.yml` 的 `server.address` 改为 `0.0.0.0` → 测试红（失败详情正是本测试断言"server.address 必须为回环地址（127.0.0.1）"）→ git 恢复 → 复绿。守卫真实可报警 |
| 边界 | 只加测试文件；不起真实服务器、不连数据库（Environment 绑定层断言）；全量测试纳入门禁 unitTest 段，门禁可见。**与登记口径的实现方式差异注记**（评审① P3-3 / ③ P3-2 采纳）：登记簿括号提示写 "RANDOM_PORT+mysql profile"，实现为 Environment 绑定层断言、不起服务器（RANDOM_PORT 对绑定层断言无意义）——核心处置要求"断言 `ServerProperties.getAddress()`=回环 + 门禁可见"已满足且更稳健（负向复验可报警）；台账关闭时同此注记 |

---

## 卡 21 ｜ DB-21 ｜ 身份证号口径三处对齐（DTO/hifi/规格补正）

| 项 | 内容 |
| --- | --- |
| 类型 | 文档与契约对齐（S3 债务；含一处 DTO 注解收紧=代码变更） |
| 依据 | 卡 4 评审②建议 2、评审③ S1/S2/S3：①DTO `VerificationRequest` 注释写"15 或 18 位"且 `@Pattern` 放行 15 位形态，服务层已一律拒 15 位——行为安全但契约自相矛盾；②hifi B6 写校验位错误返回 1004B0004，实现为 1000C0001（`ErrorCodes.PARAM_INVALID`）；③规格 C-1.1 行为 7 第 3 条"渠道调用全程留痕"未含卡 4 事务边界后"并发败者本地留痕随事务回滚"的例外 |
| 交付物 | ① DTO `@Pattern` 收紧为仅 18 位（`\d{17}[0-9Xx]`）+ javadoc 更正"18 位"——15 位在参数校验层即被 400 拒绝（对外效果仍为"15 位一律拒绝"，拒绝点提前到接口层，与服务层口径一致）；② hifi B6 加**补正说明**（校验位非法=1000C0001；1004B0004 语义收窄为"法人姓名与证照识别值不一致"，沿 -1714 文档补正先例，不改已确认设计原意）；③ 规格 C-1.1 行为 7 第 3 条补**例外条款**（并发事务败者：本地留痕随事务回滚，以提交为准）+ 版本行补正留痕；④ 行为 1 联系电话/管理员账号等其余口径不动 |
| 边界 | DTO 一处注解 + 两处文档；不改服务层校验逻辑（15 位仍双重拒绝）；不动 Flyway/表结构 |
| PO 审批留痕 | 2026-09-19 编排师裁决"本次一并做"（规格例外条款=已实现且已人工验收行为的补写，非新需求；见本文件头表裁决行） |
| 验收口径 | subject-service 全量测试绿（15 位仍被拒——DTO 层 400 或服务层 1000C0001）；三处**口径**与服务层实现一致（hifi 为"表格原文只读 + 补正说明标注"达成，非逐字改写——评审③ P3-4 措辞更正） |

### 卡 21 执行记录

| 项 | 内容 |
| --- | --- |
| 状态 | ✅ 已完成（三处口径与服务层实现一致） |
| ① DTO | `VerificationRequest.java`：`@Pattern` 由 `(\d{14}[0-9Xx]\|\d{17}[0-9Xx])` 收紧为 `\d{17}[0-9Xx]`（仅 18 位）；javadoc 更正"18 位，末位可为 X/x"并补 DB-21 口径说明；message 更新为"身份证号格式不正确（须为 18 位，末位可为 X）"（查证无测试断言旧文案）。15 位在参数校验层即 400 拒绝——对外总效果不变（15 位仍一律拒绝，DB-05 服务层校验保留为第二道），拒绝点提前到接口层 |
| ② hifi | `docs/designs/WBS-3.1.3-hifi.md` 行为清单表后加**补正说明**引用块：B6"校验位合法否则 1004B0004"与实现不符，实现为参数类 **1000C0001**（`CertificationService.requireIdChecksum`，DB-05 交付）；1004B0004 语义收窄为"法人姓名与证照识别值不一致"。表格原文未动（历史文档只读 + 补正标注先例），代码零改动 |
| ③ 规格 | `docs/specs/C-1.1-主体注册与实名认证.md`：行为 7 第 3 条内联补**例外口径**（并发事务败者本地留痕随事务回滚、不落库，"全程留痕"以提交成功事务为准；外部渠道侧调用不随本地回滚，对账以渠道侧流水为最终依据）；版本行补 V1.2 补正留痕（系把已实现且已人工验收的卡 4 事务边界行为补写进规格，无新增行为；PO 审批=编排师"本次一并做"裁决，见本文件头表） |
| 验证 | subject-service 全量 134/134 绿（15 位用例仍拒：服务层直调用例不受 DTO 影响、渠道零调用断言原样）；DTO 改动经全量回归零破坏 |

---

## 批次验证记录

| 验证 | 命令/方式 | 结果 |
| --- | --- | --- |
| selftest 全量（含 S8/S9） | `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\gates\selftest.ps1`（实施期 ×3 次 + 评审修复后复跑） | `SELFTEST PASS - 50 assertion(s), 0 failure(s)`，退出码 0，沙箱自清理（评审④ P3-1 采纳 S8 补注入物锚定断言后 49→50；各评审智能体独立复跑均与声明一致） |
| subject-service 全量 | `mvn -B -ntp test`（subject-service 模块） | **134/134 全绿**（`Tests run: 134, Failures: 0, Errors: 0, Skipped: 0`；Testcontainers 集成真实执行 0 跳过，含 LoopbackBindingGuardTest 2 例） |
| DB-20 负向复验 | 临时改 `server.address=0.0.0.0` → 复跑 | 测试红且失败详情正是绑定断言 → git 恢复 → 复绿（守卫真实可报警） |
| DB-19 注入实测 | 临时注入中文断言失败用例 → 门禁跑 frontendTest | 报告 `gate-report-20260919-211427.md` FAIL 明细中文逐字可读 → 用例 git 恢复 |
| 交付态全量门禁 | `run-gates.ps1`（评审修复前后各一次） | **两次均 GREEN / 退出码 0**（评审前 `PASS=10 FAIL=0 SKIP=2 ERROR=0`，RunLabel `20260919-211904-1773`，报告 `gate-report-20260919-212651.md`；评审修复后 RunLabel `20260919-222000-5817`，报告 `gate-report-20260919-223011.md`，结果行相同——后端 compile/lint/unitTest 与前端 lint/test 全 PASS，secretsScan 新 SKIP 行正常且不影响退出码） |

## 批次业务可读交付说明

- **完成了什么功能**（按卡）：
  - **卡 17（DB-17）**：给"体检仪自检"补上两条此前只有一次手工演示背书的规矩——"真红灯不许被环境错误盖住"与"查出非法配置就绝不执行命令"，现在每次跑自检（约 1 分钟）都会自动验证，改回去就会报警（自检从 40 条断言增至 49 条）。
  - **卡 18（DB-18）**：密钥扫描的三个"查无此人"死角全部显性化——打不开的目录不再当不存在（记错误、退出码 2）；超过 1MB 的入库文件与二进制文件不再只是总数里的 anonymous，入库的会逐条列名供人工复核。
  - **卡 19（DB-19）**：门禁报告抓到红灯时，"为什么红"的中文明细不再乱码（实测：注入中文失败后报告逐字可读）。
  - **卡 20（DB-20）**：给演示服务的"只许本机访问"开关装上机器守卫——配置一旦被改掉，`mvn test` 当场红灯（正负双向验证），不再依赖人工 netstat/curl。
  - **卡 21（DB-21）**：身份证号的三处说明书互相打架的问题清算——接口契约、设计文档、规格三处口径与服务层实现完全一致（18 位；校验位错误码 1000C0001；并发败者留痕随事务回滚的例外已写进规格）。
- **如何演示**：① `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\gates\selftest.ps1` → `SELFTEST PASS - 49 assertion(s)`；② `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\gates\run-gates.ps1` → 末行 `-> GREEN`、`ExitCode: 0`（约 8 分钟，报告中可见 `secretsScan.binary`/`secretsScan.oversized` 新行）；③ `mvn -B -ntp test`（subject-service）→ 134/134。
- **是否需要更新验收剧本**：**不需要**。五卡均无用户可见的界面/业务流程变化；卡 21 的 DTO 收紧对"15 位身份证号被拒"的对外效果与剧本演示口径一致（拒绝点提前，演示剧本不含 15 位输入步骤，无步骤变化）。
- **注意事项**：① 门禁配置（gates-config.json）零改动；② 卡 21 规格补正的 PO 审批 = 编排师 2026-09-19 "本次一并做"裁决（任务卡头表留痕）；③ 全量门禁报告中 22072 excluded 为 node_modules 等未入库产物的正常计数（细分计数新行首次让该数字可解释）。

---

## 4 视角评审结论（2026-09-19，独立评审智能体×4 串行只读；评审循环 1/3）

| 视角 | 结论 | 必修 | 关键确认 / 发现 |
| --- | --- | --- | --- |
| ① 规格与设计符合性 | **通过** | 0 | 五卡交付物与登记簿处置要求逐条相符、无超范围实现；DB-21 三处口径与实现一致；规格补正未越权（例外三要点均为已实现已验收行为）；selftest 实跑复验 49 断言。3 条 P3（卡 17 边界声明缺 README 字样 / 卡 18 观察数字未注明时点 / DB-20 实现方式与 RANDOM_PORT 字面差异）——已采纳 |
| ② 安全与供应链 | **通过** | 0 | gates-config.json 零变化（diff 为空）；检查强度只增不减逐 hunk 核实；命令注入面零新增（detail 统一转义、S9 注入仅存于 %TEMP% 沙箱、illegal 检查先于任何 Process.Start）；DTO 正则线性无回溯风险（10 形态抽验 + 200 字符恶意回溯探针 2ms）；零新依赖；独立复跑 selftest 49 断言全绿。1 条 P3（enumGap 行路径为绝对路径，与同报告其他行相对口径不一致；报告不入库 + Repo 头已有绝对路径先例，不阻塞）——留档观察 |
| ③ 一致性与重复 | **有条件通过** | **2** | 数字三方对上（49 断言逐场景清点、134=130 @Test + 4 @ArchTest 深挖坐实、PASS=10 逐项清点）；跨文档发现 README:20 SKIP 语义句在 DB-18 后失实（**P2-1**）、卡 18 把注入态运行误标"交付态"（**P2-2**）——两处均为文档小改，更正后放行；4 条 P3（退出码 2 清单补 enumGap / 台账收口四处联动 / 规格验收标准"任一次"量词限定 / 卡 21"逐字一致"措辞）——已采纳 |
| ④ 测试质量 | **通过** | 0 | S8/S9 断言逐条锚定业务承诺、假绿方向排查无静默通路（评审方独立反向探针：改坏期望值 → `SELFTEST FAIL - 49, 1 failure`，已恢复）；S9 归因分析属实（allowlist 移除/后移两类回归均失去 NOT executed 行文）；LoopbackBindingGuardTest 对照组有效排除默认值巧合；DB-19 注入恢复彻底（git status 零残留、`git log -S` 核验）；卡 20 负向复验被评审方独立重放证实。3 条 P3（S8 补注入物锚定——采纳 / 前端段 allowlist 无自动锚点——留档 / SERVER_ADDRESS 环境变量覆盖边界——设计取舍无动作） |

**必修项修复记录（P2-1 / P2-2，文档改动）**：

| # | 修复 | 落点 |
| --- | --- | --- |
| P2-1 | README"SKIP 不是放行"句更正为三类 SKIP 语义（未入库不可读 / ≥1MB / NUL 二进制，入库文件逐条列名）+ 退出码 2 清单补 `enumGap` + 变更留痕块补 DB-18/DB-19 行——README 与 DB-18 改动后行为重新一致 | `scripts/gates/README.md` |
| P2-2 | 卡 18"真实仓库观察"更正标注：数字快照来源 = DB-19 注入实测同轮（211427，FAIL/1），"SKIP 不影响退出码"由交付态运行（212651，GREEN/0）独立证实；注明观察时点与两轮差异原因（app.log 跨 1MB 线、npm 缓存；tracked 数 540 两轮一致） | 本文件卡 18 执行记录 |

**采纳的建议级修复**：①P3-1 卡 17 边界声明补 README.md 字样；①P3-2 并入 P2-2 时点注记；①P3-3/③P3-2 卡 20 与 DB-20 台账关闭注记（Environment 绑定层断言 vs RANDOM_PORT 字面差异）；③P3-1 退出码 2 清单补 enumGap（随 P2-1）；③P3-3 规格行为 7 验收标准"任一次"补"以提交成功的事务为限"联动限定；③P3-4 卡 21 验收口径措辞更正；④P3-1 S8 补 `zz-bad-log-name` 注入物锚定断言（49→50，复跑 `SELFTEST PASS - 50 assertion(s), 0 failure(s)`）。

**留档观察项（不阻塞合并，沿"随变更单留痕"先例）**：① `secretsScan.enumGap` 行路径为绝对路径（与同报告其他行相对口径不一致——报告不入库、泄露面可忽略，后续小卡统一为相对形态）；② 前端三阶段 allowlist 与 Maven 段同构但 selftest 无对应注入场景（本卡交付物只承诺 Maven 段，后续随门禁收敛登记）；③ 门禁报告明细含 vitest 强制彩色输出 ANSI 转义码（非编码问题，不影响人工识别红灯原因）；④ `LoopbackBindingGuardTest` 在 `SERVER_ADDRESS` 环境变量被预置的机器上可能误判（javadoc 与 ADR-016 §2.7 已声明"不得以环境变量覆盖"为人工启动纪律，守卫范围与文档一致）。
