# DB-22 任务卡：集成测试"两把钟"（测试造数用数据库时钟）· 修复（2026-09-20）

| 项 | 内容 |
| --- | --- |
| 来源 | 台账债务登记簿 **DB-22**（2026-09-20 新登记）+ 编排师指示"**先清 DB-22 再进 3.1.8 高保真**"（2026-09-20 会话留痕） |
| 级别与性质 | **S3 缺陷修复**（测试代码缺陷，产品代码无恙）→ 按章程 2.6.2 **声明免设计**：S2/S3 缺陷修复默认免设计；本次结论不改变任何业务行为与对外契约 |
| 缺陷本体 | `CertificationIntegrationTest.insertVerificationRows`（`services/subject-service/src/test/java/com/ctds/subject/CertificationIntegrationTest.java:465-477`）造"当日/昨日"核验失败行时用 **SQL `DATE_ADD(NOW(), INTERVAL ? DAY)`**（MySQL 容器时区 = UTC 的服务器时钟），而服务判定"当日"用 **应用时钟**（`CertificationService:444,557` 经注入 `Clock` 取 `LocalDate.now(clock).atStartOfDay()`、`LocalDateTime.now(clock)`）——本地 00:00~08:00 窗口内两钟日期分叉，造数被记到 UTC 昨日 → "今日 5 次失败"未命中 → 2 例 `expected 400 but was 200` |
| 修复口径（登记原口径） | **造数改为经应用时钟生成时间戳（Java 侧传参替代 SQL `NOW()`）**，与产品代码写入路径（`LocalDateTime.now(clock)`）同源同形 |
| 同类用法排查 | 全仓测试代码 `NOW()` 命中 3 处（本类 456/471、`SubjectRegistrationIntegrationTest:304`）——另两处用于直插 `subject` 行的 `created_at/updated_at`，与"当日窗口"判定无依赖；本次一并改为应用时钟传参以消除同族隐患（口径统一，零行为变化） |
| 交付物 | ① 测试代码修复（上述 2 文件）；② 验证证据（**时区偏移模拟窗口**的红绿对照 + 常规连跑 3 次全绿）；③ 台账 DB-22 关闭（含证据）；④ 会话日志 |
| 关闭条件（台账登记） | 造数改为经应用时钟传参，**本地任意时刻连跑 3 次全绿** |
| 分支 | `fix/DB-22-test-two-clocks`（沿 `fix/DB-17-21-…` 命名先例） |
| 纪律声明 | 只改测试代码；**不动产品代码**（DB-09"两把钟"为独立债务、口径为统一注入时钟，属产品侧议题，不夹带）；不新增依赖；不改门禁配置 |

---

## 执行记录

| 项 | 内容 |
| --- | --- |
| 状态 | 🔄 修复中（RED 复现 → 修复 → 验证 → 评审 → 台账关闭） |
| 冷启动锚点 | 台账 DB-22 行（证据：`gate-report-20260920-003339.md` unitTest FAIL 明细 + 23:14 全绿/00:33 复现的交叉定性）；承接日志 `-0044`"下一步②"与 `-1955`"下一步④" |
| 验证设计 | ① **窗口模拟（强证据）**：JVM 以 `-Duser.timezone=GMT+12` 运行（UTC+14 亦可）——本地时刻跨日、UTC 仍为前一日，**在任意钟点确定性复现"0~8 点窗口"的两钟分叉**；修复前应 RED（2 例 `expected 400 but was 200`，与台账记录同形），修复后应 GREEN；② **登记口径**：常规时区连跑 3 次全绿（关闭条件） |
| 环境前置 | Docker Desktop 必须在线（Testcontainers；无 Docker 时集成用例静默跳过=假绿，ADR-010 §4）；本次实测 Docker 29.8.0、容器 `sc-mysql/sc-minio/sc-redis` 在线 |
| 评审结论 | 待 4 视角评审（规格与设计符合性 / 安全供应链 / 一致性重复 / 测试质量） |