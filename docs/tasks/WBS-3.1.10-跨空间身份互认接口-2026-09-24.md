# WBS-3.1.10 任务卡：跨空间身份互认接口（2026-09-24）

| 项 | 内容 |
| --- | --- |
| 来源 | 台账「下一包」= **WBS 行 242**；编排师 2026-09-24 裁定「三项都按你推荐做法推进」（含 3.1.10 立卡），承接日志 `-2256`（DB-25 合并收口） |
| 级别与性质 | L1-3 平台功能**实施包**（1 会话预算）→ 章程 2.6.3：**两级设计同批一次确认**；本卡交付 = 立卡 + 低保真 + 高保真（**确认通过后另会话编码**） |
| 需求锚点核对 | V1.0 PRD（C-1.2 DID 定义）；WBS 行 242「互认协议实现（收口 std-adapter）」；规格 `C-1.2` **行为 5**（规则 1~4 + 验收标准 1~4）；剧本 `C-1.2` **S4** 步骤 1~5 + 附录 A 组 E1；ADR-008（收口纪律 / `did` 域 / 1003 段 / 替换规则）；ADR-017（§2.9 解析验证契约、§2.2 应用时钟、"后续包约束"：不得新建平行错误码段/状态枚举/库表） |
| 上游能力边界 | 3.1.8（签发 / 吊销 / KMS 代签，`4dc0444`）、3.1.9（解析 `GET /api/v1/did/{did}` / 验证 `POST /api/v1/did/{did}/verifications` / 留痕表 `did_verification_log`，`3db721b`）——均为**只读复用**，本包不重复实现 |
| 交付物 | ① `std-adapter` `did` 域真实实现（替换占位）+ 模拟对端三态样例（真实 SM2 签名）② `services/did` 互认三端点（来访 / 出向 / 样例清单）+ 留痕表 `did_interop_log`（V3）③ example-service 探活接线替换 ④ ADR-008 变更补记 + ADR-017 补记 ⑤ 测试锚点 T1~T12（含三处守卫与一处反向探针）⑥ 走查材料（`build-output/demo-files/did-interop/`，gitignored）⑦ 台账 / 日志 / 体量登记 |
| 关闭条件 | ① 规格行为 5 四条验收标准全部有测试承载且绿灯；② 剧本 S4 步骤 1~5 可执行（以 HTTP 承载，界面归 3.1.11）；③ 本地全三门禁（编译 / 单测 / 静态检查）与交付态门禁 GREEN；④ 4 视角评审通过、编排师验收通过 |
| 分支 | `feat/C-1.2-跨空间身份互认接口`（沿 `feat/*` 命名先例） |
| 设计文件 | `docs/designs/WBS-3.1.10-lofi.md`（**Q1~Q7 待确认**）、`docs/designs/WBS-3.1.10-hifi.md`（编码契约，**待确认**） |
| 纪律声明 | **不臆造信通院协议签名**（规范文本未入库，ADR-008 特别说明）；互认协议实现**只落 `std-adapter`**（业务服务不得自实现）；**不新增依赖/模块**；**不改门禁配置**（红线 3）；**不夹带** DB-23 / DB-24 / DB-26 与 3.1.11 界面；留痕**不保存业务数据原文**；对外文案不拼接域信息/用户输入 |

---

## 一、范围与设计（本卡设计见两级设计文件，正文不重复）

- **做什么 W1~W8 / 不做什么 N1~N7**：见 `docs/designs/WBS-3.1.10-lofi.md` §1；
- **结构组成**：lofi §2；**主要流程**：lofi §3；
- **编码契约**（接口契约表 / 库表 / 错误码 / 配置与衔接 / 边界值与异常 / 测试锚点 / 演示口径 / 变更登记 / 实施前置检查项）：见 `docs/designs/WBS-3.1.10-hifi.md` §1~§10。

**设计要点摘要（供快速表决）**：

1. **落位**：协议实现落 `std-adapter` 的 `did` 域（替换占位、`implemented=true`），HTTP 端点落 `services/did`（`/api/v1/did-interop/...`）——收口纪律 + std-adapter 为无端口纯 jar 库（ADR-008 §2）；
2. **协议签名**：只冻结"业务口径方法"（来访验证 / 出向验证 / 样例清单），并在 ADR-008 补记明示"非信通院协议签名，规范文本入库后按变更流程冻结协议签名"；
3. **模拟对端**：三态样例（有效 / 对端已吊销 / 签名被篡改）以资源 JSON 入库，签名为制作期**真实 SM2 签名**（私钥不入库、运行期不内置）；
4. **三查对端口径**：签名 / 对端状态 / 对端绑定全部经互认通道取数，**不查本空间主体库**（规格行为 5 规则 2 明示）；
5. **留痕**：新表 `did_interop_log`（方向 / 对端空间标识 / DID / 结果 / 原因 / 时间），不存原文，不改 3.1.9 既有表；
6. **错误码零新增**：复用 `1005C0004` / `1005S0002` / `1003C0001`；
7. **出向验证**：端口倒置（`LocalDidStatusPort`），不引入 HTTP 自调用。

## 二、待确认决策点（请一次确认；章程 2.6.3）

| 编号 | 问题 | 建议口径（详见 lofi §3） |
| --- | --- | --- |
| Q1 | 互认能力落位（协议实现 / HTTP 端点分别在哪） | **A**：协议实现落 `std-adapter` `did` 域、端点落 `services/did` |
| Q2 | "协议方法签名"如何冻结 | **A**：只冻结业务口径方法 + ADR-008 补记诚实声明（不臆造协议签名） |
| Q3 | 三态样例承载与"签名真实"保证 | **A**：资源 JSON 入库 + 制作期真实 SM2 签名（私钥不入库） |
| Q4 | 来访三查在互认场景如何落地 | **A**：三查全按对端口径经互认通道取数（不查本空间主体库） |
| Q5 | 互认留痕承载 | **A**：新表 `did_interop_log`（V3），不改 3.1.9 表 |
| Q6 | 互认端点访问控制 | **A**：演示期无鉴权 + ADR-017 补记诚实边界 |
| Q7 | 出向验证（双向口径）实现 | **A**：端口倒置 `LocalDidStatusPort`（进程内委托，无 HTTP 自调用） |

> 确认后：lofi §确认记录 + hifi §确认记录 两处签署 → **编码契约生效** → 编码会话按 hifi §10 前置检查项开工（测试先行 RED）。

## 三、验收标准 → 测试用例 → 剧本步骤 映射表（编码会话填写）

| 规格验收标准 | 承载测试锚点（hifi §7） | 剧本步骤 |
| --- | --- | --- |
| 行为 5 验收标准 1（有效来访 → 通过 + 留痕四要素） | 待填（T1） | S4 步骤 1 |
| 行为 5 验收标准 2（对端已吊销 / 签名篡改 → 不通过 + 原因） | 待填（T2 / T3） | S4 步骤 2~3 |
| 行为 5 验收标准 3（双向：出向结论与本空间状态一致） | 待填（T7） | S4 步骤 4~5 |
| 行为 5 验收标准 4（未开放占位答复，不返回伪结果） | 待填（T10 / T12） | 非剧本承载（占位契约由 std-adapter 测试承载） |

## 四、执行记录

| 项 | 内容 |
| --- | --- |
| 状态 | **立卡 + 两级设计草案已落盘 → 待编排师一次确认**（未确认前**禁止编码**，章程 2.6.1） |
| 冷启动锚点 | 台账「下一包」行；日志 `-2256`（DB-25 合并收口）；WBS 行 242；ADR-008；规格行为 5；剧本 S4 |
| 立卡会话 | 2026-09-24（日志 `-2344`）——本卡 + lofi + hifi 三文档落盘；未动代码、未动门禁、未立其它卡 |
| 编码会话 | 待确认后另开会话执行（hifi §10 前置检查项为第一步） |
| 评审与验收 | 待编码交付后执行（4 视角评审 → 编排师验收） |

## 五、提交前自检单（章程附录 B1，编码会话填写）

待编码会话按 B1 逐项填写（规格→设计→测试→剧本映射、复用声明、规格外实现声明应为空、命令与结果、高风险点自查、剧本更新建议、业务可读交付说明）。

## 六、体量登记（编码交付时填写）

待填（预计：`std-adapter` 少量新增 + `services/did` 三端点 + V3 迁移 + 测试锚点 T1~T12 + 三份文档；若超 400 行指引按先例如实登记构成）。
