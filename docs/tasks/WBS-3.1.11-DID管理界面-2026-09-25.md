# WBS-3.1.11 任务卡：DID 管理界面（2026-09-25）

| 项 | 内容 |
| --- | --- |
| 来源 | 台账「下一包」= **WBS 行 243**；编排师 2026-09-25 会话回复「**是。按建议推进**」（采纳立卡建议：分支 `feat/C-1.2-DID管理界面`、卡名 `WBS-3.1.11-DID管理界面-2026-09-25.md`），承接日志 `-0135`（3.1.10 合并收口） |
| 级别与性质 | L1-3 平台功能**实施包 + 界面类**（1 会话预算，WBS 行 243「1」）→ 章程 2.6.3：**两级设计同批一次确认**（≤1 天任务卡）；本卡交付 = 立卡 + 低保真（含线框原型）+ 高保真（**确认通过后另会话编码**） |
| 需求锚点核对 | V1.0 PRD 行 101（C-1.2 DID：为每个主体签发 DID、支持跨空间身份互认，P0）；WBS 行 243「DID 管理界面 / 签发记录·状态·吊销管理页 / 依赖 3.1.8」；规格 `docs/specs/C-1.2-分布式数字身份DID.md` **行为 1（规则 5 运营重试）、行为 2（状态可见）、行为 3（验证留痕）、行为 4（规则 1~4，吊销与"重签由运营管理员在 DID 管理页发起"）、§6.6（平台侧代签入口边界 = "归 3.1.9/3.1.11 设计强制项"）**；剧本 `docs/manuals/C-1.2-分布式数字身份DID-验收剧本.md` **S1 步骤 1~6（界面点名 3.1.11）、S2 步骤 1~6（入口点名 3.1.9/3.1.11）、S3 步骤 1~6（界面点名 3.1.11 + 二次确认强制项）**、附录 A 组 D1/D2/D3；契约 `docs/adr/ADR-017-DID服务契约.md`（§2.4 管理面端点、§2.9 解析/验证、**§3 后续包约束：3.1.11 不得新建平行错误码段/状态枚举/库表**）、`ADR-005`（REST 与九位错误码、分页 §3.2、鉴权 401/403 文案）、`ADR-016 §2.7`（演示期安全边界）、`ADR-015`（KMS 托管：界面只见密钥引用） |
| 上游能力边界 | 3.1.8（签发/重试/重签/吊销 + `did_identity`、`did_operation_log`，`4dc0444`）、3.1.9（解析 `GET /api/v1/did/{did}` + 验证 `POST /api/v1/did/{did}/verifications` + `did_verification_log`，`3db721b`）、3.1.10（互认三端点 + 契约留痕，`2c714f2`）——**全部只读复用**，本包不重复实现，不改既有端点语义 |
| 交付物 | ① 后端读端点 3 个 + 演示签名端点 1 个（`services/did`，`did.admin`）：记录列表（分页/筛选）/ 单 DID 操作留痕 / 验证留痕列表 / 演示代签 ② 前端 DID 管理界面（记录列表与筛选、详情与状态、吊销（理由必填 + 二次确认）、重签、重试；演示与验证区：代签、验证、解析、验证留痕）③ 契约留痕：ADR-017 变更补记（管理面读数端点 + 演示签名边界 + 记录状态展示口径）④ 测试锚点 T1~T17（后端 T1~T9 / 前端 T10~T17）⑤ 界面说明书（hifi §6）+ 线框原型 HTML ⑥ 剧本 S1~S3 界面核对修订（交付后一次完成，提示 PO 审批）⑦ 台账 / 日志 / 体量登记 |
| 关闭条件 | ① 规格行为 1（规则 5）/2/3/4 的**界面承载**验收标准全部有测试承载且绿灯（含"吊销理由必填被拒"与"二次确认取消不留痕"双向用例）；② 剧本 **S1 / S2 / S3 可在界面上逐步执行通过**（走查）；③ 本地三门禁（编译 / 单测 / 静态检查）+ 交付态门禁 GREEN；④ 4 视角评审通过、编排师验收通过 |
| 分支 | `feat/C-1.2-DID管理界面`（已建，基线 = `main` = `7534f11`） |
| 设计文件 | `docs/designs/WBS-3.1.11-lofi.md`（**Q1~Q8 待确认**）、`docs/designs/WBS-3.1.11-hifi.md`（编码契约，**待确认**）、`docs/designs/WBS-3.1.11-原型-DID管理界面.html`（线框原型，含吊销二次确认画面） |
| 纪律声明 | 界面**只做已交付契约的界面化**，不为界面新增业务规则；**零新增错误码、零新增状态枚举、零新增库表**（ADR-017 §3 后续包约束）；**不绕过后端校验**（理由必填、非有效不可吊销一律以后端为准，前端只做前置体验）；**演示签名入口默认关闭**（生产禁用 = 代码门槛，非注释）；界面**不展示任何明文私钥**（只见 KMS 引用与公开要素，红线 7 / 规格行为 1 规则 3）；**不改门禁配置**（红线 3）；**不夹带** DB-23/DB-24/DB-26、3.1.12 与 3.1.10 §七 7.3 建议项 |

---

## 一、范围与设计（本卡设计见两级设计文件，正文不重复）

- **做什么 W1~W9 / 不做什么 N1~N7**：见 `docs/designs/WBS-3.1.11-lofi.md` §1；
- **结构组成**：lofi §2；**主要流程**：lofi §3；**待确认问题**：lofi §3 末（Q1~Q8）；
- **编码契约**（接口契约表 / 库表与复用 / 错误码 / 配置与装配 / 前端设计 = 界面说明书 / 边界值与异常 / 测试锚点 / 演示口径 / 变更登记 / 实施前置检查项）：见 `docs/designs/WBS-3.1.11-hifi.md` §1~§10。

**设计要点摘要（供快速表决）**：

1. **落位**：读数端点与演示签名端点落 `services/did`（DID 能力归属服务，单一事实源）；界面落 `frontend`（Vue 3 + Element Plus，1 个菜单项「DID 管理」+ 2 个视图）；
2. **后端只加"读"与"演示"两类端点**：写操作（吊销/重签/重试）**全部复用 3.1.8 既有端点**，本包不新增写端点、不改其语义；
3. **记录状态展示口径**：列表 = 签发记录视角，显示既有 `DidStatus` 三值（`ACTIVE` 有效 / `REVOKED` 已吊销 / `PENDING_ISSUE` **记录中间态**），解析端口径仍锁两值（规格行为 2 规则 1）——**不新增状态枚举**；
4. **吊销交互（界面强制项）**：**理由必填**（前置校验 + 后端 `1005C0002` 兜底）→ **二次确认弹窗明示"吊销不可逆"** → 取消 = 状态不变且不产生留痕（不发起请求）→ 确认 = 调既有吊销端点 → 留痕五要素在"操作留痕"区可见；
5. **重签入口（规格行为 4 规则 4 点名界面）**：列表行操作，成功后新 DID 行出现、旧行保留（旧标识永不复用）；另提供"待签发"记录的**运营重试**（行为 1 规则 5）；
6. **演示与验证区**：仅演示/调试期——**代签**（规格 §6.6 平台侧代签边界，默认关闭的代码门槛）→ **验证三查结果**、**解析结果（含"未登记"业务答复非报错样式）**、**验证留痕列表**（含失败原因，**不含任何数据原文**）；
7. **零新增依赖面**：分页复用 `common/pagination`（`PageQuery`/`PageResult`，ADR-005 §3.2；`did-service` 新增平台内部模块依赖）；签名复用 `DidKmsClient` 扩展 `sign` → KMS 既有签名面（ADR-017 §2.6）；前端复用 `apiJson` 封装与既有 `ElMessageBox` 二次确认范式。

## 二、待确认决策点（请一次确认；章程 2.6.3）

| 编号 | 问题 | 建议口径（详见 lofi §3 末） |
| --- | --- | --- |
| Q1 | 界面范围：是否含"演示与验证"区（代签 / 验证 / 解析 / 验证留痕） | **A**：含——剧本 S1/S2/S3 均点名 3.1.11 界面入口，规格 §6.6 已把"平台侧代签入口"列为 3.1.11 设计强制项；不含则 S2 无法界面走查 |
| Q2 | 记录列表数据源与端点 | **A**：新增只读端点 `GET /api/v1/did/records`（`did.admin`，分页 + `subjectNo`/`status` 过滤，复用 `common/pagination`） |
| Q3 | 记录状态展示口径 | **A**：列表显示既有三值（含 `PENDING_ISSUE` 标注为"待签发（记录中间态）"），解析端口径仍为两值；**不新增枚举** |
| Q4 | 吊销交互形态 | **A**：弹窗填理由（必填前置）+ `ElMessageBox` 二次确认（明示"吊销不可逆"）；取消不发起请求；后端 1005C0002 / 1005C0003 兜底并如实展示 |
| Q5 | 重签 / 重试入口 | **A**：两者都做（重签 = 规格行为 4 规则 4 点名界面；重试 = 行为 1 规则 5），复用既有端点，均带操作后刷新 |
| Q6 | 演示签名入口的生产禁用形态 | **A**：配置开关 `ctds.did.demo-signature.enabled`（**默认 false**）→ 关闭时返回既有 `1000C0003` + 明确文案，**不新增错误码** |
| Q7 | 前端 `did.admin` 角色头口径 | **A**：仅在 `api/did.ts` 内做**页面级角色头**（`applicant,reviewer,admin`），**不改** 全局 `demoRolesHeader()`（避免在 example-service 意外激活 `greeting.delete`） |
| Q8 | 列表"关联主体"展示口径 | **A**：只展示主体申请编号（`subjectNo`）+ 签发序号，**不跨服务取主体名称**（最小暴露；剧本界面核对修订时按实际列名对齐） |

> **确认留痕（2026-09-25，编排师会话回复"都按照建议口径推进"，代录）**：
> **Q1~Q8 均采建议口径**——Q1=A **界面含"演示与验证"区**（代签/验证/解析/验证留痕）；Q2=A 新增 `GET /api/v1/did/records`（`did.admin`，分页复用 `common/pagination`）；Q3=A 记录状态复用 `DidStatus` 三值（`PENDING_ISSUE` 标注"待签发（记录中间态）"，解析端口径仍两值，**不新增枚举**）；Q4=A 吊销两段式（理由必填前置 + 二次确认明示"吊销不可逆"，取消不发起请求）；Q5=A 重签与重试两个入口都做（复用既有端点）；Q6=A 演示签名入口配置开关**默认关闭**（关闭时复用既有 `1000C0003`，**零新增错误码**）；Q7=A `did.admin` 走 **`api/did.ts` 页面级角色头**（不动全局 `demoRolesHeader()`）；Q8=A 列表只展示主体申请编号 + 签发序号（**不跨服务取名**）。
> **体量裁决（D1）：不拆分**——采纳 lofi §5 选项 A（单卡单目标一次交付管理面 + 演示面；拆分将导致剧本 S2 与 S3 步骤 1/5 的界面走查跨卡停滞），本次验收按整体受理，§六"超 400 行指引说明"即最终结论。
> **确认后状态**：**编码契约生效**（lofi/hifi 两处「确认记录」已同步签署）→ **编码归新会话**，第一步 = hifi §10 前置检查项七项逐项实测留痕（测试先行 RED）。

## 三、验收标准 → 测试用例 → 剧本步骤 映射表（编码会话回填）

**规格验收标准 → 测试 → 剧本（界面承载部分）**

| 规格验收标准 | 承载测试锚点（hifi §7） | 剧本步骤 |
| --- | --- | --- |
| 行为 1 验收标准 1（签发记录含留痕四要素） | **T1** 记录列表分页与过滤；**T3** 待签发记录展示；**T4** 单 DID 操作留痕（签发四要素） | S1 步骤 3 |
| 行为 1 验收标准 4（未入驻主体不存在有效 DID） | **T1**（过滤空结果）+ **T10** 界面空态文案 | S1 步骤 1、6 |
| 行为 1 验收标准 5（失败停留"待签发"可重试） | **T3** + **T13** 重试入口（列表行 → 复用既有端点） | 非剧本主链（剧本 S1 以正常路径承载） |
| 行为 1 规则 3（只见 KMS 密钥引用，无明文私钥） | **T1/T4** 出参字段断言（无 `privateKey` 类字段）；**T17** 前端源集扫描守卫 | S1 步骤 4 |
| 行为 2 验收标准 1~3（解析可见状态 / 已吊销 / 未登记答复） | **T15** 解析区显示状态与公开要素、"未登记"以业务答复样式展示 | S2 步骤 5~6、S3 步骤 4 |
| 行为 3 验收标准 1~2（验证通过 / 签名核验失败原因） | **T14** 代签 → 验证链路；失败原因如实展示 | S2 步骤 1~3、S3 步骤 5 |
| 行为 3 验收标准 4（留痕三要素 + 无原文） | **T6** 验证留痕列表（分页 + 列级无原文断言） | S2 步骤 4 |
| 行为 4 验收标准 2（理由留空被拒） | **T11** 前端必填拦截 + **T12** 后端 1005C0002 如实展示 | S3 步骤 2 |
| 行为 4 验收标准 3（取消不改状态、不产生留痕） | **T11** 取消分支断言"零请求 + 零留痕" | S3 步骤 3 |
| 行为 4 验收标准 1（确认后状态变更 + 留痕五要素） | **T4** 操作留痕五要素（操作人/时间/理由/DID/状态变更）+ **T11** 确认分支 | S3 步骤 3 |
| 行为 4 验收标准 5（无"恢复"操作；旧记录保留） | **T13** 重签后旧行保留 + 界面无恢复入口（**T17** 源集扫描守卫） | S3 步骤 6 |
| 规格 §6.6（演示签名入口仅演示/调试期） | **T7** 开启态真实验签通过；**T8** 默认关闭 → 1000C0003 | S2 步骤 1、S3 步骤 1 |

**测试落点（编码会话回填，全部绿灯）**

| 锚点（hifi §8） | 测试落点（文件 · 用例数） |
| --- | --- |
| T1~T6、T8（集成侧 E19 默认关闭 + 零 KMS 调用）、权限 401/403 | `services/did/src/test/java/com/ctds/did/DidManagementIntegrationTest.java` · 11 |
| T7（真实验签）、E20/E21/E22（集成侧） | `services/did/src/test/java/com/ctds/did/DidDemoSignatureIntegrationTest.java` · 3 |
| T8（单元：默认关闭 + 零 KMS 调用）、T9（代签边界四类） | `services/did/src/test/java/com/ctds/did/application/DidDemoSignatureServiceTest.java` · 7 |
| T17③（后端源集：无密钥材料读取 + 默认关闭不可硬编码） | `services/did/src/test/java/com/ctds/did/architecture/DemoSignatureGuardTest.java` · 2 |
| T10~T13（清单/空态/筛选分页/吊销两段式/行内操作派生/详情留痕） | `frontend/src/views/did/IndexView.spec.ts` · 14 |
| T14/T15（代签→验证链路、系统态与业务态分离、未登记业务答复） | `frontend/src/views/did/DemoView.spec.ts` · 8 |
| T17①②（前端源集：无直接 fetch、无回滚入口与私钥字样） | `frontend/src/views/did/DidSourceGuard.spec.ts` · 4 |
| T16（菜单 7→8、路由结构、`/did` 守卫拦截） | `frontend/src/layouts/MainLayout.spec.ts` / `frontend/src/router/router.spec.ts`（既有断言同步 + 新增 2 用例） |

## 四、执行记录

| 项 | 内容 |
| --- | --- |
| 状态 | ✅ **编码就绪（2026-09-25，设计一次确认通过）**：立卡会话落盘本卡 + lofi（含线框原型）+ hifi；**设计确认会话**确认 Q1~Q8 均采建议口径 + 体量 D1 不拆分 → **编码契约生效**；分支 `feat/C-1.2-DID管理界面`（自 `7534f11`）→ **编码归新会话** |
| 冷启动锚点 | 台账「下一包」行；日志 `-0135`（3.1.10 合并收口）；WBS 行 243；规格 C-1.2 行为 1/2/3/4 + §6.6；剧本 S1/S2/S3 + 附录 A；ADR-017 §2.4/§2.9/§3 |
| 立卡会话 | 2026-09-25（日志 `-0946`）——本卡 + lofi + hifi + 原型 HTML 四文档落盘；**未动代码、未动门禁、未立其它卡** |
| 设计确认会话 | **2026-09-25（日志 `-0946` 追加节）**——编排师会话回复"**都按照建议口径推进**"→ Q1~Q8 均采建议口径 + **D1 体量不拆分**；三处留痕（本卡 §二/§四、lofi/hifi 确认记录）；**零代码变更** |
| 编码会话 | **2026-09-25（本日志 `-1057`）**——hifi §10 七项前置检查逐项实测留痕 → 测试先行 RED（后端 11 用例 10 红）→ 实现 → GREEN；前端 RED → 实现 → GREEN；本地三门禁 + 全量门禁 GREEN（`gate-report-20260925-104829.md`） |
| 设计偏离登记 | **7 条（P1~P7，全部留痕于 `docs/designs/WBS-3.1.11-hifi.md` §11）**：P1 关闭态 HTTP 400（非设计稿 404，平台统一映射）；P2 S 类错误对外文案被平台遮蔽；P3 T17③ 断言口径澄清；P4 E8 待签发无 DID 的界面口径；P5 吊销提示措辞避让 T17② 守卫字样；P6 vite 开发期转发补充；P7 ADR 补记落在 ADR-017 §9。**均不改变业务判定与契约语义**，无一条属"私自补需求" |
| 规格外实现声明 | **无**（未实现任何规格/hifi 之外的功能；DB-23/DB-24/DB-26 与 3.1.10 §七建议项未夹带；未改门禁配置） |
| 评审与验收 | **4 视角评审循环 1/3（2026-09-25 11:3x）**：②安全与供应链 / ③一致性与重复 / ④测试质量 = **PASS**；①规格与设计符合性 = **FAIL**（**R1** 演示入口未带 `?did=`、**R2** 页面散写状态文案 + **R3** 体量登记更正）→ **循环 2/3：R1/R2 已修复 + 视角①复评 PASS（同会话自查，见 §七 7.7）** → 待**编排师走查**（剧本 S1/S2/S3 界面，需环境就绪）。 |
| 修复会话 | **2026-09-25 11:5x~12:0x（本卡 §七 7.7）**——R1/R2 测试先行修复（RED 4 失败 → GREEN 30 通过）+ R3 登记更正 + 守卫口径增强；前端全量 **14 文件 / 113 用例全绿**、lint 0 错误；**全仓门禁 GREEN**（`gate-report-20260925-120159.md`）；hifi §11 **P8** + §6.3/§6.4 补注；**零后端改动、零依赖变更、零门禁配置改动、零迁移** |
| 编排师裁决登记（2026-09-25） | ① **演示根密钥丢失 → 采选项 A**（用"重签"为既有主体生成新一代 DID 作代签演示目标；台账"环境风险"行同步）：执行时机 = 走查准备会话（需环境：重打构件 + 起三容器/三服务），**本包代码与测试不依赖该数据**；② **剧本 S1 步骤 1 / S2 步骤 1、5 界面名称核对修订 → 获批生效**（编排师代 PO，剧本顶部留痕已由"待 PO 审批"更新为"获批生效"） |

## 五、提交前自检单（章程附录 B1，编码会话填写）

| B1 项 | 内容 |
| --- | --- |
| ① 两级设计门禁 | ✅ lofi/hifi「确认记录」已于 2026-09-25 签署生效（Q1~Q8 均采建议口径 + D1 不拆分）；实现与 hifi §1~§10 逐条一致（差异 7 条见 §四 设计偏离登记，均不改变契约语义） |
| ② 映射表 | ✅ §三（规格验收标准 → 测试锚点 → 剧本步骤）已回填，并附"测试落点"表（文件级） |
| ③ 复用声明（含检索过程） | ✅ 检索过程：先查 `common/`（`pagination`/`auth`/`errorcode`/`crypto`）→ 命中 `common-pagination`（分页）与 `common-auth`（`did.admin` 权限注解）→ 复用；KMS 侧复用既有内部签名面与 `DidKmsClient`（新增 `sign` 一个方法）；错误码全部复用既有 1005 段与 1000 通用段。落库复用既有三表，**零新增库表/迁移**。前端复用 `apiJson` 与 `ElMessageBox` 二次确认范式（先例 `views/review/DetailView.vue`）。**未引入任何新第三方依赖**；`services/did` 仅新增平台内部模块依赖 `common-pagination`。 |
| ④ 本地检查命令与结果 | ✅ 后端：`mvn -B -ntp -pl services/did -am clean test checkstyle:check` → **BUILD SUCCESS**（exit 0）；did 模块 **108 用例 / 0 失败 / 0 错误**（16 测试类）；checkstyle **0 违规**（8 模块）。前端：`npm test` → **14 文件 / 109 用例全绿**；`npm run lint` → **0 错误**。全量门禁：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\gates\run-gates.ps1` → **GREEN**（`PASS=10 FAIL=0 SKIP=1 ERROR=0 PENDING=9`，报告 `scripts/gates/reports/gate-report-20260925-104829.md`）。附注：`npx vue-tsc -b` 仍报 4 处 **既有**（非本包文件 `views/subject/CertificationView.spec.ts`）类型错误——本包新增/修改文件的类型检查已清零；该既有问题登记为改进建议，不在本包修（最小实现，不夹带）。 |
| ⑤ 高风险点自查 | ✅ ①**加解密/私钥**：全程走 KMS 内部签名面，私钥不出 KMS；界面与接口出参无私钥/材料字段（`DidManagementIntegrationTest` 逐字段断言 + `DemoSignatureGuardTest` 源集扫描）；演示入口**默认关闭**且关闭态零 KMS 调用、不区分目标存在性（不构成状态枚举通道）。②**并发/事务**：本包只读查询 + 单次 KMS 调用，未新增写事务，不动既有乐观门槛语义；吊销/重签/重试并发语义由既有端点承担、本包未改。③**分页**：复用 `PageQuery`（越界 `1000C0001`），查询 SQL 一律参数化（过滤值走占位符，列名/操作符为服务端常量）。④**权限**：三读数端点 + 代签端点一律 `did.admin`，401/403 双向用例。 |
| ⑥ 剧本更新建议 | ✅ 已执行并留痕：S1 步骤 1、S2 步骤 1、S2 步骤 5 界面入口名称/路径按实际交付核对修订（菜单"DID 管理"、页面"DID 演示与验证"、"演示代签"区、空态与提示条样式），**业务判定标准一字未改**；已在剧本顶部登记"待 PO 审批"。 |
| ⑦ 业务可读交付说明 | ✅ 见本会话开发日志「做了什么（业务可读）」节（完成了什么 / 如何演示 / 注意事项）。 |

## 六、体量登记（编码交付时填写）

| 项 | 数值 |
| --- | --- |
| 预估（立卡阶段） | 后端 ≈ 4 端点 + 2 仓储方法组 + 4 视图 DTO + 测试 ≈ **700~850 行**；前端 ≈ 2 视图 + 1 API 模块 + 1 常量模块 + 路由 + 测试 ≈ **800~1050 行**；合计 **约 1500~1900 行有效变更** |
| **实测（编码会话回填）** | **31 文件 / +3073 −16 行**（含文档；**评审会话复核更正见下一行**）。构成：后端主源 ≈ **570 行**（2 应用服务 175 + 2 控制器 113 + 4 视图 DTO 78 + 仓储/客户端/领域/配置 ≈ 204）；后端测试 ≈ **850 行**（新增 4 类 793 + 既有测试替身同步 58）；前端主源 ≈ **1110 行**（2 视图 798 + API 模块 212 + 常量 71 + 路由/代理/既有断言同步 ≈ 30）；前端测试 ≈ **565 行**（3 新 spec 共 565） |
| 超预估说明 | 实测为立卡预估上限的 **约 1.6 倍**——主因 = ①测试体量（本包测试代码占总量 ≈ 46%，含双向用例与反向探针）②前端两页交互细节（吊销两段式、四区演示页）比线框原型展开更细。**范围零扩大**：全部落在已确认的 hifi §1~§10 契约内，无规格外实现（§四 已声明） |
| 超 400 行指引说明 | **裁决（D1，2026-09-25）：不拆分**——采纳 lofi §5 选项 A：本包为**单卡单目标**（"DID 业务的运营与演示面经界面可完成"，WBS 行 243 一行）；拆分 B（管理面 + 演示面两卡）将使剧本 S2 六步与 S3 步骤 1/5 的界面走查跨卡停滞，并在同一 API 模块/页面上产生两卡返工窗口。**本次验收按整体受理**，本行即最终结论 |
| 拆分母卡（如裁决拆分） | 管理面（记录/状态/吊销/重签/重试）与演示面（代签/验证/解析/留痕）可分两卡；后者建议新立 WBS 行（需编排师批） |
| **实测复核更正（2026-09-25 评审会话，`git show --numstat 7ede686` 实测）** | 编码提交 `7ede686` = **36 文件 / +3143 −34**（其中非 docs **35 文件 / +3131 −34**；docs 1 文件 = 台账 +12）。原登记"31 文件 / +3073 −16"与日志交接"35 文件"因统计时点/口径未随提交末次刷新而失真，**以本行为准**（R3，见 §七） |
| **修复会话体量（循环 2/3，2026-09-25 12:0x，`git diff --stat` 实测）** | **8 文件 / +152 −21**（代码 3 文件 +38/−15：`constants/did.ts` +15、`IndexView.vue` +21/−14、`DemoView.vue` +2/−1；测试 2 文件 +71/−1；文档 3 文件 +43/−5）。**测试代码占本轮约 2/3**；**无规格外实现**（全部落于已确认 hifi 契约与 §七 7.7 修复范围内） |
| 文档 | 任务卡（本卡）/ lofi / hifi / 原型 HTML / ADR-017 补记 / 剧本核对修订 / 台账 / 会话日志 |

---

## 七、4 视角评审记录（循环 1/3 · 2026-09-25 11:3x · 评审会话）

**评审输入包**：分支 `feat/C-1.2-DID管理界面`（HEAD `ff6b07c`，编码提交 `7ede686`）+ 变更面（`git diff 7534f11..HEAD --stat` = 40 文件，其中编码提交 36 文件）+ 规格 `docs/specs/C-1.2-分布式数字身份DID.md`（行为 1~4 + §6.6）+ 已确认设计 `docs/designs/WBS-3.1.11-{lofi,hifi}.md`（hifi §1~§11）+ `docs/adr/ADR-017-DID服务契约.md` §9 + 剧本 + 门禁报告 `scripts/gates/reports/gate-report-20260925-104829.md`（交付态）/ `-110605.md`（复跑）。

**评审方式**：按 `docs/manuals/ai-review/cards/01~04` 逐条清单核对；证据采集由 3 个只读子智能体（后端实现 / 测试质量 / 前端实现与一致性）分别采回**带行号的原文证据**，主评审人对**敏感面、体量、字段清单、路径映射**做 git 与源码实测复验（4 组命令级复核：`git diff --stat` / `--name-only`（敏感面）/ `show --numstat`（体量）/ 源码抽样）。

### 7.1 结论总览

| 视角（卡片） | verdict | 必修 | 建议/跟踪 |
| --- | --- | --- | --- |
| ① 规格与设计符合性（`cards/01`） | **FAIL** | **R1、R2（P1）+ R3（登记更正）** | — |
| ② 安全与供应链（`cards/02`） | **PASS** | 无 | S1 |
| ③ 一致性与重复（`cards/03`） | **PASS** | 无 | S2、S3 |
| ④ 测试质量（`cards/04`） | **PASS** | 无 | S4~S9 |

**总判定**：**循环 1/3 未通过**（视角① FAIL）→ 打回修复 **R1~R3** 后**重审视角①**（循环 2/3）；②③④ 的 PASS 结论保持。修复若触及前端源集与 spec，修复会话须按视角①（设计一致性）、视角③（文案收口）、视角④（断言有效性）三项复核受影响文件。

### 7.2 必修项（R1~R3）

| 编号 | 级别 | 问题（业务可读） | 证据 | 修复要求（可直接开工） |
| --- | --- | --- | --- | --- |
| **R1** | P1 | **从列表进入演示页时没有自动带上那张 DID**：设计写明"DID 输入默认从列表带入（`?did=`）"，实现只跳页不带参数，导致运营进入演示页后必须手工粘贴 DID（剧本 S2 步骤 1 仍可执行，但体验与设计不符，且已写进界面的读取能力被闲置） | 设计：`docs/designs/WBS-3.1.11-hifi.md` §6.3 区块 A；实现：`frontend/src/views/did/IndexView.vue:97-99`（`router.push('/did/demo')` 无 query）vs `frontend/src/views/did/DemoView.vue:33`（已支持 `route.query.did`）；测试：`frontend/src/views/did/IndexView.spec.ts:307-314` 仅断言路由名 | `goDemo()` 改为带参跳转（列表行按钮带该行 `did`；顶部入口可带当前筛选命中首行或留空）；`IndexView.spec.ts` 增断言"跳转携带 `query.did` = 该行 DID"。**若判定"应手输"更合适，须改 hifi §6.3 并重新确认**——不得静默偏离 |
| **R2** | P1 | **状态文案没有完全收口、且同一状态出现两种说法**：设计强制"状态/操作/结果/原因文案集中在一处常量文件、页面内禁止散写"，实际筛选下拉与解析结果区仍直接写死中文（且筛选项写"待签发"、列表标签写"待签发（记录中间态）"，同一状态两处不一致） | 设计：hifi §6.4；实现：`frontend/src/views/did/IndexView.vue:37-39`、`:370`、`frontend/src/views/did/DemoView.vue:239` vs `frontend/src/constants/did.ts:7-11` | 三处改为引用 `RECORD_STATUS_LABELS`；若筛选下拉确需短文案，则**在常量文件新增短文案常量**并注明用途（不得就地散写）；同步 spec 断言；可选：`DidSourceGuard.spec.ts` 增"页面源集不出现裸状态中文常量"扫描断言 |
| **R3** | 登记更正（P2 级，随 R1/R2 一并处理） | **体量登记数字与实测不符**：实测编码提交 = **36 文件 / +3143 −34**，而任务卡原登记"31 文件 / +3073 −16"、日志交接"35 文件"，三者互不一致 | `git show --numstat 7ede686`（评审会话实测）；本卡 §六；`docs/logs/Ctds-项目开发日志-26-09-25-1057.md` | §六 **已在本会话就地更正**（新增"实测复核更正"行）；修复会话提交时在日志复述本结论即可（**无需改代码**） |

### 7.3 视角①其余核对结论（逐条对账 + 已登记偏离裁决）

- **未实现项：无**。hifi §1 行为 B1~B15 逐条有实现落点与测试锚点承载：B1/B2→T1/T10；B3→T3；B4→T4/T17①；B5/B6/B7→T11/T12；B8→T14/T15；B9→T13/T17②；B10→T13；B11→T15；B12→T7/T8；B13→T14；B14→T6；B15→T2；
- **超范围项：无**。未发现规格/hifi 之外的端点、字段、枚举、库表、依赖；`git diff 7534f11..HEAD --name-only` 实测：`scripts/gates`、`frontend/package.json`、`frontend/package-lock.json`、`docs/dependencies.md`、`services/did/src/main/java/com/ctds/did/domain/DidErrorCodes.java`、`services/did/src/main/resources/db` **全部零改动**；
- **已登记偏离 P1~P7 裁决：全部可接受**（判定为"如实登记 + 平台口径/数据模型优先"，无静默偏离）：
  - P1（`1000C0003` 实为 HTTP 400 而设计稿写 404）：平台 `ErrorType` 统一映射（C/B→400）、主体服务既有同码同为 400 → **平台一致性优先**，错误码与文案未变 ✔；
  - P2（S 类错误对外文案被 `GlobalExceptionHandler` 统一遮蔽）：不暴露内部实现（章程 4.3），服务端业务文案仍由单测断言 ✔；
  - P3（T17③ 由"源集不得出现 `demo-signature`"改判"不得读密钥材料 + 开关不得硬编码开启"）：**原字面断言不可达**（该字样是配置键与端点路径的必要组成），替代断言更强且可证伪 ✔（**建议**：hifi §11 P3 行末补一句"T17③ 原文已被本行替代"，使替代关系可追溯）；
  - P4（E8 待签发记录 `did` 为空 → 数据模型上不可达，界面按空态且不发请求）✔；P5（"不可恢复"→"不可再次启用"，"吊销不可逆"明示保留，避让 T17② 源集守卫）✔；P6（`vite.config.ts` 开发期转发，不改服务契约与门禁配置）✔；
  - P7（ADR 补记落 §9）：实测 `docs/adr/ADR-017-DID服务契约.md` 第 144 行确为 `## 9. 变更补记（WBS-3.1.11 DID 管理界面，2026-09-25）` ✔；
- **"剧本是否需要更新"已回答**：✔（S1 步骤 1 / S2 步骤 1、5 已按实际交付核对修订，顶部留痕在 `docs/manuals/C-1.2-分布式数字身份DID-验收剧本.md:14`，业务判定未变，"待 PO 审批"已登记）；
- **映射表完整性**：✔（§三"规格验收标准 → 测试锚点 → 剧本步骤"12 行 + "测试落点"8 行，与 hifi §7/§8 编号逐一对应）。

### 7.4 视角②③④已核关键证据（PASS 依据）

| 视角 | 已核实项（证据） |
| --- | --- |
| ② 安全与供应链 | ① 硬编码凭据/密钥/生产数据：对本包变更文件敏感串复扫 **0 命中**（`password=` / `AKIA…` / `BEGIN … PRIVATE KEY` / `secret=` / 内网地址模式）；② 密码学合规：签名仅经 KMS 既有内部面 `POST /api/v1/key-pairs/{keyRef}/signatures`（`infrastructure/DidKmsHttpClient`），**零自行实现**（`Sm2Service.verify` 仅出现在测试侧验签）；③ 依赖：仅新增平台内部模块 `common-pagination`（`services/did/pom.xml` +5 行），前端依赖清单**零变更**（git 实测），无幻觉包名；④ 输入处理：SQL 全参数化（`DidJdbcRepository` 新增方法值走 `?` 占位、列名/操作符为服务端常量），路径变量与过滤值经 `1005C0001` 校验，**无注入/越权**；⑤ 演示入口门槛：`@Value("${ctds.did.demo-signature.enabled:false}")` + `application.yml` 占位默认 `false` + 守卫双向断言（`DemoSignatureGuardTest:40-45`）；关闭态**零 KMS 调用**（`verifyNoInteractions`）且**不区分目标是否存在**（不构成状态枚举通道）；⑥ 界面零私钥：4 个视图字段逐字段白名单断言，仓储不读 `kms_key` 任何材料列 |
| ③ 一致性与重复 | ① 分页复用 `common-pagination`（`PageQuery.of` 承担越界 `1000C0001`），**未重复实现**；② 前端复用 `apiJson` + `ElMessageBox.confirm`（先例 `src/views/review/DetailView.vue`）+ `el-table`/`el-pagination`，**无 `fetch(` 直连**（守卫断言）；③ 分层：Controller 只注入应用服务、不直连数据层；`domain` 包 import 仅 `java..`/`common.errorcode..`/`domain..`（`LayerRulesTest` 约束内）；分页以 `offset/limit` 表达（不跨层）；④ 路径无冲突：`/records`、`/verification-logs` 字面量段优先于既有 `GET /{did}` 模板（集成用例 `recordsPathDoesNotShadowResolutionEndpoint` 锚定）；⑤ 错误码**零新增**（全部复用 `DidErrorCodes` 既有常量与 `1000C0001/2/3/5`），`DidErrorCodes.java` 不在变更面（git 实测） |
| ④ 测试质量 | ① **无空洞断言**（`assertTrue(true)`/`expect(true)` 全仓 0 命中；凡 `status().isOk()` 均附字段/`jsonPath` 断言；无"只调用不校验"）；② **双向用例齐备**：理由留空 → 前端零请求（`IndexView.spec.ts:174-175`）+ 后端 `1005C0002` 兜底（既有 `DidIssuanceIntegrationTest:225-229`）；二次确认取消 → 零请求（`:189`）；关闭态零 KMS（`verifyNoInteractions(kmsClient)` + 桩计数 `assertThat(kmsClient.signCalls).isZero()`）；源集守卫**正反对照**（`DidSourceGuard.spec.ts:38/52`、`DemoSignatureGuardTest:35-36`）；③ 字段级白名单：4 个视图 `containsExactlyInAnyOrder(...)` 逐字段断言（含"无私钥字段/无原文"列级口径）；④ 异常/边界：`1000C0001`/`1005C0001`/`1005B0003`/`1005B0001`/`1005S0002`/`1000C0003` + 401/403，前端 `FAIL`↔`UNAVAILABLE` **样式分离**与"未登记"业务答复样式均有断言；⑤ 测试卫生：无调试输出、无 `TODO`/`@Disabled`、无真实个人信息（示例公司名为既有演示样例） |

### 7.5 建议项（非阻塞，登记跟踪；不属本轮必修）

| 编号 | 级别 | 内容 | 建议动作 |
| --- | --- | --- | --- |
| S1 | P2 | `DidSourceGuard.spec.ts` 只扫英文 `privateKey`，**中文"私钥"不扫**；而 hifi §6.2 **要求**界面出现"不展示任何私钥"提示 → 属设计使然、**非缺陷** | 在守卫注释写明该边界，防后续误判为漏洞 |
| S2 | P2 | `PageData<T>` 在 `frontend/src/api/did.ts:28-34` 与 `api/subject.ts:18-24` **逐字重复声明** | 前端类型收口到共享模块（单独小卡或随下次前端改动） |
| S3 | P3 | `IndexView.vue:239-248` `handleOperationError` 为本页私有错误分发（含状态类错误码触发刷新），与其它页内联 `catch` 范式不同 | 属局部增强：建议在 hifi §11 或交付说明补一句；认可其价值时于下次前端改动沉淀为公共范式 |
| S4 | P2 | "取消 = 零留痕"仅由"零请求"间接证明（逻辑充分，无独立负向断言） | 补注释说明，或在用例中断言留痕列表未新增 |
| S5 | P2 | `DidSourceGuard.spec.ts:41-46`（恢复/`restore`/`unrevoke`）**无专用反向探针**，依赖共享 `hits()` 已被别处反证 | 为该模式补一条反向探针，使四条扫描各自可证伪 |
| S6 | P2 | 出参字段白名单仅校验 `list.get(0)`（`DidManagementIntegrationTest:89/275`） | 改为对所有元素断言 |
| S7 | P2 | 权限用例 GET 分支仅断 HTTP 401，未断业务码 `1000C0002`（POST 分支已断） | 补齐业务码断言 |
| S8 | P2 | `DidManagementIntegrationTest:270` 硬编码 `total=5`（依赖类内唯一写入的隐性约定）；`:125-131` 断言全量 `total` == 库表 COUNT（全局计数口径） | 改与库表 COUNT 比对或按 `did` 隔离断言；至少加前提注释 |
| S9 | P2 | 前端未覆盖分页越界；`DemoView` 验证留痕空态文案未断言；本包**未覆盖并发/幂等**（本包为只读查询 + 单次 KMS 调用，风险低） | 随 3.1.12（测试补全与集成）吸收；当前如实登记 |

### 7.6 评审方法与限制说明

- 视角②③④证据由只读子智能体按卡片清单采回（含行号原文），**涉及"本次是否改动"的关键判定（依赖清单 / 错误码定义 / 门禁配置 / 迁移 / 体量）由主评审人用 git 命令实测复核**（`diff --stat` / `--name-only` / `show --numstat`），子智能体无 shell 的限制不构成证据缺口；
- **未复跑门禁**：交付态 GREEN（`gate-report-20260925-104829.md`）与文档入库后复跑 GREEN（`-110605.md`）均为原始证据，本会话未改代码/配置/依赖 → 按交接提醒"门禁无需复跑"执行；本会话**仅改动文档**（任务卡 + 台账 + 日志）；
- **本会话不做代码修复**（评审与实现须分离，沿 `docs/manuals/ai-review/README.md` 流程）：R1/R2 修复归**新会话**，修复后重审视角①。

### 7.7 修复与复评（循环 2/3 · 2026-09-25 11:5x~12:0x · 修复会话）

**范围**：R1 / R2（代码修复，测试先行）+ R3（登记更正，已在 7.2 前回填）+ 设计补注（hifi §11 **P8**、§6.3 两处带入口径、§6.4 筛选选项来源）。

**测试先行（RED → GREEN，全程留痕）**：
1. 先改测试：`IndexView.spec.ts`（新增"状态文案经常量收口"契约用例 + R1 三条用例：页头带参 / 无可用 DID 不带参 / 详情抽屉带参）、`DidSourceGuard.spec.ts`（新增"状态文案必须经 `constants/did.ts` 收口"守卫 + 反向探针）→ 实测 **RED：2 文件失败 / 4 用例失败（26 通过）**；失败点逐条可读：`RECORD_STATUS_FILTER_OPTIONS` 未定义、两处跳转 `query.did` 为 `undefined`、守卫命中页面散写状态中文；
2. 再改实现 → **GREEN：DID 相关 3 文件 / 30 用例全绿**。

**修复内容（逐项对号）**：

| 必修 | 修复动作 | 落点 |
| --- | --- | --- |
| **R1** | `goDemo(did)` 改为带参跳转 `router.push({ path:'/did/demo', query: did ? { did } : {} })`；**页头按钮**带当前清单**首条已登记记录**的 DID（派生 `demoEntryDid`）、**详情抽屉按钮**带当前记录 DID；清单无可用 DID 时**不带参数**（不留 `?did=` 空值） | `frontend/src/views/did/IndexView.vue:45/95/263/402`；`IndexView.spec.ts` 3 用例 |
| **R2** | 新增 `RECORD_STATUS_FILTER_OPTIONS`（label **一律取自** `RECORD_STATUS_LABELS`）；筛选下拉、详情解析状态、演示页解析状态全部改引用常量 → 同一状态全页**一种说法**（"待签发（记录中间态）"），页面零散写状态中文 | `frontend/src/constants/did.ts`、`IndexView.vue:28/279/370`、`DemoView.vue:239`；spec 常量契约用例 + 守卫用例 |
| **R3** | 体量登记按 `git show --numstat 7ede686` 实测更正 | §六"实测复核更正"行（评审会话已回填） |

**守卫口径增强（可证伪性）**：新增"状态文案收口"守卫，判据 = **剥离注释后**的源集中不得出现"作为独立字符串字面量"的状态中文（含引号匹配，故 `'已吊销该 DID'` 之类提示语不误判；注释引述亦不误判）；随附 **3 条反向探针**：单引号散写必命中、双引号散写必命中、注释引述必不命中（证明断言非空转且剥离生效）。

**复评（视角①，同会话自查）**：
- **verdict：PASS**（R1 / R2 逐条闭环，无新增设计偏离；R3 已更正）；
- 独立性说明：本次复评在**同一会话**内完成（修复会话自查），**独立性弱于独立会话评审** → 建议编排师按需另开独立会话复审（若复审发现新问题，循环 3/3 处理）；
- 已核证据（可复核）：① 修复面残留检查 `STATUS_OPTIONS` / 裸状态字面量在 `IndexView.vue`、`DemoView.vue` **0 命中**；② 修复前 RED（4 失败）→ 修复后 GREEN（30 通过）**双向留痕**（可证伪）；③ 变更面 = 5 个前端文件 + 2 份文档（**零后端改动、零依赖变更、零门禁配置改动、零库表/迁移**）；
- **新登记偏离**：**P8**（hifi §11：筛选项常量与文案统一；属"回归 §6.4"而非新行为）；hifi §6.3 / §6.4 两处口径补注；
- **新建议 S10（非阻塞）**：`RecordStatusFilterValue`（`constants/did.ts`）与 `DidRecordStatus`（`api/did.ts`）两处声明同一三值集合，建议后续前端类型收口时合并为一处。

**验证结果（修复会话）**：

| 验证 | 命令 | 结果 |
| --- | --- | --- |
| 前端 DID 用例 | `npx vitest run src/views/did` | **3 文件 / 30 用例全绿**（RED 阶段 4 失败留痕） |
| 前端全量 | `npm test` | **14 文件 / 113 用例全绿**（交付态 109 + 本次新增 4；**用例数不回退**） |
| 前端静态检查 | `npm run lint` | **0 错误**（exit 0） |
| 全仓门禁（代码变更后必跑） | `powershell -File scripts\gates\run-gates.ps1` | **GREEN**：`PASS=10 FAIL=0 SKIP=2 ERROR=0 PENDING=9`，退出码 0 → `scripts/gates/reports/gate-report-20260925-120159.md`（`compile` / `lint` / `unitTest` / `frontendLint` / `frontendTest` 全 PASS；SKIP=2 = oversized 1 + binary 20） |

---

## 八、走查准备记录（2026-09-25 12:2x~12:4x · 走查准备会话 · 编排师指示"执行走查准备"）

**目标**：使编排师可直接按剧本 S1 / S2 / S3 在界面上逐步走查（含根密钥选项 A 的落地）。

### 8.1 动作与实测结果

| 项 | 动作 | 结果（实测证据） |
| --- | --- | --- |
| ① 构件 | `mvn -B -ntp -pl services/did -am package -DskipTests` | **BUILD SUCCESS**（17.6s）；`did-service-2.0.0-SNAPSHOT.jar`（12:23:47，**含 3.1.11 四个新端点**） |
| ② 容器 | 核验 Docker 三容器 | `sc-mysql` / `sc-minio` / `sc-redis` **Up** |
| ③ KMS 根密钥 | 生成演示根密钥（16 字节 Base64，仓库外临时文件，不落仓库） | 就位；**交接项：KMS 重启必须沿用同一把**，否则本次新密钥材料会再次不可解密 |
| ④ subject SM4 密钥 | 生成 `subject-demo.keys`（`subject-cert-material=Base64(16B)`；`.gitignore` 拦截） | 格式校验通过（对齐 `LocalFileKeyProvider` 口径） |
| ⑤ 三服务 | mysql profile + 回环 + 服务间 URL（did→kms/subject）+ **`CTDS_DID_DEMO_SIGNATURE_ENABLED=true`** | `/actuator/health`：kms **8081=200** / did **8082=200** / subject **8080=200** |
| ⑥ 前端 | `npm run dev`（**未用 `--host`**） | `http://localhost:5173` **HTTP 200**；仅监听 IPv6 回环 `[::1]:5173` |
| ⑦ **选项 A** | 对两个既有演示主体执行"**吊销旧有效身份 + 重签**"（生成新密钥对） | `S20260921000001`：`.2` REVOKED（12:29:53）→ 新 **`.3` ACTIVE**（12:29:54）；`S20260922000001`：`.1` REVOKED（12:31:11）→ 新 **`.2` ACTIVE**（12:31:11） |
| ⑧ 选项 A 验证 | 代签 → 验证（原文未改 / 改一字）→ 解析 | 代签 **200**（签名 96 字符 Base64）；验证 **PASS**；改一字 **FAIL / SIGNATURE_INVALID**；解析 `ACTIVE` + `controller` + 公钥类型 SM2 |
| ⑨ 走查材料 | `build-output/demo-files/did-management/`（gitignored） | 走查步骤卡（S1/S2/S3 + 自检 + R1/R2 验证项）/ 演示库现状说明 / 接口调用样例说明 / 未登记样例 |

### 8.2 走查目标与数据现状

- **首选代签/验证目标**：`did:ctds:S20260922000001.2`（主体 `S20260922000001` = `ADMITTED` → 三查齐备：签名 ✓ / 状态 ACTIVE ✓ / 主体绑定 ✓）；
- **备用（仅"代签成功"）**：`did:ctds:S20260921000001.3`（该主体不在主体库 → 验证 reason 为 `SUBJECT_BINDING_FAILED`，如实登记）；
- **剧本点名主体（D1 蓝天 / D2 星河 / D3 云栖）当前不在演示库**：S1 走查即按 C-1.1 流程新建蓝天（审核通过后自动签发 → **新密钥对，天然可代签可验证**）；S3 需按附录 A 预置 D3 云栖。

### 8.3 发现与如实登记的偏差（非缺陷）

1. **代签入参语义 = 明文**：`POST /{did}/demo-signatures` 的 `data` 是**待签原文（明文）**，响应里的 `data` 才是原文的 Base64——走查准备首次按 Base64 传参导致验签稳定失败（`SIGNATURE_INVALID`），属**调用姿势**问题，已写入材料警示（与前端 `demoSign(did, 明文)` 口径一致）；
2. **验证三查含"主体绑定"**：DID 记录无对应 subject 记录时必然 `SUBJECT_BINDING_FAILED`（`1000C0003` 口径）——故选项 A 的可用目标选在**有主体**的编号上；
3. **走查准备期产生的验证留痕**（准备期若干条 `FAIL`）会出现在 S2 步骤 4 的留痕列表中，**不影响该步判定**（判定 = 时间/DID/结果/原因四要素齐备且**无原文**）；
4. **写库动作留痕**：本会话对演示库的 4 次写操作（2 次吊销 + 2 次重签）均在编排师裁决（选项 A）范围内；**吊销不可逆**，旧记录全部保留可追溯。

### 8.4 走查入口（编排师可直接执行）

1. 浏览器打开 `http://localhost:5173`（**仅本机**），以**管理员**演示角色登录 → 左侧菜单"**DID 管理**"；
2. 按 `build-output/demo-files/did-management/走查步骤卡.md` 逐步执行 S1 / S2 / S3（含 R1 修复验证项：进演示页 DID 应自动带入）；
3. 走查结论回填步骤卡末"走查结论"表（步骤号 + 现象 + 截图）。

---

> **边界复述（红线自查）**：本卡不含任何明文私钥（只见 KMS 引用与公开要素）；不新增错误码/状态枚举/库表；不改门禁配置；不夹带 DB-23/DB-24/DB-26 与 3.1.12；不实现规格外功能（界面只承载已确认行为）；发现规格缺口一律走变更流程，不在设计里补需求。
